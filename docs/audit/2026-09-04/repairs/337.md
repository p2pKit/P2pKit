# #337 — Android provisioning lifetime presentation

Recorded 7 September 2026: issue-scoped result, not whole-repository readiness.

Public outcome: <https://github.com/p2pKit/P2pKit/issues/337#issuecomment-5564403296>.

## Independently reviewed correction pushed to the audit branch

**#337 is fixed and independently reviewed on `audit/complete-2026-09-04`, not merged or released. Keeping this issue OPEN.**

- Reviewed baseline: `32dc5c03658145741c6e23cdfec3c14cd2eba5e7`.
- Final source: [`170cc86b652d421b9d3118735c63cdf4a5530a66`](https://github.com/p2pKit/P2pKit/commit/170cc86b652d421b9d3118735c63cdf4a5530a66), tree `23d44d3a59f0051b4475b637930263f84aed7c8a`, clean at integrated verification.
- Complete series includes preserved draft `eb564e9`, correction `5f454a9`, and final review correction `170cc86`. The original preservation commit was not itself a completed fix.

The Android sample now observes provisioning before enabling calls, records each resource's lifetime independently, and reconciles successful API returns against terminal events. System hotspot stop/network release cannot leave a stale live card or resurrect one through a late result. Last-owner manager snapshots and untagged cleanup warnings do not erase the other resource. Repeated calls, busy/Pending admission, cancellation, manager close and replaced-kit callbacks are fenced. Presentation owns subscriptions, not native cleanup; events are not cleanup acknowledgements.

#324's ephemeral/masked credentials and no permission-result auto-join remain intact. Genuine success clears input even when success and release coalesce before a card renders. Dismissal hides only that acquired binding; it does not hide a later binding with equal SSID/IP values.

Independent R1 returned **REQUEST CHANGES** for a dismissal-identity race. Two added regressions reproduced actual `null`-instead-of-Joined failures. The final correction binds dismissal to the exact live resource identity. Fresh reviewer `/root/review_337_r2` inspected the **whole** baseline-to-final range, surrounding callers and actual Android manager, and returned **APPROVE**, with no remaining actionable #337 finding. It independently verified exact tested patches, raw test counts, evidence hashes and cleanup.

### Verification of final inputs

```sh
./gradlew :p2p-sample-android:check :p2p-sample-android:assembleDebug \
  :p2p-network-provisioning-android:check --console=plain \
  --max-workers=2 --no-parallel --rerun-tasks --no-build-cache \
  --dependency-verification=strict -Pkotlin.compiler.execution.strategy=in-process \
  '-Dorg.gradle.jvmargs=-Xmx2048m -XX:ActiveProcessorCount=2 -Dfile.encoding=UTF-8'
./gradlew check --console=plain --max-workers=2 --no-parallel \
  --rerun-tasks --no-build-cache --dependency-verification=strict \
  -Pkotlin.compiler.execution.strategy=in-process \
  '-Dorg.gradle.jvmargs=-Xmx2048m -XX:ActiveProcessorCount=2 -Dfile.encoding=UTF-8'
```

- Affected Android gate: **75 sample + 144 provisioning tests**, zero failures/errors/skips; assembly/lint and ABI checks passed; 150 executed tasks.
- Clean integrated gate: **2,216 passed, zero failures/errors, one unchanged manual interop-capture skip**; 205 executed tasks. Includes JVM, Android host and Kotlin/Native arm64 iOS simulator tests. Do not add overlapping scoped counts.
- Layout, OSV lockfile coverage, Markdown links, release metadata and full-range whitespace checks passed.
- Every invocation, including expected red runs, unconditionally stopped its wrapper, terminated owned survivors and removed disposable generated outputs after retaining evidence. Source, shared caches, private evidence and stashes were preserved.

Private final review SHA-256: `6721e2f9748d87e9fcf3f47552d380329110d51316a03a2a1d4e4f55efd0906a`. Raw logs remain private; cloneable summaries/issue and coverage ledgers are maintained in `AUDIT_CHECKPOINT.md` and `docs/audit/2026-09-04/`.

These are host/controller/Robolectric/static and Kotlin simulator results, not rendered Android UI, ART/OEM, physical-device, Swift XCTest, hostile-network, independent interoperability or professional cryptographic assurance. Full audit/release readiness remains incomplete. The distinct pre-existing wrong cleanup Retry action is tracked as #354 and is next, not certified by this fix. Progress after adding it to the denominator is **47/168 reviewed repairs (28.0%)**, 121 remaining (72.0%). `main` is unchanged.
