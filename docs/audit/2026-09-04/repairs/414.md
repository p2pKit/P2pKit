# #414 — port-only resolved-service updates

**Finally APPROVED on the audit branch; no release or broad-platform qualification.**
Independent `APPROVE_FINAL_414` at2026-09-12T01:22:16.978637UTC, no actionable findings.
Review SHA256: `02b3bee9ef70dfc53fec4eb4227c77f7147063c961c5fd925b6f7e50b2daa11f`.

- Source commit: `268a367ae9d6f770183b6f40d5d3c3bdab25a190`.
- Tree: `80b55ae3ff3a3246207492ae8e507f5b721228e4`.
- Exact12-file diff SHA256: `110e4da85d6f217099b52491bc7c1a0c9b0f700e39d38d273b56af6ed04b84a4`.
- Normal push completed01:24:22.992722UTC; remote ref/tree verified01:24:24.093502UTC.
- [Verified public outcome](https://github.com/p2pKit/P2pKit/issues/414#issuecomment-5642531619);
  issue remains open. The repair is not merged to main or published as a release.

## Cause and focused correction

Vendored JmDNS resolved-record equality compared name/TXT/addresses but omitted the SRV port.
Consequently a changed valid port could be suppressed before JVM/Android endpoint hints refreshed.
The repair adds the port inequality without changing synchronization, cloning, other equality checks,
public signatures or protocol. Component revision `.410.4` binds source/resource/SBOM/consumer
expectations and ordered410→415→416→414patches; original upstream provenance and prior patch bytes remain.

Four joined repository methods cover secure and explicit-legacy profiles with resolved-first and
complete-added seeding. They apply real detached, nonexpired, same-server SRV A→B→A updates,
check distinct unchanged clones remain deduplicated, assert raw real-listener callback counts,
and observe the actual first-party Kotlin Found/Updated endpoint hints through the deterministic
backend. Stop/cancelAndJoin runs in teardown. No fake peer hint replaces the consumer boundary.

**Detached updates are explicitly forwarded to the real wrapper.** Automatic live cache/executor
dispatch, multicast delivery, TCP dialing and outage duration are not runtime-proved.

## Actual bounded verification

| Invocation | Preserved result | Command/stop/final |
| --- | --- | --- |
| `414-kotlin-preimage-r1` | Controller FileNotFound from misplaced option before helper launch; zero compilers/tests, not reproduction | 125/0/125 |
| `414-kotlin-preimage-r2` | Raw JUnit4methods/4intended failures: expected ports45001,45002; actual45001 | 0/0/0 |
| `414-kotlin-postimage-r1` | Raw JUnit11passed: same4new+1invalid-reresolution+6TXT-ingress guards | 0/0/0 |
| `414-provenance-regressions-r1` | 6affected SBOM methods,11licenseZIP+67embeddedZIP/metadata fixtures; inventory/patch/shell controls | 0/0/0 |

Preimage JUnit exits1; its expected-red controller correctly exits0. Later assertions short-circuit
and are not separately claimed failed. Receipt XML totals0 do not replace the actual raw JUnit counts.
Identical SLF4J NOP notices are retained; emitted-log behavior was not validated.

The retained source helper command was `python3 -B "$B/414-repair-r1/run-source-regression.py"`
with `preimage` or `postimage`. `$B` is the private evidence root, not a checked-in dependency.
Fresh JDK17 `javac --release 8 -Werror` compiled all60vendor Java files each phase. Kotlin2.4.10
`-Werror` compiled14LAN JVM-slice sources and5selected test files once. Sixteen checksum-bound
JARs and a **historical core4a409a8 artifact** were used, not relabelled current core. Postimage
reused identical146LAN/37test output files, recompiling the vendor; dependent outputs were then removed.

Provenance commands selected six affected methods from `scripts/tests/check-sbom-test.py`, ran
`scripts/tests/check-publish-license-test.py` and `scripts/tests/check-osv-lockfile-coverage.sh`,
checked affected shell syntax/whitespace and replayed the new patch forward/reverse. Actual upstream
inventory and10populated-lock coverage passed; fixture archives are not a published consumer and
lock coverage is not a network OSV scan. Prior415strict-help mechanism evidence is reused, not rerun.

All invocations bind **parent e78a33e/tree34b79e4 + exact staged110e4da8**, not clean268a executions.
Exact argv, UTC windows, receipt/log bindings, final review and publication evidence are summarized in
[checkpoint.json](../checkpoint.json) `nativeFollowups.postR22Followthrough.repair414`. Raw evidence
and private helpers are not included in the clone; these are historical executions, not a fresh rerun.
The meaningful regression is checked in as `ListenerStatusPortChangeIngressTest.kt`.

All stops0, no owned survivors. Preimage disposed1,090,358bytes and retained only dependent compiled
outputs; postimage disposed2,004,839bytes. Provenance output11,175bytes was removed. Scoped work is
absent. Shared caches, protected guides, required evidence and the source build package are preserved.

## Remaining boundaries

No Android/ART/D8, Apple, automatic live-network, KMP metadata, normal Gradle lock resolution,
aggregate ABI, current root/sample/consumer/release or cryptographic qualification is established.
Mandatory broader gates remain unwaived; #410 stays parked without approval credit. #413native4
passed separately in R22 but aggregate ABI/final approval remain pending; R21FAIL is preserved.
#409 is the sole active implementation at this report input; #419documentation correction is pending.
Current inventory203/233approved (87.1%),204resolved (87.6%),29unresolved (12.4%):5repairs+21external+
3owner decisions. These are row counts, not effort/readiness. Audit **NOT_READY**; #133**NOT_STARTED**.
