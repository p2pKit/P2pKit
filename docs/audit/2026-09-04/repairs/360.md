# #360 — preserve immutable archives in all-tree whitespace gates

**8 September 2026: complete repository correction independently approved and pushed; issue remains OPEN.**
Low-severity build/CI correctness, not a runtime/security defect. See the
[new issue](https://github.com/p2pKit/P2pKit/issues/360) and [verified outcome](https://github.com/p2pKit/P2pKit/issues/360#issuecomment-5580382007).

## Exact source and review

- Branch: `audit/complete-2026-09-04`; not merged into `main`.
- Original-code reproduction: `6ca4acf0445a7e63a9664fd3ff3988fb7912db07`, tree `606b74ba6233f381ecbf2f823b64538724ead701`, before #144.
- Correction baseline: `f10c590e4bf72075d2bb2e3ed223d179c38ba633`, tree `ce173a570fcadefe4ab2c7f10d5f9404b3fd36fb`.
- Complete seven-file correction: [`6995130bcdb2594257c42bed006f7d23f5bc4d6a`](https://github.com/p2pKit/P2pKit/commit/6995130bcdb2594257c42bed006f7d23f5bc4d6a).
- Final reviewed tree: `323fc39587c626df7ac9da1edce7cc0ab80ac313`; clean at verification/review.
- Seven-path diff SHA-256: `b4f6a77a46a46ee2ef10ade7681dbb5e67899b45ab178b760e5b565d55b3102f`.
- Fresh `/root/review_360_r1`: **APPROVED**, no actionable findings.
- Review SHA-256: `65de75d34645f4a497395f76cae78f10f0ea146bf4dad6dfadf24edbc032b9a2`.
- Shared evidence-manifest SHA-256: `08ae831555d21d2fd7e09c9bfc0fa7f26f5e88f67675e94d83de18e0dcc4c337`.

Source push/outcome were refetched and verified. Main remained `eb444cccfc290be5435c5c10629c24183293606f`.
No merge, issue closure, release or tag movement occurred. #360 approval preceded, but did not substitute for,
the separate [#144 review](144.md). Later administrative commits do not change this source-evidence binding.

## Verified cause and deduplication

During #144's actual YAML replay, the resolver correctly selected full/all-tree scope but the real whitespace
step failed. Executing the **original pre-#144** manual resolver and both original gates in a disposable repository
with the exact baseline tree reproduced the problem: resolver exit 0, whitespace exit 2, dependency gate exit 2.
A subsequent ordinary clean commit-range control passed.

An empty-tree comparison treats every retained historical line as new. It reported:

| Preserved path | Original diagnostics |
| --- | --- |
| `docs/archive/evidence/v0.3/jvm-cli.log` | trailing whitespace at lines 2, 7, 9 |
| `docs/archive/remediation/2026-06/PROBLEMS_P2PKIT.md` | Markdown hard-break spaces at lines 4, 5 |
| `docs/archive/remediation/2026-07/OPEN_DECISIONS_2026-07.md` | extra EOF blank line at line 207 |

Complete #301/#303/#223 bodies, comments and timelines and merged PR #59 were read before filing. All-state
issue searches found no duplicate archive-format/all-tree gate failure. #301/#303 are closed retention assessments;
their decision to preserve these bytes remains. #223 concerns `gradlew` line-ending conversion and stays separate
and pending. #144's graph-based reuse has a different root cause; #360 is its newly discovered prerequisite.

The original failure is not justification for deleting, normalizing or weakening checks across historical records.

## Complete correction and contracts

- `.gitattributes` exempts only the necessary whitespace category for each of the three exact paths. It also sets
  `-text` so automatic line-ending conversion cannot alter the retained bytes. No directory-wide exclusion exists.
- New `scripts/check-archive-whitespace.sh` requires an available exact commit and verifies SHA-256 for all three
  paths in the compared head, index and regular non-symlink worktree file. Missing, altered, unmerged/non-blob,
  unreadable or unsupported hashing inputs fail closed. Diagnostics contain paths/layers, not archived payloads.
- Both `scripts/check-git-whitespace.sh` and the range mode of `scripts/check-dependency-update.sh` require that
  integrity helper before normal whitespace checks. Existing dependency-state mode and exact HEAD binding remain.
- `scripts/tests/check-git-whitespace-test.sh` adds independent digest tripwires and actual-Git layered fixtures.
  `scripts/tests/check-dependency-update-policy-test.sh` binds the separate direct caller.
- `docs/testing/local.md` distinguishes plain Git's attribute handling from wrapper-enforced hashes and forbids
  extending these exceptions to new archive material. Older-checkout advice preserves local user changes.

The shared helper handles three fixed paths without temporary files, asynchronous work or repository mutation.
Conflict markers, neighboring archives and nonexempt whitespace categories remain checked. Existing API-file EOF
policy is unchanged. **Plain `git diff --check` honors attributes but does not verify archive hashes.**

No archive contents changed. These SHA-256 values match both baselines, final head, index and worktree:

```text
7994521ea430c15f748acbbb925970d33dfbafcceab3ae1bb776dd6e1f27ec3c  jvm-cli.log
edc4723a50445bf95d927c56d39aa4ba0600cd363adaa658bddd7a06ddf0d32e  PROBLEMS_P2PKIT.md
4a7b7d8f5dcc4ba32f8410d7e78781c139e21c25f18c6ba192adb28f4fb8bff3  OPEN_DECISIONS_2026-07.md
```

The entire `docs/archive` tree remains `e0ce764d36d50d73568cab9f743ab1244221bbbb`. No production/sample, public
API/ABI, protocol/security, identity, transfer durability or dependency change is part of this correction.
These three pins are a narrow immutable-format policy, not a full historical-evidence or cryptographic audit.

## Executed verification

| Verification | Result |
| --- | --- |
| Original baseline/manual actual entry points | Resolver passes/full; both whitespace gates fail with six expected diagnostics; ordinary-range control passes |
| Final whitespace suite | **51 labeled assertions pass**: three archives × head/index/worktree × replace/delete; ordinary/all-tree/multicommit checks, invalid objects, neighbors, unrelated whitespace, conflicts, symlink, autocrlf and existing ABI exception |
| Frozen controls | **Ten intended-red controls and two restored-source positive controls**; exact exits/diagnostics, all disposable fixtures removed |
| Five separate Bash syntax parses and ShellCheck | **PASS**; first multi-file `bash -n` only parsed its first file and is not counted as five parses |
| Shared actual YAML replay | **Five commands pass**, including both actual scheduled/all-tree entry points and the dependency regression suite |
| Shared final static/policy gates | **23 pass**, including actual Android ABI graph verification and actionlint |
| Shared full check, Android assembly and strict core Dokka | **2,459 passed**, zero failures/errors, one unchanged manual LAN capture skip; 297 XMLs, 256 executed tasks |

Controls detect the original callers/attributes, omitted head/index/worktree checks, blanket archive exceptions,
disabled indentation checks, lost byte stability, coordinated archive/helper edits and the independent dependency
caller. They are expected verification rejections, not additional passing product tests. The final suite pins approved
hashes independently before fixture creation; changing both helper and archive does not silently update acceptance.

Repeatable focused checks:

```bash
bash scripts/tests/check-git-whitespace-test.sh
bash scripts/tests/check-dependency-update-policy-test.sh
empty_tree=$(git hash-object -t tree /dev/null)
head=$(git rev-parse HEAD)
scripts/check-git-whitespace.sh "$empty_tree" "$head"
scripts/check-dependency-update.sh "$empty_tree" "$head"
```

Inspect prerequisites and run each with unconditional failure-safe wrapper/owned-worker/output cleanup.
The complete combined Gradle command and platform scope are in [#144's verification record](144.md#executed-verification).

The initial #144 YAML failure remains FAIL. The initial #360 control orchestrator also remains an evidence-verifier
FAIL: a positive fixture hid the expected original-attributes diagnostic, despite the correct exit 2. R2 exposed only
output in a disposable copy; assertions and committed source were unchanged. The first sealer's incorrect expected
resolver success wording was separately corrected. Original failed drivers/logs/cleanup remain preserved; none was
relabeled as passing. Later successful runs provide the final evidence.

## Independent review, cleanup and limitations

The reviewer inspected all seven files, surrounding CI/release/dependency callers, tests, history and public
contracts. It independently rehashed **3,558 sealed files / 18,444,319 bytes**, reconstructed control-input hashes,
matched actual YAML bindings and parsed all 297 raw XMLs. It verified **106 finalized receipts / 95 nonoverlapping
leaf invocations**, exact source/diff/blobs/archive identities and the separate successful final-sealer receipt.
No actionable issue-specific defect remains. The reviewer started no builds/tests/background workers.

All invoked checks, including failures and expected-red controls, finalized wrapper stop and scoped cleanup.
The final full run retired its owned JDK21 survivor and removed ten build directories; no P2pKit-invocation-owned
workers or disposable module outputs remained. Other projects' background workers were deliberately untouched.
Protected guides, source, stashes, shared caches and required private evidence remain.

Raw logs/manifests and machine-specific verification drivers are deliberately not Git deliverables. These safe
summaries and hashes do not recreate absent artifacts in a clone. Local macOS autocrlf tests are not Windows runtime
validation; YAML replay is not hosted scheduling/matrix execution. The full run covers JVM, Android host and arm64
Kotlin/Native simulator, not Swift XCTest, Intel runtime, ART/OEM or physical devices.

Whole-audit release/consumer/Swift/XCFramework checks remain pending; earlier #133 consumers stay bound to `7127616`.
#133 independent interoperability remains **NOT_STARTED**. No hostile-network, physical-device or professional
cryptographic acceptance is inferred. #144 required its own later independent review; #223 remains pending.
Overall **NOT READY**, not a merge/release approval.
