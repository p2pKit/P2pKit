# #130 — provisioning fingerprint-delegation regression coverage

**8 September 2026: complete repository correction independently approved and pushed; issue remains OPEN.**
This is a Low-severity test-fidelity gap. Production already forwards the correct pins; no authentication bypass
or production behavior change is claimed. See the [issue](https://github.com/p2pKit/P2pKit/issues/130),
[executed investigation](https://github.com/p2pKit/P2pKit/issues/130#issuecomment-5576541032) and
[verified outcome](https://github.com/p2pKit/P2pKit/issues/130#issuecomment-5576902316).

## Exact reviewed source

- Branch: `audit/complete-2026-09-04`, not `main`.
- Baseline: `d9dcae104d92ae41d060dcd88ed204e22352eb2c`, tree `f55818f028d0a9714d226990c7e1de4190c87837`.
- Correction: [`1847ed330e3a15212982994a84a55d2b9e3ae49d`](https://github.com/p2pKit/P2pKit/commit/1847ed330e3a15212982994a84a55d2b9e3ae49d).
- Reviewed tree: `3fe527a16a52e536dedad610aebff53a7a026cdb`; clean during final verification/review.
- Complete diff: two existing test files, 100 insertions and 10 deletions; no production, dependency, API/ABI or wire change.
- Fresh reviewer `/root/review_130_r1`: **APPROVE**, no actionable findings, covering the complete diff and effects.
- Review SHA-256: `3b57e2b49efa08dfd57150d4dd58fefa09bcf8e1537f860abc8522a0a4416101`.
- Evidence-manifest SHA-256: `fe61ea37a064ad8f2e9344b6de0ca96240f8195e4d500ec2d7bfdd7ab1f31990`.

Later administrative commits do not change this source-verification binding. The source push and public outcome were
refetched and verified; `main` remained `eb444cccfc290be5435c5c10629c24183293606f`. No merge, tag, release or closure occurred.

## Verified cause and correction

The complete #130 discussion and relevant closed #139/#192/#154 histories were read. Both private `RecordingRegistrar`
fakes accepted `expectedFingerprint` but omitted it from `Call`; existing unit delegation tests used only the deprecated
unpinned overload. Temporary removal of both production forwarding arguments left the selected old manager classes
green: Desktop 14 plus Android 53 tests. This deliberately excluded the compensating Desktop manual-IP integration;
it does **not** establish that the entire project accepted omitted pins.

Changed paths, at the reviewed correction:

- `library/p2p-network-provisioning-desktop/src/test/kotlin/dev/p2pkit/provisioning/desktop/JvmNetworkProvisioningManagerTest.kt:332-418`
- `library/p2p-network-provisioning-android/src/androidHostTest/kotlin/dev/p2pkit/provisioning/android/AndroidNetworkProvisioningManagerTest.kt:1523-1574,1895-1921`

Each recorder now retains the nullable pin. Each new `createManualPeerWithFingerprintForwardsEachExactRemotePin` test
calls the real sidecar twice with distinct canonical remote values and a different non-null local fingerprint. Complete
ordered call-record equality covers exact pins, host, port, LAN kind, null device name, count and order. Existing
deprecated tests now assert null even with local identity available. Returned-peer checks remain; managers always close
in `finally`. Desktop uses the existing empty-address scanner, while Android asserts no hotspot-start/join calls.

`PeerFingerprint` is an immutable value class: exact value equality is the contract, not boxed-reference identity.
The #139 legacy decision is preserved; secure core still rejects missing pins and legacy mode rejects claimed pin
authentication. #192/#154's centralized host/port validation and hostname decisions remain unchanged. Production
sidecars, Apple/fallback implementations, core registry/session pin enforcement and integration callers were inspected.

## Executed verification and reproduction commands

All Gradle runs used these options, with the applicable wrapper stopped and owned outputs/workers cleaned after each
invocation, including expected failures:

```bash
options=(--rerun-tasks --no-build-cache --no-configuration-cache --dependency-verification strict
  -Pkotlin.compiler.execution.strategy=in-process
  '-Dorg.gradle.jvmargs=-Xmx2048m -XX:ActiveProcessorCount=2 -Dfile.encoding=UTF-8'
  --console=plain --max-workers=2 --no-parallel)
```

| Invocation | Result |
| --- | --- |
| Desktop `:p2p-network-provisioning-desktop:test --tests '*JvmNetworkProvisioningManagerTest*'` plus Android `:p2p-network-provisioning-android:testAndroidHostTest --tests '*AndroidNetworkProvisioningManagerTest*'`, old tests/temporary dropped pins | 67 passes, zero failures/errors/skips; 2 XMLs, 26 executed tasks |
| Same two tasks, filters `'*JvmNetworkProvisioningManagerTest.createManualPeer*'` and `'*AndroidNetworkProvisioningManagerTest.createManualPeer*'`, corrected source | 4 passes, zero failures/errors/skips; 2 XMLs, 26 executed tasks |
| `./gradlew :p2p-network-provisioning-desktop:check :p2p-network-provisioning-android:check "${options[@]}"` | 161 passes, zero failures/errors/skips; 8 XMLs, 36 executed tasks |
| `./gradlew check :p2p-sample-android:assembleDebug "${options[@]}"` | **2,389 passes, zero failures/errors, one unchanged manual skip; 290 XMLs, 246 executed tasks** |

The precommit focused/module patches byte-equal the complete committed correction. The final full run used the clean
frozen commit. Its unchanged manual skip is `IosLanDiagnosticTest.advertiseForSixtySecondsForInteropCapture`;
`iosX64Test` runtime is separately skipped on this arm64 host, despite x64 compilation/linking. Do not add overlapping
test totals or treat them as unique device scenarios.

Eight independent mutation controls each run one selected regression test: omitted pin, substituted local pin,
constant first remote pin and deprecated overload inventing a local pin, separately on Desktop and Android. Every
case produces exactly one intended assertion failure, zero errors/skips; the exact committed source is restored.
The reviewer checked failure values/stacks, not just nonzero exit codes. No mutation is committed.

Fourteen final gates pass: layout, OSV lock coverage, Markdown links, release metadata, whitespace, scoped source
style, dependency verification, toolchain policy, release-workflow policy, wrapper/check regressions, actual Android
ABI graph guard, platform-test policy and platform-test runner. Style inspection covers the changed files' relevant
EditorConfig rules; full ktlint is not claimed. These checks are not a complete release rehearsal.

## Independent review, cleanup and remaining limits

The reviewer independently reconciled **1,930 sealed file hashes/sizes**, **310 XMLs across runs** (290 final), eight
expected-red controls and **30 finalized cleanup receipts**. It inspected both entire changed test files, surrounding
production/platform/core/integration code, ownership/cancellation and compatibility contracts. No reviewer builds ran.
All builds were serialized and bounded; all receipts record wrapper stop, no final owned survivors or cleanup failures,
and no outputs retained for dependent validation. Reports were preserved before disposable module outputs were removed.
Protected instructions, source, stashes, shared caches and private evidence were preserved.

Raw evidence and machine-specific runners are intentionally not in Git; request the private evidence backup or rerun
scoped checks rather than claiming historical raw files exist in a clone. These hashes bind evidence, not a public
signature or independent cryptographic assurance.

Android fake/Robolectric checks are host tests, not ART/OEM/physical Wi-Fi. The combined check includes Kotlin/Native
arm64 iOS simulator, not Swift XCTest, Intel/older-OS runtime, physical devices or complete XCFramework validation.
Whole-repository corroboration, eventual final release/publication/isolated-consumer/Swift/XCFramework gates, hostile
networks, physical Android/Apple, independent interoperability (#133 NOT_STARTED) and professional cryptographic review
remain pending. Earlier consumer evidence remains bound to `7127616`, not this correction. Overall **NOT READY**.
