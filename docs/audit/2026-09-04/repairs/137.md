# #137 — compatible diagnostic-cause visibility

**8 September 2026: complete repository correction independently approved and pushed; issue remains OPEN.**
This is the accepted Low-severity, 0.7-compatible maintenance correction, not a demonstrated stale-read race or
immutable-throwable redesign. See the [issue](https://github.com/p2pKit/P2pKit/issues/137),
[executed investigation](https://github.com/p2pKit/P2pKit/issues/137#issuecomment-5577176739) and
[verified outcome](https://github.com/p2pKit/P2pKit/issues/137#issuecomment-5577645103).

## Exact reviewed source

- Branch: `audit/complete-2026-09-04`, not `main`.
- Baseline: `dd380c0b3bef4716bf88766fb9a303828f4cb8c7`, tree `6cb62ecf5113522dec7b4e203a968b661cbe862c`.
- Correction: [`a5d3145d9cfbfa633569d48c875a20befc48dbde`](https://github.com/p2pKit/P2pKit/commit/a5d3145d9cfbfa633569d48c875a20befc48dbde).
- Reviewed tree: `bcef6e91cd92e990ac37efd3e2e2840cd89ff2f9`; clean during final verification/review.
- Complete diff: one production file and four new test files; 248 insertions and two deletions.
- Fresh reviewer `/root/review_137_r1`: **APPROVE**, no actionable #137 findings.
- Review SHA-256: `a1e881522f2ef23ba5758c695eeb413948a0ca16a321eae2e09125aefd8238e5`.
- Evidence-manifest SHA-256: `97dbbf87ac08e1d9656b0589883f35a37ada7f17068abcd222ce2d3b3b5b2aed`.

Later administrative commits do not change this source-verification binding. Source push and public outcome were
refetched and verified; `main` remained `eb444cccfc290be5435c5c10629c24183293606f`. No merge, tag, release or closure occurred.

## Verified cause and correction

The full #137 body/comments/timeline and relevant closed #149/#193/#263 decisions were read. Four ordinary mutable
cause slots remained in `Errors.kt`. All **19** production attachments occur on newly constructed errors before
return, throw, channel or StateFlow publication; no SDK mutation after publication or actual stale read was found.

`LocalIdentityUnavailable`, `ConnectionFailed`, `FileTransferFailed` and `AuthenticationFailed` now annotate those
slots with `kotlin.concurrent.Volatile`. Public KDoc states attachment at most once before publication and corrects
the misleading suggestion that Kotlin `internal` prevents Java access. **Volatile is not immutable.** Existing mangled
Java setters remain callable but unsupported, and `copy()` deliberately drops diagnostic causes. Constructors,
equality/hash/destructuring, API baselines, all production attachment sites, protocol/security policy and dependencies
are unchanged. Constructor-carried immutable causes remain a future ABI/product decision, not unfinished scope of
this explicitly compatible correction.

Changed paths under `library/p2p-core/src/`:

- `commonMain/kotlin/dev/p2pkit/core/Errors.kt:78-87,101-104,126-128,149-151,195-197`
- `commonTest/kotlin/dev/p2pkit/core/ErrorCauseContractTest.kt`
- `jvmTest/kotlin/dev/p2pkit/core/ErrorCauseVisibilityJvmTest.kt`
- `androidHostTest/kotlin/dev/p2pkit/core/ErrorCauseVisibilityAndroidHostTest.kt`
- `jvmTest/kotlin/dev/p2pkit/core/internal/ErrorCausePublicationTest.kt`

Four common tests cover initial null causes, retained reference identity and unchanged copy/equality/hash semantics.
Four assertions per JVM/Android-host compilation inspect actual field volatility. Two real secure-kit tests subscribe
before advertising/discovery failure on an owned different thread, then assert original-cause identity, identity of
the thrown/published wrapper, and retention after shutdown. Memory-only identity storage, explicit `RejectUnknown`,
fake discovery/path observation, real-clock bounds and cancellation-safe kit/store/executor teardown avoid personal
storage or physical-network dependence. StateFlow already synchronizes publication: these protect the observable
contract, not a reproduced memory-model race.

## Executed verification and reproduction

Gradle invocations used these options, with wrapper stop and owned-worker/output cleanup after every run, including
expected failures:

```bash
options=(--rerun-tasks --no-build-cache --no-configuration-cache --dependency-verification strict
  -Pkotlin.compiler.execution.strategy=in-process
  '-Dorg.gradle.jvmargs=-Xmx2048m -XX:ActiveProcessorCount=2 -Dfile.encoding=UTF-8'
  --console=plain --max-workers=2 --no-parallel)
```

| Invocation | Result |
| --- | --- |
| Original production plus new tests: `:p2p-core:jvmTest --tests '*ErrorCause*' :p2p-core:testAndroidHostTest --tests '*ErrorCause*' --continue` | 14 tests: eight intended structural failures, six passing controls; zero errors/skips; four XMLs, 19 executed tasks |
| Corrected focused JVM/Android-host tests and affected existing contract classes | 72 passes, zero failures/errors/skips; ten XMLs, 19 executed tasks |
| Clean `check :p2p-sample-android:assembleDebug :p2p-core:dokkaGeneratePublicationHtml`, with read-only compiler-output query | **2,407 passes, zero failures/errors, one unchanged manual skip; 295 XMLs, 257 executed tasks** |

Portable focused rerun command:

```bash
./gradlew :p2p-core:jvmTest --tests '*ErrorCause*' --tests '*SendErrorContractTest*' \
  --tests '*TransportManagerTest*' --tests '*SecureIdentityServiceTest*' \
  --tests '*SecureIdentityConstructionRollbackTest*' --tests '*AuthenticatedV2SecurityEngineTest*' \
  --tests '*VerifiedJvmFileTransferTest*' \
  :p2p-core:testAndroidHostTest --tests '*ErrorCause*' "${options[@]}"
```

The precommit focused patch byte-equals the complete committed correction. The final full run used the frozen clean
commit. A private read-only init task located actual compiler outputs before cleanup; it is not a repository task
available in a clone. Actual ABI checks passed with unchanged baselines; `javap` confirmed eight volatile fields and
retained mangled setters. Strict rendered public Dokka accurately exposes the new guidance.

Five separate mutation controls were executed and restored exactly. Removing each annotation individually produces
the intended two field-flag assertion failures, one per compiler platform, with six passing controls. Removing feature
cause attachment produces the two intended publication-test assertion failures. The reviewer checked testcase values
and failure stacks, not merely exit codes. No mutation is committed.

Fourteen final gates pass: layout, OSV lock coverage, Markdown links, release metadata, whitespace, scoped source
style, dependency verification, toolchain policy, release-workflow policy, wrapper/check regressions, actual Android
ABI graph guard, platform-test policy and platform-test runner. Scoped style checks are not a full ktlint run.

The unchanged manual skip is `IosLanDiagnosticTest.advertiseForSixtySecondsForInteropCapture`. Intel simulator runtime
tasks are separately skipped on this arm64 host despite compilation/linking; they are not execution evidence. Do not
add overlapping focused/full totals or treat them as unique device scenarios.

## Independent review, cleanup and remaining limits

The reviewer independently reconciled **all 1,798 sealed hashes/sizes**, testcase-level reports, five mutation controls
and **25 finalized cleanup receipts**. It inspected the complete diff, all 19 attachment sites, surrounding callers,
platform implementations, public contracts, compatibility, cancellation, lifecycle, resources and transfer effects.
No reviewer builds ran. A separate unchanged file-transfer cast concern is explicitly
[unverified](../followups.md#file-destination-authentication-error-casts), not a #137 blocker or confirmed new issue.

All build/test runs were serialized, bounded and finalized successfully, with no remaining owned workers or cleanup
failures. Disposable module outputs were removed after required reports/eight small classfiles/rendered Dokka evidence
were captured. Protected instructions, source, existing stashes, private evidence and shared caches remain preserved.
Raw evidence and machine-specific runners are intentionally not in Git; request a private backup or run fresh scoped
checks. The hashes are evidence bindings, not public signatures or independent cryptographic assurance.

Android host tests are not ART/OEM or physical Wi-Fi. The combined check executes Kotlin/Native arm64 iOS simulator,
not Swift XCTest, Intel/older-OS runtime, physical devices or complete XCFramework validation. Whole-repository
corroboration, final release/publication/isolated-consumer/Swift/XCFramework gates, hostile networks, physical
Android/Apple, independent interoperability (#133 NOT_STARTED) and professional cryptographic review remain pending.
Earlier consumer evidence remains bound to `7127616`, not this correction. Overall **NOT READY**.
