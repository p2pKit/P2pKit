# #369 continuation — actual publication and trial3 boundary

**9 September 2026. Prior independently approved source was PUBLISHED with `bafe905ac030d090219352de8a2358695755b528`,
tree `91d469eb09c8a612bf7dd9f7c5e2ce1f081634bb`.** Audit push exited0 at02:00:42.324919 UTC; remote ref
verified02:00:44.154019, after the global lease/source freeze/local-test pause was held before push.
[Source-outcome comment5594738849](https://github.com/p2pKit/P2pKit/issues/369#issuecomment-5594738849) was refetched at2026-09-09T02:13:51.295213+00:00 with an exact body match,
SHA-256 `54de939679ce1a29bf95660731336e81c6bef3a096930abba2022e1c43249de8`; issue OPEN. This publication event does not rewrite the original source review or filing.

Absolute Git Bash and the actual pc-cygwin banner were admitted before the native suite. This is a narrow native prerequisite observation, not full-host approval.
Authentic [trial3](../hosted-validation.md#trial3-publication-and-terminal-native-control-failure),
run34301487847/attempt1 atbafe, selected48 native methods:47PASS/1FAIL/0errors/0skips.
The unchanged ordinary nested-output cleanup test failed with125; outer suite/stop/final1/0/1.
SDK, wrapper-checkout fixture, P2pKit product/sample/consumer and both Apple hosts did not start.
Three synthetic XMLs establish zero product cases. PARTIAL_SALVAGE/unsafe,18 unavailable JSONL omissions;
full remote tree and cleanup NOT_PROVEN. The local02:42:38.431897 lease release is not remote cleanup.

The distinct cause was filed as [#373](373.md), then separately corrected and independently source-approved
at128136ff/tree747189; locally committed NOT_PUSHED, corrected Windows suite/junction test NOT_RUN here.
It does not add another #369 row, redo #369, or retroactively make trial3 pass. Source-outcome publication,
narrow native observations, later source approval and final administrative approval remain separate.
General shell-checkout CRLF and generated-batch CRCRLF hypotheses remain separate and UNCONFIRMED.
Whole audit NOT_READY; #133 and #268 NOT_STARTED. See the current [hosted continuation](../hosted-validation.md)
for exact publication/review/cleanup bindings and lease-before-trigger requirements.


## Preserved pre-trial3 #369 report at bafe905

The complete following text is preserved from published `bafe905`; its older source/count/publication/trial states
are historical and superseded by the dated continuation above, not rewritten. Earlier failures, read bindings,
verification scopes and applicable safety requirements remain in force.

> **Current continuation (2026-09-09T01:18:20.220814+00:00):** Same-#369 shell follow-up `aab8cab`, separate #370 SDK correction `d3c8120` and #371 native-fixture byte-emission correction `842d0aa` are independently source-APPROVED, locally committed and NOT_PUSHED in this record; trial3 NOT_EXECUTED. 74/185 independently approved repository-repair rows (40.0%), 111 remaining (60.0%): 89 repairs, 21 external validation rows and architecture decision #120; 52 new audit findings. Whole audit NOT_READY; no checkpoint stop.
> The full following `638d005`/R3 report is preserved historical text, including its formerly-current opening.
> Dated additions after that block provide the separate current source, product, GitHub and publication states.
> Source approval is not independent approval of these administrative bytes or native/SDK/external acceptance.

# #369 — bind Git for Windows Bash in both audit callers

**8 September 2026: final source APPROVED and locally committed; NOT_PUSHED, native rerun NOT_EXECUTED.**
[Issue #369](https://github.com/p2pKit/P2pKit/issues/369) is a Low hosted-validation tooling defect,
not library/runtime security behavior. #223's wrapper LF policy and #367's classifier repair remain intact.
The audit now has **72/183 independently approved repository-repair rows (39.3%), 111 remaining (60.7%)**:
89 repairs (88 Low plus informational #333), 21 external rows and architecture decision #120.
#268 and independent #133 remain **NOT_STARTED**; whole audit **NOT_READY**.

## Actual failure, issue capture and scope

The first hosted [run34268510397](https://github.com/p2pKit/P2pKit/actions/runs/34268510397), attempt1,
Windows job102203889802, executes `bb0fd25f9ac40ca9bceb8bdfbb0ad104baae6d63`, tree
`7568fd93ac50fafe09f5fad91f03f5d8be138622`. At19:22:09UTC, native Python's bare
`["bash", "--version"]` returns1 with the WSL no-installed-distributions diagnostic. Python/Git/Java17/
Java21 return0; RAM exceeds the prerequisite. `Required tool version query failed` occurs before the first
`Host.invoke`: components remain empty. Native controls, SDK setup, wrapper fixture, product Gradle/tests
and all Apple work are **NOT_STARTED**. Version/setup subprocesses did run; their complete retirement is not proved.

Fresh independent `/root/corroborate_host_trial1` corroborated the authentic failure and returned archive,
not cleanup or product success. Artifact10072826523 has7,741 bytes,13members and SHA-256
`7ff7102aa8deaa509ca2e065e70566bf6247b69feeb3ffd6ce66d811fbd10816`.
**PARTIAL_SALVAGE, safeToContinue=false, driverSafeOutput=false, full-tree/remote cleanup NOT_PROVEN**
remain unchanged. No audit build/test leaf began, so no applicable Gradle stop existed and none ran.
[The appended hosted-trial record](../hosted-validation.md#first-hosted-trial--terminal-prerequisite-failure)
separately records unsafe retention and narrow local cooperative-lease release; it is not a safe-host handoff.

The19:17–19:22 full issue/comment/timeline/PR capture and independent underlying-cause duplicate review
found no existing owner among #223, #367, #141/#150, #208, #221 and #325. A19:59 all-state list was unchanged;
#369 was individually created20:00:00Z/refetched20:00:01.894153UTC, OPEN. This raises known counts to290issues/
183open/79PRs, not a post-addition full refresh. The original CONFIRMED_NOT_STARTED filing is historical.
No #369 outcome comment or closure is recorded here.

## Exact revision and correction

- Commit: `bd61175167337fee585855cbbe5bfae9c0122dc5`, tree `95bb599cebc45d80d0a5c632fb2b84e240e14a30`.
- Parent: `bb0fd25f9ac40ca9bceb8bdfbb0ad104baae6d63`, tree `7568fd93ac50fafe09f5fad91f03f5d8be138622`.
- Four-file diff SHA-256: `1187b99c56c1798cbf2a137857d15cdabb362ca0f46c425cf9ec5a4c48f81b3f`;
  308 additions/4 deletions. Local commit receipt20:40:01.302427UTC is not a push/ref or native-run receipt.

`resolve_windows_shell` binds an absolute compatible Git-for-Windows `bin/bash.exe` from supported
native `git.exe` cmd/bin layouts and requires its Bash file and usr/bin directory. Prerequisites record resolution,
version/flavor and failure diagnostics; missing/unusable/unsupported Git/Bash fails closed. The actual Windows
wrapper fixture uses that same successful shell and scoped tool PATH, including its nested env-Bash/checker
utilities. Changing only the version probe would leave the real fixture ambiguous.

Query/invocation environments are copied; native product PATH and `gradlew.bat` routing remain unchanged.
No WSL installation/fallback, global PATH rewrite, skipped control, weakened timeout/assertion or ownership/
receipt/stop/source/artifact gate is introduced. macOS prerequisite behavior remains unchanged. Workflow
change is only the reviewed run-revision1-to-2 comment; trigger/permissions/pins/serial gates remain intact.
The operational guide describes this exact scope. Protected guides, wrappers, .gitattributes, immutable
archives/releases and existing #223 behavior remain unchanged. A possible separate CRLF checkout-policy
interaction is **unconfirmed**, not an observed failure and not bundled into this repair.

## Independent final review and Linux verification

Fresh `/root/review_369_r1`, who neither implemented nor planned the repair, gives **APPROVE** for the
exact source and controlled trial; **no actionable findings**. The reviewer read the complete issue/comments/
timeline, all four changed files in full, actual shell/fixture/native executor callers, original hosted archive
and every final gate receipt/log. Exact precommit SHA/blob/read attribution is in [checkpoint metadata](../checkpoint.json).
Approval report SHA-256: `becc0070d6f049399e206e98cc8ff872417abd91b74e07ce4c55c73dd745b8fe`.
Readset SHA-256: `39a34f0e5d3c2553a22641cc068849e02f7f8810832982e7e05c7e8b3463bfdf`.

**19 final serialized gates PASS**,20:24:25.770212–20:26:48.984872UTC, at parentbb0 plus staged1187,
not a cleanbd61175 or administrative execution:

| Gate | Result |
| --- | --- |
| `python3 scripts/tests/audit-host-test.py -v` |78PASS; all65original tests/helpers unchanged,13new actual-caller tests |
| `python3 scripts/tests/audit-host-workflow-test.py -v` |51PASS |
| `python3 scripts/tests/audit-leaf-hooks-test.py -v` |27PASS |
| `python3 scripts/tests/audit-consumer-test.py -v` |32PASS; synthetic fixtures, not a real consumer build |
| Private exact-original + four mutation controls |Five intended inner failures; successful outer control oracles |
| Private unchanged-source control |Inner pass and successful control oracle |
| Wrapper integrity and Linux Git-checkout regression |PASS; not native Windows checkout certification |
| Layout, OSV lock coverage, Markdown links, release metadata, whitespace |PASS; not an OSV scan/release |
| `git diff --check`; `git diff --cached --check` |BothPASS |

Regressions drive actual prerequisites/Windows/invoke boundaries: WSL-first PATH; cmd/bin layouts with spaces/
Unicode; absent Git/Bash/tools; unsupported layout; nonzero/OSError/timeout; wrong Git/MSYS-Bash flavor;
prerequisite bypass; shared fixture PATH and unchanged native product routing. Windows process boundaries
are mocked on Linux. The exact-original driver and bare-version mutant fail the prerequisite; bare-fixture,
missing-fixture-PATH and global-product-PATH mutants fail their intended caller assertions. Inner red logs
are retained, not counted as passing product tests.

Earlier outcomes remain distinct: the first original-source attempt fails with fixture `KeyError: cwd`,
not the intended defect. Original r2/r3 then fail at the prerequisite, with modeled WSL text retained inr3.
Initial fixedr1 passes74 before four added cases; final-sourcer2 passes78 and the final cohort reruns78.
These overlapping attempts are not additive coverage. All five earlier records remain in the24-leaf seal.

## Evidence, cleanup and continuation

Verification manifest SHA-256: `63cde08f98ce27a172a7915b1aec9f5fdff6e2d1640bbc48c9d935c9cbc171be`,
**203 files/11,950,534 bytes**. The reviewer independently rehashed all203 and matched all24 invocation outcomes;
its earlier184-file readset overlaps164 of these files, so totals must not be summed. Primary review seal
SHA-256 `c5a1a9f0e5cf916b0a9cc917b3d47d819d596b432c86e0e6b9f23c1f1c1cfb2e`, late seal
`a23910ce4d531fb0b49b655158c8a285886edc50abaa47b59daaa0b5b7b0e784`.
Full-byte hashing is not a new semantic read of every evidence file.

Every one of the24 local fixture/static leaves retains its actual outcome, same task-isolated wrapper-stop0,
no invocation-owned survivors and no output deletions. Stop logs report no Gradle daemons running.
These records do not upgrade trial1's remote cleanup. There are **zero new product XML cases**; no product
Gradle `check`, real consumer build, native Windows/macOS/Apple, physical/device/hostile-network/independent/
professional-crypto/release acceptance is implied. Latest product runs remain the separately corroborated
clean731 rootcheck/CLI/Desktop1,607/199XML/205tasks and precommit6a32+stageda95 check/Androidsample/Dokka
1,607/199XML/234tasks; same identities, not additive and not rebound to369.

Obtain separate independent final-byte administrative approval, publish the focused repair/safe ledgers only
to the audit branch, and verify the ref/outcome separately. Refresh live Actions, hold one global local/hosted
build lease, freeze exact source and rerun controlled native Windows then ARM Mac then Intel Mac, inspecting
native ownership controls, strict/source/stop/cleanup receipts and full artifact identity before accepting a
component or advancing. Failures/uncertain cleanup stay red. No second trial outcome is recorded here.
Continue #268 and remaining dependency-aware repairs after actual prerequisites; preserve Binary defensive
ownership and all security/0.8.0+ restrictions. No checkpoint stop, merge, tag/settings change, release or issue
closure is authorized; source approval alone does not finish #367, independent #133 or the whole audit.

## Same #369 follow-up after the actual second trial

**2026-09-09T01:18:20.220814+00:00: final follow-up source APPROVED/local`aab8cab`, not yet pushed; trial3 NOT_EXECUTED.**
Time-only #369 events in this section occurred on 2026-09-08 UTC.
The original115-line report above is preserved verbatim as the initial638d/R3 checkpoint narrative.
Its then-NOT_PUSHED/no-second-trial wording is historical, not the current state or a broadened initial review.

Initial source`bd61175` and independently approved administration`638d005` were normally published to the
audit branch22:27:38UTC. Actual[trial2](https://github.com/p2pKit/P2pKit/actions/runs/34286015922)/attempt1
at638d/tree920191e4 fails before first audit invocation: selected absoluteGitBash returns0 with the
observed `GNU bash, version 5.3.15(2)-release (x86_64-pc-cygwin)` banner, rejected by the new MSYS-only
predicate. This is a same369correction, not a new issue, #223/#367 reimplementation or a CRLF diagnosis.
[The hosted successor](../hosted-validation.md#same-369-successor-after-second-hosted-trial) preserves
artifact10079489154, independent terminal-failure corroboration, five successful version probes and the
strict before-first-invoke boundary. Both failures remain **unsafe/PARTIAL_SALVAGE; remote cleanup/full-tree
NOT_PROVEN**. Local lease release is not safe-host/remote cleanup proof. No audit Gradle stop was applicable.

### Exact follow-up and fresh source review

- Commit`aab8cabf68958a7a3fdedcddc70c84a39e050df4`, tree`f094de05ca60a625e3b9f37abb4f3ce4f9b8dbc2`; parent`638d005784776a059c465d6940abac218e1a0d71`.
- Four-file diff SHA-256`1ccc4bc02be3c538bea3e083bda07f8203866e886dc2ece2317388f270ae7d92`; 106 insertions /3 deletions.
- Fresh `/root/review_369_followup`: **APPROVE**, no actionable findings; report`50c16b712d6bfd5099ced5c11c2064e2cc206fc0d033fa670d10d704dd02214e`,
  source-read receipt`95cfb978b2fbb6108a53671dc40ad721dbd43ded467aef6054561b398c152075`. This is exact final-source/controlled-trial approval, not
  another native run, product gate, later administration approval or completion of external obligations.

The predicate accepts exactly the two supported x64 build triplets,`x86_64-pc-msys` and`x86_64-pc-cygwin`,
only after existing installation/layout and nativeGit`.windows.N` validation. Both callers retain the
absolute boundBash and shell-only toolsPATH; nativeproductPATH/gradlew.bat remain unchanged. Wrong targets,
standalone non-Windows Git, failed version probes, WSL fallback and prerequisite bypass remain rejected.
The guide documents this boundary; the workflow change is only trial revision2→3, not triggers/permissions/
checks. Protected guides, wrappers, .gitattributes and immutable archives/releases are unchanged.

Five new tests use the exact observed banner: cmd/bin layouts both reach the actual prerequisites→Windows
wrapper/productinvoke boundary; ten invalid/malformed banners reject; nonzero probe and non-WindowsGit
controls reject. All78previous methods and their existing helpers are preserved byte-for-byte. Synthetic
Linux process boundaries are not real Windows checkout, native ownership or product execution.

### Verification and preserved failures

Finalselected**14gates PASS** at638dplus staged1ccc, not a clean latercommit or native-host execution:
- Four r1main suites:host83(78unchanged+5new),workflow51,leaf27,consumer32.
- Ten r2static/preservation/wrapper/repository/diff gates:unchanged-definition preservation, wrapper integrity, wrapper checkout, layout, OSV lock coverage, Markdown links, release metadata, whitespace, working diff and staged diff.
- Exact final windows:2026-09-08 22:58:58.224350–23:01:15.414200 UTC (r1 suites), 23:03:36.420918–23:03:47.679239 UTC (r2 static).

The clean original-driver r3control executes five methods:two intended classifier errors,three passing
rejection controls. Earlier originalr1`FileExistsError` and originalr2missing-fresh-Hostownership give harness
failures (r2ten fixture failures plus two actual classifier errors); they remain failed attempts, not the
clean reproduction. Greenr1passes83. A private preservation checker later rejects a legitimate duplicate
nested`mutate`name; ordered-list keying corrects the inspector without changing repository bytes. That
failed r1gate remains historical and is not called a product/source defect or silently promoted toPASS.

Verification seal`d98384424b28620a7f85f801fb6230882a30e6cda9918d6f0ddf079ebdee8e77` binds143files/464,948bytes and19
finalized invocation receipts, including all earlier outcomes. All 19 retain their original outcomes, wrapper-stop 0, no invocation-owned survivors, source unchanged within each invocation and no output deletions. Stop logs report no Gradle daemons; this does not prove remote cleanup.
Those 14 fixture/static gates produced no product XML and are not a follow-up Gradle product run, native
Windows or Apple evidence. The separate clean-bd611 check+Android/CLI/Desktop/Dokka result (1,607 cases,
199 XML files, 245 tasks) is recorded in the hosted report with independent evidence corroboration; it
does not validate the changed predicate or add unique case identities. Any newer clean-follow-up product
observation is recorded separately in that report with its own execution and review boundary.

This same-#369 correction adds no second row. Separate #370/#371 yield **74/185 independently approved repository-repair rows (40.0%), 111 remaining (60.0%): 89 repairs, 21 external validation rows and architecture decision #120; 52 new audit findings**. The 2026-09-08 22:39:48 posted/refetched #369 comment records initial publication and failed trial2/pending correction, not final follow-up approval. No final follow-up publication/outcome is recorded here.
Independent final-byte administrative review, normal audit-branch publication/ref check and a new controlled
native trial remain required. Keep failed/uncertain cleanup red; #268/#133NOT_STARTED and wholeauditNOT_READY.

The [new clean-aab8 product observation](../hosted-validation.md#new-clean-followup-product-observation) has separate independent evidence corroboration. The later [#370 SDK repair](370.md) is a distinct row/source verdict. The subsequent [#371 fixture byte-emission repair](371.md) is also distinct; neither is a #369 reimplementation or retroactive product result.
