# #415/#416 — focused embedded JmDNS repairs

**Finally APPROVED, committed NOT_PUSHED at this administrative input (2026-09-11).**
202/232 repair-approved (87.1%),203resolved (87.5%),29unresolved (12.5%):5repairs+21external+3owner.
The prior200/201/31 projection is preserved at published `194da7f`; #410 receives no repair credit.

| Issue | Commit / tree | Complete committed semantic-diff SHA256 |
| --- | --- | --- |
| #415 | `628336b97afafcbbb0481e02e674b25a2ee4715f` / `98a27ba71ca40c4040b262cf90eef5cd22e8f6d2` | `37d0be841d02cd032e601aac6da803866972ae65111a8209eca51f42f6f30b1a` (14files,181+/49-) |
| #416 | `593175ff935d447907e2aa466f17072217ea5907` / `0056bd96b35e18a2c03dcc76d615a69ae5949439` | `87bde6c327fd259d6ca6e69513c667b337ddbd6d36d405fff6df20352576b9af` (13files,166+/23-) |

#415 corrects whole-OPT-TTL extended RCODE extraction before combining the low header bits. The
established caller is a wrong-version diagnostic, not receive acceptance; arithmetic ran, while the
warning caller was source-reviewed (no emitted-warning claim). #416 recognizes only terminal,
bounded `in-addr.arpa`/`ip6.arpa`, case-insensitively, preserving original prefix/case and excluding
ordinary instance substrings and nonterminal/unbounded markers.

| Actual repository Java/JUnitCore invocation | Preserved preimage | Corrected postimage |
| --- | --- | --- |
| `415-junit-{preimage,postimage}-r1` | 2methods/1failure; expected Unknown65535, got NoError0;1/0/1 | Same2methods pass;0/0/0 |
| `416-junit-{preimage,postimage}-r1` | 5grouped methods/4failures;1/0/1 | Same5methods pass;0/0/0 |

Exit triples mean command/stop/final. Receipt XML totals0 do not negate actual JUnitCore execution.
#416 postimage executes real DNSEntry maps and DNSRecord.Pointer.getServiceInfo for canonical,
subtype/unqualified, reverse and ordinary/secure service/host/meta cases, **not listener dispatch or
network**. Preimage grouped assertions short-circuit, including before Pointer; not every later case
independently failed. Both original failing invocations remain evidence, not hidden reruns.

`415-provenance-regressions-r1` and `416-provenance-regressions-r1` each pass0/0/0:22SBOM methods,
11licenseZIP and67embeddedZIP/metadata fixtures, actual upstream inventory/OSV-input coverage,
forward/reverse patch equality and shell/whitespace controls. These are distinct controls, not an
additive product-suite total. Schema2 preserves410's lifecycle patch, then ordered415/416 patches;
private identity410.2→410.3 binds source/resources/packaging/SBOM/consumer expectations, not an
actually qualified published consumer. #416 leaves410and415patch bytes unchanged.

`415-gradle-configuration-r1` passes strict Gradle `help`0/0/0 (3executed tasks), **configuration only**;
Linux Apple-cinterop warnings remain. #416's Gradle edits only changed two patch rosters and were
source-reviewed; mechanism-level415help is reused, not a fresh416configuration run. No complete
locks/fullcheck/LAN Dokka/ABI/native/ART/consumer qualification follows.

All four415envelopes used `194da7f`+staged37d0be; all three416envelopes used `628336b`+staged87bde6,
unchanged, **not clean628/593executions**. Each preimage selects original production with the same
new repository test. Exact UTC windows, receipt/log hashes and source bindings are centralized in
[checkpoint.json](../checkpoint.json) `nativeFollowups.repairs415416`; raw evidence remains private.
All stops0/no owned survivors; owned build outputs and newly created project caches removed,
shared caches/source build package preserved. All12locks/protected instructions unchanged.
Administrative read scope: complete semantic diffs/small receipts, selected JUnit commands/results,
not whole existing source files or full large hash-roster logs; no tests rerun by this author.

Final independent reviews were root-relayed conversation-only. `review_415_final` clean-source
rebind was observed18:23:10.770649031UTC; `review_416_final` final evidence inspection was observed
18:42:39UTC. **Neither is a verdict clock**: `APPROVE_FINAL_415`/`APPROVE_FINAL_416` completion/
delivery is UN_CLOCKED/UNRECORDED. #416 had no actionable findings; no review-report path/hash invented.
Earlier194dapublication18:16UTC is not415/416publication.

**#413 is active PLANNING/compilation-admission preparation; implementation/runtime NOT_STARTED,
native admission unknown**, then414real Kotlin caller boundary and409conditional readiness.
#410 remains PARKED/PENDING_REMEDIATION, final WITHHELD; return only with new admission evidence,
no unchanged full-graph retry, lock bypass or route/permission override. #417 remains unimplemented
under existing restricted planning, not retried/rephrased/delegated. Audit NOT_READY/#133NOT_STARTED;
physical/hostile/ART/independent/crypto/final-current gates and owner120/274/284 remain. Prior failures,
source0.7.0-SNAPSHOT, immutableRC3 and existing0.8.0+restrictions are unchanged.
