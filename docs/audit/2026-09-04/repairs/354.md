# #354 — Android hotspot cleanup retry

**Reviewed correction is pushed to `audit/complete-2026-09-04`; not merged or released.**
The issue remains open under the completion policy. This is a scoped sample repair,
not a native-cleanup or production-readiness certification.

## Exact revisions and corrected behavior

- Baseline: `10b0fba16a3a8457a57c8e58cf3bc457eb072d82`.
- Initial correction: `12e6cfac9ccd510b31b52945fbfd39ce04aaa457`.
- Final correction: `b6af5b8b78bd846be9f456438ef0586aa10569bc`.
- Final tree: `909690f39b071971c566a792b462a7362c95580e`; clean at final static verification/review.

The Failed card previously retried Start after Stop failed, even though the Android
manager refuses Start while retained hotspot cleanup needs Stop. The presenter now
keeps explicit, unconfirmed stop intent separately from resource-liveness claims.
**Retry hotspot stop** calls Stop without acquisition permissions; successful
cleanup clears that intent without releasing an independent join. Failed/cancelled
stops remain retryable. Kit retirement clears presentation without claiming cleanup
succeeded; untagged cleanup events neither establish nor acknowledge stop intent.

Ordinary startup failures still retry startup. Ambiguous startup `CleanupFailed`
also exposes explicit hotspot Stop and accurate whole-kit fallback guidance.
No library ABI, native manager, manifest permission or wire protocol changed.

## Independent review and correction

R1 returned **REQUEST_CHANGES** at `12e6cfa`: a stale acquisition callback could
request permission after Stop admission by using the last composition's permission
list. `b6af5b8` moves permission admission behind the presenter's current-owner,
busy and stop-intent checks. The VM refreshes permissions only inside that admitted
startup path. Three added regressions cover stale callbacks, deferral/grant retry
and retirement before rendering.

Fresh reviewer `/root/review_354_r2` inspected the complete five-file baseline-to-final
diff, actual manager/callers, #337/#324 contracts, tests and retained evidence, and
returned **APPROVE**, with no remaining actionable #354 finding. It independently
verified 1,901 evidence-manifest entries, final test XML and exact tested patches.

Private R1 report SHA-256: `63ba8db41ade59d7464261efb89ea6212e2ffcf850a93bed08dd3f40670747b5`.
Private final R2 report SHA-256: `79a5ab673868d5969f6c2a2b1a124249908a7d8a5183c880d0ef92c09cd4f356`.
These hashes identify private reports; the raw artifacts are not included in a clone.

## Verification and provenance

Final affected command:

```sh
./gradlew :p2p-sample-android:check :p2p-sample-android:assembleDebug \
  :p2p-network-provisioning-android:check --console=plain \
  --max-workers=2 --no-parallel --rerun-tasks --no-build-cache \
  --dependency-verification=strict -Pkotlin.compiler.execution.strategy=in-process \
  '-Dorg.gradle.jvmargs=-Xmx2048m -XX:ActiveProcessorCount=2 -Dfile.encoding=UTF-8'
```

| Gate | Result and exact scope |
| --- | --- |
| Negative pre-correction action extraction | 45 cases, one expected assertion failure: Retry made one Stop call instead of two. Not untouched-baseline or rendered UI execution. |
| Initial affected invocation | Compilation failed on a missing `OutlinedButton` import; corrected before subsequent runs. |
| Initial corrected focused run | 84 sample + 144 provisioning cases passed; superseded by final affected verification. |
| Final affected run | **87 sample + 144 provisioning cases passed**, zero failures/errors/skips; assembly/lint/ABI passed; 150 executed tasks. |
| Final static gates | Layout, OSV lockfile coverage, Markdown links, release metadata and full-range whitespace passed at clean `b6af5b8`. |
| Earlier integrated `./gradlew check --console=plain` with the same bounded/strict/rerun options | **2,225 passed, zero failures/errors, one unchanged manual interop skip**; 205 executed tasks, at clean **`12e6cfa`**, before the final permission-admission correction. |

The final focused run began with `12e6cfa` plus a patch byte-identical to
`12e6cfa..b6af5b8`; it is not described as a clean-commit run. Earlier integrated
results do **not** verify `b6af5b8`. Fresh combined-tree integrated/release checks
remain pending. Do not add overlapping scoped test counts.

Every invocation, including failed and negative runs, stopped its applicable
wrapper, checked/terminated invocation-owned survivors and removed disposable
generated outputs after retaining evidence. Source, shared caches and stashes were
preserved. Neither reviewer started builds or background workers.

Controller/fake-manager/source-wiring and Android host-shadow results are not
rendered Android UI, real permission dialogs, ART/OEM or physical cleanup proof.
The earlier integrated run includes Kotlin/Native arm64 simulator tests, **not
Swift XCTest**. Physical Android/Apple, hostile networks, independent protocol
interoperability and professional cryptographic review remain pending.

Progress after this final approval: **48/168 reviewed repairs (28.6%)**, **120
remaining (71.4%)**. #317 is next, followed by the dependency-aware queue. `main`
remains unchanged; no issue was closed and no release was published.
