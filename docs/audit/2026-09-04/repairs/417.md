# #417 — preserve response context across JmDNS overflow

**APPROVED for the exact source repair and bounded serializer regression.**
Independent final review at2026-09-12T05:29:21.293800UTC found no actionable issues.

- Commit: `b17ea286d8f5c189c07826706a05e25f1239abd2`.
- Tree: `b7e388fd4892348be8e5a0ab62e72c32339c2859`.
- Twelve-file diff SHA256: `f504e932ec040d1ed0188e0172dcf8e9abf138cc9a5a1a823b9bf9483f87c6ae`.
- Review SHA256: `238eebb072c57d0843d8c748579fc92797f5e88a859810207cf5f784000c645c`.

Normally pushed with2f99f4e; remote source verified05:38:13.949920UTC.
[Public outcome](https://github.com/p2pKit/P2pKit/issues/417#issuecomment-5643876577)
was POST/GET-verified05:40:06.142187UTC. The issue remains open.

The shared `DNSTask` continuation preserves the requester destination and effective
transaction ID before flushing, then restores them on the replacement packet.
Flags, multicast-ID policy, capacity, TC marking, record order/retry and exception
propagation remain unchanged. The protected send seam forwards to the existing
sender. Private producer revision `.410.5` and its five ordered patches are bound
consistently through provenance, version, SBOM policy and consumer expectations.

## Bounded verification

| Invocation | Retained result | Command/stop/final |
| --- | --- | --- |
| `417-preimage-r1`, `417-preimage-r2` | Test-compilation/package-access setup failures; not reproduction | Each1/0/1 |
| `417-preimage-r3` | JUnit4.13.2:5 methods,3 intended destination/ID assertion failures | 1/0/1 |
| `417-postimage-r1` | Same regression source:5 methods passed | 0/0/0 |
| `417-provenance-r1` | 6 SBOM methods,11 license ZIP and67 embedded ZIP/metadata fixtures; inventory/patch/shell controls passed | 0/0/0 |

The regression uses real TXT construction, outgoing serialization and incoming
parsing, covering repeated continuations, all five add helpers, record contents
and order, multicast defaults, non-overflow, send exceptions and oversized records.
Its unscheduled subclass creates no socket or JmDNS runtime. JDK17 compiled all60
vendor sources with `--release 8 -Werror` and the test with `--release 17 -Werror`.
Raw JUnit logs establish the counts; zero receipt XML is not zero test execution.

Executions bind7bb5686 plus their recorded staged diffs, not a cleanb17ea28 run.
The later upstream-inventory manifest rebind changed no compiled input. Ordered
five-patch replay and10-populated-lock coverage passed; neither is an online OSV
scan. All five receipts retain stop0, unchanged source and no owned survivors.
Exact stage, command, log and receipt bindings are in [checkpoint.json](../checkpoint.json)
`nativeFollowups.postR23Continuation.repair417`; raw evidence/helpers stay private.

## Limits

No live Responder scheduling, socket delivery/requester reception, Android runtime,
native lifecycle/#410, produced publication/SBOM artifact or release qualification
is established. The common Java8 producer preserves JVM/Android source parity;
that is not Android execution. Ordinary Gradle was not run during that repair-verification stage.
The later clean `7d21aef` ordinary request failed the whole graph on stale locks,
but produced the retained 295,670-byte private JAR, SHA256
`8ae77118c5d88685cc9df3d8a30d8c239c361e6784403590a7686dba466dac7b`.
Its separate producer/failed-graph review is in `postR23Continuation.linuxOrdinary`;
no LAN tests, outer publication/SBOM or full root/sample/consumer/ABI acceptance follows.
Those gates remain unwaived. This approval adds one repair row,
not issue closure, external acceptance or audit readiness. Prior failures remain.
