# #223 — pin the POSIX wrapper's checkout line endings

- Issue: <https://github.com/p2pKit/P2pKit/issues/223>; **Low, checkout/developer-experience defect**,
  not evidence of wrapper tampering or a production/API/protocol defect.
- Approved source: `8dd65ce3f4e8f0274c24d09a667b11c653d9eb4b`, tree
  `facccdaae07e66ae7d3017ef8525c09611b2c70a`; parent `dfdb36744ea2a33f4b9e6455b4359e4639e8a359`.
- Exactly `.gitattributes` and `scripts/tests/check-gradle-wrapper-test.sh` changed (77 insertions/1 deletion); patch SHA-256
  `552870a648c4bbd6c4e8327bf2461c790f8b16b614e98d95ac0a69322009b541`.
- Fresh independent `/root/review_223_r1`: **APPROVED**, no actionable findings. Source review SHA-256
  `ef1bc19690ed2d78d4731504d80b390feb24e7f6fa79305d36d96ef9c384a47f`.
- Source pushed/ref-verified **2026-09-08 12:30:53 UTC**;
  [outcome](https://github.com/p2pKit/P2pKit/issues/223#issuecomment-5585231528) posted/refetched
  **12:36:30 UTC**, body equality verified, issue OPEN. Not a merge, release or closure.

## Confirmed cause and correction

The wrapper checker hashes the worktree launcher against an independently reviewed constant. With no explicit
`gradlew` line-ending attribute, a real isolated Linux Git checkout of the unchanged `c221d54` inputs produced
**248 CR bytes** under `core.autocrlf=true`. The committed/index launcher stayed LF and `gradlew.bat` stayed LF.
The actual unchanged checker exited **1** with exactly `FATAL: gradlew checksum mismatch`; `false` and `input`
controls passed. This is fresh Linux reproduction of checkout conversion, not the issue's earlier Windows claim.

The correction adds `gradlew text eol=lf` and a short explanation. The existing batch LF rule, ABI EOF exception
and three exact immutable-archive rules are unchanged. Wrapper files, reviewed hashes, versions, dependencies,
production source and protected guides are byte-identical. No blanket text normalization was introduced.

The strengthened test uses an actual isolated Git repository in a spaced path, with global/system configuration
and attributes isolated and hooks/signing disabled. It re-checks out only both launchers under `false`, `input`
and `true`, runs the real checker, verifies unchanged index/HEAD content and requires exact `text eol=lf` attributes.
Missing-POSIX-LF and explicit-POSIX-CRLF mutations must fail at the exact POSIX checksum diagnostic; a restored
`true` checkout passes. All four existing wrapper-tamper controls remain.

Actual CI, release, dependency-maintenance/range and Desktop workflow callers retain their original wrapper
checks and host obligations. The complete archive subtree is unchanged; #301/#303 retention decisions and
#360's independent hash tripwires remain intact. No broader all-text-file checkout behavior is certified.

## Verification and preserved controls

The primary performed these serialized invocations; neither this administrative preparation nor the independent
reviewer reran them. Exact command arrays, source/diff bindings and receipts are recorded in
[checkpoint metadata](../checkpoint.json) and retained private evidence.

| Invocation | Result and binding |
| --- | --- |
| `223-original-checkout-r1` | Original `c221d54` inputs: `false`/`input` checker PASS; `true` checker FAIL1/248 CR bytes; outer reproduction oracle exits zero and fixture is removed |
| `223-wrapper-regressions-r1` | PASS on precommit `dfdb367` plus the recorded final-candidate two-file diff; not a clean `8dd65ce` execution |
| `223-original-attributes-negative-r1` | **FAIL1 as intended** with original attributes plus final regression; failure occurs at the `true`-mode POSIX checksum, then corrected attributes are restored exactly |
| `223-precommit-whitespace` | PASS on the recorded precommit two-file candidate |
| Thirteen `223-final-*` gates | **PASS** on clean committed `8dd65ce`: wrapper, wrapper regression, archive-whitespace regression, dependency verification, layout, OSV lock coverage, Markdown links, release metadata, actual whitespace/dependency ranges, dependency regression, CI policy and release-workflow policy |

The final wrapper suite retains four wrapper-tamper controls and adds two checkout mutations. The archive suite
executes all 51 labeled assertions. These static/policy tests are not product XML cases or a new Gradle product
`check`. Repeated wrapper/CI-policy executions are not additive coverage. The original-attributes rejection and
all earlier #145/#367/#368 failures remain their real historical results, not relabeled passes.

The latest recorded product checks remain the separate #368 source at **`c221d54`**: combined
check/Android sample/strict core Dokka **1,607 passes, 199 XMLs, 234 tasks**, and separate root check
**1,607 passes, 199 XMLs, 194 tasks**. Those commands were not rerun at `8dd65ce` or this administration.

## Independent inspection, evidence and cleanup

The reviewer inspected the complete issue/current callers, related decisions and exact two-file correction;
20 complete tracked-file reads have a genuine source-bound receipt. That read receipt does not itself approve
unrelated files, later administration or platform execution. Read-scope SHA-256:
`3e36a7c6f224e54164053c6b64c44657f6a83ded4eaefc46bd0e59361b209a10`.
Independent evidence-inspection SHA-256:
`3588434db052c54d4b35c3ca694198891c0acfe0a0f445c04562145ea9c3f07b`.

The private seal contains **215 unique regular files / 3,129,690 bytes**, independently rehashed with no mismatch.
Manifest SHA-256: `9174079a4d05d2d143dd929a37457cfbdafd4e91c236220f7509598f37123367`.
All **17 own serialized leaves plus a separate sealer**, including the expected failure, retained logs and finalized
task-isolated wrapper `--stop` with exit zero, stable source and no owned survivors. Fixture removal was verified.
These were policy/fixture invocations, not a product Gradle build. Sealer cleanup ended **12:22:07 UTC**. Source, protected
guides, shared caches, private evidence and unrelated tasks remain; `buildSrc/src/main/java/dev/p2pkit/build` is source.

## Limits and continuation

This is Linux Git/shell evidence, not fresh native Windows/macOS, Apple, ART/device or hostile-network validation.
Only launcher checkout conversion is covered; unrelated CRLF-sensitive text files and final consumer/release gates
remain separate. Existing clones with local changes must not be blanket-renormalized or overwritten.
The **12:28:25 UTC all-state list** observed 289 issues/182 open/79 PRs, not newly refreshed full histories;
the 12:36 outcome verification is individual, not a complete post-write refresh.

Continue the authorized serialized hosted-validation facility first; its private preparation is not an approved
revision or execution in this record. Freshly reproduced prerequisites require sequential repair and independent
review before planned #268 and the remaining dependency-aware queue. #268 is not started by this report.
Physical-device, independent-interoperability and professional-crypto acceptance remain external.
**#133 NOT_STARTED; whole audit NOT_READY.** No administrative checkpoint stop is requested.
