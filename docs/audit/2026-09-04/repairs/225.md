# #225: enduring version-input policies without weakening approval tripwires

## Disposition and source

- Issue: [#225](https://github.com/p2pKit/P2pKit/issues/225), Low policy/fixture defect, **OPEN**.
- **Independently approved and pushed** on `audit/complete-2026-09-04`; not merged or released.
- Baseline: `4ccef762571a6132919f63585c51ea8269f67405`, clean tree `24fff2844fb99cd9f8acacc749a4126085e7803a`.
- Complete seven-file correction: `82be9c58f82d28253d9abc72b300caef8e681086`, then reviewer correction
  **`46ed124ded59f6cbdcccbec3df1140b994efad69`**.
- Final frozen tree: **`b9d0ee3010aa15f46672a76b9de0a4522bb7e307`**, clean at verification/review.
- [Verified investigation](https://github.com/p2pKit/P2pKit/issues/225#issuecomment-5571076317) and
  [outcome](https://github.com/p2pKit/P2pKit/issues/225#issuecomment-5571608911).

## Correction

Baseline static gates accepted arbitrary hardcoded Kotlin consumer versions and mixed Netty locks containing older
or unsupported-qualified versions. Historical denylists did not express a lasting invariant. The current Netty floor
is **4.1.137.Final**, not the original report's older value.

`scripts/check-version-input-policy.rb` requires the maintained expanding consumer root-build heredoc to use Kotlin/AGP
input variables and rejects numeric plugin pins elsewhere. This is a source-format policy, not a general Bash/Kotlin
parser. Every active Netty lock row is numerically compared with the independently supplied stable-version floor;
malformed rows, unsupported qualifiers and missing active HTTP entries fail closed. The positive HTTP pin remains.

Independent Kotlin/BCV, wrapper URL/checksums, XcodeGen version/checksum, Netty and jsoup expectations are preserved
and their update counterparts documented. The fake XcodeGen input follows the sourced installer; the same assertions
run against a synthetic future input. **The real installer is unchanged.** The reviewer requested precise documentation
of the actual live-lock entry point, `scripts/tests/release-workflow-test.sh`; the second commit corrects that one line.

No production/API/ABI, dependency/lock, workflow, deployment-target or protected-instruction changes. Related #210
remains closed as invalid; its Apple-policy decision is not reopened. An unrelated installer hypothesis remains
[explicitly unverified](../followups.md#xcodegen-version-probe-status-and-failed-install-cleanup).

## Verification and independent review

- Four invalid baseline mutations accepted by the actual callers; independent Kotlin/wrapper mutations still reject.
- **24 final caller controls** complete with expected outcomes at `82be9c5`: retained independent-tripwire families,
  actual outer callers, future inputs and disabled-guard controls. One expected-zero case demonstrates the old fixture
  ignoring changed input, not acceptance of the fix. Only the reviewed documentation line changed afterward.
- **14 final gates** pass at `46ed124`: input-policy suite (**39 negative controls**), toolchain/release policies,
  synthetic installer (**14 assertions**), wrapper/checker mutations, ShellCheck/syntax, layout, OSV lock coverage,
  Markdown links, release metadata, dependency metadata and actual-range whitespace.
- Frozen-source full invocation:

```bash
./gradlew check :p2p-sample-android:assembleDebug \
  --rerun-tasks --no-build-cache --no-configuration-cache \
  --no-configure-on-demand --dependency-verification strict \
  -Pkotlin.compiler.execution.strategy=in-process \
  '-Dorg.gradle.jvmargs=-Xmx2048m -XX:ActiveProcessorCount=2 -Dfile.encoding=UTF-8' \
  --console=plain --max-workers=2 --no-parallel
```

Result: **2,276 passes, zero failures/errors, one unchanged manual LAN interop-capture skip; 278 XMLs, 246 executed tasks**.
JVM, Android host and Kotlin/Native arm64 iOS simulator tests are included—not Swift XCTest, Intel execution or ART/devices.

Fresh reviewer `/root/review_225_r1`: **APPROVE**, after the documentation correction and final revision/evidence review.
It inspected the complete diff and surrounding callers, independently reconciled all **1,932 evidence entries**,
recounted all 278 XMLs and checked **101 finalized cleanup receipts**. No actionable findings remain in #225.

Review SHA-256: `8253854daa2d777bb8a015a2645b9e881450286a1dbfc657c5b69f6464846698`.
Manifest SHA-256: `ba933e08e62aff07a060d878b8dc52e4f21c2302f7e1011bed2747cb49967f0e`.
Raw evidence stays private; these hashes identify retained evidence, not downloadable logs. Exploratory ShellCheck
failures and a first caller-driver selector failure are preserved; the complete corrected runs supersede them.

## Cleanup and remaining scope

Every invocation finalized wrapper stop and scoped worker/output cleanup, including failed/negative attempts.
No generated project build outputs remain. Protected guides, original stashes, user files and shared caches are preserved.
The reviewer started no builds or background workers.

This is not hosted CI, real XcodeGen installation, independent publisher/protocol/crypto assurance, physical devices,
hostile-network acceptance or whole-audit approval. Earlier #133 publication/consumer evidence remains bound to `7127616`,
not rerun here; independent interoperability remains NOT STARTED. Full release/consumer/Swift/XCFramework gates and
whole-repository corroboration remain pending. Issue OPEN; overall **NOT READY**.

Checkpoint: **57/169 reviewed repository repairs (33.7%)**, **112 remaining rows (66.3%)**. External acceptance on approved
rows is not counted complete. Next: #226, then the remaining queue sequentially.
