# #367 — current host-toolchain artifact verification coverage

**8 September 2026: repository-source correction APPROVED; issue remains OPEN.**
This bounded approval does **not** complete every foreign-host acceptance gate in
[the full issue](https://github.com/p2pKit/P2pKit/issues/367), approve the whole audit or authorize release.

## Exact source, independent review and publication

- Original investigation baseline: `0423e96e15c894c26a7d45a5dfb2a012e0c1ba8e`, tree
  `26984a1b7d2048bdbda8e1808bb38f299e7a6866`; affected metadata equals transfer `d823bb9`.
- Complete five-file correction: `0d88be3e3769b5133a985e4fe2fca782ba0ef2a6`, tree
  `db432eb14791d9637e812333305e811edceb834e`; parent `9316607618bf9710dffc36a3ba95185b24712152`
  is separate #145 administration. The source delta equals the original `0423e96`-based review packet.
- Full-index binary diff SHA-256: `591ea0d2143e9d94ac8b448b31f9789fe7565438b6f480112301021a9266b627`.
- Fresh reviewer `/root/review_367_r1`: **APPROVED**, no actionable source/provenance findings.
  Review SHA-256: `ead4d4b561f9bb9723c00380b881c92dfe3161cbbd3dcc2b209e95299ecbd386`.
- Source push/ref verified **10:59:45 UTC**; main observed unchanged at
  `eb444cccfc290be5435c5c10629c24183293606f`. [Approved outcome](https://github.com/p2pKit/P2pKit/issues/367#issuecomment-5584111070)
  posted/refetched **11:01:17 UTC**, body equality confirmed, issue OPEN.
- Private evidence manifest: `6ba9c96944e71638df4845db6c5c85beaf9b01b851884d679e5072026ed6c768`:
  **2,160 files / 21,605,300 bytes**, 20 own serialized invocation leaves plus a separate successful sealer.
  The two shared original #145 failed invocations and shared #368 diagnostic are not additional own leaves.

The reviewer did not implement the fix or run builds, tests, GPG or downloads. Review covered the exact diff,
all four changed code/document files, metadata configuration and the complete old/new affected component
subtrees, callers and retained execution evidence. It did **not** read all 11,822 XML lines as full-text
corroboration; full-file hashing does not expand that scope. XML testcases were independently recounted.
Later administrative or #368 edits do not inherit this exact-source approval or verification.

## Confirmed cause and correction

Strict dependency verification correctly rejected unlisted current host artifacts. The original metadata
admitted only Native's macOS arm64 archive and AAPT2's `osx` JAR, plus POMs. Linux Native/AAPT2 failures
were freshly reproduced during #145; Windows x64 and Intel Mac absence was statically confirmed.
Eleven affected failed-task entries repeated two missing Linux records, not eleven distinct defects.
This is Medium non-security fail-closed build availability, not a checksum bypass, runtime vulnerability
or #145 regression. Original failed commands remain failures in the unchanged [#145 report](145.md).

Exactly five artifact/SHA-256 records were added. Existing Mac/POM/historical records, versions, locks,
wrapper and strict trust configuration remain unchanged; no required task or check was disabled.

| Added artifact | Admitted SHA-256 |
| --- | --- |
| `kotlin-native-prebuilt-2.4.10-linux-x86_64.tar.gz` | `c9e356e8518144f275f1514cfe38b07db949f93e47e054832b8974fff1fd33e0` |
| `kotlin-native-prebuilt-2.4.10-macos-x86_64.tar.gz` | `7bfda60c2a4ce859fc85011ea2c3229961b1eb40e9cc0b6b85fee885f23973cb` |
| `kotlin-native-prebuilt-2.4.10-windows-x86_64.zip` | `ce99eba1f4faec1d77f4bbd747bb722404ef11f2c349ec70c59d4c002859380f` |
| `aapt2-9.3.1-15703166-linux.jar` | `e772a3dae8354764f1b0793903218427f483982445207f2e4ffc8c2026755bd4` |
| `aapt2-9.3.1-15703166-windows.jar` | `b1006ecec7e5936257e95e97f3eba7ef439d3e44178967cc048f86c9119fb231` |

The unchanged maintained curator downloaded and matched all five byte hashes and actual detached signatures.
Kotlin signer `6F538074CCEBF35F28AF9B066A0975F8B1127B83` is paired by actual `VALIDSIG` with primary
`2FBA29D08D2E25EE84C132C30729A0AFF8999A87`, independently anchored by official
[Kotlin security guidance](https://kotlinlang.org/docs/security.html). Native hashes also match official
JetBrains release **archive** digests; Maven SHA-256 sidecars were absent, not successfully verified.
Google signer `0E225917414670F4442C250DFD533C07C264648F`, independently named by Android verification
guidance, is paired with official Google primary `EB4C1BFD4F042F6DDDCCEC917721F63BD38B4796`.
Both Google authoritative SHA-256 sidecars match. Raw Google `KEYEXPIRED` warnings concern other historical
subkeys; the expected signer is valid through 2028. Successful `GOODSIG`/`VALIDSIG` and all warnings remain
evidence. Fresh-keyring trust/UIDs alone were not publisher identity, and curation is not a professional
cryptographic audit, arbitrary-key trust or reproducible-build proof.

The new policy independently pins Kotlin/AGP/AAPT2 versions and requires four Native hosts plus three AAPT2
classifiers. Ten methods call the real checker with independent fixture expectations: every missing/renamed
host, changed coordinates/version/catalog, duplicate components/artifacts, and absent/malformed/multiple/
alternative SHA-256 records. Exact original Mac-only metadata also fails through the actual outer policy;
corrected bytes were restored. Historical records may coexist. The Kotlin toolchain policy unconditionally
calls checker/regressions and is used by maintained CI/release gates. [Local testing guidance](../../../testing/local.md)
documents cross-host curation. This is a completeness/format tripwire, **not** authentication of any arbitrary
well-formed hash; future bytes still require publisher review and separate host execution.

## Fresh execution and retained failures

The primary serialized all invocations using strict verification, uncached task reruns, in-process Kotlin,
at most two Gradle workers and `--no-parallel`. Gradle 9.7.0 was unchanged; the documented JDK17 launcher
and configured JDK21 daemon were used. Actual Gradle daemons had a 2 GiB heap / 768 MiB metaspace bound.
Early policy/curation runs retained dirty candidate snapshots with separate #145 administrative changes;
their five-file contents match the final patch. The three Gradle reruns below used clean exact `0d88be3`.

| Invocation/evidence | Actual result |
| --- | --- |
| Original #145 combined check/sample/Dokka | Historical **FAIL before tests**, six tasks, missing Linux Native checksum |
| Original #145 affected command | Historical **FAIL**, 851 passing core tests/100 XMLs/97 tasks; two missing Linux artifacts repeated across eleven failed tasks |
| Maintained publisher curation | **PASS**, exactly five artifacts/signatures, official digest/identity comparisons above |
| Host completeness / regression methods | **PASS**, seven required records / ten methods with negative subcases |
| Exact original metadata outer-gate control | Expected inner **exit 1** for missing Native Linux; corrected bytes restored exactly |
| Outer Kotlin policy and twelve static/range gates | **PASS**; no weakened required gates |
| Clean Linux affected command | **PASS**, 851/851 (789 JVM + 62 Android-host), 100 XMLs, 110 tasks; no failures/errors/skips |
| Clean combined `check`/Android assembly/core Dokka | **FAIL**, 1,297 passes + one failure, 1,298 cases/151 XMLs/172 tasks, no errors/skips |
| Separate clean `./gradlew check --console=plain` | **FAIL**, same counts and diagnostic case; not a new full-check pass |
| Message-only #368 diagnostic | Intended assertion **FAIL**, one case/no errors/skips, 12 tasks; original source restored afterward |

The affected task selection was `:p2p-core:jvmTest :p2p-core:testAndroidHostTest :p2p-core:checkAndroidAbi
:p2p-core:checkJvmPublicConstants :p2p-sample-android:assembleDebug :p2p-core:dokkaGeneratePublicationHtml`.
It passed actual Native Linux download, AAPT2 resource processing, Android sample assembly, strict core Dokka
and Android ABI/compiled-constant guards. Common strict flags for the affected and both full-check invocations:

```text
--rerun-tasks --no-build-cache --no-configuration-cache --dependency-verification strict
-Pkotlin.compiler.execution.strategy=in-process
-Dorg.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=768m -XX:ActiveProcessorCount=2 -Dfile.encoding=UTF-8
--max-workers=2 --no-parallel --console=plain
```

The JVM-args value was passed as one shell argument. Only the affected task invocation also used `--continue`.
No counts are summed across overlapping reruns. Actual command arrays, logs and XML remain private.

Both whole-gate failures are
`DiagnosticApi24CompatibilityTest.exportFailureWithoutApi26CleansOwnedStagingAndKeepsDestination`.
The fixture obstructs lowercase `test_…zip`, but the recorder normalizes the ID and the exporter targets
`TEST_…zip`; source blobs predate #367. Diagnostic-only failure text proves `failureType=null`, normalized
`TEST`, both differently cased paths and `exportRecorded=true`. The exporter succeeded at the unobstructed
path; `IllegalStateException` is the fixture's failed check, not an exporter exception. The instrumented
assertion/setup and every preservation check remained, then the message change was restored exactly.
This distinct Low test-portability defect was duplicate-checked, filed and refetched as
[#368](https://github.com/p2pKit/P2pKit/issues/368) at 10:50:42 UTC. It is not fixed by #367.

## Cleanup, continuity and next acceptance

Every invocation preserved evidence, ran its task-isolated wrapper `--stop` (exit 0), then narrowly retired
invocation-owned survivors and removed confirmed disposable outputs after dependent verification. No owned
survivor remained; wrapper stop alone is not claimed to synchronously remove every worker. Owned inner/ambient
GPG homes and artifact workspace were retired. The separate sealer finalized cleanup at 10:58:38 UTC.
Source, `AGENTS.md`, `CLAUDE.md`, shared caches and `buildSrc/src/main/java/dev/p2pkit/build` remain intact.

The seal includes retained shared failures; it does not reconstruct missing historical raw evidence or put
private payloads in the clone. Hashes/reports are not fresh executions. Original #145 harness/deprecation,
assertion-oracle/Android Log fixture and integrated failures remain unchanged historical evidence.

Next: repair/review #368, then #223 and a serialized hosted-verification facility; rerun actual full checks.
Windows AAPT2/Native, Intel Mac, Apple simulator/Swift/XCFramework and final consumer/release acceptance remain
**NOT_EXECUTED** in this cycle. GitHub Actions is authorized, but curation/Linux download/cross-compilation
does not satisfy those gates. #367 source approval does not waive them. Host/classloader tests are not ART,
physical devices, hostile networks, independent interoperability or professional crypto validation.
#133 independent interoperability remains **NOT_STARTED** and the whole audit **NOT_READY**.
