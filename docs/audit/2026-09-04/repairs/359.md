# #359 — cancellation-safe failed-destination settlement

**8 September 2026: complete repository correction independently approved and pushed; issue remains OPEN.**
This Low-severity local lifecycle defect is not an authentication bypass, admission-budget leak or demonstrated
file leak/data loss. See the [original issue](https://github.com/p2pKit/P2pKit/issues/359) and
[verified outcome](https://github.com/p2pKit/P2pKit/issues/359#issuecomment-5579078847).

## Exact reviewed source

- Branch: `audit/complete-2026-09-04`, not `main`.
- Baseline: `42743ae53823ce7b6ecf00146c158f2c4d98a676`, tree `eba147a2f76b8d70216346cf23f7e4a3fa7512d2`.
- Complete correction: [`de882840e1e24aa04339b2285e8cd753af265b40`](https://github.com/p2pKit/P2pKit/commit/de882840e1e24aa04339b2285e8cd753af265b40).
- Reviewed tree: `89c498a7e24307d7cc23fb4eb6d7f3a6e53d8997`; clean during final verification/review.
- Four files: 448 insertions, 39 deletions; one production file and three test files.
- Complete diff SHA-256: `1ed5c3d3e2e5739d840185c82a614922247312240ef8a45f0538ee3ad24a3742`.
- Fresh reviewer `/root/review_359_r1`: **APPROVED**, no actionable findings.
- Review SHA-256: `0dc939114f60bd666cc7dceba848a69a3c86e3c13c85f2ad984108d79a869f5b`.
- Evidence-manifest SHA-256: `cf076b1b7362f069f815687caf54217c35e9caa3f21ba5d553024add470edab2`.

Source push and public outcome were refetched and verified. `main` remained
`eb444cccfc290be5435c5c10629c24183293606f`. No merge, release, tag movement or issue closure occurred.
Administrative checkpoint commits do not move this source-verification binding.

## Verified cause and correction

The #358 reviewer raised a separate unverified cleanup observation. Full #358/#121/#126/#162/#172/#174/#351
histories and the all-state inventory were checked by underlying mechanism. A distinct issue was filed only after
reproduction; the historical #358 cast report remains unchanged. Closed-invalid allegations were not reopened.

The generic destination-setup catch already retired the incoming entry, pending snapshot, byte budget and terminal
replay before awaiting destination abort. Actual cancellation of the accepting caller during that await interrupted
the remaining Failed publication and initial notice. Original tests observed abort=1, pending=0, initial result=0,
replay result=1, successful full-budget replacement, but the retained old object stayed Offered through shutdown.
Both ordinary IOException and local AuthenticationFailed reproduced it; callback-only cancellation was a passing control.

Both catches now call private `failDestinationSetup`: retire ownership, perform independently bounded abort, publish
the original typed failure, and attempt bounded notification with the original entry epoch, all within NonCancellable
settlement. After leaving that context, `ensureActive()` restores structural caller cancellation at the actual
`accept()` boundary; an active caller receives the original typed storage failure. Existing terminal-state CAS,
referential entry checks and epoch fencing prevent stale cleanup from overwriting another terminal winner or new session.

Changed paths under `library/p2p-core/src/`:

- `commonMain/kotlin/dev/p2pkit/core/internal/FileTransferDispatcher.kt:655-665,689-699,2643-2668`
- `commonTest/kotlin/dev/p2pkit/core/internal/FileTransferFlowTest.kt`
- `commonTest/kotlin/dev/p2pkit/core/internal/SecureSessionIntegrationTest.kt`
- `androidHostTest/kotlin/dev/p2pkit/core/internal/FileDestinationCleanupCancellationAndroidHostTest.kt`

The install sibling is aligned, not a separately demonstrated throwing application callback. The shared bounded
helper, platform destinations, public API/ABI, wire, authentication, dependencies and durability implementations
are unchanged. Bounds remain per operation, not a new single deadline for the entire transaction. Timed-out arbitrary
application/native work is not promised forcible termination. Genuine channel authentication remains distinct from
local destination errors, as repaired in #358. Best-effort terminal delivery does not prove remote processing (#351).

`P2pFileOffer` itself has no state flow, and failed acceptance does not return a successful handle. The local regression
retains the built-in object's implemented `P2pFileTransfer` interface. Do not overstate ordinary successful-handle
exposure, data loss or permanent remote retention; a conforming sender has an independent watchdog.

## Executed verification

The final clean-tree combined invocation was:

```bash
./gradlew check :p2p-sample-android:assembleDebug :p2p-core:dokkaGeneratePublicationHtml \
  --rerun-tasks --no-build-cache --no-configuration-cache --dependency-verification strict \
  -Pkotlin.compiler.execution.strategy=in-process \
  '-Dorg.gradle.jvmargs=-Xmx2048m -XX:ActiveProcessorCount=2 -Dfile.encoding=UTF-8' \
  --console=plain --max-workers=2 --no-parallel
```

Every build/test invocation used unconditional scoped cleanup: stop the applicable wrapper and owned workers,
preserve reports, then remove disposable outputs. The command flags alone do not implement this cleanup; follow
[the handoff rules](../../../../AUDIT_CHECKPOINT.md#toolchain-resource-control-and-device-limits) when rerunning.

| Invocation | Result |
| --- | --- |
| Original three-test JVM reproduction | Two intended assertion failures, one passing control; zero errors/skips |
| Final tests with original JVM production | 10 tests: eight intended failures, two controls; zero errors/skips |
| Final tests with original Android production | 16 host tests: seven intended failures, nine controls; zero errors/skips |
| Corrected focused JVM/Android-host selection | **33 passed**, zero failures/errors/skips; five XMLs, 19 executed tasks |
| Remove final caller cancellation check | Two intended assertion failures at the accepting call boundary |
| Use current rather than entry epoch | One intended stale-epoch assertion failure |
| Double abort deadline | One intended failure at the original exact deadline |
| Final combined command | **2,459 passed**, zero failures/errors, one unchanged manual LAN skip; 297 XMLs, 256 tasks |
| Final static/policy gates | **14 passed**, including actual Android ABI task-graph verification |

All mutations were restored exactly. Two genuine compilation failures—initial probe missing import and first focused
deprecated coroutine overload—and the initial evidence-verifier failure are retained, not relabeled as passes.
The final verifier requires the exact previously unenumerated replay assertion; no product assertion was relaxed.
Owned child coroutine scopes replaced deprecated usage without suppression. Focused-r2 used the repository 2048 MiB
heap default and two workers but omitted explicit ActiveProcessorCount; later controls and the full run supplied it.

Eight deterministic dispatcher methods cover IO/authentication/preflight failures, callback versus structural
cancellation, externally cancelled suspended abort, abort/notification errors, exact non-cooperative bounds,
same-ID shutdown/reopen, replay, repeated calls, no false accept/commit, and full-budget recovery. Synthetic workers
are released in finally. Android-host wrappers execute the same eight cases against Android compiler outputs.

The ninth common method uses production pinned secure-v2 kits over exact, fragmented and coalesced copying in-memory
transports. It checks source non-opening before acceptance, typed local/sanitized remote errors, an unaffected transfer,
subsequent messaging, and bounded kit/identity cleanup. The earlier #358 integration assertions are preserved through
a shared helper. Nine common methods run on JVM and arm64 Native simulator plus eight Android wrappers:
**26 new method executions included in the integrated total**, not additional passes or independent interoperability.

Static gates cover layout, OSV lock coverage, Markdown links, release metadata, whitespace, scoped source/added-line
style, dependency verification, toolchain/release-workflow policy, wrapper integrity/regressions, actual Android ABI
ownership and platform-test policy/runner. Scoped style inspection is not full ktlint. Ordinary ABI guards, lint,
sample Debug assembly and strict core Dokka execute in the combined command.

## Independent review, cleanup and remaining limits

The reviewer inspected the complete diff, surrounding lifecycle/epoch/replay/budget paths, bounded helpers, platform
dispatchers/destinations, public/security contracts, legacy siblings and session close/rearm ordering. It independently
verified **1,797 sealed entries (20,628,370 bytes)**, 297 final XMLs, all targeted executions and **28 cleanup receipts**.
The 25 leaf invocations did not overlap; parent orchestration receipts are not additional builds. It started no builds
or background workers and found no actionable issue-specific defect.

No invocation-owned workers or disposable module outputs remain. Protected guides, source, stashes, private evidence
and shared caches are preserved. Raw reports and machine-specific runners are deliberately untracked; a clone contains
safe summaries and source, not private evidence or running agents. Hashes bind reports, not public cryptographic assurance.

Android host is not ART/OEM/device-storage proof; arm64 Native simulator is not physical Apple, Swift XCTest or Intel
runtime execution. The unchanged testcase skip is `IosLanDiagnosticTest.advertiseForSixtySecondsForInteropCapture`;
Intel runtime tasks are separately host-inapplicable. Debug unstripped-library warnings are not release packaging proof.
Real filesystem/power-loss/provider, physical-device and hostile-network campaigns were not performed here. Final
whole-audit/release/consumer/Swift/XCFramework checks, #133 independent interoperability (NOT_STARTED), and professional
cryptographic review remain pending. Earlier consumer evidence remains bound to `7127616`. Overall **NOT READY**.
