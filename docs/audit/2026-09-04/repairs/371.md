# #371 continuation — actual publication and trial3 boundary

**9 September 2026. Prior independently approved source was PUBLISHED with `bafe905ac030d090219352de8a2358695755b528`,
tree `91d469eb09c8a612bf7dd9f7c5e2ce1f081634bb`.** Audit push exited0 at02:00:42.324919 UTC; remote ref
verified02:00:44.154019, after the global lease/source freeze/local-test pause was held before push.
[Source-outcome comment5594739895](https://github.com/p2pKit/P2pKit/issues/371#issuecomment-5594739895) was refetched at2026-09-09T02:13:57.626691+00:00 with an exact body match,
SHA-256 `87495347206483e8ac0bd3c1d3f99265683a4e84ca650bd3cb6c9afa7af78547`; issue OPEN. This publication event does not rewrite the original source review or filing.

The unchanged shared exact-byte success method and new newline-policy method passed within trial3. This narrowly observes the fixture correction natively, not the full host, SDK or product gates.
Authentic [trial3](../hosted-validation.md#trial3-publication-and-terminal-native-control-failure),
run34301487847/attempt1 atbafe, selected48 native methods:47PASS/1FAIL/0errors/0skips.
The unchanged ordinary nested-output cleanup test failed with125; outer suite/stop/final1/0/1.
SDK, wrapper-checkout fixture, P2pKit product/sample/consumer and both Apple hosts did not start.
Three synthetic XMLs establish zero product cases. PARTIAL_SALVAGE/unsafe,18 unavailable JSONL omissions;
full remote tree and cleanup NOT_PROVEN. The local02:42:38.431897 lease release is not remote cleanup.

The distinct cause was filed as [#373](373.md), then separately corrected and independently source-approved
at128136ff/tree747189; locally committed NOT_PUSHED, corrected Windows suite/junction test NOT_RUN here.
It does not add another #371 row, redo #371, or retroactively make trial3 pass. Source-outcome publication,
narrow native observations, later source approval and final administrative approval remain separate.
General shell-checkout CRLF and generated-batch CRCRLF hypotheses remain separate and UNCONFIRMED.
Whole audit NOT_READY; #133 and #268 NOT_STARTED. See the current [hosted continuation](../hosted-validation.md)
for exact publication/review/cleanup bindings and lease-before-trigger requirements.


## Preserved pre-trial3 #371 report at bafe905

The complete following text is preserved from published `bafe905`; its older source/count/publication/trial states
are historical and superseded by the dated continuation above, not rewritten. Earlier failures, read bindings,
verification scopes and applicable safety requirements remain in force.

# #371: Native executor fixture stop markers depend on text newline translation

Issue: [#371](https://github.com/p2pKit/P2pKit/issues/371). Severity: Low, non-security test-fixture portability.
**Source APPROVED at the separately bound final revision below; locally committed, NOT_PUSHED in this
record. Native trial3 NOT_EXECUTED.** Source approval is not independent approval of this report or native
Windows/Apple, product or release acceptance.

## Confirmed behavior and actual callers

At `d3c812020b41aeceef45381b1d24871b9477607b` / tree `7baa8e2233bc7dd23a903178fa24dff7f9ae9ec2`, the
embedded `FIXTURE` in `scripts/tests/run-audit-command-test.py` used text `print(..., flush=True)` for
stop markers but binary writes for product markers. The unchanged shared success test demands exactly
`b"STOP-ONLY-STDOUT\n"` and `b"STOP-ONLY-STDERR\n"` in the retained raw stop logs. Windows text stdout/stderr
translates LF to CRLF. `WindowsNativeTests` inherits and selects this actual test; `Host.run()` requires
native controls before SDK setup and product gates. Production evidence capture correctly preserves raw
pipe bytes. Normalizing logs or loosening the exact-byte assertions would hide the defect.

The independent static investigation acquired official CPython 3.12.10 `create_stdio` source corroborating
Windows LF-to-CRLF text translation, including unbuffered mode. That source observation is not native
Windows execution. Fresh all-state/full-history cause searches found no existing owner: #223 concerns
checkout normalization, #369 Bash selection/admission, #370 SDK metadata, and #141/closed duplicate #150
platform test coverage. Their prior repairs/decisions are not reimplemented. #371 was actually filed
**2026-09-09 00:32:54 UTC**, then independently refetched with a matching 4,339-byte body. The later narrow
GitHub observation is separate from the earlier full capture; see [hosted evidence](../hosted-validation.md).

## Fresh reproduction and exact repair

The original actual-caller runs used the **unchanged existing success test** and real Linux executor,
receipt assembly, native ownership and teardown:

- **00:28:05.801208–00:28:08.155802 UTC:** original LF fixture, one method PASS, exit 0.
- **00:28:08.227700–00:28:10.525111 UTC:** original fixture with only stdout/stderr configured for modeled
  CRLF before its generated baseline commit, one intended exact-byte stdout assertion failure, exit 1.
  Both raw stop logs contain CRLF; product bytes remain LF. Assertions and original stop statements are
  unchanged. Real leaf product/stop/final statuses are **0/0/0**, no errors or survivors, source unchanged.
- The new regression against the original fixture separately fails four exact-byte subtest assertions,
  one method /zero errors/skips, exit 1. These negative outcomes are retained, never relabeled as passes.

These are actual Linux calls with modeled Windows text policy, **not native Windows, Job Object,
Gradle-stop or product failures**. Neither earlier hosted trial reached this native-control suite.

Final commit **`842d0aafa9d84a0114d895a9f933f1663cc01a9d`**, tree `48343ddd4da5fd01e052493bc85f5e38cbbb2b5e`, parent
`d3c812020b41aeceef45381b1d24871b9477607b`, locally committed **2026-09-09 01:01:43.715116 UTC**.
Exact one-file diff SHA-256 `f4531694fbad98238efd426f43a7b28d6352be9fcd2a3fc90e9763158b24e72e`:
**32 insertions /2 deletions**, unchanged executable mode `100755`.

The actual fixture now writes both stop markers to binary buffers and explicitly flushes them, matching
the existing product-byte contract. It preserves stop exit statuses/hang behavior and all source,
receipt/evidence/native-ownership/stop checks. No production executor, hosted driver, workflow, wrapper,
protected guide, public API or protocol is changed.

One new `PurePolicyTests` method executes the actual `FIXTURE` under LF and CRLF text wrappers over buffered
binary streams: product success/failure and stop success/nonzero status, both raw streams, explicit flush
before close, one invocation record and exact argv. It does not duplicate the marker implementation.
All **99 original function definitions and 55 original test definitions are byte-unchanged**; platform
selection is unchanged. Definition totals are not the Linux-selected test count.

## Final verification and independent review

Fresh reviewer **`/root/review_371_source`: APPROVE**, no actionable findings, sealed
**2026-09-09 00:59:08.951933 UTC**. The exact final precommit source was read completely, original definitions
compared and all retained final/historical verification evidence independently inspected. The later commit
binding does not invent a retroactive read-commit or clean-commit test execution.

- Review SHA-256: `780700bdea9db1ec53f19113d539afd4ecd35ee6886770a305f4a56697213c15`.
- Readset SHA-256: `4b3b0f51737c20401e86620d8fc4e56bc0189a25f8d01f5fcd7fe92ff579fbd7`.
- Review seal SHA-256: `9aca4440958aaf9eb1eca0652e380cec817450baaaf7a4bf0d7ea432e3b247cc`.

**21 selected final gates PASS**, serialized **2026-09-09 00:43:29.002454–00:47:04.094063 UTC** on
`d3c8120` plus staged `f4531694`, not a clean later commit:

| Cohort | Recorded result |
| --- | --- |
| Original actual-caller LF and modeled-CRLF replay | One unchanged existing method passes in each separate invocation; raw product and stop streams stay literal LF, real leaf 0/0/0 |
| Four in-memory mutations | Intended inner assertions detected: stdout text 2, stderr text 2, missing stdout flush 4, wrong stop status 2; zero errors/skips |
| `python3 -B scripts/tests/run-audit-command-test.py --expected-host linux-x64 --evidence-root <private>` | **50 methods**, zero failures/errors/skips: 16 PurePolicy +27 shared +6 POSIX +1 Linux-specific |
| Host /workflow /leaf /consumer fixtures | 91 /51 /27 /32 methods, all PASS; not consumer product builds |
| Ten preservation/wrapper/repository/diff checks | PASS; exact commands and receipt hashes retained in checkpoint data |

The focused one-method green run and original-method replays overlap the final suite; do not add them to
unique-method totals. Mutation outer passes mean detection of intended inner failures, not passing mutated
source or product execution. Native-Linux fixture coverage is not native Windows/Apple coverage. The reviewer preserved a preliminary
helper-prefix counting error (52 corrected to actual 50 before verdict), SHA-256
`19147a9978c0f034ba014f9998a743653bb09242c2bb337924a24444f04377cc`; no runtime failure or source change occurred.

Verification manifest `466f5765d0b55861aaa3f72508e1b1c67b020f5b940a93bd35f630270c26ff78` binds
**1,229 unique files /2,190,632 bytes**; summary
`9fceb4f13581cda4931e4fb8e8867f52e11f19a5f8e40a63db69ae3463a32975`. All **25 outer invocation receipts**,
including original failures, have applicable task-isolated real wrapper-stop 0, no owned survivors and
unchanged repository source. Those outer receipts record no disposable product-output deletions and zero
product-test XML; generated fixture cleanup has its separate accounting below.
A separately finalized sealer ends **00:51:36.749776 UTC**, exit/stop 0 and no survivors; receipt
`fbcf6215f43594475e7916e779284ec649fa606dbd227cd99ca3816aeef6a0f6` is not recursively counted above.

The fixture evidence separately preserves **41 cleanup records /38 generated bases**, including three
intentionally injected incomplete cleanup records. They remain red historical evidence of fail-closed
preservation; later final records prove complete cleanup and absent generated bases after retention.
No incomplete record is overwritten or normalized into success. These local retained-record inspections
and outer stops do not prove remote Windows/Apple cleanup. Protected source, shared caches and unrelated
tasks are preserved; raw evidence stays private.

## Remaining acceptance boundary

The separately corroborated clean-`aab8cab` Linux check/Android/CLI/Desktop/Dokka run predates both #370
and #371; it is not rebound to this source or called a hosted native-control/SDK result. Both real hosted
failures remain unsafe/partial, with remote cleanup/full-tree **NOT_PROVEN**. A fresh controlled exact-source
Windows-to-ARM-Mac-to-Intel-Mac trial still needs its own complete native control, SDK, product, simulator,
consumer, artifact and stop/ownership evidence before any host acceptance or advancement.

General shell-checkout CRLF and generated-batch CRCRLF concerns remain **separate and UNCONFIRMED**.
This approved distinct test-fixture repair does not create evidence for them, close the issue or satisfy
physical-device, hostile-network, #133 independent interoperability, professional cryptographic validation
or architecture decision #120. **Whole audit NOT_READY; #133/#268 NOT_STARTED.** Continue feasible
sequential work; no checkpoint stop.
