# #373: Windows cleanup mixes cached and full device identities

Issue: [#373](https://github.com/p2pKit/P2pKit/issues/373). Severity: Low, non-security audit-tooling correctness.
**Source APPROVED and locally committed; NOT_PUSHED. Corrected native Windows suite/new junction test NOT_RUN.**
This report separates source repair/review from the failed original hosted trial and later host acceptance.

## Confirmed cause and actual callers

At `bafe905ac030d090219352de8a2358695755b528` / tree `91d469eb09c8a612bf7dd9f7c5e2ce1f081634bb`,
`inspect_disposable_tree()` read the root device through full no-follow `Path.lstat()` but compared it
against a descendant's cached `DirEntry.stat(follow_symlinks=False).st_dev`. Official CPython3.12.10
documentation/source states that Windows cached identity fields are zero and directs identity consumers
to full path metadata. Mixing those contracts can reject an ordinary same-device nested directory.

The actual executor `cleanup` CLI is called by `Host.clean_outputs`/finalization and the mandatory native
executor suite, before SDK and product gates. Authentic Windows trial3/run34301487847/attempt1 atbafe
selected48 methods:47PASS/1FAIL/0errors/0skips. The unchanged exact-output cleanup test first creates
`build/test-results/fixture/result.xml` through a successful fixture leaf0/0/0, then actual cleanup returns125:
`Cross-device descendant directory blocks cleanup`. Outer suite/stop/final1/0/1, source unchanged, no outer
owned survivors. Numeric root/child Windows device values were **not recorded**; documentation semantics
are not invented measurements. Its later fixture teardown cannot turn the failed command green.

The host correctly blocked SDK/#370, wrapper-checkout, P2pKit product/sample/consumer and both Apple hosts.
Returned artifact10085101463 is612,745 bytes, SHA-256
`965256ae28b644282751e4ee33e2575e1390037da5446e89c5742bc00c2f71bc`.
Independent terminal-failure-only review and complete retained scope are in the [hosted report](../hosted-validation.md).
Trial3 remains **unsafe/PARTIAL_SALVAGE**,18 unavailable JSONL omissions; full remote tree/cleanup
**NOT_PROVEN**. Three synthetic XMLs contain no testcase children and establish no product cases.

Full retained cause searches and freshly selected complete histories/closed decisions found no duplicate:
#141/closed #150 are cross-host product coverage; #223 checkout attributes; #367 dependency-verification
metadata; #369 Bash admission; #370 SDK metadata; #371 fixture newlines. The broad upstream checkout2530
match is Git-config regex cleanup, not this device-identity mismatch. Their prior repairs remain intact.
#373 was individually filed/refetched open with exact body SHA-256
`47a430841a61322abb1a1ea228e5581b1886201a39f9eaecd3eb3c5b6f6d76f1`.
GitHub created_at02:47:11Z and client verifiedUtc02:47:10.901706 are separate recorded clocks, not a
rewritten order. Filing gave74/187 approved, not75; only the later final source approval adds this repair row.

## Fresh reproduction and focused correction

The original production method plus new regressions ran on Linux at **02:57:33.292912–02:57:34.390924 UTC**:
three methods, zero assertion failures, **two intended AuditError subtests** in the identity method
(cached0/full47 and cached47/full0), with matching-cache/unknown-reparse/special-file controls retained.
Original command/stop/final **1/0/1**. Actual source and tiny real trees were exercised with independently
modeled metadata boundaries; this is not native Windows or mounted-device execution.

Final source commit **`128136ff8ab040f0b5c61dc5402d2b9736d63c86`**, tree
`747189670275c32b9ef67b3f1842cb3fad3cb381`, parent `bafe905ac030d090219352de8a2358695755b528`;
local commit **03:41:14.043526–03:41:14.084991 UTC**. Exact two-file diff SHA-256:
`e0836d4312f918e3288dc1001dfdd6b635592ecdc11f39ba430a91f766818d18` (237 insertions /2 deletions).

- `scripts/run-audit-command.py` SHA-256
  `c3fa28adcde3efbacb0f2b6aa70c1c77906c7f2dd9688bd49cf2e75261139a17`.
- `scripts/tests/run-audit-command-test.py` SHA-256
  `83f955dbf888228e5cdfa4808b9c63917cffd91d76fc7d56e7dd2f632a025d1e`.

Only the ordinary-directory branch changes: keep initial no-follow `DirEntry` reparse/link classification;
then obtain full child `lstat()`, require a physical directory without the reparse attribute, compare its
full device exactly to the full root device and only then enqueue. Full zero device IDs are not wildcards.
Unknown reparse types must still reject before a full path lookup, because Windows can resolve some
non-name-surrogate reparse points at that boundary. Known symlinks/junctions remain unlink-entry-only.

Real differing-device children, special files, full-stat/scan failures, depth/entry/link-evidence bounds,
root/ancestor validation, exact owned-output/source/evidence checks, all-target preflight and mandatory
finalization remain. No blanket build deletion, assertion weakening, platform bypass, public API/protocol,
workflow or protected-guide change. Existing quiescent-owned-output scope remains; this does **not** prove
hostile concurrent pathname/mount containment.

Ten new methods cover eight independent metadata-boundary controls, real all-target cleanup orchestration,
and Windows ordinary nested output plus a real junction/outside sentinel. The Windows method retains
actual device values without requiring future Python caches to stay zero, source/receipt preservation,
exact link-only inventory and no target traversal. That new native method is still **NOT_RUN** here.
All100 original test-file definitions/56 test methods and existing #371 fixture/native selection are
byte-unchanged;46 of47 runner definitions are unchanged, only the inspector changes.

## Verification and independent final review

Fresh non-implementing reviewer **`/root/review_373_source`: APPROVE**, no actionable findings. Complete
final source reads are1058 runner lines and2131 fixture lines; actual caller ranges, full issue/closed
context, preserved official CPython evidence and all final/historical retained evidence were inspected.

- Final report: `360d89cce04603cfd2a5184a794efbc64088af01416f396775815a10a5d92b77`.
- Original review seal: `d27cbb254884dfdccdba9bead7c6e62cb3261ba7111c264e3f729104ba069098`.
- Actual commit-binding addendum: `7d49c983f72f3a2900cac76f2e264f673d0c851e9ef004e77974c4b96a8698ad`.
- Commit-binding seal: `09a00e76e98c6d29c03f9e4cfcb0861541857693f8995f784e5b4e7f7d863236`.

Twenty substantive commit-byte comparisons pass. A03:43:31 later staged workflow change was retained as
an out-of-scope current-state discrepancy, not relabeled a clean128 worktree or approved by the #373
review. The initial clean03:41 source commit and subsequent separately reviewed workflow request are
distinct events. Staged-source executions are **not retroactively clean-commit reruns**.

**26 selected final-r2 gates PASS**, serialized onbafe plus stagede083. Native-r2 ran
**03:08:21.924716–03:09:31.326647 UTC** and is included, not rerun; other selected gates ran
**03:12:25.661022–03:15:15.630966 UTC**. Exact command arrays/receipt hashes are in checkpoint metadata.

| Cohort | Recorded result |
| --- | --- |
| `python3 -B scripts/tests/run-audit-command-test.py --expected-host linux-x64 --evidence-root <private>` | 59/59:24 PurePolicy +35 LinuxNative, no skips |
| Focused actual-method metadata selection | 8/8, overlaps native selection |
| Host /workflow /leaf /consumer-script suites | 91/51/27/32 methods, all pass; not P2pKit consumer products |
| Preservation/wrapper/checkout/layout/OSV/links/release metadata/workflow/whitespace/diff gates | Pass; repository controls, not a release |
| Nine actual-function in-memory mutations | Intended reds detected, table below; outer success is oracle detection |

| Mutation | Intended inner outcome |
| --- | --- |
| Reuse cached device | 2 errors |
| Remove full-device guard | 6 failures |
| Treat zero child /zero root as wildcard | 2 failures each |
| Remove immediate type /reparse guard | 3 failures each |
| Follow full-path stat | 4 failures |
| Replace initial classification with path stat | 1 failure |
| Follow initial entry stat | 4 failures |

These reds have no skips and remain preserved. For some type/reparse mutations a later ancestor check
still rejects; the diagnostic mismatch proves loss of the immediate pre-enqueue boundary, not a hostile
race exploit. Earlier focused-r1 passed8/8 at55c499; its new cross-device test was strengthened inr2 for
zero devices on either side. Its original evidence is retained, not substituted for final-r2 coverage.

The verification seal binds **1,178 unique files /2,635,900 bytes**,28 own outer leaves including original
red/r1 and26 final-r2 leaves; all applicable outer stops0, source unchanged, no owned survivors, no
outer disposable-output deletions or product XML. Summary SHA-256
`030c61b9d272e4f024a42df248633b184b1b2275d4e52ff061753ea616b4d023`, manifest
`ac6050abc693c2e1cdf2bc36c097df1b4479441f1bbf2b2c15afcdadad92b8d0`, seal
`133f78d5023256a89f9fee4ff4b485c4aefdb63e93b24fefdd6d1d0c144b96ba`.
Separate sealer receipt `bcf66a0982104b92c63aedb785badc99694c62f49f6896ae14638bda6785efe6`
finalized **03:28:26.882699 UTC**,0/0/0 and no survivors/deletion; not recursively counted above.

Linux fixture retention separately has924 regular files /1,410,765 bytes. Its38 teardown attempts cover35
bases; three deliberate incomplete/non-disposal attempts remain, each followed by separately bound
complete recovery. Latest35 records are complete and their generated bases were observed absent.
Forty-six unique nested invocations plus three byte-identical archived copies include finals15zero/
30infrastructure125/one intentional7; stops23zero/21null admission/one9/one timeout-15. Do not report all
inner stops as zero or call these deliberate controls P2pKit product failures. Source, shared caches,
required evidence and unrelated tasks were preserved.

The reviewer preserved two false-inspector outputs:54 nested-archive provenance labels and four verbose
same-line-status labels. Separate reconciliation accounts for822 archive bindings (795 direct/27 relocated
across8 copied manifests),1,127 supplemental archive comparisons,260 selected method outcomes and301
verbose-log comparisons, no unresolved discrepancies. Original deleted provenance paths were not opened;
no raw evidence/source/test was changed to reconcile these assumptions. These are data comparisons, not
extra test execution or a claim that the original inspector outputs were clean.

## Remaining acceptance and continuation

Controlled native Windows correction/new junction coverage is still **NOT_RUN**. Its expected final
selection is58 methods (24 pure/34 Windows); Linux59 is not that acceptance. A separately source-bound
hosted run needs finalization/full artifacts/stop/ownership review before host advancement.

At the #373 source/evidence review, the accepted product evidence was the independently corroborated
cleanaab8 Linux1,607-case/199XML/245-task event, predating this repair. That source review itself establishes
no product execution on the #373 commit. Later product events remain separately source/receipt-bound in
the [hosted continuation](../hosted-validation.md), never retroactive to this source review. Neither this
review nor the Linux fixture suite establishes corrected native Windows, Apple/simulator/device,
hostile-network, independent #133 interoperability or professional cryptographic acceptance. Trial3's
failed/partial/remote-cleanup limitations remain unchanged.

#372 remains a separate pending Medium Android17 policy-integration repair, runtime NOT_RUN; it is not
part of #373. Source approval gives **75/187 (40.1%)**,112 remaining (59.9%); not issue closure or effort.
**Whole audit NOT_READY; #133/#268 NOT_STARTED; #120 decision separate.** Follow the current
[hosted continuation](../hosted-validation.md), including final independent administrative/workflow review
and a held global lease/source freeze/local pause **before** any triggering audit-branch push. Continue
all feasible sequential work; no checkpoint stop.
