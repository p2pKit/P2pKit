# #325 — Complete Android SDK setup prerequisites

**Reviewed correction is pushed to `audit/complete-2026-09-04`; not merged or released.**
The issue remains open under the completion policy. Feasible remediation is complete
under the protected-file constraint below; this is not release approval.

## Revisions and correction

- Baseline: `b332da106dcbb570fdee4e2b572c70e5e1721f00`.
- Baseline tree: `e7cfb9da5d1e033ec768af80a85dd6e42dfefc29`.
- Reviewed correction: `6ebfb9f81de45164cc2f148db753c65a7061f835`.
- Reviewed tree: `79c25845132f7851bfec746a2582a8bf102b5636`; clean at verification/review.
- [Verified public outcome](https://github.com/p2pKit/P2pKit/issues/325#issuecomment-5565989401).

The version catalog separates the library/shared-module compile SDK36 from the
Android sample's SDK37. Earlier onboarding named only the former. Canonical
[Android setup](../../../testing/local.md#android-sdk-setup) now identifies both,
the sample's actual SDK Manager package `platforms;android-37.0`, installation and
licensing commands, SDK path precedence, and the separate runtime minimum API24.
`CONTRIBUTING.md` and the external validation catalog link to that section rather
than independently copying platform pins.

The layout gate calls `check-android-setup-policy-test.rb`. It compares the catalog,
canonical table, installation commands and actual CI installer; 15 negative controls
plus a coherent future-SDK positive control demonstrate consistency enforcement.
Existing lightweight/full CI and release callers inherit the check. Independent
toolchain/security tripwires are unchanged, as are production code, ABI baselines,
SDK consumers, dependency pins and lockfiles.

## Protected-file exception and independent review

**`AGENTS.md:9` still names only SDK36 because the owner prohibited modifying it.**
The canonical guide calls out that incomplete shorthand. Do not claim every
originally cited prerequisite sentence was corrected. Literal alignment of the
protected sentence requires new owner authorization. `CLAUDE.md` is also unchanged.
#208's fast-gate/Ruby prerequisite omissions and #225's tripwire maintenance remain
separate and pending.

Fresh reviewer `/root/review_325_r1` inspected the complete five-file correction,
source/catalog/CI/consumer callers, complete issue history and raw verification.
It returned **APPROVE** of the feasible correction, with no actionable changes,
while requiring the protected-file exception to remain visible. It independently
rehashed all **1,501** manifest entries and parsed all **269** test XML reports.

Private review report SHA256:
`a0d38a9eee14c659565c6a0f50d921000ea3191fb9151f49cec478968f740fa0`.
Verification manifest SHA256:
`f86c3589f58417554ee33616d130b28bbf9908e00f519ad81354f3b5d717d4b9`.
Raw evidence stays private; these hashes do not make unpublished logs available
to a fresh clone or substitute for rerunning checks.

## Verification

At the exact clean correction commit:

```sh
./gradlew check :p2p-sample-android:assembleDebug --rerun-tasks \
  --no-build-cache --no-configuration-cache --no-configure-on-demand \
  --dependency-verification strict --console=plain --max-workers=2 --no-parallel
```

| Gate | Result and limitation |
| --- | --- |
| Real detached-baseline SDK probe R2, downloads disabled/offline | Platform36-only fails resolving `android-37.0`; adding installed Platform37.0 resolves actual sample bootclasspath. Neither fixture has an `android-37` alias. Not a fresh SDK download or isolated full assembly. |
| Clean integrated check plus Android assembly | **2,236 passed**, zero failures/errors, one unchanged manual iOS interop-capture skip; **236 executed tasks**. Android lint and library ABI gates pass. |
| Eight final static gates | PASS: layout, OSV lockfile coverage, Markdown links, release metadata, full-range whitespace, new checker under system Ruby2.6.10, independent toolchain/ABI-graph policy, release-workflow policy. |

An exploratory SDK probe R1 failed because its probe used a removed AGP property;
it is not reproduction evidence. Early checker-development failures were corrected
before the final commit/checks. The unstripped `libandroidx.graphics.path.so` build
warning was retained, not suppressed. Do not add overlapping test totals.

The integrated run covers JVM, Android host and Kotlin/Native arm64 iOS simulator
execution, not Swift XCTest, Intel execution, ART/OEM or physical devices. The full
release gate was inspected, not executed. Isolated publication consumers, hostile
networks, independent interoperability and professional cryptographic assurance
remain unverified for this revision. Overall readiness is **NOT READY**.

## Cleanup and continuation

Every invocation, including expected/experimental failures, stopped its wrapper
and invocation-owned survivors, retained required evidence and removed disposable
generated outputs. Detached SDK fixtures/worktree were removed. The reviewer
started no builds/tests/workers. Source, protected files, user changes/stashes,
shared SDKs/caches and private evidence were preserved.

Progress at this checkpoint: **50/168 reviewed repairs (29.8%)**, **118 remaining
(70.2%)**, including 96 pending repairs, 21 external-validation items and #120's
architecture decision. #190 is next. `main` is unchanged; no issue was closed and
no release was published.
