# #370 continuation — actual publication and trial3 boundary

**9 September 2026. Prior independently approved source was PUBLISHED with `bafe905ac030d090219352de8a2358695755b528`,
tree `91d469eb09c8a612bf7dd9f7c5e2ce1f081634bb`.** Audit push exited0 at02:00:42.324919 UTC; remote ref
verified02:00:44.154019, after the global lease/source freeze/local-test pause was held before push.
[Source-outcome comment5594739379](https://github.com/p2pKit/P2pKit/issues/370#issuecomment-5594739379) was refetched at2026-09-09T02:13:54.332912+00:00 with an exact body match,
SHA-256 `d70bcbfb8feb511de052b4901cc0cfc697b06a40fbfb69b1acc80991e781edf8`; issue OPEN. This publication event does not rewrite the original source review or filing.

Trial3 failed in the mandatory native suite before SDK setup. No native execution of the corrected SDK admission is established.
Authentic [trial3](../hosted-validation.md#trial3-publication-and-terminal-native-control-failure),
run34301487847/attempt1 atbafe, selected48 native methods:47PASS/1FAIL/0errors/0skips.
The unchanged ordinary nested-output cleanup test failed with125; outer suite/stop/final1/0/1.
SDK, wrapper-checkout fixture, P2pKit product/sample/consumer and both Apple hosts did not start.
Three synthetic XMLs establish zero product cases. PARTIAL_SALVAGE/unsafe,18 unavailable JSONL omissions;
full remote tree and cleanup NOT_PROVEN. The local02:42:38.431897 lease release is not remote cleanup.

The distinct cause was filed as [#373](373.md), then separately corrected and independently source-approved
at128136ff/tree747189; locally committed NOT_PUSHED, corrected Windows suite/junction test NOT_RUN here.
It does not add another #370 row, redo #370, or retroactively make trial3 pass. Source-outcome publication,
narrow native observations, later source approval and final administrative approval remain separate.
General shell-checkout CRLF and generated-batch CRCRLF hypotheses remain separate and UNCONFIRMED.
Whole audit NOT_READY; #133 and #268 NOT_STARTED. See the current [hosted continuation](../hosted-validation.md)
for exact publication/review/cleanup bindings and lease-before-trigger requirements.


## Preserved pre-trial3 #370 report at bafe905

The complete following text is preserved from published `bafe905`; its older source/count/publication/trial states
are historical and superseded by the dated continuation above, not rewritten. Earlier failures, read bindings,
verification scopes and applicable safety requirements remain in force.

# #370: Hosted SDK validation rejects canonical Android Platform 37.0 metadata

Issue: [#370](https://github.com/p2pKit/P2pKit/issues/370). Severity: Low, non-security hosted infrastructure.
**Source APPROVED; locally committed, NOT_PUSHED in this record. Native trial3 NOT_EXECUTED.**
The repair and its new report are separate from the same-#369 Git Bash follow-up. Source review is not
independent approval of this administrative report, native SDK installation, product or release acceptance.

## Confirmed behavior and distinct ownership

On clean `aab8cabf68958a7a3fdedcddc70c84a39e050df4` / tree
`f094de05ca60a625e3b9f37abb4f3ce4f9b8dbc2`, actual `Host.setup_sdk()` requested both
`platforms;android-36` and `platforms;android-37.0`, but required the latter API-level text to be integer
`37`. The installed package says `AndroidVersion.ApiLevel=37.0`; the old end-anchored predicate rejects it.
Its exact source-properties SHA-256 is
`fa89029fc0cee414cb5e44a37cd3033771217eeecb5086577bb6d9b1b03b5b5f`.

The retained installed package XML and bounded official Google repository metadata agree on package
`platforms;android-37.0`, API `37.0`, revision 2, archive `platform-37.0_r02.zip`, without a host-OS
discriminator. This corroborates metadata identity, not a fresh archive download/authentication or a
Windows/macOS SDK-manager installation. No SDK contents were modified for this investigation.

The actual workflow enters the driver through native Python. `Host.run()` requires prerequisites and
native ownership controls, then SDK setup, then Windows/ARM/Intel product branches. An SDK error blocks
products and reaches the real finalizer with safe continuation false. Neither earlier hosted trial reached
SDK setup; this defect was not inferred from their different Bash failures or from the passing Linux build.

Fresh pre-filing all-state lists and complete #325/#367/#369/#148/#157 histories were inspected on
2026-09-08 at 23:47 UTC. No matching owner was found: #325 is onboarding guidance, #367 is artifact
verification, #369 is native Git Bash selection/validation, and closed #148 belongs to #157's execution
accounting scope. Their earlier approvals are not reimplemented or withdrawn. #370 was actually created
at **23:51:42 UTC**, then refetched open with zero comments and an exact body match. That individual
addition was not itself a fresh post-filing full-list/history capture; later GitHub observations remain
separately bound in the [hosted report](../hosted-validation.md).

## Fresh reproduction and exact correction

The isolated original actual-method run on 2026-09-08 at **23:47:09.499958–23:47:10.634227 UTC** used real
`Host.setup_sdk` and `Host.invoke`, exact copied installed 36/37.0 property bytes, and real receipt/evidence
assembly. Only the Windows process/SDK-manager boundary was synthetic; no manager, native host, product
build or SDK installation was executed.

- Canonical `37.0`: one intended `ValueError: Wrong Android platform metadata: android-37.0`; the successful
  synthetic manager receipt remains, but no success `android-platforms.json` is produced.
- Integer-`37` control: passed on the original driver. Original outer **exit 1** and both methods remain
  preserved, not relabeled as a passing test.
- The original eight newly added repository regressions also retain **exit 1**, nine assertion failures
  and six canonical-input errors. These failures are distinct from the first two-method reproduction.

Exact correction: commit **`d3c812020b41aeceef45381b1d24871b9477607b`**, tree
`7baa8e2233bc7dd23a903178fa24dff7f9ae9ec2`, parent `aab8cabf68958a7a3fdedcddc70c84a39e050df4`;
locally committed **2026-09-09 00:26:08.173624 UTC**. Three-file diff SHA-256:
`c21e66aa94d1113771b7a11ebd87a1eadb4c76428a7dabba43cb93409130a3be`.

Two production lines map the requested package to literal `37.0` and escape both the property-key dot
and expected API value. There is no broad version normalization. Platform 36, both required package IDs,
SDK-root/manager selection, failed-manager rejection, prerequisite order, write-once evidence, native
product routing and strict/source/ownership/stop checks remain. Operational documentation explains the
narrow contract. No workflow, protected guide, public library API or protocol was changed.

Eight new test methods exercise all three roles with fresh per-case evidence namespaces: LF/CRLF property
inputs, retained raw-file hashes and decoded content, exact manager/wrapper/argv/environment routing,
missing/nonabsolute SDK or applicable manager, failed manager, missing properties, literal keys/values,
wrong platform pairings and real run-path success/blocking/finalization. Synthetic process boundaries
are not native host execution. All **131 original definitions and 83 original test methods are unchanged**;
reusing an existing fixture helper does not inherit or duplicate its registered suite.

## Verification and independent review

Fresh `/root/review_370_source`: **APPROVE**, no actionable findings, sealed
**2026-09-09 00:19:21.362033 UTC**. The reviewer read the complete issue/caller context and all three final
source files, compared original definitions, and independently traversed all sealed evidence. Approval was
on staged tree `7baa8e2`; the later exact commit binding is not a retroactive read-commit or new execution.

- Review SHA-256: `aea94ab1109bec3f9607b8aec1cd209740152a7c7c65bdc5d1bb362aa3e77bff`.
- Readset: `a71a228d0571df2436c5cdc704d622f71c666bb130389c842fb6a76ec063732b` (237 inspected/hashed files,
  with semantic versus integrity-only read scopes distinguished).
- Review seal: `f5ece24e14f4b24665d9b9676435189395287fcd1876cd901ed6b27a6924b72e`.

**18 selected final gates PASS**, executed serially on 2026-09-09 at
**00:02:49.299694–00:05:29.354401 UTC**, on `aab8cab` plus staged `c21e66a`, not a clean later commit:

| Gate cohort | Recorded outcome |
| --- | --- |
| Original canonical-input replay | One passing method, with original input/exercise/assertions unchanged |
| Three in-memory mutations | Intended inner failures detected: unescaped API 3 assertions, key 6, SDK bypass 9; zero errors/skips |
| `python3 scripts/tests/audit-host-test.py -v` | 91 distinct methods: 83 unchanged +8 new |
| `python3 scripts/tests/audit-host-workflow-test.py -v` | 51 methods |
| `python3 scripts/tests/audit-leaf-hooks-test.py -v` | 27 methods |
| `python3 scripts/tests/audit-consumer-test.py -v` | 32 methods; fixture boundaries, not a native consumer build |
| Ten preservation/wrapper/repository/diff checks | PASS; exact command arrays and receipt hashes in checkpoint data |

The canonical replay selected only the original canonical method, removing the now-inapplicable integer-37
positive-control selection without editing either original method or its assertions. Integer rejection is
explicitly covered by the final suite. The focused eight-method green run overlaps the final 91, not an
additional test total. Mutation outer passes mean the deliberate mutations were detected; their inner red
logs are preserved and are not described as passing mutated-source or native-product tests.

Verification manifest `e4adcc0cf92eb6beb55c5a122f3de5f69a4f7028eacc085a49c3c35ae0ee24d9` binds
**216 unique regular files /2,299,601 bytes**; summary
`a16fbf339a1843fd49eaf945ccdae2404ceae65b8e1e228c9f1b5fdbd30cc553`.
All **21** underlying invocation receipts, including the two original failing commands, are serialized,
with wrapper-stop 0, no owned survivors, source/protected guidance unchanged, no output deletion and zero
product XML. Separate seal finalization ends **00:09:38.034989 UTC**, also exit/stop 0 and no survivors;
it is not recursively counted in those 216 files or 21 leaves. Outer Linux stop logs report no running
Gradle daemon. These local stops and synthetic manager receipts do not prove native Windows/Apple cleanup.

## Remaining acceptance boundary

The separately corroborated clean-`aab8cab` Linux check/Android/CLI/Desktop/Dokka run occurred **before**
this SDK source correction and bypassed hosted `Host.setup_sdk`. It is not rebound to `d3c8120` or called a
native SDK result. Both original hosted failures remain unsafe/partial, with full-tree/remote cleanup
**NOT_PROVEN**. New source, actual native ownership/SDK/product/simulator/consumer gates and complete
release evidence require their own exact-source observations and independent corroboration.

The later [#371 native-fixture byte-emission repair](371.md) is independently approved and separately
tracked, not silently included in #370 or a retroactive expansion of this SDK source review.
Physical devices, hostile networks, #133 independent interoperability, professional cryptographic review
and architecture decision #120 remain distinct obligations. Source repair approval adds one distinct
approved repository row; it does not close the GitHub issue or make the whole audit ready.
**Whole audit NOT_READY.** Continue feasible sequential work; no checkpoint stop.
