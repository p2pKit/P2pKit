# Receiver-only hosted capacity proposal — 22 September 2026

**SOURCE REVIEWED / STATIC CHECKS COMPLETE / HOSTED PROBE UNEXECUTED /
NO PUSH AUTHORIZATION.** This isolated branch is
**NEVER MERGE**. Nothing in this proposal activates Stage1, either ordinary HOLD,
the whole-JVM interlock, a provider, a build, a PR or publication.

The initial independent review requested one capture-finalization correction.
The corrected implementation has source-only approval; the results and their
limits are recorded below. Neither selected graph case nor the hosted probe has
run. An owner execution request must bind the final containing commit/tree.

## Why this experiment, not another local retry

The current receiver has no complete1066 positive under the retained CPU20
envelope. The latest independent source assessment found no defensible substantial
optimization that preserves all existing observations. Five full structural scans
per checked boundary make the final1066 rereads alone perform21,320 scans; final
directory sweeps and leaf work are additional. Earlier slower public-getter
alternatives are rejected. No speculative source optimization or unchanged local
complete retry is proposed.

The question is instead whether this exact model case fits its unchanged limits
on the actual intended GitHub-hosted Ubuntu24 environment. The VPS exposes a
generic Intel Xeon KVM CPU; there is **no evidence that the hosted CPU is faster
or that this attempt will pass**. A failure is useful target-environment evidence
and stops the experiment, not a reason for machine shopping or repeated attempts.
This is a changed-environment measurement, **not a source repair**.

The registration run35730126848/1 already passed; it is not repeated. Its retained
setup identifies image20260907.300.1. The public image README at tag
ubuntu24/20260907.300 lists Python3.12.3; retained README SHA-256:
4edbefde1df512f024363b1f66f78448f944f071c9e1775056b220d5bdc8dc81.
Those dated facts do not pin the next runner's image, tool bytes or speed.

## Exact scope and inputs

The source parent is 362aa96cdeff29a534b000814317dea7fcef6799, tree
e315c088dcda8c435a493fede35ec3481f7812cf. The final reviewed probe commit/tree
must be supplied in the separate execution request; this document grants none.

The held receiver and original complete test body are unchanged:

- scripts/run-hosted-initial-recipient.py:
  b13a6c4299548ef59e3da3c5b4a634e3cd8a7db211061315801c1719a0bb4680.
- scripts/tests/hosted-initial-recipient-original-graph-test.py:
  28672c012bd308a57cc32f042fceb78116a0fb46a2d98cd003e745e047925fa7.
- scripts/tests/initial-recipient-graph-fixture.py is byte-identical to the
  retained R9 split-fixture adapter:
  1e119e884f7c38e3bf54d98d1770fa0894995f877408867193f9e14c3b63ff84.

The old R9 receiver-only invocation already separated preparation. This proposal
does **not newly exclude fixture preparation from that failing case**, and does
not claim a pass of the earlier combined setup/receiver aggregates.

Exactly two selected Python test invocations are proposed, serially:

1. FixturePrepareModels.test_prepare_and_close_synthetic_graph: create a
   **fresh synthetic** fixture and inventory, check original preparation/model
   closes, bind receiver/test hashes, and retain only tiny model files.
2. Only after1 succeeds:
   FixtureReaderModels.test_complete_fixed_graph_from_closed_synthetic_fixture.
   It invokes the unchanged complete1066 test, all its file/reread/roster/cap/type
   assertions and actual enclosing model-owner close, then compares the fixture
   inventory. No inherited-suite discovery, selected-negative replay, native
   process, GPG, provider, network request or app build occurs inside either case.

These are two invocations for **this experiment only**, not a count of all future
Release tests. Imports, setup, assertions, receiver, close and cleanup all spend
the respective original CPU/wall envelope. Per-method printed CPU measurements
are narrower than whole-process accounting and must remain separately labelled.
The canonical-init120 clock/fence models are unchanged, not genuine native timing.
No live Owner, authority or original-return capability is serialized or restored.

## Confinement and originals

The launcher requires the real hosted workspace/temp spellings, fresh output and
test directories, attempt1, a clean exact source checkout and Ubuntu24 metadata.
It captures the actual image label/version, source commit/tree, CPU description,
Python/system package versions and executable hashes. Every copied Python input
and the public recipient policy are bound to a manifest and checked against the
checkout before/after both invocations. Only those source bytes and the retained
harness form the project input view; no Gradle/SDK/provider bundle is fetched.

The maintained R9 ordinary-UID/read-only-source/audit-guard model is retained.
The new launcher additionally uses preinstalled util-linux network, PID, mount,
IPC and UTS namespaces, a fresh proc view, UID/GID65534, empty groups, dropped
capabilities and no-new-privileges. Source/harness files are root-owned0444;
only the new model-work directory is writable by the test UID. This is **not a
claim of a new complete filesystem sandbox**: the pre-project path guard bounds
reads/writes, as in R9. No host credential or user home is an admitted input.
No sandbox package, Python alternative or dependency is installed on failure.

Each invocation retains **CPU20 / AS512MiB / file32MiB / FD128 / core0 /
wall30+kill2**, Python3.12 -I -B -S, no optimization flags and a cleared
five-name environment. Four synthetic guard controls and zero unexpected
denials/ResourceWarnings are required. The exact original commands, pre/post
integrity, stdout, stderr, exit, timestamps and wrapper user/system/wall times
are retained. Startup refusal, timeout, signal, incomplete close, integrity
failure or missing originals is not a receiver pass. Preparation failure
prevents the receiver invocation. There is no automatic retry.

Only **public model transcripts and public runtime/source metadata** enter one
14-day Actions artifact, never the synthetic fixture contents, actual private
evidence, a private key/passphrase, app bytes or dependency caches. The artifact
retention starts at its actual GitHub creation time; no future date is invented.
Actions job logs have the repository's separate log-retention setting and contain
only public summaries. This does not replace encrypted qualification evidence.
Final original-result review must inspect the real run/image, artifact bytes,
complete capture manifest and outer job/command exits; source preparation is
not that review.

The workflow creates fresh private capture directories, then records the
completed elevated launcher's actual return in `launcher.exit`, with its original
stdout/stderr and enclosing timestamps. The launcher separately records its
entering body status in `body/body.exit`; any failed body-output finalization
must return nonzero without replacing an earlier body failure. Finalization
errors remain in `launcher.stderr` when capture is possible. Only after the
launcher returns does the workflow produce the complete `captures.sha256`
covering both the body originals and its own completed-return captures. Failure
to write these records or the manifest fails the step; missing originals or a
partial upload cannot establish a pass. A body success marker alone is not
completed-launcher, capture-finalization or job success.

## Source review and completed static checks

The initial review was **REQUEST_CHANGES_CAPTURE_FINALIZATION_ONLY**, report
SHA-256 `f1805c6f942ded9d85bd2a2a77e1f472c7be4054b1dee16939e98a6a4c5abc0b`.
The corrected source received
**APPROVE_EXACT_CAPACITY_PROBE_SOURCE_ONLY_NOT_EXECUTION**, report SHA-256
`2c3cd7fc0b09e87d71e2fe79664ca718b353fed435a3ec2069a852006e7d72bd`.
The four executable-source files remain at that reviewed snapshot:

| File | SHA-256 |
| --- | --- |
| `.github/workflows/dependency-cache-bootstrap.yml` | `777b8c832c10ee345c9d5eb1b0c1734581a4e35d871c80a21f8f2063890fab1a` |
| `scripts/tests/run-initial-recipient-graph-probe.sh` | `41e6fd605ab98d8dffd094f3ea56e1e5b5d185434590aff0468c212309668e50` |
| `scripts/tests/initial-recipient-graph-probe.py` | `1856333cd1a8417882e9417ec4c6e143c280356dea503bb4b384e4ee2831db5d` |
| `scripts/tests/initial-recipient-graph-fixture.py` | `1e119e884f7c38e3bf54d98d1770fa0894995f877408867193f9e14c3b63ff84` |

At **14:44:08–14:44:09UTC**, one new Ruby source-only invocation passed
**32 YAML-shape controls** (one positive/31 negative), **15 literal other-workflow
event exclusions**, and the shared heavy-queue composition check. It did not
evaluate GitHub's expression engine or run any workflow command. The following
Python AST/hash helper ran six methods: **five passed, one failed** because it
mistakenly counted every top-level tuple-target loop, including the diagnostic
`event, path` loop, rather than selecting the `name, limit` resource loop.
The enclosing aggregate returned1 and stopped before either shell syntax check.

Only that offline helper was corrected to select the exact target AST; the
unchanged expected limit tuple and all probe source stayed intact. At
**14:44:58UTC**, its six methods passed, followed by two separate successful
`bash --noprofile --norc -n` invocations for the launcher and workflow shell.
The corrected enclosing command returned0. The already-passing Ruby controls
were not repeated. These are **five total static interpreter invocations**,
including the failed helper, not five receiver tests or a total for future
Release qualification. The retained helper sources/results live in the scoped
local evidence packet, not in the proposed hosted workflow.

All five commands used UID/GID65534, cleared groups/environment, read-only
source/runtime and no-network bubblewrap namespaces, with CPU20/AS512MiB/
file32MiB/FD128/core0/wall30+kill2 bounds. Original commands, stdout/stderr,
individual and enclosing exits, timestamps and matching pre/post input/tool
checksums are retained. Neither adapter nor receiver was imported or executed;
the two shell bodies were parsed only. No private-key/provider/app operation
occurred. The initial failed aggregate remains preserved, not relabelled:

- Initial capture/enclosing manifest SHA-256:
  `7fdbc7b4ef1f555d2317d46b55abdcfb04f57037dd042242564d24ed3adb84c5` /
  `0d963840494eede283814f03b1c8b2f38126d282412cf90d4c749706520ddfe0`.
- Corrected capture/enclosing manifest SHA-256:
  `8593c7587dbc33d331c00e23f5624749b4c7e4b512e275de0cb5d29a31badbe5` /
  `2f5bd78093b34d29eff5861ff7e2c056598da20096cb151e6a0f978605f69879`.

Independent original-result/final-document review will be mapped separately in
#437; source approval alone does not assert those results. The actual paginated
metadata refresh was **14:38:32–14:38:40UTC**: main/integration unchanged,135/124
comments in #437/#424, seven dependency PRs/no campaign PR, all four required
checks intact, zero queued/in-progress/waiting runs and the probe ref absent.
These observations are dated metadata, not execution permission.

## Execution boundary and outcome

The proposed workflow is changed **only on**
work/receiver-hosted-capacity-20260922-362aa96c. An initial normal, no-tags push
would trigger the one Linux job; no manual dispatch is needed. The condition
requires branch creation, no deletion/forced push, exact event-head equality and
attempt1. Subsequent pushes/reruns cannot execute its body. The ordinary/main
workflow triggers and all their required checks remain unchanged. The separate
probe concurrency group does not take or alter the heavy-build queue.

The job permits only pinned public-source checkout, the two-case model experiment
and pinned upload of its bounded public captures. No environment gate, read token,
cache/Gradle setup or publisher is mapped. **Do not push before independent
implementation/event-interaction review and explicit owner authorization of the
final commit/tree and single push/run.** Never merge or repurpose this branch.

A fully inspected pass would mean
**EXACT_HOSTED_LINUX_OFFLINE_MODEL_CAPACITY_ONLY**, not native/provider/cache/
resolver/custody/scheduling or Release acceptance. It would not erase any earlier
failed aggregate. Current authority/initializer, productive Stage1 integration,
exact personal Stage1 admission, ordinary qualification/required checks, manual
PR authorization/merge and fresh main evidence/publication approval remain.
Public development sample delivery is still **0/4; Release NOT_READY**.
