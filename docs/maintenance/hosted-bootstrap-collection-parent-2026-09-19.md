# Dormant original-call collection parent — 19 September 2026

Refs [#437](https://github.com/p2pKit/P2pKit/issues/437) and the shared
[#424](https://github.com/p2pKit/P2pKit/issues/424) prerequisite. This increment
continues from `10d84ff74cd0ded793471826cb18fb3b8f15c09e`, tree
`f3c19aacfd8357ece016a7a8141f4331923e02d4`. It does not repeat the accepted
[original-producer binding](hosted-bootstrap-collection-origin-2026-09-19.md) or
[supplied-original file-copy repair](hosted-bootstrap-file-collection-2026-09-19.md).
Main remains `3bc76f956f8f47447b51a62474fc878b9c43173c`.

**Source and offline models only. Apps are not in Releases; audit/release
NOT_READY.** No public command or workflow calls the new operation. Both ordinary
activation HOLDs remain. This record does not authorize native execution or
promote a configuration-only producer into ordinary FULL/Desktop acceptance.

## New original-call boundary

`collect_after_entry(transition)` once-claims the exact transition before calling
the unchanged original producer-binding operation. Constructor/copied-prefix
data cannot substitute for that call, and failed claims remain consumed. The
actual binding return is retained before fallible checks. Its private BOUND
state and the producer's COMPLETE state are not reopened or advanced.

A distinct parent, owner, reader and file handles use source-proposed
**WORK120 / FINAL≤actual-start45,first165 /
READ≤actual-start30,first195**. Every phase also intersects the original
cumulative allocation/job fence and independent LOCAL ceilings. Failed starts
consume their slot; equality expires. READ is only post-close return accounting,
**not file-read execution**. All admission, metadata, copy, membership and
pending-record I/O must finish in WORK. No old owner, cleanup responsibility or
READ30 is revived. These are UNADMITTED/UNMEASURED bounds, not a measured schedule.

New handles reread original source/admission/service/allocation and producer
records. Canonical start/receipt bytes **and their original file bindings** come
from the closed producer. The first report-manifest read has its explicit4MiB
bound; same-byte file replacement is not a new original. The two additional
collection modules' current bytes/bindings are retained before NEW native Git
readmission and compared afterward and after copying. Existing shared stage,
cache-key, source-roster and ordinary caller contracts are unchanged.

Exactly one unchanged file-copy leaf receives these originals. Its actual return
is retained before kind/byte/record validation. Before-copy retained/ must be
empty; after-copy uses a separate current-membership/root/file-binding oracle,
not the producer's empty-retained oracle. Warmed H is not classified as an empty
home or an attested dependency cohort. Newly opened descendant directories verify
their own lifetime; **no earlier descendant continuity, atomic snapshot or
freeze is claimed**. Pending metadata lives in the exclusive initializer sibling
`configuration-collection-parent`, never retained/, canonical invocation, H or S.

Known resource close, handler restoration and final RAW/LOCAL observations
precede the returned `CollectionPrefix`. Its no-loader state remains
`NOT_OBSERVED`, population `NOT_ATTESTED`, tests `NOT_PERFORMED`, budget
`NOT_ADMITTED`, and next-phase/export/save authority false. Partial originals and
first errors stay referenced; encrypted failure delivery is not promised.

## Independent R1 findings and repair

Passive R1 verdict was **REQUEST_CHANGES_EXACT_R1_SOURCE_NOT_APPROVED**. It
identified two source defects, not observed native incidents:

1. A pre-close observation could establish UNKNOWN after the initial guard, yet
   the adapter would still call the resource's close method. The repair checks
   the current private frame, original bindings and owner UNKNOWN at the actual
   dispatch boundary. Uncertain resources remain unattempted; mere expiry still
   permits known once-only cleanup and does not replace the original failure.
2. The NEW query's terminal graph could accept a changed original deadline pair,
   location or invalid/incomplete resource roster as its first baseline. The
   repair retains original supplier/dictionary/root/list/pair references before
   later callbacks, then independently pins the ordered actual pre-final rows,
   owner/type/flag references and original record/readback graph. Even a removed
   non-required row remains referenced. A successful finalizer may add only its
   session-result writer and readback; original rows must remain unique, exact
   and closed. The provisional session receipt is compared with the original
   records, not treated as terminal retirement evidence.

Pre-final binding uncertainty quarantines without dispatching the supplier
finalizer or inventing a return. If the finalizer actually returns but later
terminal validation fails, its actual returned fact stays true and separately
unaccepted. The supplier's original LOCAL pair is not replaced by the later
outer RAW-final/WORK-LOCAL observation. The pre-final-to-terminal pins cannot
prove that no row disappeared **before** their snapshot. No ordinary query or
accepted producer implementation was edited for these repairs.

R1 report SHA-256:
`cb21946c091f9c0ab1227bf1c9cb24acb98d8b38ad94fa8da26ab96ca4809b7a`.
Its separate custody-scope clarification:
`6a25c5a6127bcb6fe01fae29a7b4354b1d9a45e20e84f643cf20f55d947f4c00`.
These reports were read/hash-checked. They do not approve R2.

## Actually executed author controls

Private originals are retained in packet
`20260919-collection-parent-7axzmuz3`. No earlier private Mac original or old
test result was reconstructed. The new test file is
`scripts/tests/hosted-cache-bootstrap-collection-parent-test.py`.

The first invocation **FAILED during test-harness setup**, before any test
method: the constructed future-import AST lacked source locations. Its original
stderr remains. The helper was corrected with `ast.fix_missing_locations`, not
production/assertion changes. On unchanged R1 source the subsequent14-method
aggregate **FAILED:12 intended regression failures,2 positive passes,0 errors**.
Three negatives observed the forbidden modeled close call; nine observed missing
terminal refusals. Neither failed aggregate is reclassified as a pass.

R2 executed **78 distinct methods in five disjoint bounded invocations**:

| Selected classes | Methods passed |
| --- | ---: |
| `ReviewBoundaryModels QueryCustodyModels` | 21 |
| `OwnerModels ReaderModels` | 13 |
| `IntentModels WindowModels` | 19 |
| `OriginalModels LeafReturnModels` | 19 |
| `QueryModels` | 6 |

The12 regression method bodies are unchanged from the failing R1b controls.
The query model was explicitly extended to append the source-shaped terminal
receipt readback and encode its original records; this fixture change is not an
unchanged whole-harness claim. Added controls cover pre-final quarantine, retained
removed owners, exact final additions, record/readback mutation and the NEW
reader's original target/history/no-FINAL-work boundary.

Each invocation used the private recorder's exact frozen sources through:

```text
/usr/bin/timeout --kill-after=2s 20s /usr/bin/python3.12 -I -B -S
  <retained-observer> <frozen-source> <selected-classes>
```

CPU10s, address space512MiB, output/file8MiB, descriptors64 and no core dumps.
An audit hook prohibited subprocess, socket, ctypes, filesystem writes and
directory operations; reads were restricted to four frozen sources and stdlib.
There were no skipped tests, errors or denied boundary events in the five final
invocations. All nine frozen dependency/source bindings and observer bytes
remained unchanged. Exact command/selector/exit/stdout/stderr hashes are retained
per invocation, not inferred from this table.

| Binding | SHA-256 or Git tree |
| --- | --- |
| R1 runner | `c30298b845445d4a68b9d0e7f7cb41d63fcace3768122b3cd90d5497e5f64402` |
| R2 runner | `99bc87263e8d46969c23e68c5e896ec3b01a4e23134d53b1aa7734b51afd5540` |
| R2 test file | `d0fb3bc753b3d344e7ac887096c4cd38b4b37fe48e72219b3c24f67a2f839f31` |
| R2 source/test tree, before these docs | `970eb5e28f27ab1147aa84e22ea53c3d4d62eced` |
| R2 source/test patch | `194322d361310051bba40a2e20b1344bb4b8c58d898dcfd06259ca6ba227d39f` |
| Read-back author result summary | `9558f0c63f8de209076a76a951a4bb193f397c20b71f5f7136810cd4433fc34d` |

The controls execute AST-selected NEW registry/parent/owner/window/reader/query/
manifest/membership/return logic over **memory-only files and modeled upstream**.
Original producer binding, historical/host/toolchain readmission, allocation,
native Git, inventory grammar and file-copy operation are explicit model seams.
The new parent's historical `read_originals` and `host` methods are replaced at
those seams. No old test suite/fixture was imported. This is not a complete
controller import, actual producer/native/private-file collection, genuine
hosted identity, no-loader/provider/cache/timing/custody or product acceptance.

## Independent R2 source and memory controls

Passive implementation verdict:
**APPROVE_EXACT_R2_COLLECTION_PARENT_SOURCE_ONLY_NOT_RUNTIME_QUALIFICATION**.
R1's two source findings were resolved, with no new blocking finding in the
focused delta/context. Report SHA-256:
`6a36da7599d76a54de75db153a9b2ca7ca53e1c7d0da4d4a135ade915e13c83d`.

That report's author-controls-unexecuted wording was stale: the author runs
already preceded the review. The original report was not rewritten. The
reviewer subsequently read all five original plans/results/stdout/stderr,
verified their hashes and exact source metadata pairs, and independently counted
78 unique methods. Additive clarification SHA-256:
`3c908d27644efd3967e489e24f834a42024d1d18362d0c3d7a9e26469560e1ec`.
It preserves the R1 failed aggregates and disclosed R2 fixture change.

The reviewer separately authored **25 controls:8 close and17 query** without
importing the author fixture or an old suite. The exact probe ran once at
**23:29:06–23:29:07 UTC**, child/supervisor exits0/0, measured wall0.968s:
**25 PASS,0 FAIL,0 ERROR,0 SKIP,zero audit denials**. There was no setup failure
or retry. One70-resource expiry control earns one verdict, not70 test cases.
Pre-final UNKNOWN skips the finalizer; post-return invalidity retains its actual
returned fact. The earlier contrary draft Q17 was corrected before execution.

The concrete retained invocation was:

```text
/usr/bin/env -i HOME=/nonexistent/independent-close-query LANG=C.UTF-8
  LC_ALL=C.UTF-8 PATH=/usr/bin:/bin PYTHONDONTWRITEBYTECODE=1
  /usr/bin/timeout --kill-after=2s 20s /usr/bin/prlimit --cpu=10:10
  --as=268435456:268435456 --fsize=8388608:8388608 --nofile=48:48
  --core=0:0 -- /usr/bin/python3.12 -I -B -S <original-probe-copy>
```

Only the frozen runner was read after the audit guard; the probe selected eight
whole functions, seven record definitions and21 exact methods from five trimmed
classes. Their original method ASTs were unchanged. Registration/startup,
predecessor/host/admission, clocks, native query and downstream files were
independent memory models. No complete runner import or native/private-file
operation ran. This is not uninterrupted custody before the pre-final snapshot,
actual producer/collection/provider/cache/loader or hosted acceptance.

| Retained independent original | SHA-256 |
| --- | --- |
| Probe | `ddf2baac4e900e2403607352286166ed092ea4ec25d5d3690aca4a76904058d9` |
| Exact execution plan | `1ccab864e9066473be3d545f9c4a6e65bf0ea9f3d260225208ed39f4d3284328` |
| Outer result | `bb531921bf2670de7c825a87511fff950ade43bdab4a888661bed21318c6b994` |
| Original stdout | `cec9319b0405ae98d4054f277ba91dfcec736c4e90eb96118e80360a756f84e8` |

Original stderr is empty. All13 bound source/manifest/patch/plan/probe/recorder/
tool hash and native-stat pairs stayed unchanged. Private packet:
`collection-close-query-controls.EDfEP78i/independent-r2-original-p4w763_b`.
The reviewer passively inspected the complete original result and independently
matched all41 reported definition/method span hashes to the frozen runner,
without replay. Exact result/source verdict:
**APPROVE_EXACT_R2_IMPLEMENTATION_AND_25_INDEPENDENT_AST_MEMORY_CONTROLS_ONLY**.
Report SHA-256:
`3148529e76f9735944eba03134381dd97f8eac3ad95b197b60ee74750f069b8b`.
This distinct approval does not cover later documentation or replace formal
GitHub approval, native qualification or permission to activate either HOLD.

## Executed source-only gates and final binding

Eight serial source-only commands and their recorder exited0 at
**23:30:31–23:30:34 UTC** on the first complete four-file tree
`e8411c888e9956db516fd4620c9c420863c382e6`, patch SHA-256
`9afedad3135583eaf679db65ca395c63b350a74d5e30b3ebba244d3f8c0517c2`:

| Command | Actual source-only result |
| --- | --- |
| `bash scripts/tests/check-markdown-links.sh` | 619 relative links /126 active Markdown files |
| `bash scripts/tests/check-repository-layout.sh` | 10 projects /15 Android SDK negative controls |
| `bash scripts/check-release-metadata.sh` | Snapshot /published RC3 metadata consistency |
| `bash scripts/tests/check-osv-lockfile-coverage.sh` | 10 lock inputs and bound upstream inventory; **not an OSV scan** |
| `python3.12 -I -B -S scripts/check-hosted-test-composition.py` | Ordinary composition source tripwires |
| `ruby scripts/check-hosted-test-workflow-policy.rb` | Ordinary policy; activation HOLD |
| `ruby scripts/check-sample-app-workflow-policy.rb` | Development-sample workflow policy; no build |
| `bash scripts/check-git-whitespace.sh <base> <base>` | Whitespace /immutable archive bytes |

Here `<base>` is `10d84ff74cd0ded793471826cb18fb3b8f15c09e` and the private
index contains the four-file candidate. Each command used timeout30/kill-after2,
CPU10, address space512MiB, file1MiB, descriptors64 and no core dumps.
All1,441 path/hash/size/executable bindings stayed unchanged; both primary and
private raw index before/after pairs were byte-identical. Every stderr is empty.
The lead and independent reviewer inspected the original streams, hashes and
index pairs. Result SHA-256:
`a97d7131f72bee1383aaafefddd1778472c71cd2bf76272e1b06eb0e885dd083`.

Private originals are in `20260919-collection-parent-7axzmuz3/static-complete-r2-original`.
Process-local `GIT_OPTIONAL_LOCKS=0` and `diff.autoRefreshIndex=false` prevent
Git's optional stat-cache writes; no comparison, assertion or repository setting
was weakened. The earlier binding increment's FAILED static-recorder aggregate
is separate and remains failed, not reclassified or repeated.

Only documentation outcome text changed after that eight-gate tree; the runner
and test bytes remain the exact reviewed R2 bindings above. The containing
commit's #437/#424 mapping supplies the final documentation/whitespace check,
complete four-file review, commit/tree and remote preservation bindings. Those
separate outcomes are not inferred from the eight-gate or memory-control counts.

## Preservation and remaining acceptance

The23:24 UTC snapshot compared all24 worktrees with this increment's retained
22:52 originals: every tip was ancestral, and the other23 raw indices, status,
diffs and untracked sets were unchanged. Twenty other worktrees remain dirty,
deliberately untouched. Protected instruction bytes are unchanged. Preservation
summary SHA-256: `4a4c1f5c210ead7e1059302648e71c39d099a05da44fdeda52d2c22dfb3042fb`.
Earlier disclosed missing-original/stat-cache limitations and the previous
binding static-recorder FAILED aggregate remain; no old index was reconstructed.

Complete paginated metadata refreshed23:27:03 UTC:78 open issues, the same seven
dependency PRs, no campaign PR, zero queried active Actions and two RC Releases
with zero assets. All29 saved endpoint values match the23:18 snapshot, including
relevant complete conversations/timelines. Comparison SHA-256:
`c6085aab3794a482b1a6af710b2dbd8a00c065b13978e2af24d3b9264d39166f`.
This does not claim that the earlier22:30-to22:53 refresh was identical.

No-loader observation → positive export/freeze/save/probe → encrypted custody/
seal/delivery remain unfinished. No production `cache.export_snapshot` or
`cache.save_set` caller or bootstrap workflow exists. The approved named routine
custodian, exact public key, finite14-day policy, legitimate trusted-original-base
bootstrap, genuine native/provider/resolver/custody/scheduling/delivery and
formal different-account PR approval remain prerequisites. Candidate source
cannot authorize its own recipient. Required checks remain `complete-gate`,
`review`, `scan / osv-scan`, `osv-scanner`; agent review cannot replace them.
Only a normal accepted PR may merge, and neither issue is closed here.

Both HOLDs, selectors and credential boundaries remain. Proposed5400 is
UNADMITTED/UNMEASURED; NativeFile900 and Snapshot576MiB are unchanged. Accepted
preview35070982169 and #424 writer35066719641/1/six exports were not repeated;
#372's20 Kotlin tests remain uncompiled/unexecuted. No local application build,
SDK/dependency download, held CI, guest, physical-phone task or production/Maven/
Store publication occurred. OSV remains EXCEPTED_NOT_FIXED, affected Kotlin2.4.10,
expiry2026-10-31, without scan/remediation/exception extension. Historical
206/234 repair-approved and207/234 resolved accounting is unchanged.
