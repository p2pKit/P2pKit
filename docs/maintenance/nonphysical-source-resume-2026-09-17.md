# Nonphysical source resume — 17 September 2026

This follows the [preserved pause](nonphysical-checkpoint-2026-09-17.md), not a
new audit or permission to restart held execution. Continue on
`work/nonphysical-integration-20260915-022112`, not main or an older topic branch.
Exact containing commits, review verdicts and remote readbacks are mapped in
[#437](https://github.com/p2pKit/P2pKit/issues/437). This branch is **not merge-ready**.

## Reviewed source progress

- Commit `77039f9cdcbd00ba21552d69b883eba4727c02bd`, tree
  `86326d6bf5b5e5c6d468e35357fb1334812d9ac4`, is pushed/read back. The checkpoint
  identity implementation received its first implementation review and 29-method
  focused suite. The actual native Git-query grammar needed the exact shallow
  query and two-full-SHA merge-base form; that five-line repair is preserved.
  [Exact checks, original failures and independent review](https://github.com/p2pKit/P2pKit/issues/437#issuecomment-5719961231)
  are source/offline evidence, not hosted identity or producer qualification.
- This continuation adds **pre-budget cohort binding** at plan/stage/intent
  boundaries. It refuses cross-cohort and consume substitution. Recognizable
  bootstrap identity fields, workflow/job and event bindings cannot silently
  fall through the ordinary path when other fields are stripped or relabelled.
  Seed/export/save entry and retained-receipt paths refuse bootstrap **before
  owned acquisition**. Fabricated ordinary FULL ABI fields do not replace the
  missing execution contract. The [maintained cache guide](../testing/hosted-dependency-cache.md)
  describes these limits. Keys still identify dependency bytes, not test results.

The seed source roster also binds the two identity suppliers. Its sole composition
expectation was changed only after independent review of that exact seed delta;
the other five supplier expectations and both ordinary workflow HOLDs are unchanged.
There is still **no bootstrap workflow, CLI/controller/producer/provider caller**,
and no production caller of `cache.export_snapshot` or `cache.save_set`.

## Verification ledger

Final same-source offline checks used installed Linux Python 3.12.3 with
`-I -B -S`, no Java, Gradle, GPG, native process provider, API execution or
dependency acquisition. The containing commit's #437 mapping supplies exact
review bindings and remote readbacks.
Tiny POSIX filesystem/connected fixtures run as the actual unprivileged `nobody`
account in an exact tracked-source-only copy; this is not Linux/Windows hosted
qualification or a fabricated runner identity.

| Executed source/offline check | Final result |
| --- | --- |
| Bootstrap cohort controls | 17/17 PASS |
| Bootstrap identity controls | 29/29 PASS |
| Dependency seed-file controls | 33/33 PASS |
| Dependency cache controls | 75/75 PASS |
| Connected consume/delivery models | 54/54 PASS |
| Selected controller seed-order/failure/FULL-contract models | 4/4 PASS, not the whole controller suite |
| Ordinary composition checker and negative controls | PASS and 16/16 PASS |

The exact commands, original logs, exit codes and before/after source manifest
are retained outside Git. The seed-only composition expectation update preceded
these final checks and followed independent authorization; it was not inferred
from a passing self-generated expectation. Independent seven-method rejection
and three-method interaction controls are separate from the overlapping author
suites, and do not add hosted coverage.

Original failures remain separate:

- First cohort suite: 15 methods, 346 failed subcases before the routing guards;
  the same suite passed after the first correction.
- Independent review found a further downgrade: all flat bootstrap fields could
  be stripped while the bootstrap workflow/job remained. Two new author methods
  reproduced it: 17-method aggregate, 51 failed subcases before the correction.
  The same final suite then passed. The unchanged independent seven-method oracle
  separately failed four assertions before and passed afterward. The reviewer's
  earlier fixture-reset error/log is retained, not reclassified as a product defect.
- Initial root-account filesystem runs failed at unchanged ordinary-UID admission:
  seed33 (9 failures/20 errors), cache75 (75 errors), connected54 (4 failures/46
  errors), selected controller4 (4 errors). These are retained failed aggregates,
  not native runtime findings; no assertion or UID guard was relaxed.

Private author/reviewer originals remain on this device outside Git. The prior
Mac originals listed in the pause are still not automatically available here.
Do not substitute public hashes for originals or recreate expensive runs to
reconstruct missing evidence.

## Next engineering and unchanged authority boundaries

The next bootstrap slice needs a **separate original execution/budget context**,
not ordinary FULL/Desktop with `help`. Retain and rederive actual source/run/clock
and service/native budget originals; a digest or fresh allowance cannot grant
authority. Then bind the canonical configuration-only producer's original start,
requested/executed argv, same-home stop and known complete native retirement;
only afterward permit positive empty-seed-derived export, before/after frozen-byte
observations, original standalone save/exact-probe outcomes and separate encrypted
custody/seal. Existing helpers do not establish those facts for their callers.

The proposed 5,400 seconds is **unadmitted/unmeasured**. Windows native-file lifetime
remains 900 seconds; its 576MiB aggregate Snapshot cannot hold the 2GiB dependency
cohort. No deadline, credential boundary, required check or exact ordinary selector
was widened. The new refusal must not be removed without the separately reviewed
execution/budget integration and meaningful controls.

`.github/test-evidence-recipient.json` is still absent from candidate and fetched
main. A named routine custodian, exact key, finite 14-day policy, legitimate
trusted-original-base bootstrap and formal approval are unresolved. Candidate
source cannot authorize its own recipient. Current native/provider/resolver/
custody/scheduling/delivery qualification remains outstanding.

Refreshed effective main rules retain `complete-gate`, `review`, `scan / osv-scan`,
`osv-scanner`. Agent review is not formal different-account GitHub approval, which
remains mandatory before normal main merge/preservation and eventual development
sample Releases. No PR, dispatch, merge, Release, issue closure, settings change or
production/Maven/Store publication is supplied by this source work.

## Accepted work is not restarted

The accepted locks and preview 35070982169/1 remain preserved. APK/MSI/ARM64-DMG/DEB
are older Actions artifacts expiring September 30, **not main Releases**; none was
rebuilt or redownloaded. #424 writer 35066719641/1 already passed CLI9/diagnostics23
with six inspected originals. #372's collector is source-reviewed, but its 20
Kotlin tests remain uncompiled/unexecuted. #455/#456 already have their source
repairs/reviews in `8808cf2`; their refreshed complete conversations confirm that
current hosted/gate/merge acceptance, not another implementation, remains.

Keep unrelated work and dependency proposals #95/#97/#106/#107/#108/#452/#453.
Historical 206/234 repair-approved and 207/234 historically resolved are unchanged;
audit/release **NOT_READY**. Advisory GHSA-r937-wjx7-w2jp/CVE-2026-53914 remains
**EXCEPTED_NOT_FIXED**, Kotlin 2.4.10 affected, expiry 2026-10-31. Only advisory metadata
was refreshed, not a scan, remediation or exception extension. Physical-phone
work stays deferred. Protected AGENTS.md/CLAUDE.md remain unchanged.
