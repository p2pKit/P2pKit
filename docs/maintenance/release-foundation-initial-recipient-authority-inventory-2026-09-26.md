# Release Foundation: existing authority DATA inventories

## Original implementation/model freeze (before review)

**UNREVIEWED implementation / 18 authored test methods UNEXECUTED / DORMANT.**
This bounded source slice starts from `1199cb743fd388d333fa36050b4b043cda9906d1`
(tree `166c9d8fa8a9b36eb9623e31684a019748cf6f77`). Its independent design approval
was source-only, not implementation or execution approval. It adds no CLI,
workflow, caller, authority acquisition, native reader, copier or export adapter.

## What the declaration helpers do

`_historical_tail_authority_indexes` binds the supplied prior ten inputs and
seal through the existing historical DATA decoder, then requires exactly the
two existing episodes: `authority-2` and `authority-seal`. Each episode's 36
small retained binding inputs declare **279 required files / 58 directories**:

- source-before: 67 files, including all five original files for each12 queries;
- source-after: the same67-file grammar;
- acquisition-queries: 137 files, including all five files for each24 queries;
- context plus six service originals and child result: eight files.

The pair is therefore **558 required file declarations / 116 directories**,
not proof that those files were read or delivered. Exact query IDs, argv order,
caps, known/success declarations and every original readback row are checked.
The declarations bind log sizes/hashes to their original query summaries and
`result.json` to the canonical bytes of that same declared query. Source-return
session/input/clock links and typed historical chronology are retained. Context,
source/job/policy/window/predecessor and actual supplied phase/child/input hashes
remain bound even when a substituted outer record is rehashed.

Only the two genuinely declared root/service pins per episode are retained as
historical pin DATA. The other112 directory rows explicitly have no native pin;
they must not acquire one merely because an index mentions their paths. Native
phase bodies and query ownership/output metadata are **not completely qualified
by these helpers**: they are hash-bound historical bytes/declarations, not
reconstructed `SourceReturn`, `OriginalPhase`, native-owner or Recipient objects.

The output is canonical private DATA with `NOT_COPIED`, readback/native acceptance
`NOT_PERFORMED`, B `NOT_IMPLEMENTED`, false productive/cache/export-save authority
and budget `NOT_ADMITTED`. Every emitted row still needs a fresh owned actual
size/hash/EOF/metadata/close read; empty streams are not silently accepted as
already read. No original owner/Step/service outcome is authenticated here.

## Authored controls, not executed results

`scripts/tests/hosted-initial-recipient-tail-inventory-test.py` contains18 new
methods. They construct small in-memory query grammar and inert prior records;
no old test/fixture is imported and no disk tree is created. Positive controls
cover the complete declared pair, original counts/hashes, and gate/worker role
fields. Negatives cover missing/extra episodes or inputs, query/readback
truncation/duplication/order, command/executable/limit/status changes, path and
field substitution, stream/result bindings, source links, independently rehashed
context/source/job/window changes, cross-episode pin alias and original chronology.
A cap control declares oversized bytes without allocating the claimed data.

Dedicated controls preserve explicit nonqualification of opaque native bodies
and unchanged registries; they deny live owner/source/key/clock suppliers. The
audit hook rejects processes/network/native loading/environment mutation and,
during guarded calls, file-open and directory-enumeration events. It is not a
claim to intercept every possible low-level stat/read. Source review must verify
the pure path. No GPG key-identity helper that samples current time is invoked.

No Python syntax/import/parser check or test execution has occurred for this
increment. Only source/Git inspection and `git diff --check` were performed.
The separately reviewed [15/15 historical seal DATA result](release-foundation-initial-recipient-sealed-data-2026-09-26.md)
keeps its exact earlier source/input scope; that suite and the accepted reader
were not replayed. These18 authored methods are not added to its pass count.

## Contemporary review and execution addendum

Independent implementation review approved the exact source/model at commit
`75fd4337daced5ff8210f08902fbf7c531c45880`, tree
`4db6aa9c3ed8fb26925c879fffc3bd8d7ea2b73f`, with no blocking finding. The
approval was specifically for the existing-authority DATA declaration slice
and its18 new model methods, not actual reads, native semantics, B or complete K.
The original pre-review freeze above is retained as dated accounting.

After separate owner authorization and independent exact packet/guard review,
**one invocation passed all18 new methods: 18/18, aggregate OK, Python/child/
wrapper/terminal exits0**. It ran from `2026-09-26T19:06:23.160853421Z` to
`2026-09-26T19:06:25.082520969Z`. Unittest reported0.697s, the wrapper1.920s,
and the actual Python process's terminal self CPU was1.814490s. There was no
rerun and no warning, ResourceWarning, traceback or failed method. Gate/worker
and four-role fields were in-memory grammar, not four-platform native runs.

The freshly extracted exact Git source inventory recorded1,458 files and
1,847 metadata rows, including the extraction root, all root-owned read-only.
Fresh HOME/TMPDIR were UID/GID65534-owned0700 and empty.
The actual Python PID10736 verified UID/GID65534, clean environment, eight exact
focused source hashes, `getrlimit` and original `/proc/self/limits` before any
candidate/model import. Immediately before
`python3 -I -B -S -W error::ResourceWarning`, **after** runuser/PAM and the final
clean environment, `prlimit` applied CPU60s, address space768MiB, output
file16MiB,128 FDs,32 processes and core0. The separate outer wall90s plus kill5s
timeout enclosed runuser and began before PAM. The same-process guard selected exactly the18 reviewed
methods fail-fast and verified unchanged limits/nonroot identity at RETURN.
ENTRY/RETURN retained the same PID; the process was absent after normal return.
All source-byte, metadata and guard comparisons passed; HOME/TMP remained
actually empty. The one-invocation marker remains present with the originals.

Independent actual-result review read the full original log/status/timing and
verified the exact method roster, aggregate, effective limits, all exit and
comparison statuses, retained hashes, current source/metadata inventories,
empty HOME/TMP and process absence. Its verdict was **PASS as local DATA models
only**, with no native, provider, B, complete-K, custody or integration acceptance.
Exact source and private-original hashes identify this narrow result:

| Item | SHA-256 |
| --- | --- |
| Declaration source at `75fd4337` | `e8fd7fa548e8d5c9acf6e65a981f98583a5a5d2d30e424512cc4969e87bc57ea` |
| New18-method control file | `4143d340d8daf4bd435c6cf21b2d47da2952971d21ae38ca1586b285aa87d950` |
| Full original control log | `e186d796850939593ceb05b05a78084092be6356f080caf6a651eae238ed2fa8` |
| Original wrapper resources | `cff773e3a49e3863808e0fab3883af32d3db7447325a4d909f40c8d4d42981ce` |
| Original invocation status | `723ae66a4adfca7d405c71b180c304b55a5eeabbc34134effc8724f80858cf00` |
| Author's result inspection | `7b9e9f0219b93b448a6e7f4a70d424564206db28a33f22413de33363f3c40720` |
| Original result hash manifest | `36dccb7725b77ed97df88740114a125ab20b7b1b4ba5b0377f4cc9788b157a7e` |

This addendum changes documentation only. Runtime/model bytes remain exactly
the reviewed/tested source. It does not authenticate supplied historical Step
outcomes, manufacture558 file reads or restore any native owner/clock/Recipient.
No accepted D15, seal suite, reader or native composition was rerun or added to
these18 passes. Genuine B protocol/originals, remaining K groups, owned capture,
tail export/carrier, U/A custody and all execution prerequisites remain below.

## Remaining prerequisites and unchanged HOLDs

This is not the complete K packet: returned/crypto-service/seal coverage, P0's
post-validation diagnostics and map reference, fresh B's actual exhaustive
originals, new same-process public Recipient validation, owned capture/freeze,
new closed tail manifest and four-file carrier remain further source work.
No B DATA schema, dummy authority or complete-K success shape is invented.

P0's two files and schema4 stay unchanged. Future K must preserve all required
private originals, with one aggregate512MiB plaintext/576MiB ciphertext/10,000
members including both archive roots. Final crypto/keyring-guard/transport raw
diagnostics have the selected finite disposition: private `NOT_DELIVERED` and
`NOT_USED_AS_QUALIFICATION_ORIGINALS`, not recursive export or declassification.

B/K work/close/output must finish before the original sealEnd; genuine U starts
before sealEnd and closes before uploadEnd; A has only its original15s and
afterEnd ceiling. No renewed allowance, unadmitted5400s or Windows900s/576MiB
expansion follows. Genuine native/provider/custody/decryption/scheduling and
productive/ordinary acceptance remain unresolved. Both activation HOLDs, the
whole-JVM interlock, credential boundaries and required formal review remain.
