# Dormant bootstrap producer-command descriptor — 18 September 2026

Refs [#437](https://github.com/p2pKit/P2pKit/issues/437) and shared
[#424](https://github.com/p2pKit/P2pKit/issues/424). This is **source and focused
offline preparation**, not a launcher, accepted cache, hosted execution or app
Release. Base `fffffc73b60e4120dee2baee75f2f226bdded7d8`, tree
`3857398620107e9188cff1ad738f1e9aad01f78d`, already preserves the reviewed
[staging parents](hosted-bootstrap-staging-parent-2026-09-18.md).

## Missing adapter and exact scope

[`hosted_cache_bootstrap_producer_command.py`](../../scripts/hosted_cache_bootstrap_producer_command.py)
connects the existing strict `producer.make_request` contract to the existing
separately hash-bound canonical helper. It does not duplicate producer/context
validation, change either supplier, import an ordinary controller, or add a
production caller. Ordinary FULL/Desktop and transcript preparation are untouched.

`command_request(admitted_raw, canonical_raw, invocation, ancestor_invocations)`
requires exact immutable bytes and isolated Python. Invocation and ancestry are
keyword-only; the existing producer rejects anything other than its exact bounded
list/tuple grammar. Its detached request and original ROOT/SCRIPTS are captured
before source/interpreter suppliers. Cwd and applicable lexical wrapper must agree
with the current source location; state is derived lexically from the checked home.
Neither supplied source/host labels nor that derivation admit a real source/host,
existing state, owner or original chain of ownership domains.

The descriptor binds exact request bytes/hash, admission/context hashes, current
resolved interpreter, independently expected helper hash and precisely two child
sources: `audit_processes.py` and `run-audit-command.py`. The fixed suffix after
the unchanged isolated two-supplier loader is:

```text
--cwd <root> --wrapper <applicable-wrapper>
--purpose cache-bootstrap-configuration --kind gradle --id <invocation>
--timeout 600 --stop-timeout 120 --
help --console=plain --no-configure-on-demand
```

There is no execution subcommand, runtime `--state`, optional `--receipt`, arbitrary
command or implicit7200 timeout. The canonical executor would append enforcement
itself; the descriptor does not append it twice. Configuration-only `help` cannot
satisfy ordinary ABI, simulator, Test-task loader/XML/transcript or dependency-
population acceptance.

Source/helper/interpreter and original location are rechecked before returning
private JSON. The existing interpreter reader observes path and filesystem metadata,
**not executable bytes**; source observations are point-in-time, not an atomic
freeze. First supplier exceptions, including cancellation, escape unchanged.
No effective environment is constructed or inherited: `environment` remains
`NOT_BUILT_OR_ADMITTED`. Real original domains, installed toolchains, credential
screens and source-custodied environment are future-parent obligations.

## Timing proposals are not enforced deadlines

The separate closed allocation counterpart must still say work600, return225,
final45 and read30, in that order. Same-home stop120 is **included in225**; native
capture membership excludes read30, giving only proposed arithmetic600+225+45=870.
Windows NativeFile900 and Snapshot576MiB stay unchanged; no Snapshot or2GiB copy
path is added. The5400 job proposal is **UNADMITTED / UNMEASURED**.

Canonical `wait_process` begins its local timeout after spawn and stream setup.
Fixed600/120 flags alone therefore cannot enforce original allocation anchors,
bound all startup/drain/tail overhead, or prove870 capture. A future parent must
shorten against original shared-clock/job/phase/capture fences and genuinely own
cancellation and retirement. This descriptor creates no clock, allowance, owner,
state/home, capture or process. Repeat calls are repeatable data, not single-use
execution authority. Test acceptance stays `NOT_PERFORMED`, budget `NOT_ADMITTED`,
and next-phase/export/save authority false.

## Executed author controls and original setup failure

Own-class test source:
[`hosted-cache-bootstrap-producer-command-test.py`](../../scripts/tests/hosted-cache-bootstrap-producer-command-test.py).
The test entry/selection is:

```sh
python3 -I -B -S scripts/tests/hosted-cache-bootstrap-producer-command-test.py CommandModels -v
```

The actual retained invocation uses an immutable source copy, a same-process
interpreter observer around that entry, actual UID65534, external90 seconds plus
kill5 and recorder105. It ran **28/28 own methods PASS**,0.617 seconds outer and
0.415 seconds unittest. It inherits/discovers no historical suite and does not
repeat accepted application/native results. All1,420 source hashes/rosters stayed
unchanged; separate raw before/after frozen-source and repository manifests are
retained. Actual before/after interpreter observations bind Python3.12.3,
`/usr/bin/python3.12`, executable SHA-256
`1643dacd9feaedc58f3cc581e4d22577dfe25c09b10282936186ccf0f2e61118`, flags, UID and
filesystem metadata. These new observations do not fill any older missing version
binding or authenticate the entire Python runtime/native host.

The preceding recorder attempt exited127 before Python started: its minimal PATH
omitted the installed `runuser` directory. **Zero tests ran** and no interpreter
observation exists for that attempt. The corrected recorder resolves external
`timeout`/`runuser` paths explicitly; source, assertions, UID and90+5/105 bounds did
not change. The failed attempt's recorder copy is retained and separately hashed
by the passive audit; its result did not self-hash that recorder. The successful
recorder adds that binding. Failure/recorder bytes and successful receipts remain
private in packet `20260918-bootstrap-producer-command-8c8fama3`.

Actual tiny POSIX source reads and fixed-helper assembly execute. Admission/context,
role labels and deliberately changed suppliers are modeled. Controls cover exact
argv/request bytes, interpreter/source/location rechecks, invalid byte/cohort/
ancestry/API inputs, detached ancestors, first failures, unchanged existing state,
non-copying of synthetic tokens, and allocation/stop/capture mutations. Process,
network, clock, native/domain and initializer routes are poisoned. Canonical child
source is read/passively parsed, **never executed**. No Gradle, initializer,
producer, same-home stop, native retirement, provider or custody execution follows.

Implementation SHA-256:
`3502c705a85a7f20d478730c60bf2cb39d3ef27b645c14dbf352b383712b6c7f`.
Test SHA-256:
`c5015278a3352e5f3a8b5082d0e1e479472d7219c5593c2698ccbbd93e2e34f3`.
Author source tree `f4283926c42a7316f412d99f42274d0889b320c9` contains the two new
files, before this documentation. The pre-execution source change was only a
docstring precision correction: source/interpreter observations do occur, while
producer outcomes do not. It changed no executable statement or assertion.

Independent design verdict:
`APPROVE_EXACT_PRODUCER_COMMAND_DESIGN_ONLY_NOT_IMPLEMENTATION`, report SHA-256
`55c86ed58dbe5d087f42954143a82c144a3d7aa3dc4adf3c492e026658362137`.

Independent implementation verdict:
**`APPROVE_EXACT_PRODUCER_COMMAND_R2_SOURCE_AND_OFFLINE_ONLY`**, report SHA-256
`a3ab41ee8b71cb749b73593a112fc969003a991dc9cfc0d2f32fce76cdab5e8b`.
No blocking implementation finding. The independent13 own-class assertions were
frozen before reading author assertions, SHA-256
`8d3ae54a48c139bff851b31f792e23b7fab9ff68380d003fbe89b15f00a2a5bb`.
After explicit serial handover they ran **13/13 PASS** in one invocation,
0.365 seconds outer/0.083 seconds unittest, under actual UID65534 and isolated
Python3.12.3. Their separate same-process before/after executable/version/metadata
observations and all1,420 unchanged source bindings are retained. Actual descriptor,
producer contract, fixed helper and tiny POSIX reads execute; independent child
source fixtures are synthetic bytes, never compiled/executed. No author test or
historical suite was imported/replayed by those independent controls.

The independent original audit SHA-256
`304a3867094b1b0d2b3b71257353e2a5c55e4a4df87b378ab4c84adb7c99058d`
checks both author attempts, source/base blobs and modes, exact patch, manifests,
method rosters and independent originals. The reviewer's first source copy already
contains implementationR2; its separately labelled reconstruction of the earlier
one-line docstring is not original evidence. No earlier execution is inferred.
Complete-patch/passive-check/commit preservation bindings are mapped separately
in the linked issues. Agent review is not formal different-account GitHub approval.

## Current metadata and remaining delivery

Complete paginated relevant metadata was refreshed at16:14:05UTC:78 open issues,
seven retained dependency PRs, no campaign PR, no queried active Actions, and two
RC Releases with **zero assets**. All44 requests were exit/hash-checked. Compared
with the previous15:48 capture, changes are the preserved staging commit/comments
and their nested repository/issue timestamps, not a new owner authorization.
Detailed dependency-PR reviews remain separately dated. Required checks still are
`complete-gate`, `review`, `scan / osv-scan` and `osv-scanner`; the owner's formal
different-account review requirement remains even though repository review count
is0. Agent review cannot satisfy it.

The bootstrap workflow and production `cache.export_snapshot`/`cache.save_set`
callers still do not exist. Separate configuration-custody preparation and actual
source-owned producer launch, original producer/same-home stop/complete enclosing
retirement, positive streamed export/freeze/save/probe, encrypted custody/seal,
native/provider/resolver/scheduling qualification and normal reviewed main merge
remain. Both ordinary activation HOLDs remain. A named routine custodian, exact
public key, finite14-day legitimately trusted-original-base policy and formal
approval are still missing; candidate source cannot authorize its own recipient.

Preview35070982169's APK/MSI/ARM64-DMG/DEB remain older Actions artifacts expiring
September30, not Releases; they were not rebuilt/redownloaded. #424's accepted
writer CLI9/diagnostics23/six original exports are not repeated or downgraded.
#372's20 Kotlin tests remain uncompiled/unexecuted. Physical phones and production/
Maven/Store publication remain excluded. Advisory GHSA-r937-wjx7-w2jp / CVE-2026-53914
is still **EXCEPTED_NOT_FIXED**, Kotlin2.4.10 affected, exception expiry2026-10-31;
metadata refresh is not a scan, remediation or extension. Historical206/234
repair-approved and207/234 resolved stay unchanged. Audit/release **NOT_READY**.
