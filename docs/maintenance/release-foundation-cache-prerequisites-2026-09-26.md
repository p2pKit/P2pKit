# Release Foundation cache prerequisites: source-only extraction

**Status: independent extraction review pending; no source execution or hosted
qualification.** This is a dependency-preservation increment, not a new audit,
workflow activation, runtime repair, successful build or Release acceptance.

> **Scope correction after extraction review:** this extraction is an
> **unintegrated reference, not a wholly dormant or active-FULL-safe increment**.
> The imported Python/Gradle `GITHUB_ACTIONS` + `complete-gate` selectors already
> require ordinary simulator custody, while Foundation's unchanged CI still
> calls FULL directly. Absence of new Action/workflow callers does not remove
> that incompatibility. Do not integrate it by weakening those anti-fallback
> requirements. The provenance below and its unchanged TSV describe extraction
> commit `8831e589176aa0b3f2df3b8810d070abf46f3ec6`, not later adapter bytes.
> See the [separate schema3 source-only increment](release-foundation-sample-schema3-2026-09-26.md)
> for its limited consumer change and still-pending implementation review.

## Exact source provenance

- Foundation parent: `3359862eb812d0130528909a2f90db716bee7843`, tree
  `3688bc3f69149891f85081cd7af0d1c39ee58764`.
- Sole extraction donor: `72cae80c0057257dd0b32b4b91cdcb8449734afc`.
- Branch: `work/cache-prereqs-foundation-20260926-094031`.
- The new branch starts at the Foundation parent. No donor merge, ancestry
  replacement, old dirty worktree read, reset or force operation was used.
- The [complete file roster](release-foundation-cache-prerequisites-2026-09-26.tsv)
  identifies all **191 donor files**: 73 runtime/support files and 118 focused
  test/fixture files, totaling 7,950,758 raw bytes. There are 185 additions and six
  replacements. Every extracted file retains its exact donor blob and mode.
- Roster SHA-256:
  `b83036064e9147b7807beada8e2d4c9ac52fed530d91b05a15111a8140d54968`.

The TSV records group, change, path, original Foundation blob (or `-`), donor
mode/blob and SHA-256. It is source provenance, not a native evidence manifest.
This note and its TSV are newly authored; they are not donor files.

## Retained dependency closure

The extraction retains the existing bootstrap identity/origin/allocation,
canonical initializer, producer, staging, collection, no-loader, dependency
export and save-set suppliers; provider Action/Node/native supervision and
readback; initial-recipient contracts, original acquisition, continuity,
initializer and custody drivers; ordinary admission/custody, ABI, simulator,
supplement, clock, native-file and evidence helpers. The matching focused
Python/Node controls and transcript Gradle fixture are retained as source.

Two local Actions are present, without workflow callers: the provider Action
(two files) and initial-recipient initialization composite (one file). The
required transcript-custody and platform-coverage Gradle contracts are present.

`hosted_lock_resources.py` is included because the Mac clock/provider's closed
supplier roster imports its shared RAW clock. Its historical filename does not
import the lock writer or authorize its resource-observer CLI. The private
`prepare-audit-central-bundle-metadata.py` helper is a required ordinary FULL
supplement supplier, not a publication operation. Existing Foundation
`prepare-audit-consumer-metadata.py` and `check-audit-receipt.py` are byte-identical
to the donor and were not rewritten.

The six overlapping files were inspected before replacement:

- `scripts/audit_processes.py`
- `scripts/run-audit-command.py`
- `scripts/run-platform-tests.py`
- `gradle/platform-test-coverage.init.gradle`
- `scripts/tests/run-audit-command-test.py`
- `scripts/tests/run-platform-tests-test.py`

All six were unchanged between original main
`3bc76f956f8f47447b51a62474fc878b9c43173c` and the Foundation parent. The donor
changes retain native lifetime/retirement, canonical stream/deadline/report
custody and ordinary simulator binding. No newer Foundation repair was replaced.

No donor workflow, historical lock writer/preview workflow, audit menu, phone or
Android instrumentation, product source, root build file, release gate,
packager/version helper, Maven workflow or publisher was imported or edited.
AGENTS.md, CLAUDE.md, published RC history and Foundation fixture files remain
unchanged. No source/Actions/settings operation or publication is authorized by
the presence of these dormant helpers.

## Source bindings and static checks

The existing `check-hosted-test-composition.py` and all seven expected supplier
digests are retained unchanged, with their exact donor supplier bytes. The
canonical helper raw pin remains
`432c06f0be8f98db286979b7adc58365d9eafde739c023ac13af1f227acaeba4`.
**No source-binding expectation was updated.** The AST composition checker was
not executed. Future supplier/ordinary packager integration changes need exact
independent review and an explicit reviewed tripwire update, not a copied pass.

Executed checks were bounded Git/shell source inspections only:

- Verified all 191 extracted files against donor Git blobs, executable modes
  and raw SHA-256 values; inspected all six Foundation overlaps.
- Inspected literal local module/tool and focused test-helper references; the
  referenced existing donor script/test names have local source counterparts.
  This is not a parser/import or dynamic dependency-completeness test.
- Verified protected instructions, Foundation workflows/release files and
  sample/library source were unchanged, and donor ancestry was not introduced.
- Checked staged whitespace and the exact staged path roster before committing.

No Python/Node/Ruby parser, syntax check, import, helper suite, Kotlin test,
Gradle/Java/Xcode command, simulator, CI dispatch, provider, download, encryption
or private-key operation was performed. No previously accepted reader, writer,
preview or build was repeated. Historical test/review results retain only their
original source/input/environment scope; they do not qualify this composition.

## Concrete unresolved integration dependencies

### Required fixture postimages are not part of this extraction

The preserved Foundation CLI fixture has four `@Test` methods and diagnostics
has seventeen. The extracted `test-transcript-custody.py` requires exactly nine
and twenty-three. This is a static source incompatibility, not an executed test
failure. Do not reduce those counts or infer the missing tests from a prior run.

Both missing postimages originate in donor ancestor
`56bbb488f5148097573aeb82dc27c25e2d974f6f`,
`test: retain subprocess originals when finalization fails` (Refs #424):

| Fixture | Foundation equals original preimage | Donor equals original postimage | Required addition |
| --- | --- | --- | --- |
| `CliShutdownTest.kt` | `1edb66c041c67ef87f397e731e12a4a1832ef118` | `72af8056fe110af24fedadc198f9b50e801ab951` | Five finalizer controls, 4 to 9 |
| `RollingJsonlFileSinkTest.kt` | `c3a597bec10883379ca2b9f40510bf5a57f672e6` | `b634cd52e4fc4e2d549f6edba36e1e5745f23312` | Five analogous controls plus enclosing-directory custody, 17 to 23 |

The postimages preserve the earlier exact inherited-JVM-startup-note repairs.
They add independent retirement/input-close/export attempts, primary-error
preservation, explicit original-output holds and deletion only after successful
retention; diagnostics also holds unknown/raw enclosing-directory contents.
These are test-harness changes, not production protocol changes. The original
commit also has 28 testing-document additions. Selective fixture integration
requires a separate reviewed increment; no sample file was changed here.

### Other source and execution boundaries

- **Packager contract:** the extracted ordinary `sample_snapshot()` requires
  manifest schema 2. Foundation's preserved packager emits schema 3 with
  `versionBinding` and inspected application identities. Adapt and independently
  review the consumer, not the accepted Foundation producer. No adaptation or
  composition-pin change was made here.
- **Typed origin:** Stage1's reviewed-head identity still cannot enter legacy
  ordinary `Admission`, staging input reconstruction, producer/context readers,
  save/probe admission, provider-native admission or the legacy bootstrap export
  adapter. It needs a separate current-authority/known-return bridge; no cast,
  removed origin field or renewed historical capability is acceptable.
- **Initializer layout:** Stage1's existing `...-recipient-initializer` layout
  differs from the legacy `...-productive/initializer` required by `stage_path`.
  Preserve the original initializer and shared literal provider path; do not
  rerun/move it to impersonate the legacy layout.
- **Custody:** the initial-recipient public CLI implements `collect-export` and
  `collect-close`, not `seal`, `upload-before` or `upload-after`. Its retained
  nonproductive gate/initializer packet is not the future productive
  producer/export/freeze/save/probe qualification packet.
- **Stage2:** staged matchers bind references only. A Stage2 original acquirer,
  distinct current authority, actual qualification/compatibility inspection and
  whole-job gate integration remain. Preserve both ordinary activation HOLDs
  and the whole-JVM interlock when workflows are later integrated.
- **Tests:** some donor files retain date-specific slice assertions. For
  example, `hosted-cache-bootstrap-custody-parent-test.py` asserts absent policy
  and donor workflow HOLD text; readmission/recipient-parent controls also assert
  an absent bootstrap workflow. These assertions were not removed or weakened.
  Their unchanged files are not a claimed passing aggregate for Foundation or
  current donor. Review exact intended selectors before future execution.

## Authority and qualification remain external gates

The exact committed **public** recipient policy is retained (SHA-256
`2e90a1ed038d5bb6759d8d22e1bb5468331b49274a6956df470c1e785691f521`), including
the previously named custodian and prepared September 21 to October 5 window.
This is neither a private-key copy nor trusted-main policy admission. Calendar
opening does not authorize execution; expiry must not be silently extended.

The imported staged contracts still pin original main B and the old integration
ref, not the new Foundation branch. Fresh final-source owner statements,
actual environment/ref configuration, exact run/attempt allocation, legitimate
original-base policy bootstrap, required GitHub checks/formal review and normal
merge remain necessary. No current GitHub metadata/configuration was refreshed
by this extraction, and no setting or approval was changed or inferred.

Bootstrap 5,400 seconds remains **UNADMITTED / UNMEASURED**. Existing native-file
900-second and 576MiB Snapshot limits are unchanged; the latter is not a 2GiB
dependency cohort. Configuration-only execution supplies no ordinary ABI,
simulator or transcript acceptance. Genuine native/provider/resolver/custody,
delivery, scheduling and source compatibility qualification remains required.
No Release readiness, issue closure, Maven/Store publication or physical-device
acceptance follows. **Execution HOLDs remain; sample delivery is not accepted.**
