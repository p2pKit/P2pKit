# P2pKit Test Coverage Plan — 2026-07 review campaign

Consolidated from the "Missing tests" sections and per-finding "Required tests" lines of all
18 campaign reports in `.review-2026-07/reports/` (A01–A16, incl. A13a/A13b, A14-robustness,
A14-sec, A15-perf). Branch `audit/exhaustive-review-2026-06`, HEAD `870bf10`.

Every proposed test from every report appears exactly once below; where several reports asked
for the same test, one row carries all the finding IDs it covers, at the highest priority any
report assigned. Doc-only findings whose "Required tests" line was `n/a` are excluded.

## 1. Summary

**Counts per priority (main table):**

| Priority | Rows |
|---|---|
| P1 | 32 |
| P2 | 70 |
| P3 | 53 |
| **Total** | **155** |

Of these, **12 rows are manual/hardware** (2 × P1, 4 × P2, 6 × P3); plus 2 standing
manual/hardware items in §5 → **14 manual/hardware rows total**.
**14 rows are blocked on a shared-fixture upgrade** (§2, F1–F9); a further ~10 rows need a
product-side injection seam or extracted helper, and 5 rows need a new test source set (§2.2).

**Per-module / per-source-set breakdown** (split rows counted in each location they touch):

| Location | New rows | Share of plan |
|---|---|---|
| `p2p-core` commonTest | ~89 | largest gain by far (~57%) — session/reconnect determinism, protocol input-validation and resource-limit caps, file transfer, registry/identity, kit lifecycle, builder |
| `p2p-transport-lan` jvmTest (incl. loopback) | ~14 | fd release, write watchdog, admission control, steady-state discovery, parity pins |
| `p2p-transport-lan` appleTest (runs via iosSimulatorArm64Test) | ~12 | discovery-callback crash prevention, rebind hooks, close semantics, manual-IP loopback |
| `p2p-network-provisioning-android` androidHostTest | 6 | handle/reservation lifecycle, timeout mapping, support gates |
| new `p2p-core` androidHostTest (does not exist yet) | 3 | Android peer-id storage, permission managers |
| `p2p-core` jvmTest | 5 | peer-id storage migration/corruption/permissions |
| new `p2p-core` iOS test source set | 1 | NSUserDefaults peer-id storage |
| `p2p-network-provisioning-desktop` test | 2 | manual-peer validation pass-through, networkState poll |
| `sample-kmp-shared` (incl. new androidHostTest) | 2 | consumer factory, consumer loopback sibling |
| build logic / scripts (TestKit, release scripts, CI greps) | ~11 | publish artifact set, signing, BuildInfo idempotence, doc-signature guards |
| samples (desktop-ui unit, new p2p-sample-desktop test set) | 2 | destination uniquification, CLI arg parsing |
| manual (device matrix / INTERNAL_TESTING / smoke) | 12 (+2 standing) | Android LAN paths, iOS build provenance, hardware-only behaviors |

## 2. Fixture upgrades required first

### 2.1 Shared test-fixture changes (from A12-tests)

These change files under `p2p-core/src/commonTest/.../testfixtures/` (plus one internal wiring
knob and one test-infra convention). Certain P1/P2 rows below **cannot be written** until they
land; each entry lists what it unblocks (by the finding IDs of the blocked rows).

| # | Fixture change | Source finding | Tests it unblocks |
|---|---|---|---|
| F1 | `FakeRawConnection` remote-termination fidelity: `breakWith` closes the receive channel **without** a cause and flips state (matching every shipped transport, whose `read()` never throws — it completes normally and sets state Closed); peer-side `close()` must propagate to the partner (partner state → Closed, partner writes then fail); add a pair-level `hangUp(side)` helper; keep an opt-in `breakWithException` only for tests that deliberately exercise the defensive path | TST-1 (also SES-10) | The two P1 remote-termination rows: abrupt remote termination → deterministic `Reconnecting` (SES-1/TST-1/CON-7) and the CLOSE-then-socket-close race variant of the clean-close row (SES-1/TST-11); regression that `observeRawState` fires on remote close |
| F2 | Write-fault injection in `FakeRawConnection`: injectable `writeFailure` (platform-shaped raw exceptions), `suspendWrites()` / gated writes, `failNextWrite(t)` (transient failure on a still-open connection), per-write latency — promote the semantics of `CloseSemanticsTest`'s private `WedgedWriteConnection` into the shared fixture | TST-2, TST-5 | `send()` typed-error contract (API-2/SES-2/TST-2, P1); keep-alive pre-send timeout with a wedged writer (A03, P2); redial-vs-stale-dead-slot (SES-4, P2); PING/PONG interleaving mid-send (PERF-6, P2); handshake bounded by handshake timeout not watchdog (SES-11, P3) |
| F3 | `FakeDataTransport.failIncoming(cause)` — allow the incoming flow to terminate with an error, as the real accept loop does (`close(e)` on accept failure) | TST-3 | Accept-loop failure → kit survives / surfaced (CON-3/TST-3, P1 — the commonTest half) |
| F4 | `FakeDiscoveryTransport` buffer semantics aligned with production (`extraBufferCapacity = 256`, `DROP_OLDEST`, `tryEmit`-shaped emit; optional `strictDelivery` flag; fix the overclaiming KDoc) | TST-4 | Registry burst-convergence-under-drop test (TST-4, P3); makes the SEC-2 bounded-registry test run against production-shaped delivery |
| F5 | `FakeDataTransport` start-contract + close-visibility: start-behavior knob (record calls; fail-after-close per the real contract; fail-on-demand), expose `isClosed`, make `emitIncoming` check its `trySend` result | TST-6 | `connect()` surfaces `TransportStartFailed` on lazy-start failure (API-5, P2); materially aids the two-transport lifecycle rows (ARCH-5) and lets kit-close tests drop private `TrackingTransport` copies |
| F6 | Internal `strictInvariants` threading: an internal-only knob (internal ctor param or commonTest `TestHooks`) so kit-level suites construct `SessionManager(strictInvariants = true)` — no public API change | TST-9 (also SES-8) | The P1 strict-suites row: every existing behavioral suite becomes an invariant net, plus one meta-test that a violation inside a kit-built store throws under the flag |
| F7 | `RecordingLogger` promoted to `testfixtures/` + optional "no unexpected warn/error at teardown" convention for heavyweight suites | TST-13 | Warn-only diagnostics become assertable: ZOMBIE-session warn (PERF-5 row, P3), stuck-Reconnecting watchdog, PONG/PING send-failure warns, SessionStore soft invariants, optional IDN-8 log assertion |
| F8 | Synchronize fixture mutable state (`writtenChunks`, `_connectCalls`) with a lock/atomic snapshot | TST-7 | Not a blocker for a specific row; prerequisite hardening so the new multi-thread/stress rows (FIL-5, IDN-2, SEC-1(b)) don't inherit a latent fixture race |
| F9 | Virtual-time migration for pure-fake suites (`runTest` + injected time; the kotlinx-coroutines-test dependency is already declared and unused in 5 modules) | TST-12 | Refresh-coalescing count at K reconnecting sessions (PERF-7, P2) and the ~3 s refresh-cadence row (A03, P3) — both need a fixed virtual-time window; also stabilizes KeepAlive cadence tests and the structural rewrite of the TST-10 rows |

Strictly blocked on F1–F9: **14 rows** (F1: 2, F2: 5, F3: 1, F4: 1, F5: 1, F6: 1, F7: 1, F9: 2);
several more are materially aided (F5/F8).

### 2.2 Other test-infrastructure prerequisites (from A05/A06/A09/A14/A16 — product-side seams and new source sets, not shared fixtures)

- **Seamed `JmDNS.create` / handle factory** (A05) — unblocks the P1 Android rebind-machinery
  row (DSC-3/4/5/13) and the refresh-rotation listener-leak row (DSC-7).
- **Injectable write-watchdog timeout** in `JvmRawConnection`/`AndroidRawConnection` (CON-14) —
  unblocks the P1 transport-level 30 s watchdog test.
- **Injectable `OS_CALLBACK_TIMEOUT_MS` + suspending fake wrapper behavior** in the Android
  provisioning manager (A09) — unblocks the timeout-mapping rows (A09 §3 row 1, PRM-8, PRM-4).
- **Build-version / targetSdk seam or Robolectric `@Config`** — unblocks the
  `requiredRuntimePermission` matrix row (A09 §3 row 8).
- **NIC-enumeration seam** in the JVM provisioning manager — unblocks the `networkState` poll
  row and the PRM-6 guard (A09 §3 row 10).
- **Extracted decision helpers on iOS** (listener `failed`-after-ready → rebind, CON-8; announce
  prune emission, DSC-8 — following the `AnnounceCacheReconcileTest` precedent) and an extracted
  `parsePeerRecord`-style TXT helper (RBS-1 unit leg).
- **New test source sets**: `p2p-core` androidHostTest (rows for Android peer-id storage and
  permission managers), `p2p-core` iOS test source set (NSUserDefaults storage),
  `sample-kmp-shared` androidHostTest (consumer factory), `p2p-sample-desktop` test source set
  (CLI arg parsing).

## 3. Main plan

Grouped by priority, P1 first. "Covers" cites the finding IDs and/or report §3 rows merged into
the row. Where-it-lives names module + source set. Type: unit / combination / integration /
manual / script / process.

### 3.1 P1 — must land before the RC tag

| Invariant/behavior untested | Why it matters (1 line) | Covers finding(s) | Where it lives | Type | Priority |
|---|---|---|---|---|---|
| Remote abrupt raw termination (EOF/reset, no CLOSE frame) with `ReconnectPolicy.Enabled` deterministically reaches `Reconnecting`, never terminal `Closed`; `observeRawState` fires on remote close; incoming-session EOF → deterministic `Failed` | The most common field event (peer app killed); the product outcome is currently racy and the fake cannot even express it | SES-1, SES-10, TST-1, CON-7, A12 §3 r1 | p2p-core commonTest (ReconnectPolicyTest / SessionFlowTest) — **blocked on F1** | combination | P1 |
| Remote CLOSE frame → exactly `Closed`, never `Failed`; dial factory never re-invoked; tighten SessionFlowTest.kt:223-230 from `Closed \|\| Failed` to the exact state; stress variant: CLOSE followed immediately by socket close, repeated under dispatcher stress | "Clean closes never retry" is a spec invariant currently pinned only disjunctively; the race victim of SES-1 | SES-1, TST-11, A03 §3 r1, A12 §3 r2 | p2p-core commonTest (SessionFlowTest + ReconnectPolicyTest); strict assertion is writable now, race variant **blocked on F1** | combination | P1 |
| Kit-level behavioral suites run with `strictInvariants = true`; meta-test that a violation inside a kit-built store throws under the flag | The e91e094 safety net is inert outside its own unit test — warn-mode + NoOp logger hides store-invariant regressions in every suite | SES-8, TST-9, A03 §3 r3, A12 §3 r3 | p2p-core commonTest infra — **blocked on F6** | combination | P1 |
| Accept-loop failure (e.g. accept error when the process fd limit is reached) → kit survives, failure surfaced (logged/typed), inbound behavior defined (resume or report) | Today `startAcceptingIncoming` has no failure handling: an accept-loop error silently ends inbound connectivity for the kit's lifetime and can propagate into the kit scope | CON-3, TST-3, ARCH-4-adjacent, A12 §3 r4 | p2p-core commonTest (**blocked on F3**) + p2p-transport-lan jvmTest (real ServerSocket closed under the loop) | combination | P1 |
| `send()` on a failing connection surfaces `P2pError.ConnectionFailed` (never raw IOException/ISE), single- and multi-chunk; variant where rearm swaps the connection between state check and write | The documented typed-error contract of the hottest API call; platform-divergent raw leaks today | API-2, SES-2, TST-2, A02 §3 r1 | p2p-core commonTest (SessionFlowTest or new SendErrorContractTest) — **blocked on F2** | unit | P1 |
| `P2pMessage.metadata` receive-side contract pinned: round-trip equality if transmitted, or asserted-empty + documented if dropped (per the RC decision) | Silent public-API data loss today; whichever way DOCA-14/15 resolves, nothing pins it | API-1, DOCA-15, A02 §3 r2, A13a §3 r2 | p2p-core commonTest (Chunker/Reassembler round-trip; loopback variant) | unit | P1 |
| P2pState machine: Idle→Starting→Running; bind failure→Failed; failed start does not latch (retry→Running); re-advertise clears Failed | Zero `P2pState.Failed/Starting/Running` assertions exist in commonTest; two AUDIT-2026-06 fixes have no regression net | A01 §3 r1 | p2p-core commonTest (KitLifecycleTest) | unit | P1 |
| Cancelling `kit.start()` mid-bind: state NOT `Failed`, `CancellationException` (not `TransportStartFailed`) propagates, subsequent `start()` succeeds | Routine Android lifecycle event corrupts public state today; CE must never be swallowed | ARCH-1 | p2p-core commonTest (KitLifecycleTest, hung-start fake) | unit | P1 |
| `stop()` completes within a bound when a fake observer's `start()` hangs and its `close()` blocks on the same mutex; cancelling the stop caller still invokes the observer's `close()` | The #17 fix's own scenario shifted one resource over (pathObserver runs outside `NonCancellable`); observer leak on caller cancellation | ARCH-2 | p2p-core commonTest (KitLifecycleTest, fake observer) | unit | P1 |
| Manual peers are exempt from staleness eviction (survive `evictStalePeers` after clock advance) | Manual peers have no heartbeats; a regression makes every manual peer vanish ~15 s after registration | A04 §3 r1 | p2p-core commonTest (PeerRegistryTest) | unit | P1 |
| `registerManualPeer` dedupes by (host, port, kind): repeat returns the same `Peer` (same id), one tracked entry; a new endpoint mints a new id | Behavior exists since b9f6311 but is contradicted by the doc of record and pinned by nothing | IDN-5, A04 §3 r2 | p2p-core commonTest (PeerRegistryTest) | unit | P1 |
| JVM legacy migration `<root>/p2pkit` → `<root>/.p2pkit` adopts and re-persists the old id | AUDIT-2026-06 identity-preserving behavior with zero coverage; silent breakage = desktop identity churn | A04 §3 r3 | p2p-core jvmTest (FilePeerIdStorageTest) | unit | P1 |
| JVM/Android discovered peer remains in `kit.peers` at t > 15 s idle (t = 20 s and 35 s, no connect activity); plus a common contract test that a `DiscoveryTransport` must re-emit within the stale timeout | The primary discovery API silently empties in steady state (no JVM/Android heartbeat; only iOS got one) | DSC-1, A05 §3 r1 | p2p-transport-lan jvmTest loopback + p2p-core commonTest (FakeDiscoveryTransport contract) | integration + unit | P1 |
| Android JmDNS rebind machinery: intent flags vs handles, create-retry bounds, restore-failure repair, cancelled `JmDNS.create` closes the produced handle, failed `start*` closes handle + multicast lock | The #5 fix shipped with "compile + review + manual" only; the module's riskiest state machine has zero automated coverage | DSC-3, DSC-4, DSC-5, DSC-13, A05 §3 r2 | seamed `JmDNS` factory → androidHostTest or jvm-style unit (**needs seam**, §2.2) | unit | P1 |
| Real 30 s write watchdog at transport level: wedged socket write → IOException, `socket.isClosed`, state Closed (`JvmRawConnection`, not the fake) | f4dd3a9's core mechanism has no transport-level coverage; REMEDIATION's cited test uses FakeRawConnection | CON-14, A06 §3 r1 | p2p-transport-lan jvmTest (**needs injectable timeout**, §2.2) | integration | P1 |
| `close()` called from an already-cancelled coroutine still releases the fd; cancelled read collector + remote close still ends with `socket.isClosed` | Routine app-cancels-connect path leaks an fd + IO thread today (`withContext` skips the block when the caller is cancelled) | CON-1 | p2p-transport-lan jvmTest | unit | P1 |
| Declared `payload_len` > `MAX_FRAME_PAYLOAD_BYTES` → immediate `ProtocolError` with no buffering (FrameReader) and decode rejection (FrameCodec) | This is THE bounded-memory guard the constants KDoc and spec §13.2 advertise; zero regression coverage | A07 §3 r1 | p2p-core commonTest (FrameReaderTest + FrameCodecTest) | unit | P1 |
| `HelloPayload.decode` input-validation guards: malformed JSON, missing fields, blank appId/peerId, 513-char fields, 33 transports; over-limit `platform` / per-transport strings including very large values | Untrusted peer-input surface; the guards were hardening fixes and are wholly untested — a refactor could silently drop them | A07 §3 r2, PRO-4, SEC-I2 | p2p-core commonTest (HelloPayloadTest) | unit | P1 |
| Malformed HELLO / FILE_OFFER frame is skipped (warn) and the events flow keeps delivering subsequent frames | The skip-not-throw policy is an audit fix preventing session teardown/reconnect loops on malformed peer input; only the happy path is tested | A07 §3 r3 | p2p-core commonTest (DefaultP2pProtocolTest / FileTransferProtocolTest) | combination | P1 |
| Outgoing file source closed exactly once on EVERY terminal path: complete, reject, cancel-before/after-accept, offer-write failure, `session.close()` / `kit.stop()` mid-stream, rearm `closeAll` (idempotency asserted) | KDoc contract "kit closes it"; FIL-1/FIL-6 show live gaps (watcher cancelled before it can close; TOCTOU on the outgoing side) | FIL-1, FIL-6, A08 §3 r1 | p2p-core commonTest (FileTransferFlowTest, close-tracking fake RawSource) | unit/combination | P1 |
| Sender-side source read failure (throws mid-stream / shorter than sizeBytes): sender `Failed`, receiver reaches a terminal state within a bound, FILE_CANCEL recorded on the wire, session stays Connected | Today the receiver of an accepted transfer hangs forever; also pins the transfer-failure-isolation invariant | FIL-2, A08 §3 r2 | p2p-core commonTest (E2E + direct-dispatcher with RecordingFileProtocol) | combination | P1 |
| Duplicate FILE_ACCEPT ignored: exactly one streamer, chunk sequence 0..n-1 emitted once, one FILE_DONE, no state regression from Sending, `bytesTransferred == sizeBytes` | A repeated accept currently launches a second concurrent streamer over the same source | FIL-4, A08 §3 r3 | p2p-core commonTest (direct-dispatcher) | unit | P1 |
| Oversize offer never surfaces on `incomingFiles` — real subscriber assertion replacing the current no-op | `assertSubscriberSeesNoOffer` asserts nothing today; the auto-reject bound is unverified | FIL-11, A08 §3 r4 | p2p-core commonTest (FileTransferFlowTest) | combination | P1 |
| Session close mid-transfer: both sides' transfer handles reach terminal states (no awaiter hang) | Guards the `transitionToTerminal` ↔ dispatcher contract | A08 §3 r5 | p2p-core commonTest | combination | P1 |
| A blank/empty/whitespace TXT `pid` on the found AND removed discovery paths never throws out of a discovery callback: record skipped, no bogus `PeerEvent`, no process crash (iOS), no worker death (JVM/Android); unit leg for the extracted record parser | Malformed discovery input currently reaches `PeerId()` unguarded on all three platforms; on iOS an uncaught throw across the callback boundary terminates the process | RBS-1, API-17, A14-rob §4 r1+r2 | p2p-transport-lan iosSimulatorArm64Test + jvmTest loopback + commonTest unit (parser helper, §2.2) | integration + unit | P1 |
| Inbound connection admission control: K sockets that never send HELLO leave the kit responsive (outbound + a legitimate inbound still succeed), fd/session count bounded, no uncaught exception in the kit scope | The primary bounded-usage gap for inbound connection setup; nothing pins any ceiling today | SEC-1, A14-sec §4 r1 | p2p-transport-lan jvmTest | integration | P1 |
| Provisioning `parentJob` cancellation closes the hotspot reservation + join binding (init `invokeOnCompletion`); a start attempted after the job is cancelled is refused | The audit's "reservation leaked for process lifetime" fix is only exercised via `close()`, never the kit-stop path production uses | A09 §3 r2, PRM-10 | p2p-network-provisioning-android androidHostTest | unit | P1 |
| Join release closes the JoinHandle (`lastJoinHandle.isClosed`) | The close-before-drop fix (manager :254-263) is unasserted; a regression silently reintroduces process-wide traffic blackholing | A09 §3 r3 | p2p-network-provisioning-android androidHostTest (extend `systemInitiatedJoinReleaseEmitsFailedEventAndClearsHandle`) | unit | P1 |
| `publishToMavenLocal` produces the full Central artifact set per module (jar/klib/aar, -sources, -javadoc, .pom, .module) | The RC checklist item cannot currently pass for 3 of 4 modules (no javadoc wiring); a script makes the gate executable | BLD-2, A11 §3 r1 | scripts/ verification script invoked by the release recipe | integration (script) | P1 |
| Built .app's Info.plist contains `NSBonjourServices` `_p2pkit._tcp` + `NSLocalNetworkUsageDescription` (post-build grep) | The documented "zero discovery" failure mode; today only a yml comment guards it against an xcodegen upgrade/refactor | A10 §3 r1, IOSB-9 | post-build check in check-xcframework.sh / scripts/run-ios-app.sh | script/manual | P1 |
| run-ios-app.sh installs the bundle produced by THIS build (`simctl get_app_container` resolves under the invoking checkout's DerivedData) | First-found-in-DerivedData install can silently run a stale app from another checkout/worktree, defeating the provenance gate | IOSB-3, A10 §3 r2 | manual verification + note in INTERNAL_TESTING.md | manual | P1 |
| Incoming-file destination uniquification in samples (two same-named offers → distinct paths, no overwrite/interleave) | Shipped in desktop-ui because nothing checks it; CLI/Android logic equally test-free | SMP-1, A16 §3 r1 | small JVM unit for the shared helper (+ manual two-offer re-send check per INTERNAL_TESTING) | unit | P1 |

### 3.2 P2 — strongly recommended for the RC line

| Invariant/behavior untested | Why it matters (1 line) | Covers finding(s) | Where it lives | Type | Priority |
|---|---|---|---|---|---|
| Reader coroutine / eventChannel released on session terminal and rearm: reader completes, channel cancelled, no parked `send` | SES-3 coroutine/memory growth has no regression guard | SES-3, A03 §3 r4 | p2p-core commonTest (new SessionTeardownTest) | unit | P2 |
| Keep-alive pre-send timeout check fires while `sendMutex` is held by a wedged writer | The remediation #4 keep-alive half has no direct test | A03 §3 r6 | p2p-core commonTest (KeepAliveTest) — **blocked on F2** | unit | P2 |
| Keep-alive PONG deadline reset on rearm (no instant false timeout post-reconnect) | A regression breaks every reconnect under short timeouts | A03 §3 r7 | p2p-core commonTest (KeepAliveTest) | unit | P2 |
| Simultaneous-open arbitration contract: tie-break direction, survivor is `Connected` AND can exchange a message, survivor uses the correct physical connection, both arrival orders | Current test asserts only session count and tolerates a `Reconnecting` survivor — passes under broken arbitration | SES-19, TST-15, A03 §3 r8 | p2p-core commonTest (SimultaneousOpenTest) | combination | P2 |
| Handshake timeout and non-HELLO-first-event reject paths | Handshake.kt:49-59 has zero coverage; both are two-line tests with the injectable timeout | A03 §3 r9 | p2p-core commonTest (HandshakeTest) | unit | P2 |
| Redial while a stale undetected-dead session holds the peer slot: re-establishment bounded well under the keep-alive timeout (post-fix); today's lockout documented as the baseline | Reconnect lockout up to the keep-alive timeout after a silent wire death | SES-4, A03 §3 r10 | p2p-core commonTest (combination) — **blocked on F2** (swallow-writes fake) | combination | P2 |
| Post-stop / post-close non-retry asserted structurally (handler job completion), not via a 150 ms real-time window | Current windows cannot catch a retry that fires 1 s later (retry loop parked in its delay at stop time) | TST-10, A12 §3 r9 | p2p-core commonTest (ReconnectPolicyTest rewrite) | unit | P2 |
| Reconnect retry classification against typed `P2pError.ConnectionFailed` (what real transports actually throw) | All commonTest factories throw RuntimeException; the typed-error path through performConnect/retry classification is unexercised | A12 §3 r6 | p2p-core commonTest (ReconnectPolicyTest) | unit | P2 |
| Address rotation driven by repeat `PeerEvent.Found` (the JVM/Android-real stimulus shape, not `Updated`) | No shipped JVM/Android transport emits Updated; rotation coverage exercises a production-unreachable shape | TST-8, A12 §3 r7 | p2p-core commonTest (SessionReconnectRotationTest, 1-line variant) | unit | P2 |
| Discovery refresh coalescing: 3 concurrently-reconnecting sessions produce ~1× (not 3×) the single-session global refresh rate over a fixed virtual-time window | Multiplied global refresh is invisible in single-session tests; a coalescing regression would also be invisible | PERF-7, A15 §3 r2 | p2p-core commonTest (SessionReconnectRotationTest ext., counting FakeDiscoveryTransport) — **needs F9** | unit | P2 |
| Control-frame (PING/PONG) writability between chunks of an in-flight multi-chunk `send()` | Remote keep-alive sees our PONG latency; the file path guarantees interleaving, `send()` does not | PERF-6, A15 §3 r1 | p2p-core commonTest (SendFairnessTest) — **blocked on F2** (gated writes) | unit/combination | P2 |
| `close()` state sequence pinned: exactly Connected → Closed (no `Closing`); observable state set over connect→close is exactly {Connected, Closed} (+Reconnecting/Failed in failure runs) | Spec-contract drift (§10 documents `Closing`) went unnoticed because nothing asserts the sequence | DOCA-16, API-10, SES-9, A13a §3 r1, A02 §3 r10 | p2p-core commonTest (SessionFlowTest / CloseSemanticsTest) | unit | P2 |
| `stop()` racing `startAdvertising`/`startDiscovery` failure → terminal state stays `Stopped` (success-latch window + failure-path windows) | Easy race window; terminal state lies to UIs | ARCH-3, A01 §3 r4 | p2p-core commonTest | combination | P2 |
| Post-stop contract: `ensureStarted`/start/connect throw ISE (direct assertion); `stopAdvertising`/`stopDiscovery` are silent no-ops | Terminal-kit contract unpinned; locks in the intended semantics | A01 §3 r5, API-6, A02 §3 r8 | p2p-core commonTest (KitLifecycleTest) | unit | P2 |
| Kit survives a discovery transport whose `events` flow throws after one emission: logged, other transports' events still processed | No `CoroutineExceptionHandler` exists in p2p-core; an uncaught collector throw crashes Android host apps | ARCH-4 | p2p-core commonTest | combination | P2 |
| `connect()` surfaces `TransportStartFailed` when lazy start fails | Spec §7.2 / API-5 contract | API-5, A02 §3 r7 | p2p-core commonTest (KitLifecycleTest) — **needs F5** | unit | P2 |
| Builder/config validation suite: IAE boundaries for keepAlive/reconnect/fileTransfer values (incl. the undocumented `timeout > interval` constraint), missing appId/deviceName/transport errors, repeated-DSL-block semantics incl. provisioning-factory persistence, Unsupported-stub message/type | The DSL is every consumer's entry point; zero dedicated tests today; re-entering `networkProvisioning { }` silently drops a factory | API-7, API-8, API-16, A02 §3 r3, A12 §3 r10 | p2p-core commonTest (new P2pKitBuilderTest) | unit | P2 |
| Unanswered file offer: sender/receiver terminal-state pair pinned (sender `Cancelled(...)` vs receiver `Rejected("timeout")`, or the corrected semantics) | Cross-side timeout semantics currently unpinned and mislabeled vs KDoc | API-19, A02 §3 r4 | p2p-core commonTest (FileTransferFlowTest) | unit | P2 |
| `accept()` after timeout/answer throws ISE; `accept()` on a dead connection throws `P2pError.ConnectionFailed` and the transfer reaches `Failed` | P2pFileOffer contract; KDoc documents only ISE | API-20, A02 §3 r5 | p2p-core commonTest (FileTransferFlowTest) | unit | P2 |
| Zero-byte file transfer end-to-end: offer→accept→FILE_DONE→Completed on both sides, no `Sending` emission | Boundary of the progress contract; guards at OutgoingFileTransferImpl.kt:54 / IncomingFileSession.kt:69 are untested | A02 §3 r6, A08 §3 r12 | p2p-core commonTest (FileTransferFlowTest) | combination | P2 |
| FILE_DATA/FILE_DONE/FILE_ACCEPT/FILE_CANCEL referencing unknown / never-offered transfer ids leave no state change | Robustness against non-conforming peer frame sequences; today only debug-logged and regression-prone | A08 §3 r6 | p2p-core commonTest (direct-dispatcher) | unit | P2 |
| `MAX_PENDING_INCOMING_OFFERS` bound: 65th concurrent offer auto-rejected, map stays bounded; stalled accepted transfers hit the post-accept inactivity deadline (Failed + entry removed) and cannot permanently starve a fresh offer | The resource-limit bound is untested; without an inactivity deadline stalled transfers permanently consume the pending-offer budget | A08 §3 r7, FIL-3 | p2p-core commonTest (direct-dispatcher; deadline half follows the FIL-3 fix) | unit | P2 |
| `onFileData` write-path failure isolation end-to-end (sink.write throws mid-stream, not just flush): transfer Failed, session stays Connected | Code claims parity with onFileDone; only the finish() leg is tested | A08 §3 r8 | p2p-core commonTest (FileTransferErrorIsolationTest) | combination | P2 |
| FILE_DATA arriving before accept is dropped without state damage | Protocol-order robustness | A08 §3 r9 | p2p-core commonTest (direct-dispatcher) | unit | P2 |
| StreamingFileReceiver terminal latch: chunk-after-finish, double `finish()`, `abort()` idempotency and after-finish | Terminal-latch semantics of the receiver | A08 §3 r10 | p2p-core commonTest (StreamingFileReceiverTest) | unit | P2 |
| `streamFileData`: source shorter than sizeBytes throws; cancellation mid-collect leaves the source open (caller-owned at that layer) | FIL-2's trigger + the layered ownership contract | A08 §3 r11 | p2p-core commonTest (StreamingFileSenderTest) | unit | P2 |
| Receiver sink concurrency stress: pump FILE_DATA while concurrently calling `cancel()`; no exception escapes the transfer scope; terminal state is Cancelled/Failed only | `acceptDataChunk` (outside lock) races `abort()`; consequences need a multi-thread stress to observe | FIL-5 | p2p-core commonTest (multi-threaded stress on Default dispatcher) | combination | P2 |
| `totalChunks > MAX_TOTAL_CHUNKS` → ProtocolError | Session-closing cap promised in spec §13.4; untested | A07 §3 r4 | p2p-core commonTest (ReassemblerTest) | unit | P2 |
| 257th concurrent partial → ProtocolError (`MAX_PENDING_REASSEMBLIES`) | Session-closing cap promised in spec §13.4; untested | A07 §3 r5 | p2p-core commonTest (ReassemblerTest) | unit | P2 |
| Multi-chunk `bufferedBytes` > `MAX_PAYLOAD_BYTES` → ProtocolError and the entry removed (accounting) | The per-message cap on the multi-chunk path has no direct test (the dup-test throws earlier) | A07 §3 r6 | p2p-core commonTest (ReassemblerTest) | unit | P2 |
| `evictStale` releases the aggregate budget: evicting a large partial lets an equally large new partial fit (`totalPendingBytes` exactness) | Only the completion path of the aggregate accounting is asserted today | A07 §3 r7 | p2p-core commonTest (ReassemblerTest) | unit | P2 |
| `FileOfferPayload.decode` input-validation guards: negative sizeBytes, name > 4096, mime > 255, malformed JSON; plus the chosen policy for path-shaped / control-character names (`"../x"`, `"a/b"`, `"C:\x"`, embedded newline) | Untrusted peer-input surface, untested; `P2pFileOffer.name` reaches apps with no sanitization warning | A07 §3 r8, FIL-8 | p2p-core commonTest (FileOfferPayloadTest) | unit | P2 |
| HELLO encode-side caps: local over-limit config rejected at builder/encode time (513-char field rejected, 512 accepted) | An over-limit local config currently fails every handshake with a generic timeout | PRO-1 | p2p-core commonTest | unit | P2 |
| FrameReader behavioral equivalence under pathological feed shapes: one 1 MiB frame in 8 KiB slices; 1000 tiny frames in one feed; a frame delivered in 1-byte segments (behavioral assertions only, no timing) | Correctness anchors for any buffer-management refactor; the segment-shape cost is peer-input-controlled | PERF-9, SEC-I1, PRO-10, A15 §3 r3, A14-sec §4 r4 | p2p-core commonTest (FrameReaderTest) | unit | P2 |
| Manual-entry provenance protections: Found/Updated colliding with a manual entry neither replaces hints nor demotes origin; Lost (incl. foreign-app pid) does not remove it; an event carrying `origin = Manual` is stored as Discovered (or dropped) | Pins the provenance invariants the origin model exists for; manual peers are eviction-exempt but Lost-vulnerable today | IDN-1, DSC-11, A04 §3 r4, A05 §3 r6 | p2p-core commonTest (PeerRegistryTest) | unit | P2 |
| `peers` flow de-noising: heartbeat `Updated` with identical payload does not re-emit; an idle eviction tick leaves the tracked StateFlow value identity-unchanged | KDoc-promised de-noising (PeerRegistry.kt:54-58) unpinned; enables the no-op-tick optimization safely | A04 §3 r5, PERF-8, A15 §3 r4 | p2p-core commonTest (PeerRegistryTest, emission counting) | unit | P2 |
| Concurrent same-endpoint `registerManualPeer` yields exactly one tracked entry | Guards the dedupe against its check-then-act race | IDN-2, A04 §3 r6 | p2p-core commonTest (PeerRegistryTest, multithreaded dispatcher + latch) | unit | P2 |
| Android `FilePeerIdStorage` + factory fallback behavior | A 74-line divergent twin with zero compilable tests; parity asserted only by eyeball | A04 §3 r7 | new p2p-core androidHostTest source set (§2.2) | unit | P2 |
| iOS `NSUserDefaultsPeerIdStorage` load/generate/blank/regenerate via injected suite | The injectable `defaults` ctor exists precisely for this; currently dead test surface | A04 §3 r8 | new p2p-core iOS test source set (§2.2) | unit | P2 |
| Acceptor-side own-peerId rejection: no session registered, nothing emitted on `incomingSessions` | Only the dialer side is asserted; an acceptor regression hides behind the NoOp logger | A04 §3 r9 | p2p-core commonTest (HandshakeIdentityTest) | combination | P2 |
| A manual-looking id arriving via a real `PeerEvent.Found` (registry entry, origin=Discovered) is rejected on HELLO mismatch | The current identity-mismatch test exercises the registry-miss fallback, not the discovered-entry path its comment describes | A04 §3 r10 | p2p-core commonTest (ManualPeerIdentityTest + FakeDiscoveryTransport) | combination | P2 |
| Oversized / control-character peer-id file → regenerate + overwrite (or the documented chosen behavior) | Corrupt-but-non-blank is the one corrupt class with no defined behavior; the loaded value becomes the advertised identity | IDN-4, A04 §3 r11 | p2p-core jvmTest (FilePeerIdStorageTest) | unit | P2 |
| Registry bounded under high discovery-event volume: N ≫ cap distinct Found events → `peers.value.size` bounded and republish work sub-quadratic (emission/derivation counter) | Bounded-usage enforcement for the `tracked` map; republish cost is O(n) per event today | SEC-2, A14-sec §4 r3 | p2p-core commonTest (PeerRegistryTest) | unit | P2 |
| N inbound connections each claiming a distinct peerId → active sessions capped, excess closed | Post-handshake session-count bound (the second half of the admission-control gap) | SEC-1(b), A14-sec §4 r2 | p2p-core commonTest (FakeDataTransport) | combination | P2 |
| `refresh()` rotation leaves exactly one registered listener when cancelled at every suspension point | New #7 code; a leaked listener duplicates the event stream and accumulates across reconnect cycles | DSC-7, A05 §3 r3 | seamed JmDNS jvm-style unit (§2.2) | unit | P2 |
| iOS `onAfterListenerRebind` honors a concurrent `stopDiscovery` (browser == null after the after-hook) | Browsing must never resurrect against host intent (already fixed once elsewhere) | DSC-6, A05 §3 r4 | p2p-transport-lan appleTest (drive the two hooks directly) | unit | P2 |
| iOS announce prune cannot emit Lost for a peer re-confirmed between reconcile and prune; browse results stamped with the delivering browser's generation | A wrong Lost persists a full browser generation | DSC-8, DSC-9, A05 §3 r5 | p2p-transport-lan appleTest (extracted prune-emission helper, §2.2) | unit | P2 |
| Cancelled dial closes the just-connected socket (remote side observes close; no lingering local socket) | One fd leaked per cancelled connect (`withContext` discards its result on caller cancellation) | CON-2, A06 §3 r4 | p2p-transport-lan jvmTest | unit | P2 |
| `stop()` racing a slow `start()` leaves no bound listener/socket | Orphaned port/listener; both intended safety nets are defeated in the race window | CON-4, A06 §3 r5 | per-platform unit (injectable bind stall, §2.2) | unit | P2 |
| iOS listener `failed`-after-ready triggers `scheduleRebind` (extracted handler decision) | Silent inbound deafness otherwise: no rebind, no error to core | CON-8, A06 §3 r6 | p2p-transport-lan appleTest (extracted handler, §2.2) | unit | P2 |
| Non-timeout write failure (peer receive side closed, write until EPIPE): state becomes Closed and socket closes without waiting for keep-alive | Write-error parity divergence: JVM/Android currently leave socket open and state=Connected | CON-6 | p2p-transport-lan jvmTest | unit | P2 |
| JVM write-after-close raw exception shape (parity twin of `IosRawConnectionTest.kt:49-59`) | The JVM↔iOS behavior-parity pair is a stated invariant; only iOS pins it | A12 §3 r5 | p2p-transport-lan jvmTest | unit | P2 |
| OS-callback timeout → typed Failed with a truthful message (hotspot + join); cancellation racing the suspended callback covered via a suspend-until-cancelled fake behavior | The bounded-wait fix has zero coverage; timeout is a hardcoded 60 s so the branch is untestable as written; timeout currently mislabeled "STOPPED_BEFORE_START" | A09 §3 r1, PRM-8, PRM-4 | p2p-network-provisioning-android androidHostTest (**needs injectable timeout**, §2.2) | unit | P2 |
| `isLocalOnlyHotspotSupported=false` / `isSpecifierJoinSupported=false` → contract `Unsupported` | The API-26/29 linkage-error fix; fakes hardcode `true` so the gates are never exercised | A09 §3 r4 | p2p-network-provisioning-android androidHostTest | unit | P2 |
| After system-initiated stop: `state == Failed`, fired handle closed (`lastHandle.isClosed`), and a new `startLocalNetwork` succeeds | Pins handle-cleared semantics the current test names claim but don't assert; watcher-orphan fix | A09 §3 r5, PRM-7 | p2p-network-provisioning-android androidHostTest | unit | P2 |
| `AndroidLanPermissionManager` reports empty; manifest warn fires/silences correctly | The 881fb31 Android-side behavior is verified only by a manual recipe | A09 §3 r7, PRM-2 | new p2p-core androidHostTest / Robolectric (§2.2) | unit | P2 |
| `AndroidP2pPermissionManager` / `requiredRuntimePermission` targetSdk × API matrix | The audit's targetSdk-keying fix (ungrantable-NEARBY bug) has no automated coverage | A09 §3 r8 | Robolectric `@Config(sdk=…)` or Build-version seam (§2.2) | unit | P2 |
| SecurityException mapping corrections: location-mode message → `RequiresUserAction` (updating the test that pins the wrong type — a semantic fix, not a relaxation); join SecurityException surfaces the original exception text | Actionably-wrong error types send users to the wrong remediation | PRM-5 | p2p-network-provisioning-android androidHostTest | unit | P2 |
| Kit-gate contract for a sidecar-managed permission manager: advertise/discovery behavior with a missing provisioning permission asserted explicitly, whichever way the decision goes | Fix #9's granularity mismatch re-creates LAN over-gating for apps wired per the docs | PRM-1 | p2p-core commonTest (PermissionGateTest) | unit | P2 |
| iOS manual-IP end-to-end: `iosManualIp()` → `createManualPeer` → dial via `nw_endpoint_create_host` | The only iOS-reachable provisioning feature has no appleTest; only the sample exercises it by hand | A09 §3 r11 | p2p-transport-lan appleTest loopback (mirrors ManualIpLoopbackTest; runs via iosSimulatorArm64Test) | integration | P2 |
| Keyed signing path: with a throwaway key, Sign tasks run and every publication gains .asc; keyless run reports SKIPPED | Central-blocking if the signing wiring regresses; only prose-verified today | A11 §3 r2 | release verification script (optional key branch) | integration (script) | P2 |
| Second no-change build leaves BuildInfo.kt byte-identical and compile tasks UP-TO-DATE | Guards the BLD-1 fix; protects build-cache economics repo-wide | BLD-1, A11 §3 r3 | Gradle TestKit test or CI script step | integration | P2 |
| Runbook/checklist log-signature strings exist in source (`Reconnect target changed`, `(wasBrowsing=true)`, `kotlin.IllegalStateException: SessionStore[` …) | An RC hardware pass scores false FAILs when expected lines can never be printed | DOCB-8, DOCB-9, DOCB-10, A13b §3 r1 | small script (e.g. scripts/check-doc-signatures.sh) grepping doc-fenced signatures against src/ | script | P2 |
| Process rule: when fixing an audit item, annotate its line in AUDIT_REPORT_2026-06.md / PROBLEMS_P2PKIT.md | The load-bearing deferred list silently rotted through 5 fix commits | DOCB-1, DOCB-4, DOCB-5, A13b §3 r2 | process rule in CLAUDE.md | process | P2 |
| `createP2pKit` on Android before `initP2pKitAndroid` fails with the documented error | KMP consumers copy this factory; a silent context-null is a runtime crash pattern | A16 §3 r2 | sample-kmp-shared androidHostTest (new, §2.2) | unit | P2 |
| Message emitted between incoming-session emission and consumer subscription: pin the loss contract (or the buffering fix) deliberately | SMP-8's root cause (SharedFlow replay=0); the SDK KDoc warns but nothing pins behavior; also de-flakes KmpConsumerLoopbackTest structurally | SMP-8, A16 §3 r3 | p2p-core commonTest (fakes) | combination | P2 |
| AndroidNetworkPathObserver register/unregister symmetry, last-network-lost flip, close-reset | 119 lines of OS-callback lifecycle with zero automation (no instrumented tests by repo policy — host-test seam or manual recipe) | A01 §3 r8 | p2p-core android host test (if seam added) or INTERNAL_TESTING.md § entry | integration/manual | P2 |
| WifiManagerWrapperImpl against the real OS: explicit smoke rows for join decline, callback timeout, location-services-off | Inherently device-bound; the manager-level fakes cannot reach these OS behaviors | A09 §3 r12, PRM-4 (residual) | manual (INTERNAL_TESTING / smoke matrix) | manual | P2 |
| check-xcframework.sh stamp-lag matrix incl. unresolvable stamp (`unknown`/shallow clone) and a build-config-only change (gradle.properties) | New adca586 logic; the existing recipe covers only pass/pass/fail cases; freshness pathspec omits build-config inputs | A10 §3 r3, IOSB-4, IOSB-5 | extend docs/STABILIZATION_AND_RELEASE.md §provenance manual recipe | manual | P2 |
| SIM_NAME override matrix: a parenthesized device name resolves and launches; a nonexistent name prints the FATAL hint and exits 1 | The documented override is broken for a large class of stock device names and currently dies without diagnostics | A10 §3 r4, IOSB-1, IOSB-2 | manual run-matrix note in iosApp/scripts/README.md | manual | P2 |

### 3.3 P3 — valuable hardening / pinning; schedule after P1–P2

| Invariant/behavior untested | Why it matters (1 line) | Covers finding(s) | Where it lives | Type | Priority |
|---|---|---|---|---|---|
| Hung transport `start()` returning failure after a lock-less stop; two-transport variant closes re-bound transports | Failure-path counterpart of the new hang test; guards the multi-transport future | ARCH-5, A01 §3 r6 | p2p-core commonTest (KitLifecycleTest; F5 helps) | combination | P3 |
| TransportManager tie-break determinism | Comment/spec claim with no net (spec §8.3 embeds a stale implementation) | ARCH-15, ARCH-8, A01 §3 r7 | p2p-core commonTest (TransportManagerTest) | unit | P3 |
| Path observers reset `status` to `Unknown` on `close()` | Documented cold-read contract for restarted/shared observers | ARCH-6 | platform tests or common fake-contract test | unit | P3 |
| IosNetworkPathObserver start/close/restart idempotence + status mapping | The real monitor is constructed implicitly by every iOS kit test but never asserted | A01 §3 r9 | p2p-core iosSimulatorArm64 test | integration | P3 |
| Kit `Failed → Running` recovery (no intervening `Starting`) after a failed-then-successful post-start advertise | API-9 KDoc completeness | API-9, A02 §3 r9 | p2p-core commonTest (KitLifecycleTest) | unit | P3 |
| `performConnect` propagates caller cancellation as CE (not `P2pError.ConnectionFailed`); a JoinPending waiter sees a defined outcome | CE must never be swallowed/wrapped | SES-5 | p2p-core commonTest (suspending fake `transport.connect`) | unit | P3 |
| Reconnect-loop cancellation between handshake completion and rearm closes the freshly-dialed connection | Small but real leak window | SES-6 | p2p-core commonTest (hooked fake) | unit | P3 |
| `sessionJob` completes for sessions ending via failure paths (no Job accumulation under the kit scope) | Unbounded Job growth over long-lived kits | SES-7 | p2p-core commonTest | unit | P3 |
| Handshake worst case bounded by the handshake timeout (~10 s), not the write watchdog (~30 s), when the connection's write parks | connect() latency contract | SES-11 | p2p-core commonTest — **blocked on F2** | unit | P3 |
| Periodic ~3 s discovery refresh cadence across a long `Reconnecting` window (virtual time; replaces the latent `refreshCalls == 1` over-tight assertion with cadence assertions — a semantic correction, not a relaxation) | CLAUDE.md documents the cadence; only the one-shot refresh is asserted today | A03 §3 r11 | p2p-core commonTest (SessionReconnectRotationTest) — **needs F9** | unit | P3 |
| Incoming session → `Failed` on path Unsatisfied | NetworkPathRecoveryTest only wires the observer on the outgoing kit | A03 §3 r12 | p2p-core commonTest (NetworkPathRecoveryTest) | unit | P3 |
| PeerRegistry convergence under an event burst with DROP_OLDEST delivery (peer set converges despite dropped intermediate events) | Production drops events under burst; the fake currently can't | TST-4, A12 §3 r8 | p2p-core commonTest (PeerRegistryTest) — **blocked on F4** | unit | P3 |
| publishPeers ordering stress: interleaved Found/Lost bursts from two coroutines → `peers.value` matches tracked-derived state at quiescence without an eviction tick | The read-then-assign race can publish a stale list over a newer one | IDN-3 | p2p-core commonTest (PeerRegistryTest stress) | combination | P3 |
| Unreadable storage file (chmod 000) → warn + ephemeral id, no throw | The "unwritable/unreadable storage" contract is asserted nowhere | A04 §3 r12 | p2p-core jvmTest (FilePeerIdStorageTest) | unit | P3 |
| `kitOne.localPeerId == kitTwo.localPeerId` direct assertion in the persistence integration test | Pins the user-visible invariant without inference via the tracking test | A04 §3 r13 | p2p-core commonTest/jvm (PeerIdPersistenceIntegrationTest) | integration | P3 |
| Manual-peer dedupe-hit with a new deviceName: pinned decision (update vs ignore) | Currently silently ignores the new name; behavior should be deliberate | IDN-7 | p2p-core commonTest (PeerRegistryTest) | unit | P3 |
| TXT malformed-input decode parity vectors: >255 B values, duplicate keys, empty `pid=`, empty-value (`"true"` on JmDNS vs `""` on iOS) | Three codecs, no shared vectors; silent cross-platform divergence | DSC-12, A05 §3 r7 | p2p-transport-lan appleTest (IosBonjourTest) + JVM ServiceInfo fixture test | unit | P3 |
| `selectRoutableHost` JVM/Android copies stay identical | Duplicated-verbatim pair, sync by convention only | A05 §3 r8 | source-checksum guard test (or extraction note) | unit | P3 |
| `LanServiceRegistration.tcpPort != 0` at advertise time | Advertising SRV port 0 is undialable; enforced only by call-ordering convention | A05 §3 r9 | assertion + p2p-transport-lan jvmTest | unit | P3 |
| No raw `Log.*` in androidMain transport sources (default-off trace contract) | 44 ungated log sites violate the documented trace contract; a grep guard prevents regression | DSC-2, CON-10 | lint-style grep script | script | P3 |
| iOS 30 s send deadline tears down the connection | Currently manual-only (REMEDIATION #18) | A06 §3 r8 | p2p-transport-lan appleTest vs a non-reading local listener, or stays in the smoke matrix | manual/integration | P3 |
| Keep-alive PING/PONG over a real socket (short ping config) | Both loopback suites set 60 s ping so keep-alive never fires outside fakes | A06 §3 r9 | p2p-transport-lan jvmTest | integration | P3 |
| iOS inbound queue bound: connections beyond capacity are cancelled (post-parity decision on UNLIMITED vs bounded) | Inbound queue parity: iOS `Channel.UNLIMITED` vs JVM/Android bounded 64 + drop-close | CON-9 | p2p-transport-lan appleTest | unit | P3 |
| iOS `close()` clears listener state (`tcpPort.value == null`) | Stale listener/port left behind today | CON-12 | p2p-transport-lan appleTest | unit | P3 |
| iOS: an outer app-level `withTimeout` around `write(...)` propagates the outer TCE without tearing down a healthy connection; `cancelIssued` untouched | Nested-withTimeout interception currently swallows cancellation and kills healthy connections | CON-5 | p2p-transport-lan appleTest | unit | P3 (report severity Medium; kept at its table's placement — schedule with the P2 appleTest batch if convenient) |
| Reason strings > 1024 B truncated (`decodeReasonCapped`) for ERROR/FILE_REJECT/FILE_CANCEL | AUDIT-2026-06 fix with no regression test | A07 §3 r9 | p2p-core commonTest (DefaultP2pProtocolTest) | unit | P3 |
| Forward-compat decode stance pinned: nonzero reserved byte, unknown flag bits, header version byte ≠ 1 (chosen behavior) | Documented decoder stance unpinned — a "tightening" refactor would break cross-version interop silently | A07 §3 r10, PRO-6 | p2p-core commonTest (FrameCodecTest) | unit | P3 |
| Multibyte UTF-8 text split across chunk boundaries round-trips | Byte-level chunking of text; reassembly-order dependent | A07 §3 r11 | p2p-core commonTest (ReassemblerTest) | unit | P3 |
| Chunk arriving in the same ingest batch as the eviction boundary survives; post-eviction stragglers do not build an uncompletable pending | Pins the PRO-7 behavior (raise to P2 if the PRO-7 fix lands) | PRO-7, A07 §3 r12 | p2p-core commonTest (ReassemblerTest / DefaultP2pProtocolTest) | unit | P3 |
| 36 bytes of garbage with a plausible length field → immediate ProtocolError (magic checked before length is trusted) | Avoids waiting for a declared payload that will never arrive | PRO-8 | p2p-core commonTest (FrameReaderTest) | unit | P3 |
| Sender-side symmetry guards: Chunker rejects an inconsistent (chunkSize, maxPayloadBytes) pair at construction; encode rejects an oversized payload | Sender-side counterparts of the receive caps | PRO-9 | p2p-core commonTest (ChunkerTest / FrameCodecTest) | unit | P3 |
| Single-frame send passes the caller's array without copy (`payload === input` for ≤ chunkSize) | Zero-copy fast path is easy to regress silently in a chunker refactor | PERF-2, A15 §3 r5 | p2p-core commonTest (ChunkerTest) | unit | P3 |
| Ghost offer cannot surface after `closeAll` (offer emit vs closed re-check ordering) | Offer emit currently happens outside the closed re-check lock | FIL-7 | p2p-core commonTest (direct-dispatcher, single-thread ordering) | unit | P3 |
| Slow receiver sink does not block PONG replies / message dispatch (post-FIL-12 decoupling) | Inbound FILE_DATA sink writes currently run inline in `routeEvents` | FIL-12 | p2p-core commonTest (integration-style, artificially slow sink) | integration | P3 |
| JVM `sendFile` wrapper: unreadable file → the documented exception type | Error-typing gap in the JVM wrapper | FIL-9 | p2p-core jvmTest | unit | P3 |
| Android URI wrapper branches: null/-1 SIZE column, FileNotFound, SecurityException | Wrapper entirely untested; no instrumented tests by repo policy → manual recipe (or Robolectric host test if that source set lands) | FIL-9, A08 §3 r13 | manual recipe in INTERNAL_TESTING.md | manual | P3 |
| `connect()` never consults the permission manager | Gate-scope invariant; a refactor adding `ensurePermissions()` to connect would pass today's suite | A09 §3 r6, DOCA-11-adjacent | p2p-core commonTest (PermissionGateTest) | unit | P3 |
| `createManualPeer` with blank host / out-of-range port: `IllegalArgumentException` from `PeerRegistry.registerManualPeer` surfaces uniformly through the sidecars (not swallowed/wrapped inconsistently) | Validation exists only in core; managers pass through; the IAE is undocumented | API-18, A09 §3 r9 | p2p-core commonTest + p2p-network-provisioning-desktop test (JvmNetworkProvisioningManagerTest) | unit | P3 |
| JVM `networkState` poll transitions: NoNetwork / ConnectedToWifi / Unknown-on-failure (via NIC-enumeration seam); per-NIC SocketException guarded | The whole poll loop is unexecuted by tests; PRM-6's missing guard needs the same seam | A09 §3 r10, PRM-6 | p2p-network-provisioning-desktop test (**needs seam**, §2.2) | unit | P3 |
| Remote-supplied strings are single-lined/escaped before logging (sanitizer helper: `"a\nFAKE LOG"` → one escaped line; a peer ERROR reason with embedded newlines produces one log line) | Keeps app-logger output trustworthy when peer input contains control characters | RBS-2, A14-rob §4 r3 | p2p-core commonTest (sanitizer unit) | unit | P3 |
| iOS `announceCache` / `IosEndpointRegistry` stay bounded under a high volume of Found events | Transport-layer counterpart of the registry bound (one layer down) | RBS-3, A14-rob §4 r4 | p2p-transport-lan appleTest | unit | P3 |
| Trace sinks perform no work when disabled (counting sink + enabled=false) | The "pays nothing when off" contract is documented but only FrameTrace honors it structurally | PERF-4, A15 §3 r7 | p2p-transport-lan jvmTest (JvmLanDiag) | unit | P3 |
| End-to-end 5 MiB loopback throughput/allocation guardrail (coarse bound or JMH-style harness, run manually, not CI-gating) | Copy-chain regressions (PERF-1/2) are invisible to all current tests; kept manual to avoid flaky timing assertions | PERF-1, PERF-2, A15 §3 r6 | p2p-transport-lan jvmTest manual/benchmark task | manual/integration | P3 |
| A non-NoOp logger still receives the ZOMBIE warn in the forced scenario (only if logger-type gating lands) | Guards the PERF-5 optimization against silencing a diagnostic | PERF-5 | p2p-core commonTest — **needs F7** | unit | P3 |
| BUILD_COMMIT stamp semantics: stamped only on executed+successful per-config assembly; not re-stamped on UP-TO-DATE; debug/release XOR | The Xcode freshness guard's trust anchor; the writer is TestKit-testable even though the shell check is not | A11 §3 r4 | Gradle TestKit on :p2p-transport-lan | integration | P3 |
| Exactly 4 modules publish; samples never acquire maven-publish | Accidental sample publication would ship test harnesses under dev.p2pkit | A11 §3 r5 | root build verification task/script | unit (build logic) | P3 |
| Doc test-count drift guard: drop exact counts from docs, or a tiny CI grep reconciling quoted totals | Manual counts rot every merge; three docs already disagree with reality | A13a §3 r3, DOCA-4, DOCA-13 | tiny CI script or doc policy | script | P3 |
| Consumer-path file transfer + `stop()` idempotence through `createP2pKit` | The loopback gate covers text-greeting only | A16 §3 r4 | sample-kmp-shared (KmpConsumerLoopbackTest sibling case) | integration | P3 |
| CLI arg parsing (`reconnect=`, `trace=`, name/appId filtering) | Regression-prone hand parser guarding harness identity (already had one audit fix) | A16 §3 r5 | p2p-sample-desktop test source set (new, §2.2) | unit | P3 |
| DATA frame with NEEDS_ACK → ACK with same messageId/chunkIndex (loopback) — **conditional**: only if the ACK responder is implemented; otherwise the spec §13.5 text is corrected instead | Spec promises a responder no code contains | PRO-2 | p2p-transport-lan jvmTest loopback (conditional) | unit | P3 |
| Cinterop helpers leak-free under sustained send/receive (Instruments Leaks step, or a simulator test asserting stable memory across N transfers) | A per-send/per-receive leak passes every functional test silently | A10 §3 r5, IOSB-7 | manual (smoke matrix Instruments step) | manual | P3 |
| Sample manual robustness checks: Ctrl-C → peer-Lost latency on the second device (no shutdown hook today); long-run soak for per-transfer collector pile-up; crafted peer-name rendering; oversized reject/cancel-reason rendering | Sample-level gaps that are not automatable at SDK level; make them explicit recipe steps | SMP-4, SMP-5, SMP-6, SMP-7 | manual (INTERNAL_TESTING.md additions) | manual | P3 |
| AndroidRawConnection behavior parity — any executed coverage | The class has zero test executions; parity holds by diff only; no instrumented tests by repo policy | A06 §3 r10, A12 §3 r11 | manual (INTERNAL_TESTING.md §recipes) | manual | P3 |

## 4. Notes on merged duplicates

- The remote-termination cluster (SES-1 + SES-10 + TST-1 + TST-11 + CON-7 + A12 rows 1/2) was
  requested, in whole or part, by four reports (A03, A06, A12, and A02 indirectly); it is two P1
  rows here plus fixture F1.
- `send()` typed error was requested by A02 (P1), A03 (P2), A12 (fixture note) → one P1 row.
- `strictInvariants` wiring was requested by A03 and A12 → one P1 row + F6.
- Metadata contract was requested by A02 (code side) and A13a (contract side) → one P1 row.
- Zero-byte file transfer was requested by A02 (P2) and A08 (P3) → one P2 row.
- Heartbeat de-noising was requested by A04 and A15 → one P2 row.
- Registry manual-entry protections were requested by A04 (IDN-1) and A05 (DSC-11) → one P2 row.
- FrameReader pathological-feed anchors were requested by A15 (PERF-9), A14-sec (SEC-I1), and
  A07 (PRO-10) → one P2 row.
- HELLO decode guards were requested by A07 (§3 + PRO-4) and A14-sec (SEC-I2) → one P1 row.
- Blank-TXT handling was requested by A14-robustness (RBS-1) and A02 (API-17) → one P1 row.

## 5. Standing manual/hardware coverage (policy — not gaps to automate, never to mask)

| Item | Status / policy |
|---|---|
| `IosLanLifecycleTest.peerLostEventFiresWhenPeerStops` and `advertiseStopRestartProducesObservablePeerChurn` | **Stay ACTIVE** in `iosSimulatorArm64Test`. Known-flaky on the simulator because its NWBrowser does not reliably deliver removed results. The peer-Lost path is validated on real hardware (smoke-matrix row A4 in docs/STABILIZATION_AND_RELEASE.md). Do NOT widen timeouts, add `@Ignore`, or relax assertions. |
| Android LAN transport paths, `WifiManagerWrapperImpl`, Android/iOS `NetworkPathObserver` on device | No instrumented Android tests **by policy**; covered by the manual recipes in INTERNAL_TESTING.md (§A–§K) and the device smoke matrix (A1–A12). Represented in this plan as manual rows (§3), not as automation gaps. New manual rows proposed above: provisioning decline/timeout/location-off, Android URI wrapper branches, AndroidRawConnection parity execution, sample robustness checks. |

## 6. Plan rules (campaign constraints honored)

- **No masking, ever**: no `@Ignore`, no widened timeouts, no relaxed assertions anywhere in
  this plan. Two rows *replace* assertions that currently pin defective or over-tight behavior
  (PRM-5's wrong-type pin; the rotation test's `refreshCalls == 1`): those are semantic
  corrections accompanying fixes, each flagged inline, not relaxations.
- Several rows pin **whichever decision the RC makes** (metadata transmit-vs-drop, offer-timeout
  labeling, forward-compat decode stance, iOS inbound-queue bound, manual-peer rename). The test
  is written after the decision; the row exists so the decision cannot land unpinned.
- Priorities: where reports disagreed on a merged row, the highest priority wins (noted in §4).
- Rows marked **blocked on F#** must not be attempted before the §2.1 fixture change lands; the
  fixture work is therefore the first scheduling unit of this plan.
