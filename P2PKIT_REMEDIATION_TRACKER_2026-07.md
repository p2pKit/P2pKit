# P2pKit remediation master tracker

Created: 2026-07-17

Source register: `P2PKIT_FULL_CODE_REVIEW_2026-07-17.md`

Baseline commit: `6a05ccd04fcb6fb8106ed47941618fb6bcfd3fa6`

Working branch: `remediation/full-register-2026-07`

## Purpose and non-negotiable rules

This is the mutable execution record for remediating the source review. The source review is evidence and must not be edited as a substitute for fixing code. The pre-existing `.review-2026-07/` directory and `DEFERRED_ITEMS_REGISTER_2026-07.md` are user-owned and remain outside this program.

The register contains exactly 150 unique findings and 54 separately listed test gaps. No row may be deleted, silently merged, downgraded, or marked irrelevant. A shared architectural fix may cover multiple rows only when the shared root cause is recorded before implementation and every row receives independent acceptance and verification evidence.

Statuses have these meanings:

- `Planned`: inventoried, but the finding-specific analysis and implementation have not started.
- `In Progress`: the current finding/group is being analyzed, implemented, reviewed, or verified.
- `Implemented`: the root fix and required tests exist locally, but all acceptance gates have not passed.
- `Verified`: every acceptance criterion passed from the focused committed state, the committed diff was reviewed, required checks were rerun, and the focused commit was pushed.
- `Blocked`: a concrete product decision, breaking-change approval, physical-device result, credential, or external service is required. Evidence and the exact unblock condition must be recorded.

`Verified` is deliberately stricter than “tests passed once.” A row needs a plan, changed-file ledger, regression tests, exact commands/results, compatibility notes, focused commit hash, pushed branch, and post-commit verification. Test retries must demonstrate determinism; they may not hide failures by weakening assertions, accepting multiple terminal outcomes, increasing timeouts, or adding arbitrary delays.

Operating safeguards:

- Do not advance to a new finding while the active finding is partially implemented, unverified, or has an unresolved regression. A documented `Blocked` boundary ends that unit without pretending it is complete.
- A finding believed invalid or already fixed remains open until concrete code/test evidence is recorded and the owner explicitly approves closure.
- Never rewrite history, force-push, destructively clean the repository, or delete existing/user work. Stage only the focused finding's files.
- Preserve `CancellationException`; do not silently swallow failures, hide warnings, skip required checks, or replace real platform evidence with mocks.
- Breaking API/protocol/security decisions, unavailable hardware, credentials, and external release actions stop before mutation and require explicit authority.
- A blocked unit blocks only itself and genuinely dependent units. Record its IDs, evidence, attempts, options, recommendation, consequences, and exact unblock request; continue every dependency-ready independent unit. Consolidate unresolved owner decisions only when no ready unblocked work remains.

## Baseline inventory

| Register | Count |
| --- | ---: |
| CORE | 30 |
| PROTO | 8 |
| FILE | 15 |
| LAN | 26 |
| PROV-A | 12 |
| PROV-D | 5 |
| SAMPLE | 39 |
| BUILD | 15 |
| Findings total | 150 |
| Explicit test gaps | 54 |

Current finding state: 3 `Planned`, 116 `Implemented`, 29 `Verified`, and 2 `Blocked`. Explicit gap state: 21 `Verified`, 19 `Implemented`, 2 `Planned`, and 12 `Blocked`; no gap remains `In Progress`. All lifecycle, immutable-model, feature-state, transport-capability, retained-offer, and typed-transfer-error owner decisions are locally implemented. The only remaining Planned findings are the approved authenticated-envelope and durable-transfer-protocol rows; the only remaining Planned explicit gaps are their negotiation, compatibility, integrity, and durability tests. Existing physical-device, hostile-network, credential, cryptographic-audit, runtime-UI, remote-release, and source-control blockers remain unchanged. The repository is not production-ready until every external gate is complete and local commits are pushed and reclassified under the strict status rules.

### Baseline gate evidence and reusable command catalog

The following failures are preserved from the source review baseline; they were not rerun while creating this documentation-only tracker:

| Gate | Baseline result at `6a05ccd` |
| --- | --- |
| `./gradlew :p2p-sample-android:lintDebug` | Red: one `CoarseFineLocation` error and three warnings |
| Forced `:p2p-core:jvmTest` | 258 tests, one failure: `FileTransferFlowTest.cancelMidStreamPropagatesToReceiver`; sender was `Completed`, required `Cancelled` |
| `:p2p-core:iosSimulatorArm64Test` | 243 tests, one 3,500 ms timeout: `SessionReconnectRotationTest.reconnectUsesRefreshedHintsAfterPeerRegistryUpdate` |
| `:p2p-transport-lan:iosSimulatorArm64Test` | 37 tests, two 30-second timeouts: `IosLanLifecycleTest.peerLostEventFiresWhenPeerStops` and `advertiseStopRestartProducesObservablePeerChurn`; one diagnostic intentionally skipped |
| Compiler/test hygiene | Existing ungated ExperimentalCoroutinesApi, deprecated multicast API, and Android nullable type warnings |

Every finding record must replace placeholders with exact commands. The standard escalation ladder is:

1. Run the smallest named test/class/task proving the regression.
2. Run the complete affected source-set/module test and compile tasks.
3. Run every affected JVM, Android, iOS simulator/device, KMP, sample, lint/static, publication, and integration target.
4. Repeat concurrency/timing tests with deterministic synchronization from the committed state.
5. Run repository-wide `check` plus the separate Android lint, publication-artifact, XCFramework/Xcode, and external-consumer gates; record every task and result rather than writing only “full gate passed.”

### Per-finding execution-record template

Copy this template below the active unit before implementation and fill every field:

| Field | Required content |
| --- | --- |
| Finding/group | IDs, raw severities, why any grouping shares one root cause |
| Status | Planned / In Progress / Implemented / Verified / Blocked |
| Root cause/reproduction | Current-code evidence and deterministic reproduction result |
| Affected surface | Files, components, modules, targets/platforms, public contracts |
| Compatibility/migration | Source, binary, runtime, wire, data, security, rollout implications |
| Plan/cleanup | Long-term design, ownership, cancellation, rollback, failure behavior |
| Acceptance criteria | Precise independently testable results for every finding/gap conjunct |
| Files changed | Exhaustive focused diff ledger |
| Tests added/updated | Exact test names and why each fails before the fix |
| Commands/results | Exact commands, counts, failures/warnings/skips, repeat evidence |
| Diff review | External-PR-style review result, equivalent-implementation search, regressions checked |
| Remaining risks | None, or concrete residual risk/blocker without downgrading |
| Source control | Branch, focused commit hash/message, pushed remote/ref, committed diff review |
| Post-commit verification | Commands rerun from committed state and exact results |

## Dependency-ordered execution plan

The order below is a dependency graph, not permission to combine every row in a wave into one commit. Each execution unit remains focused; independent findings get independent commits.

| Order | Unit | Scope | Why it is ordered here | Gate before advancing |
| ---: | --- | --- | --- | --- |
| 0 | GOV-01 | Tracker/baseline; test-foundation findings CORE-30 and gap LAN-T11 are executed before the affected suites | Establishes traceability and deterministic test infrastructure before source fixes | 150 findings/54 gaps reconciled; baseline gate catalog preserved; fixture isolation completed before dependent work |
| 1 | SEC-01 | CORE-06, CORE-07 | Security must own the sole raw byte stream and determines identity/wire/public compatibility; coupled prerequisites remain separately assigned as recorded below | Explicit approval of all SEC-01 decisions; implementation resumes after focused prerequisite sub-units |
| 2 | REL-REMOTE-01 | BUILD-02 | High release blocker is actionable locally even though real upload is external | Portal choice/configuration and local dry-run complete; ENV-07 alone retains credentialed upload evidence |
| 3 | REL-ABI-01 | BUILD-01; gaps CORE-T13, PS-T09 | Public artifacts must be independently consumable before new public security/API dependencies land | Temp-published JVM/Android/KMP/iOS consumers compile with correct API scopes |
| 4 | LIF-SES-01 | CORE-01, CORE-03, CORE-08, CORE-09, CORE-10, CORE-11, CORE-12, CORE-13, CORE-16, CORE-23, CORE-24; gaps CORE-T01, CORE-T02, CORE-T07, CORE-T08, CORE-T09, CORE-T10 | Terminal generation, connect ownership, setup deadline, inbound recovery, and close contract underpin later systems | Exact cancellation/rollback/bounded cleanup results; CORE-13 follows approved SEC-01 setup order; CORE-24 API decision approved |
| 5 | PEER-CTRL-01 | CORE-02, CORE-04, CORE-05, CORE-14, CORE-15, CORE-17, CORE-27, CORE-28; gaps CORE-T03, CORE-T04, CORE-T05, CORE-T06 | Correct aggregation/arbitration/control plane/state/public values precede higher layers | Multi-source/direction/backpressure/monotonic tests prove one precise result |
| 6 | ID-01 | CORE-18, CORE-19, CORE-20, CORE-21, CORE-29, SAMPLE-35; gap CORE-T11 | Secure identity depends on collision-safe, concurrent, stable, atomic persistence and per-sample isolation | All CORE-T11 conjuncts pass, including concurrent first creation; approved migration/key contract implemented |
| 7 | PATH-PERM-01 | CORE-22, CORE-25, CORE-26; gap CORE-T12 | Path generation/cleanup and Android permission defaults precede provisioning | Platform observer lifecycle/generation and permission matrix pass |
| 8 | PROV-A-HIGH-01 | PROV-A03 | Mixed Medium/High lifecycle race is sequenced at its maximum severity after core lifecycle prerequisites | Concurrent close/start/join has one owner/result; real-platform proof remains linked through PS-T01 |
| 9 | PARSE-01 | PROTO-01, PROTO-02, PROTO-03, PROTO-04, PROTO-05, PROTO-06, PROTO-07, PROTO-08; gaps PT-T01, PT-T02, PT-T03, PT-T04, PT-T05, PT-T21 | Parser/version/API decisions precede hostile records and transfer protocol changes | Linear work, early/version/type validation, strict text/structure, isolated diagnostics, metadata contract, fuzz/property gates |
| 10 | XFER-01 | FILE-01, FILE-02, FILE-03, FILE-05, FILE-06, FILE-07, FILE-08, FILE-09, FILE-10, FILE-11, FILE-12, FILE-14, FILE-15; gaps PT-T06, PT-T07, PT-T08, PT-T09, PT-T10, PT-T11, PT-T12, PT-T13, PT-T14, PT-T15, PT-T17, PT-T19, PT-T20 | Transactional ownership/state is prerequisite to durability protocol and sample receives | Deterministic cancellation/timeouts/sink serialization/release/boundaries/platform metadata; FILE-04/PT-T16 are not claimed here |
| 11 | XFER-PROTO-01 | FILE-04, FILE-13; gaps PT-T16, PT-T18 | Receiver acknowledgement and digest intentionally alter the wire contract | Approved protocol revision; durability/integrity failures produce one exact sender result |
| 12 | SAMPLE-HIGH-01 | SAMPLE-01, SAMPLE-02, SAMPLE-03, SAMPLE-04 | High sample data-integrity/security/availability work immediately follows completed transfer dependencies | Failure propagation, atomic names, consent/quota/free-space, bounded binary history proven |
| 13 | LAN-LIFE-01 | LAN-01, LAN-02, LAN-03, LAN-04, LAN-05, LAN-06, LAN-07, LAN-08, LAN-09, LAN-10, LAN-11, LAN-12, LAN-23, LAN-24, LAN-25, LAN-26; gaps LAN-T02, LAN-T03, LAN-T04, LAN-T05, LAN-T06, LAN-T10 | Resource ownership/admission/cancellation/recovery precede selection/trust work | Repeated JVM/Android/Apple lifecycle gates; no ghost, stale, unbounded, or undrained resource |
| 14 | LAN-NET-01 | LAN-13, LAN-14, LAN-15, LAN-16, LAN-17, LAN-18, LAN-19, LAN-20, LAN-22; gaps LAN-T01, LAN-T07, LAN-T08, LAN-T09 | Network selection/liveness/schema/trust/permissions/diagnostics build on sound lifecycle | All hostile-record and platform-selection conjuncts pass; hardware criteria remain explicit |
| 15 | PROV-A-01 | PROV-A01, PROV-A02, PROV-A04, PROV-A05, PROV-A06, PROV-A07, PROV-A08, PROV-A09, PROV-A10, PROV-A11, PROV-A12; gaps PS-T01, PS-T02 | Remaining Android provisioning builds on fixed PROV-A03 lifecycle ownership | Fake plus real callback/permission/LinkProperties/process-binding tests pass |
| 16 | PROV-D-01 | PROV-D01, PROV-D02, PROV-D03, PROV-D04, PROV-D05; gap PS-T03 | Desktop provisioning validation/interface behavior and test isolation | Boundary/ranking/fatal propagation/global-state restoration tests pass |
| 17 | SAMPLE-XFER-01 | SAMPLE-05, SAMPLE-06, SAMPLE-07, SAMPLE-12, SAMPLE-13, SAMPLE-14, SAMPLE-15, SAMPLE-22, SAMPLE-23, SAMPLE-24, SAMPLE-34; gaps PS-T06, PS-T07, PS-T08 | Remaining sample transfer/path/history changes consume fixed transfer APIs | Every conjunct for desktop/Swift/path tests passes; partial files/resources are cleaned deterministically |
| 18 | SAMPLE-LIFE-01 | SAMPLE-08, SAMPLE-09, SAMPLE-10, SAMPLE-11, SAMPLE-16, SAMPLE-17, SAMPLE-18, SAMPLE-19, SAMPLE-20, SAMPLE-21, SAMPLE-25, SAMPLE-26, SAMPLE-27, SAMPLE-28, SAMPLE-29, SAMPLE-30, SAMPLE-31, SAMPLE-32, SAMPLE-33, SAMPLE-36, SAMPLE-37, SAMPLE-38, SAMPLE-39; gaps PS-T04, PS-T05 | Lifecycle/CLI/UI/permission/logging/consumer correctness after library contracts settle | Full Android sample tests and every CLI collector/shutdown conjunct; all samples build/lint |
| 19 | REL-PROV-01 | BUILD-03, BUILD-07, BUILD-09, BUILD-10, BUILD-11, BUILD-12, LAN-21 | Reproducibility/provenance/XCFramework/launcher work follows stable source/artifact flow | Repeat builds deterministic; declared outputs and scripts behave under deletion/concurrency |
| 20 | REL-GATE-01 | BUILD-08, BUILD-14, BUILD-15 | Deterministic repository gate blockers/warnings must be removed, not waived | Android lint and warning/static gates are green |
| 21 | REL-SUPPLY-01 | BUILD-04, BUILD-05, BUILD-06, BUILD-13 | Publication validation, wrapper, supply-chain and release maintenance close after artifact shape stabilizes | Wrapper/checksums/signatures/scopes/archives/CI/locks/SBOM/ABI/release policy validated |
| 22 | FINAL-01 | All 150 findings, all 54 gaps, ENV-01, ENV-02, ENV-03, ENV-04, ENV-05, ENV-06, ENV-07 | Repository-wide closure only after every focused unit | Repeated full gate green; physical-device/two-machine/hostile-network/external release evidence complete; every row Verified |

### Known external and approved-contract boundaries

- SEC-01's security, wire, migration, and storage-A decisions were approved and frozen on 2026-07-17. Implementation is active; only external professional cryptographic audit and physical-device/hostile-network certification remain blocked release evidence.
- The owner approved all nine remaining API/protocol contracts on 2026-07-22. Unimplemented rows are Planned, not decision-blocked; wire compatibility is not claimed until negotiated envelope/transfer batches land.
- PROV-A12, parts of LAN-T01/LAN-T07, and FINAL-01 require physical Android/Apple devices or platform instrumentation that may not be available locally.
- BUILD-02 requires release-service configuration and ultimately external credentials; credentials must never be committed.
- CORE-11/CORE-T09 restartable rollback and CORE-24 provisioning disposal are implemented locally; push and physical platform/network evidence remain open.
- Remote publication, signing identity, future live advisory-database results, two-machine validation, and hostile-network certification remain explicit external evidence, not inferred from unit tests.

## Finding traceability register

The `Unit/dependencies` column identifies ordering, not automatic commit grouping. `Plan/code`, `Tests`, `Commit/push`, and `Verification` must be replaced with links or exact evidence as work progresses.

### Core runtime, API, identity, and sessions (30)

| ID | Severity | Finding | Unit/dependencies | Status | Plan/code | Tests | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| CORE-01 | High | Stop does not serialize terminal lifecycle with ongoing operations | LIF-GEN-01 (first LIF-SES-01 slice) | Verified | Terminal generation gate, stale-resource compensation, atomic session-registration commit | CORE-T01 | `a4e0bb0`; tracker evidence pending push | Focused committed suite + iOS Simulator + Android compilation pass; only registered FILE-04 full-JVM baseline remains |
| CORE-02 | High | PeerRegistry is not a correct multi-transport aggregator | PEER-CTRL-01 after LIF-SES-01 | Implemented | Per-transport contributions with merged hints/capabilities and source-specific loss/staleness | CORE-T03 | `ee2cae5`; local, push blocked | Focused + complete JVM/iOS pass |
| CORE-03 | High | Cancelled connect can poison coalescing and leak a live session | CORE-SESSION-01 | Verified | Transactional raw/session/pending ownership with forced rollback before commit | CORE-T02 | `68934be`; test follow-up `82a9b41`; pushed | Dial/HELLO/security/pre-commit cancellation and exact retry pass on JVM/iOS |
| CORE-04 | High | Application receive backpressure blocks protocol controls | PEER-CTRL-01 after LIF-SES-01 | Implemented | Separate delivery coroutine behind count/byte budget; overflow fails rather than drops | CORE-T05 | `ee2cae5`; local, push blocked | Slow collector cannot block PONG/CLOSE; overflow exact Failed |
| CORE-05 | High | Duplicate arbitration treats every active duplicate as simultaneous-open | PEER-CTRL-01 after LIF-SES-01 | Implemented | Store registration direction; arbitrate only opposite-direction candidates | CORE-T04 | `ee2cae5`; local, push blocked | Repeated inbound candidates never churn either PeerId ordering |
| CORE-06 | High security limitation | Identity is unauthenticated and traffic plaintext | SEC-01 | Implemented | Authenticated v2 is the default; explicit deprecated legacy only | Noise/KAT, identity, raw-confidentiality, authorization, integration, LAN loopback | `b79c9ba`, pushed to `origin/remediation/full-register-2026-07` | Local platform/module gates pass; external certification remains |
| CORE-07 | Medium architecture | Security extension cannot safely implement encryption | SEC-01 | Implemented | Security owns the sole raw reader before protocol construction | Single-collector, cancellation, close-once, encrypted-HELLO tests | `b79c9ba`, pushed to `origin/remediation/full-register-2026-07` | Local platform/module gates pass; external certification remains |
| CORE-08 | Medium | SessionStore reads mutable HashMap without mutex | CORE-SESSION-01 | Verified | Immutable published registration snapshot | Concurrent read/mutation regression | `68934be`; pushed | 2,000 mutations with four concurrent readers; repeated JVM + iOS pass |
| CORE-09 | Medium | Remotely terminated sessions retain active child Job | CORE-SESSION-01 | Verified | Cancel session runtime after terminal resource cleanup | CORE-T07 | `68934be`; pushed | Exact remote CLOSE and failure job-completion tests pass on JVM/iOS |
| CORE-10 | Medium | Public sessions can retain terminal entries after stop | CORE-SESSION-01 | Verified | Atomic store shutdown drain before watcher cancellation | CORE-T08 | `68934be`; test follow-up `82a9b41`; pushed | stop returns with exact empty public sessions; JVM/iOS pass |
| CORE-11 | Medium | Partial startup/advertising/discovery is not rolled back | CORE-LIFE-02 + DATA-TRANSPORT-LIFECYCLE-API-01 | Implemented | Every attempted data start now rolls back in reverse through restartable `stop()`; cancellation returns to retryable Idle; pending dials/listeners are released and cleanup failure blocks restart | CORE-T09 | `60aecf6`, `391ae3c`; local, push blocked | Focused and complete core/LAN JVM+iOS, Android compile, ABI, Dokka, and published implementer gates pass |
| CORE-12 | Medium | Teardown can report success while resources remain open | CORE-LIFE-02 | Implemented | Independently bounded cleanup attempts; aggregate typed failure; terminal state/result shared by every caller; JmDNS-compatible close bound without timing out the teardown body | CORE-T10 | `60aecf6`, follow-up `b21ed84`; local, push blocked | 417/417 JVM and 392/392 iOS core tests pass |
| CORE-13 | Medium | Inbound setup timeout excludes operations that can hang | CORE-SESSION-01 | Verified | One outer deadline covers secure preface/security/HELLO; timeout closes raw and releases inbound admission | Full-transaction deadline tests | `68934be`; test follow-up `82a9b41`; pushed | Secure/legacy outbound and idle-inbound deadline+retry pass on JVM/iOS |
| CORE-14 | Medium | Keepalive uses wall clock and misses exact deadline | PEER-CTRL-01 | Implemented | Platform monotonic elapsed clock and `>=` deadline | CORE-T06 | `ee2cae5`; local, push blocked | Exact virtual deadline ignores forward/backward wall jumps |
| CORE-15 | Medium | Global P2pState hides independent feature failures | PEER-STATE-API-01 | Implemented | Separate retained advertising/discovery state machines; global state remains kit lifecycle only; concurrent starts coalesce and stop intent wins startup | Feature-state, permission, cancellation, cleanup-retry, ABI and consumer tests | `18723c0`; local, push blocked | 37/37 focused JVM and 37/37 Apple tests; complete core/platform gates pass |
| CORE-16 | Medium | One incoming-flow exception permanently disables a transport | CORE-LIFE-02 | Implemented | One active collector per transport with sequential recollection and bounded exponential backoff | Recollection/backoff tests | `60aecf6`; local, push blocked | Deterministic failure/recovery test; full JVM/iOS core green |
| CORE-17 | Medium | Public values expose mutable backing storage | IMMUTABLE-MODEL-API-01 | Implemented | Every documented collection-bearing public model and published peer/session list now owns a stable cast-proof snapshot; binary arrays remain defensive copies | Kotlin/Java/Apple mutation, ownership, value-semantics, ABI and external-consumer tests | `ee2cae5` partial, `04aaef0`; local, push blocked | Complete core/platform/sample/XCFramework/Swift gates pass; Kotlin `data class` reflection identity intentionally changed under owner approval |
| CORE-18 | Medium | Distinct AppIds collide in persistent identity storage | ID-STORE-01 | Verified | Full AppId hash namespace + rollback-safe migration | CORE-T11 | `ee69d09`, pushed | Focused/platform tests pass |
| CORE-19 | Medium | First-use PeerId creation is not concurrency-safe | ID-STORE-01 | Verified | Process/cross-process commit lock + reread winner | CORE-T11 | `ee69d09`, pushed | 16-thread + four-process tests pass repeatedly |
| CORE-20 | Medium | Persistence failure breaks same-instance identity stability | ID-STORE-01 | Verified | Instance memoization on every outcome | CORE-T11 | `ee69d09`, pushed | JVM/iOS failure tests pass |
| CORE-21 | Medium | Android/JVM identity fallback can truncate durable value | ID-STORE-01 | Verified | Android AtomicFile + JVM fsync/atomic move | CORE-T11 | `ee69d09`, pushed | JVM atomic/migration suite + Android compile pass |
| CORE-22 | Medium | Android/iOS path observers retain stale state/cleanup ownership | PATH-PERM-01 | Implemented | Shared generation-gated callback state; Android retains callback ownership across unregister failure and retries; iOS invalidates late callbacks under an NSLock before restart | CORE-T12 | `1903b11`; local, push blocked | Full core JVM/iOS suites and all target compiles pass; physical callback/unregister evidence remains |
| CORE-23 | Medium | New session can miss authoritative Unsatisfied path state | CORE-SESSION-01 | Verified | Retained versioned path authority applied after every registration | Registration after prior `Unsatisfied` without re-emission | `68934be`; pushed | Exact Failed outcome passes on JVM/iOS |
| CORE-24 | Medium API gap | NetworkProvisioningManager has no close contract | PROVISIONING-CLOSE-API-01 | Implemented | Required suspending terminal close, typed post-close behavior, retained native cleanup, active-operation cancellation, and core-before-transport teardown ordering | Close contract/ABI/external implementer tests | `391ae3c`; local, push blocked | JVM/Desktop, Android host, Apple, core ordering, ABI, Dokka, and published implementer gates pass |
| CORE-25 | Low | Android permission diagnostics omit two declared requirements | PATH-PERM-01 | Implemented | Construction diagnostic checks the complete `INTERNET`, `ACCESS_NETWORK_STATE`, `ACCESS_WIFI_STATE`, and `CHANGE_WIFI_MULTICAST_STATE` install-time matrix without adding runtime gating | Permission matrix | `1903b11`; local, push blocked | Exact common matrix test and full JVM/iOS suites pass; physical PackageManager/logcat evidence remains |
| CORE-26 | Low limitation | Android defaults to no-op path observer | PATH-PERM-01 | Implemented | Registered application context now constructs the ConnectivityManager observer by default; initialization-free hosts retain the Unknown no-op fallback | Default wiring test | `1903b11`; local, push blocked | Context-selection seam and full JVM/iOS suites pass; physical Android lifecycle evidence remains |
| CORE-27 | Low API mismatch | Factories cannot express discovery-only transport | TRANSPORT-FACTORY-API-01 | Implemented | Required immutable pre-build descriptors plus nullable data/discovery paths; discovery-only, data-only, and combined providers validate before startup | Factory/API, capability, mismatch, ABI and external-implementer tests | `18723c0`; local, push blocked | JVM/Android/Apple production providers compile; published Kotlin/Java/KMP/iOS consumers pass |
| CORE-28 | Low | Builder validation is incomplete | PEER-CTRL-01 + TRANSPORT-FACTORY-API-01 | Implemented | Names/manual hosts/same-instance duplicates plus descriptor-first duplicate-kind rejection before any factory build | Boundary/duplicate/build-counter tests | `ee2cae5` partial, `18723c0`; local, push blocked | Duplicate kinds leave both build counters at zero; complete affected gates pass |
| CORE-29 | Low | JVM identity fallback can silently use working directory | ID-STORE-01 | Verified | Validate home/temp roots; explicit failure if none | Property/path failure tests | `ee69d09`, pushed | Blank/invalid property tests pass |
| CORE-30 | Low | Test fixtures hide important race conditions | GOV-PACKAGE-01 test foundation | Implemented | Production-shaped discovery fixtures use replay zero, explicit subscriber readiness, shared JVM property leases, suite-scoped Apple defaults, and per-test Bonjour namespaces/current-PeerId selection | Fixture/lease/namespace meta-tests plus complete core/LAN JVM and Apple suites | `52e173b`, `90b5f20`; local, push blocked | 426 JVM + 400 Apple core and 84 JVM + 64 Apple LAN tests pass; one documented manual diagnostic skip |

### Protocol and messaging (8)

| ID | Severity | Finding | Unit/dependencies | Status | Plan/code | Tests | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PROTO-01 | High availability | FrameReader performs quadratic copying | PARSE-01 | Verified | Reusable one-frame buffer; header-first allocation; in-place window decode; no tail copies | PT-T01/PT-T21 parser portion | `aa3ac0c`; pushed with batch | Linear relocation/property tests pass JVM/iOS; exact committed state green |
| PROTO-02 | Medium | Frame header version is ignored | SEC-01 dependency; PARSE-01 | Verified | Reader and direct codec reject a mismatched major from the fixed header | PT-T02 | `aa3ac0c`; pushed with batch | Secure-v2/legacy-v1 version tests pass with no fallback |
| PROTO-03 | Medium security/availability | Packet-specific size limits are absent | PARSE-01 | Verified | Header-time per-packet caps plus outbound codec/payload validation | PT-T03/PT-T05 | `aa3ac0c`; pushed with batch | HELLO/OFFER/reason/data/empty-control/unknown cap boundaries pass JVM/iOS |
| PROTO-04 | Medium | Control/chunk structures are too permissive | PARSE-01 before XFER-01 | Verified | Central structural validator plus transfer phase table; REJECT after ACCEPT/streaming is ignored as invalid | PT-T02/PT-T04/PT-T07 | `aa3ac0c`, `68c579f`; pushed | Parser and exact transition regressions pass JVM/iOS from committed state |
| PROTO-05 | Medium | Diagnostics can change protocol behavior | PARSE-01 | Verified | Throwing trace/logger sinks are isolated and disabled; cancellation is preserved | Throwing trace tests | `aa3ac0c`; pushed with batch | Protocol PING still delivered after throwing trace; exact committed JVM/iOS pass |
| PROTO-06 | Low | Remote text accepts invalid/canonicalization-hostile data | PARSE-01 | Verified | Strict UTF-8/Unicode; bounded nonblank control-safe text; leaf file names; exact reason/null distinction | PT-T04/PT-T05 | `aa3ac0c`, `6171588`; pushed with batch | Inbound/outbound malformed UTF-8, controls, bidi, separators, dot segments, blank and boundaries pass |
| PROTO-07 | Low | Malformed/unknown frames can flood logs | PARSE-01 | Verified | Four-message burst plus one suppression summary per fixed category/connection | PT-T03 | `aa3ac0c`; pushed with batch | 100 hostile frames produce exactly five warnings and delivery continues |
| PROTO-08 | Low API surprise | P2pMessage metadata is never transmitted | PARSE-META-01 after SEC-01 | Planned | Owner approved a negotiated authenticated secure-v2 metadata envelope; legacy v1 remains metadata-free | Versioned envelope/contract tests | — | Approved 2026-07-22; protocol batch queued |

### File transfer (15)

| ID | Severity | Finding | Unit/dependencies | Status | Plan/code | Tests | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FILE-01 | High | Cancellation during accept can orphan accepted transfer/sink | XFER-01 after LIF-SES-01/PARSE-01 | Verified | Transactional accept with bounded write and non-cancellable compensating cancel/receiver release | PT-T09 | `68c579f`; pushed | Deterministic gated FILE_ACCEPT cancellation passes JVM/iOS |
| FILE-02 | High availability | Accepted inbound transfers have no idle/overall timeout | XFER-01 | Verified | Positive-progress idle deadline, fixed overall deadline, 64-slot release | PT-T08/PT-T14 | `68c579f`; pushed | Exact virtual-time and full-capacity regressions pass |
| FILE-03 | High data race | Receive, finish, and cancel race on sink | XFER-01 | Verified | Per-transfer operation mutex serializes write/finish/terminal release outside map lock | PT-T10 | `68c579f`; pushed | Gated sink proves cancel waits and terminal cleanup is ordered |
| FILE-04 | High data-integrity contract | Sender completes before receiver durability | XFER-PROTO-01 after XFER-01 | Planned | Owner approved negotiated durable receiver commit after verification/finish and fail-closed legacy behavior | PT-T16 | — | Approved 2026-07-22; protocol batch queued |
| FILE-05 | Medium | Actionable offers disappear or arrive stale/out of order | XFER-OFFER-API-01 | Implemented | Authoritative immutable `pendingFileOffers` retains admission order until accept/reject/cancel/expiry/close; exact duplicates are idempotent and conflicting IDs fail typed | PT-T13 | `ddb7142`; local, push blocked | Capacity 64; late subscription, concurrency, expiry, duplicate, and terminal-removal tests pass on JVM/Apple |
| FILE-06 | Medium | Offer timeout starts before offer is writable/observable | XFER-01 + XFER-OFFER-API-01 | Implemented | Sender watchdog starts after wire write; receiver retains the offer under lock before its response timer starts | PT-T12 | `68c579f` sender, `ddb7142` receiver; local, push blocked | Both sender and receiver observability conjuncts pass deterministic boundary tests |
| FILE-07 | Medium | Timeout terminal states are nondeterministic | XFER-01 | Verified | Receiver normal timeout precedes a grace-delayed sender safety watchdog; exact REJECT result | PT-T14 | `68c579f`; pushed | End-to-end test asserts only `Rejected("timeout")` |
| FILE-08 | Medium | User I/O runs under global dispatcher mutex | XFER-01 | Verified | Map lock only transfers ownership; source/sink work uses per-transfer gate outside it | PT-T11 | `68c579f`; pushed | Blocking sink/cancel regression and diff review pass |
| FILE-09 | Medium | Terminal handles retain sources/sinks | XFER-01 | Verified | Atomic nullable source plus serialized nullable receiver cleared on all terminal paths | PT-T17 | `68c579f`; pushed | Completed/cancelled/failed matrix plus internal retention assertions pass |
| FILE-10 | Medium | Chunk arithmetic can overflow valid configuration | XFER-01 | Verified | Subtraction-based Long arithmetic and explicit Int chunk-count configuration/send bounds | PT-T06 | `68c579f`; pushed | Overflow rejected before source read; boundary tests pass |
| FILE-11 | Medium API | Transfer failures use misleading errors and lose causes | XFER-ERROR-API-01 | Implemented | Structured `FileTransferFailed` exposes stable kind, phase, retryability, transfer ID, bounded reason, and preserved internal cause while cancellation/rejection stay distinct | Typed cause-preservation tests | `ddb7142`; local, push blocked | JVM/Apple/API/consumer tests cover transport, timeout, source, storage, metadata, protocol, and disconnect mappings |
| FILE-12 | Medium | Progress can advance after terminal state | XFER-01 | Verified | State/byte commits share per-transfer mutex; terminal state freezes progress | PT-T15 | `68c579f`; pushed | Gated outgoing and concurrent incoming regressions pass |
| FILE-13 | Low integrity | Byte count checked but content is not | XFER-PROTO-01 after SEC-01 | Planned | Owner approved prepared SHA-256 source snapshots authenticated by the secure envelope and verified before durable commit | PT-T18 | — | Approved 2026-07-22; protocol batch queued |
| FILE-14 | Low | Transfer ID collision overwrites ownership | XFER-01 | Verified | Bounded unique allocation under lock across both maps; failure closes uncommitted source | PT-T19 | `68c579f`; pushed | Constant-random collision regression passes |
| FILE-15 | Low | Platform file helpers lack snapshot semantics | XFER-01/platform follow-up | Implemented | JVM descriptor-size snapshot; Android descriptor/query consistency, negative rejection, descriptor ownership | PT-T20 | `68c579f`; pushed | JVM tests and Android compile pass; hostile real-provider instrumentation remains external |

### LAN transport (26)

| ID | Severity | Finding | Unit/dependencies | Status | Plan/code | Tests | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| LAN-01 | High | Cancellation leaks ghost JmDNS service/listener | LAN-JMDNS-02 | Implemented | Create tokens before blocking side effects; compensate ambiguous cancellation under `NonCancellable`; generation-gate listeners | LAN-T02 | `83db1ab`; local, push blocked | Deterministic register/listener/create cancellation tests pass repeatedly; JVM integration and Android compile green |
| LAN-02 | High | Failed restoration committed as successful rebind | LAN-JMDNS-02 | Implemented | Treat close/create/register/listener restoration as one transaction; commit target only after all intended resources are live | Transactional restoration/retry tests | `83db1ab`; local, push blocked | Dropped registration/listener restoration self-retries to one healthy binding |
| LAN-03 | High | Apple browser/listener failure can pin ghost peers permanently | LAN-APPLE-03 | Implemented | Separate host intent/live leases; terminal browser/listener depublish; bounded self-recovery; failed browser suppresses ghost heartbeats; incomplete removal resolves only previously validated service identity | LAN-T07 recovery/loss conjuncts | `703182a`; local, push blocked | Real simulator cancellation recovery and both formerly timing-out Bonjour lifecycle tests pass repeatedly |
| LAN-04 | High availability | Apple inbound connection buffering is unbounded | LAN-CONN-01 | Implemented | Fixed 16-slot handoff; overflow, post-close handoff, and terminal drain cancel started NWConnections | LAN-T07/LAN-T08 queue/admission conjuncts | `b21ed84`; local, push blocked | Bounded queue ownership tests pass; Apple simulator compile/runtime focused gate green |
| LAN-05 | Medium | Cancelled JVM/Android outbound connect can orphan a socket | LAN-CONN-01 | Implemented | Socket remains locally owned through cancellable connect and is closed before cancellation propagates | LAN-T04 | `b21ed84`; local, push blocked | Deterministic JVM cancellation regression; mirrored Android code compiles |
| LAN-06 | Medium | Cancelled Apple connect leaves NWConnection active | LAN-CONN-01 | Implemented | Parent cancellation and timeout both cancel the uncommitted connection; cancellation is rethrown | LAN-T04 | `b21ed84`; local, push blocked | Deterministic Apple connection-factory regression passes |
| LAN-07 | Medium | Fatal JVM/Android accept-loop exit leaves a stale advertised port | LAN-CONN-01 | Implemented | Failed listener is atomically depublished and sequential core recollection rebinds its advertised port | LAN-T06 | `b21ed84`; local, push blocked | Real JVM ServerSocket failure then Charlie reconnect/message regression passes; Android compiles |
| LAN-08 | Medium | Ordinary write failure leaves raw connection connected | LAN-CONN-01 | Implemented | Every ordinary socket write IOException closes the fd and publishes Closed | LAN-T05 | `b21ed84`; local, push blocked | Forced-output IOException regression passes; watchdog success/timeout regressions remain green |
| LAN-09 | Medium | Raw-read cancellation does not unblock I/O | LAN-CONN-01 | Implemented | Collector cancellation closes the socket/NWConnection and unblocks the outstanding native read | Cancellation tests all targets | `b21ed84`; local, push blocked | Real JVM socket and Apple Network.framework loopback regressions pass; Android mirror compiles |
| LAN-10 | Medium | Cleanup failure discards ownership | LAN-JMDNS-02 | Implemented | Retain tokens/handle/callback/multicast ownership until successful cleanup; close-and-rebuild fallback; repeated stop retries | LAN-T03 | `83db1ab`; local, push blocked | Unregister/remove/close/watcher/release failure matrix passes; real Android instrumentation remains LAN-T01 |
| LAN-11 | Medium | Apple listener start/close races and retains stale state | LAN-APPLE-03 | Implemented | Cancellable listener readiness; serialized host intent/start/rebind/close; native terminal leases; retained cleanup; bounded release acknowledgement; missing-listener rebuild | LAN-T07/LAN-T10 | `703182a`; local, push blocked | Cancellation/retry, cancelled-caller close, exact-port release, terminal recovery, repeated lifecycle, and restart refusal pass |
| LAN-12 | Medium | Apple cache pruning can delete fresh rediscovery | LAN-APPLE-03 | Implemented | Serialize reconcile, endpoint removal, event publication, and current-generation confirmation under one native lock | LAN-T07 cache-race conjunct | `703182a`; local, push blocked | Deterministic gated prune/rediscovery interleaving retains generation 2 and publishes exact Lost then Found order |
| LAN-13 | Medium | Network rotation support incomplete on every platform | LAN-NET-01 after LAN-LIFE-01 | Implemented | JVM interface/address fingerprint watcher; Android selected-network callbacks with forced same-network address rebind; Apple interface-address fingerprint | LAN-T01/LAN-T07 | `21bc2fc`; local, push blocked | Local JVM/Android/Apple checks pass; physical/device rotation evidence remains |
| LAN-14 | Medium | Synthetic cache heartbeat treats presence as liveness | LAN-NET-01 | Implemented | Remove synthetic JVM/Android/Apple cache heartbeats; native DNS-SD/browser removal or TTL owns liveness and exact `Lost`; isolate exact JmDNS admission/removal ownership from UDP delivery timing | TTL/liveness and deterministic admission-removal tests | `21bc2fc`, `639c508`; local, push blocked | 35-second multicast liveness and 5/5 exact removal-ownership tests pass; real clean/abrupt native departure remains hostile-network evidence |
| LAN-15 | Medium | Local TXT values unbounded and failures silent | LAN-NET-SCHEMA-01 | Implemented | Central UTF-8 TXT-entry bounds; only display name truncates safely; identity/app/security/capability fields fail before registration; Apple native setter result is mandatory | LAN-T08 schema conjuncts | `2895ff1`; local, push blocked | Common/JVM/Apple hostile-record tests, all target compiles, XCFramework and Swift sample pass |
| LAN-16 | Medium security | Discovery records trusted too broadly | SEC-01 dependency; LAN-NET-01 | Implemented | Authenticated handshake remains SEC-01; LAN schema/service ownership plus same-subnet affinity and bounded ordered alternate candidates are implemented locally | LAN-T08 | `21bc2fc`; local, push blocked | Hostile records, TXT-less ownership, subnet filtering, and fallback tests pass; hostile-network/device proof remains |
| LAN-17 | Medium | Apple discovers AWDL endpoints data path may reject | LAN-NET-01 | Implemented | Enable peer-to-peer on shared Apple listener/dial parameters and retain path diagnostics | LAN-T07/device check | `21bc2fc`; local, push blocked | Native parameter seam and Apple recovery tests pass; real AWDL interoperability remains device-blocked |
| LAN-18 | Medium | Only one advertised address is retained | LAN-NET-01 | Implemented | Preserve up to eight ordered, deduplicated IPv4/IPv6 candidates with bounded fallback and subnet filtering on JVM/Android | LAN-T08 | `21bc2fc`; local, push blocked | Selector and failed-first/second-success tests pass; multi-interface/hostile-network proof remains |
| LAN-19 | Low | Library manifests omit normal network permissions | GOV-PACKAGE-01 after LAN-NET-01 | Implemented | Android LAN AAR declares `INTERNET`, `ACCESS_NETWORK_STATE`, `ACCESS_WIFI_STATE`, and `CHANGE_WIFI_MULTICAST_STATE`; isolated app consumer inspects the authoritative final merged manifest | Published application-consumer merge gate | `52e173b`; local, push blocked | 221 publication tasks + 18 isolated consumer tasks pass; all four permissions present; installed-device PackageManager/logcat matrix remains LAN-T01 |
| LAN-20 | Low | Apple packaging denial has weak diagnostics | GOV-PACKAGE-01 after LAN-NET-01 | Implemented | Start preflight reports missing/nonblank `NSLocalNetworkUsageDescription`, exact profile Bonjour service, and browser/listener native error codes on waiting/failed states | `AppleLanPackagingTest` plus complete Apple LAN suite | `52e173b`; local, push blocked | Pure preflight cases and Apple compilation/simulator suite pass; denied-permission/device logging remains external |
| LAN-21 | Low | Provenance stamp may become unknown/missing | REL-PROV-01 with BUILD-07 | Implemented | Typed writer fails on invalid Git identity and declares all three exact-commit/state/fingerprint sidecars as outputs | Deleted-sidecar recovery and exact-HEAD preflight | `e19c5c5`; local, push blocked | Missing sidecar restored while framework stayed UP-TO-DATE; clean committed preflight matches `639c508` |
| LAN-22 | Low | Diagnostics accept peer control characters | LAN-NET-SCHEMA-01 | Implemented | Bounded Unicode-safe control sanitization on JVM/Android/Apple; Apple history is explicit opt-in; Android routine diagnostics are default-off and sample opt-in; bounded warnings stay visible | Sanitization/retention/live-subscriber tests | `2895ff1`, `09af6b5`; local, push blocked | JVM/Apple sink tests pass; Android library/sample forced compile passes; real logcat instrumentation remains LAN-T01 |
| LAN-23 | Low | Terminal close retains listener ports/references | LAN-CONN-01 | Implemented | Terminal close serializes lifecycle and clears listener/socket, port, flow, and registration state | Repeated close/state tests | `b21ed84`; local, push blocked | JVM and Apple idempotent-close/restart-refusal assertions pass; Android mirror compiles |
| LAN-24 | Low race | Apple queued writer can run after close | LAN-CONN-01 | Implemented | Writer rechecks terminal state after acquiring the write mutex | Write-close interleaving test | `b21ed84`; local, push blocked | Deterministic gated queued-writer regression passes |
| LAN-25 | Medium | Retry budget carries into genuinely new network | LAN-JMDNS-02 | Implemented | Scope bounded retry counter to the observed primary/default target pair | Per-target generation tests | `83db1ab`; local, push blocked | Every genuinely new target receives an independent exact retry budget |
| LAN-26 | Low concurrency risk | Rebind scheduling is unsynchronized | LAN-JMDNS-02 | Implemented | Serialize debounce replacement with a dedicated mutex and deterministic await seam | LAN-T03 | `83db1ab`; local, push blocked | Concurrent schedules collapse to one binding transaction |

### Android network provisioning (12)

| ID | Severity | Finding | Unit/dependencies | Status | Plan/code | Tests | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PROV-A01 | Medium | Start can return Failed while hotspot remains live | PROV-A-01 after CORE-24/LIF-SES-01 | Implemented | A started reservation is installed only long enough to build the public result; a failed result atomically removes and closes the handle, resets network state, and emits one typed failure | `failedStartedHotspotClosesReservationAndPublishesFailedState` | `21dad61`; local, push blocked | Android host suite 23/23; real OEM callback/result evidence remains in PS-T01 |
| PROV-A02 | Medium | Start allowed after parent cancellation | PROV-A-01 | Implemented | Shared terminal closed gate set before parent-job resource release; post-cancel start returns `HotspotStopped` without invoking the wrapper | Closed-parent tests | `5bc31ce`; local, push blocked | Deterministic parent-cancel test passes; real callback evidence remains in PS-T01 |
| PROV-A03 | Medium/High race | Close not serialized with start/join | PROV-A-HIGH-01 | Implemented | Atomic handle ownership transfer and terminal gate; close retries no second owner, and late hotspot/join callbacks close their handles before returning one deterministic failure | Concurrent lifecycle tests/PS-T01 | `5bc31ce`; local, push blocked | Deferred hotspot and join races pass in the complete 20-test host suite; real callback evidence remains |
| PROV-A04 | Medium | Hotspot callback cancellation can lose a reservation handle | PROV-A-01 | Implemented | `HotspotReservationOwner` atomically rejects late callbacks after cancellation and closes every delivered reservation, including continuation-cancellation races | `provisioningOwnershipAndManifestSeamsAreDeterministic`; existing deferred hotspot race | `21dad61`; local, push blocked | Host owner seam and Android compile pass; physical LocalOnlyHotspot callback ordering remains in PS-T01 |
| PROV-A05 | Medium | Join callback cancellation can leak process binding | PROV-A-01 | Implemented | `JoinCallbackOwner` owns callback/handle teardown, clears process binding and unregisters callbacks exactly once, and closes pending onAvailable/onUnavailable races | `provisioningOwnershipAndManifestSeamsAreDeterministic`; existing deferred join race | `21dad61`; local, push blocked | Host owner seam and Android compile pass; real cancellation/unbind evidence remains PS-T01/PS-T02 |
| PROV-A06 | Medium | bindProcessToNetwork result/exception ignored | PROV-A-01 | Implemented | Initial and reconnect binding now require a successful Boolean result; rejected/throwing binds produce typed join failure or release the live handle | Existing join failure/state tests; callback path reviewed | `21dad61`; local, push blocked | Android host suite 23/23; OEM return/exception matrix remains PS-T01 |
| PROV-A07 | Medium | Queued onAvailable can rebind after handle close | PROV-A-01 | Implemented | Reconnect callbacks rebind only through a live handle that still owns the process-binding token; close marks the owner terminal first | `provisioningOwnershipAndManifestSeamsAreDeterministic`; deferred close/join race | `21dad61`; local, push blocked | Host owner seam and Android compile pass; queued real callback evidence remains PS-T01 |
| PROV-A08 | Medium | Process binding has no global ownership arbitration | PROV-A-01 | Implemented | `ProcessBindingArbiter` serializes process-wide binding ownership across manager instances and releases only the matching token | `provisioningOwnershipAndManifestSeamsAreDeterministic` | `21dad61`; local, push blocked | Deterministic host arbitration passes; two-manager device proof remains PS-T02 |
| PROV-A09 | Medium | Joined snapshot enumerates unrelated interfaces | PROV-A-01 | Implemented | Joined snapshots read only `ConnectivityManager.getLinkProperties(joinedNetwork).linkAddresses`, with distinct address normalization | `provisioningOwnershipAndManifestSeamsAreDeterministic` address projection | `21dad61`; local, push blocked | Host projection passes; real LinkProperties/AP-subnet evidence remains PS-T01 |
| PROV-A10 | Medium | Normal-permission failures absent/misdiagnosed | PROV-A-01 | Implemented | Exact install-time Wi-Fi permission matrix is checked at construction and logged as a diagnostic; runtime permission reporting remains target-SDK aware | `provisioningOwnershipAndManifestSeamsAreDeterministic`; manifest matrix review | `21dad61`; local, push blocked | Android compile and exact matrix pass; merged-manifest/logcat SDK matrix remains PS-T01 |
| PROV-A11 | Low | Capability/input validation is shallow | PROV-A-01 | Implemented | SSID blank/UTF-8 byte limit, OPEN-password prohibition, and secured password bounds are validated before `WifiNetworkSpecifier.Builder` | `joinInputValidationPreventsPlatformBuilderErrors` | `21dad61`; local, push blocked | Host input boundaries pass; SDK/hardware builder matrix remains PS-T01 |
| PROV-A12 | Test gap | Real Android callbacks are untested | PROV-A-01 | Blocked | All host seams and Android compilation are complete; callback ordering, OEM results, process binding, permission diagnostics, and unregister behavior require Android instrumentation on real/emulated platform services | PS-T01/PS-T02 | `21dad61`, `5bc31ce`; local, push blocked | Exact unblock is an authorized Android instrumentation/device matrix; host fakes cannot close this row |

### Desktop provisioning (5)

| ID | Severity | Finding | Unit/dependencies | Status | Plan/code | Tests | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PROV-D01 | Medium | pollIntervalMillis is not validated | PROV-D-01 | Implemented | Positive interval is required before the polling scope starts, preventing busy-spin and invalid delay behavior | `pollIntervalMustBePositive` | `5efee8f`; local, push blocked | Desktop test suite 10/10; API/constructor compatibility remains local evidence |
| PROV-D02 | Medium/Low | Interface scan can report unreachable addresses as Wi-Fi | PROV-D-01 | Implemented | Scan excludes down, loopback, virtual, and link-local candidates, deduplicates addresses, and ranks private LAN addresses first | `addressSelectionDropsInactiveAndLinkLocalCandidates` | `5efee8f`; local, push blocked | Deterministic ranking/filter test and desktop suite pass; OS route/device evidence remains external |
| PROV-D03 | Low | Broad Throwable catch can hide fatal errors | PROV-D-01 | Implemented | Network polling catches only recoverable `SocketException`; cancellation and fatal errors propagate instead of becoming `Unknown`/empty results | `fatalScannerErrorsAreNotConvertedToNoNetwork` | `5efee8f`; local, push blocked | Fatal scanner and desktop suite pass; platform-specific socket behavior remains local-only |
| PROV-D04 | Low | Tests allow null manual result without proving behavior | PROV-D-01 | Implemented | Injected address scanner proves both deterministic non-null identity/port output and null result when no usable address exists | `manualConnectionInfoCarriesIdentityAndPortWhenPresent`; `manualConnectionInfoReturnsNullWhenScannerFindsNoUsableAddress` | `5efee8f`; local, push blocked | Desktop suite 10/10 with no conditional assertions |
| PROV-D05 | Low | Tests mutate process-global properties | PROV-D-01/GOV-01 | Implemented | Manual-IP loopback tests use per-kit in-memory identity stores without mutating `user.home` or other process-global properties | `ManualIpLoopbackTest.manualPeerConnectsAndRoundTripsAMessage` | `5efee8f`; local, push blocked | Desktop suite 10/10; global-property scan is clean for this module |

### Sample applications (39)

| ID | Severity | Finding | Unit/dependencies | Status | Plan/code | Tests | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SAMPLE-01 | High data integrity | iOS suppresses sink write failure | SAMPLE-HIGH-01 after XFER-01 | Implemented | iOS sink synchronizes and remembers write failures, surfaces them after terminal state, removes the partial destination, and records a visible error | Generic-simulator Swift app build passes; device sink-failure injection remains | `c4b31a1`; local, push blocked | Apple/device transfer validation required |
| SAMPLE-02 | High data integrity | iOS same-name destination is non-atomic | SAMPLE-HIGH-01 after XFER-01 | Implemented | iOS sanitized destinations use an atomic `FileManager.createFile` claim with numbered suffixes rather than timestamp-only fallback | Generic-simulator Swift app build passes; concurrent Apple test remains external | `c4b31a1`; local, push blocked | Apple/device concurrent same-name transfer validation required |
| SAMPLE-03 | High availability/security | Every sample auto-accepts untrusted files | SAMPLE-HIGH-01 after XFER-01 | Implemented | Android, desktop CLI/GUI, and iOS queue incoming offers for explicit accept/reject; acceptance checks 50 MiB quota and free space before opening a file; teardown rejects pending offers | All four sample implementations compile; desktop UI tests 6/6; device consent/timeout evidence remains | `c4b31a1`; local, push blocked | PS-T04/PS-T07 and physical hostile-sender validation required |
| SAMPLE-04 | High availability | Android retains binary payloads in UI history | SAMPLE-HIGH-01 after XFER-01 | Implemented | Android `RoomMessage` stores timeline text only; binary payloads are converted to a size marker before entering Compose state | Android sample compile; byte-budget/runtime history evidence remains | `c4b31a1`; local, push blocked | Android instrumentation/soak evidence required |
| SAMPLE-05 | Medium | Failed/cancelled incoming transfers retain partial files everywhere | SAMPLE-XFER-01 | Implemented | Terminal watchers own sink/source closure and remove destinations for every non-Completed receive outcome, including cancellation/stop cleanup | JVM sample gate; generic iOS simulator build; physical cancellation/sink-failure evidence remains | `fix(samples): harden transfer cleanup and histories` (local, push blocked) | Local gates pass; PS-T04/PS-T06/PS-T07 runtime evidence remains |
| SAMPLE-06 | Medium | Desktop UI/Android leak stream during accept cancellation | SAMPLE-XFER-01 | Implemented | Desktop and Android transfer watchers cancel byte collectors, close streams from non-cancellable cleanup, and remove failed destinations | JVM sample gate; physical accept-cancellation evidence remains | `fix(samples): harden transfer cleanup and histories` (local, push blocked) | Local gates pass; PS-T04/PS-T06 runtime evidence remains |
| SAMPLE-07 | Medium | Android/CLI transfer collectors never terminate | SAMPLE-XFER-01 | Implemented | Android uses one terminal `state.first` watcher with a cancellable byte child; CLI send progress stops on all terminal states | Android/desktop CLI compile; physical stop/terminal evidence remains | `fix(samples): harden transfer cleanup and histories` (local, push blocked) | Local compile passes; PS-T04/PS-T05 runtime evidence remains |
| SAMPLE-08 | Medium | P2pKit.create failure wedges GUI Start | SAMPLE-LIFE-01 after LIF-SES-01 | Implemented | Android and desktop reset startup ownership and surface create failures, leaving Start retryable | Desktop GUI compile; Android compile; failure-injection UI evidence remains | `b90b8bd`; local, push blocked | PS-T04/PS-T06 runtime failure injection required |
| SAMPLE-09 | Medium | Desktop UI can overlap old/new kits | SAMPLE-LIFE-01 | Implemented | Desktop serializes lifecycle ownership, disables Setup while stopping, and rejects startup during active stop/start | Desktop GUI compile and complete affected gate | `b90b8bd`; local, push blocked | PS-T06 rapid lifecycle runtime evidence required |
| SAMPLE-10 | Medium | Stop failure swallowed after ownership discarded | SAMPLE-LIFE-01 | Implemented | Android, desktop, and iOS retain kit ownership and visible failure state when stop fails, permitting an explicit retry | Cross-platform sample compile; generic iOS simulator build | `b90b8bd`; local, push blocked | Injected stop-failure runtime/device evidence required |
| SAMPLE-11 | Medium | CLI exceptions bypass shutdown | SAMPLE-LIFE-01 | Implemented | CLI wraps start and REPL ownership in `try/finally`, cancels and joins app jobs, then stops the kit | CLI test/compile gate; loopback consumer test | `b90b8bd`; local, push blocked | PS-T05 injected REPL/shutdown failure evidence required |
| SAMPLE-12 | Medium | iOS stops watchers/sinks before SDK writers quiesce | SAMPLE-XFER-01 | Implemented | iOS keeps transfer watchers alive through `kit.stop()`, then cancels them so SDK writers quiesce before sink/source cleanup | Generic iOS simulator build; physical stop/write race evidence remains | `fix(samples): harden transfer cleanup and histories` (local, push blocked) | Local compile passes; PS-T07 runtime evidence remains |
| SAMPLE-13 | Medium | Swift flow adapters spawn unstructured Tasks | SAMPLE-XFER-01 | Implemented | Debug, incoming-session, message, and file collectors use stored lifecycle-owned `Task`s rather than detached tasks; stop/session removal cancels their handles | Generic iOS simulator build; physical collector cancellation evidence remains | `fix(samples): harden transfer cleanup and histories` (local, push blocked) | Local compile passes; PS-T07 runtime evidence remains |
| SAMPLE-14 | Medium data loss | Android collision cap can return occupied file | SAMPLE-XFER-01 | Implemented | Android bounded `createNewFile` claims return only an atomically created candidate or explicit exhaustion failure | Android compile; physical collision stress remains | `fix(samples): harden transfer cleanup and histories` (local, push blocked) | Local compile passes; PS-T04 runtime evidence remains |
| SAMPLE-15 | Medium path isolation | Remote peer-name dot segments escape sender directory | SAMPLE-XFER-01 | Implemented | Android/desktop peer and filename sanitizers reject dot segments and path/control characters; iOS claims sanitized names inside the fixed inbox | Desktop path test; generic iOS build; hostile-name/device evidence remains | `fix(samples): harden transfer cleanup and histories` (local, push blocked) | Local tests pass; PS-T08 hostile-name evidence remains |
| SAMPLE-16 | Medium | Android sample omits CHANGE_NETWORK_STATE | SAMPLE-LIFE-01/PROV-A-01 | Implemented | Manifest declares `CHANGE_NETWORK_STATE` for provisioning/network binding paths | Android `compileDebugKotlin` and `lintDebug` pass | `b90b8bd`; local, push blocked | Merged-manifest/device permission evidence remains |
| SAMPLE-17 | Medium | Android lint fails due missing coarse location | SAMPLE-LIFE-01 | Implemented | Manifest declares `ACCESS_COARSE_LOCATION` through API 32 alongside the existing fine/nearby permissions | Android `lintDebug` passes with zero errors | `b90b8bd`; local, push blocked | OEM/API permission-flow instrumentation remains |
| SAMPLE-18 | Medium | CLI option parsing can turn options into identity | SAMPLE-LIFE-01 | Implemented | Dedicated parser supports named/positional launch arguments and help, while rejecting unknown/duplicate/malformed options | `CliOptionsTest` 5/5 | `b90b8bd`; local, push blocked | PS-T05 packaged/runtime invocation evidence remains |
| SAMPLE-19 | Medium | CLI first-match targeting is ambiguous | SAMPLE-LIFE-01 | Implemented | Connect, send-file, send, and close target resolution rejects ambiguous peer/session matches explicitly | CLI compile and affected sample gate | `b90b8bd`; local, push blocked | PS-T05 multi-peer runtime evidence remains |
| SAMPLE-20 | Medium | Manual IPv6 parsing rejects common local addresses | SAMPLE-LIFE-01/LAN-NET-01 | Implemented | Desktop endpoint parser handles bracketed/unbracketed IPv6 at the last colon and preserves valid `%` scope IDs | `ManualEndpointTest` 3/3 across DNS, IPv4, IPv6, and scoped IPv6 cases | `b90b8bd`; local, push blocked | PS-T08 physical scoped-link connection evidence remains |
| SAMPLE-21 | Medium | KMP demo leaks session on send failure | SAMPLE-LIFE-01 | Implemented | Demo encloses session close and kit stop in nested `try/finally`, including timeout/send failures | `KmpConsumerLoopbackTest` and `KmpCallsiteSmokeTest` 2/2 | `b90b8bd`; local, push blocked | PS-T09 physical Android/iOS consumer runtime remains |
| SAMPLE-22 | Medium | Desktop accept failure leaves empty claimed file | SAMPLE-XFER-01 | Implemented | Desktop UI/CLI reject deterministic claim failures and remove any claimed destination on open/accept failure | Desktop UI 7/7 and CLI compile; physical accept-failure evidence remains | `fix(samples): harden transfer cleanup and histories` (local, push blocked) | Local gates pass; PS-T05/PS-T06 runtime evidence remains |
| SAMPLE-23 | Medium | iOS transfer history is unbounded | SAMPLE-XFER-01 | Implemented | iOS transfer rows evict oldest terminal rows and enforce a 24-row cap while preserving active rows where possible | Generic iOS simulator build; long-run device soak remains | `fix(samples): harden transfer cleanup and histories` (local, push blocked) | Local compile passes; PS-T07 soak evidence remains |
| SAMPLE-24 | Medium | GUI text histories are count-, not byte-bounded | SAMPLE-XFER-01 | Implemented | Android, desktop GUI, and iOS message histories trim oldest entries by both row count and UTF-8 byte budget; desktop binary payloads are stored as size text | Desktop UI 7/7, Android compile, generic iOS build; runtime byte-soak remains | `fix(samples): harden transfer cleanup and histories` (local, push blocked) | Local gates pass; PS-T04/PS-T06/PS-T07 soak evidence remains |
| SAMPLE-25 | Low | Rapid advertise/discover toggles race stale UI booleans | SAMPLE-LIFE-01 | Implemented | Android and desktop serialize advertise/discover mutations with dedicated mutexes and gate auto-mesh until Running | Android/desktop compile and complete affected gate | `b90b8bd`; local, push blocked | Rapid-toggle UI instrumentation remains |
| SAMPLE-26 | Low | Android labels connecting/reconnecting as connected | SAMPLE-LIFE-01 | Implemented | Reviewed Android row mapping: connecting/reconnecting/connected labels remain distinct and derive from actual session state | Android compile/lint gate | `b90b8bd`; local, push blocked | Android UI state instrumentation remains |
| SAMPLE-27 | Low | Permission diagnostic failure becomes “nothing missing” | SAMPLE-LIFE-01 | Implemented | Diagnostic exceptions preserve prior missing permissions and are logged/surfaced instead of becoming an empty result | Android compile/lint gate | `b90b8bd`; local, push blocked | Injected PackageManager/device diagnostic evidence remains |
| SAMPLE-28 | Low security UX | Android displays credentials in clear text | SAMPLE-LIFE-01 | Implemented | Hotspot password is masked in Compose output while still remaining available to the local connection workflow | Android compile/lint gate | `b90b8bd`; local, push blocked | UI semantics/screenshot policy evidence remains |
| SAMPLE-29 | Low security | Desktop logs do not consistently sanitize terminal controls | SAMPLE-LIFE-01 | Implemented | CLI and desktop GUI centralize terminal-safe rendering for peer, SDK, error, and control text | `TerminalTextTest` 1/1; desktop suites pass | `b90b8bd`; local, push blocked | Hostile peer-name terminal integration evidence remains |
| SAMPLE-30 | Low | iOS PeerRow equality suppresses updates | SAMPLE-LIFE-01 | Implemented | Peer-row equality now includes platform and supported transports so metadata changes refresh the UI | Generic-iOS-Simulator build passes | `b90b8bd`; local, push blocked | Physical metadata-change UI evidence remains |
| SAMPLE-31 | Low | iOS cross-check diagnostics flood normal state | SAMPLE-LIFE-01 | Implemented | Cross-check warnings are signature-deduplicated and reset for each start generation | Generic-iOS-Simulator build passes | `b90b8bd`; local, push blocked | Device log-rate/state-transition evidence remains |
| SAMPLE-32 | Low | Unknown transfer state is permanently nonterminal | SAMPLE-LIFE-01 | Implemented | Unknown future Swift transfer states terminate as a visible failure rather than leaving an immortal watcher | Generic-iOS-Simulator build passes | `b90b8bd`; local, push blocked | Future-enum/device injection remains external |
| SAMPLE-33 | Low | Desktop cleanup uses disposing scope | SAMPLE-LIFE-01 | Implemented | Desktop owns disposal cleanup in an independent supervisor scope derived from the UI dispatcher and handles in-progress startup | Desktop GUI compile and affected test gate | `b90b8bd`; local, push blocked | Window-disposal/runtime failure evidence remains |
| SAMPLE-34 | Low | Unique-file helpers are duplicated/divergently unsafe | SAMPLE-XFER-01 | Implemented | Platform-specific helpers now share one bounded atomic-claim contract: sanitized names, dot-segment rejection, numbered suffixes, and explicit failure after 10,000 alternatives; desktop UI tests pin the contract | Desktop UI `UniqueSaveFileTest` 7/7 including concurrent/path claims; Android/CLI compile; physical cross-platform collision evidence remains | `fix(samples): harden transfer cleanup and histories` (local, push blocked) | Local gates pass; PS-T04/PS-T06/PS-T08 runtime evidence remains |
| SAMPLE-35 | Low | Alice/Bob run configs share PeerId | GOV-PACKAGE-01 after ID-01 | Implemented | Maintained Alice/Bob configurations keep the shared default appId but select validated stable `alice`/`bob` profile homes passed only to the child JVM | Static run-profile gate plus two real CLI launches | `52e173b`; local, push blocked | Both launches exited normally with distinct persisted PeerIds; script is CI-wired and passes |
| SAMPLE-36 | Medium | Android reports Running after partial startup | SAMPLE-LIFE-01 after LIF-SES-01 | Implemented | Android/desktop publish Running only after advertise and discover both succeed; partial startup stops the owned kit | Cross-platform sample compile and affected gate | `b90b8bd`; local, push blocked | Injected partial-start instrumentation remains |
| SAMPLE-37 | Low | CLI reports close success after failure | SAMPLE-LIFE-01 | Implemented | CLI emits close success only after the close call completes successfully | CLI compile/test gate | `b90b8bd`; local, push blocked | Injected close-failure terminal evidence remains |
| SAMPLE-38 | Low | CLI stops kit before cancelling app jobs | SAMPLE-LIFE-01 | Implemented | CLI cancels and joins application jobs before stopping the kit; transfer cleanup runs non-cancellably | CLI compile/test and loopback consumer gate | `b90b8bd`; local, push blocked | PS-T05 shutdown ordering integration evidence remains |
| SAMPLE-39 | Low | Android onCleared cleanup has no durable owner | SAMPLE-LIFE-01 | Implemented | Reviewed durable independent cleanup scope: `onCleared` joins pending stop and retains failed-stop ownership for retry | Android compile/lint gate | `b90b8bd`; local, push blocked | Android lifecycle instrumentation remains |

### Build, publication, tooling, and configuration (15)

| ID | Severity | Finding | Unit/dependencies | Status | Plan/code | Tests | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| BUILD-01 | High | Published compile dependency metadata is wrong | REL-ABI-01 | Verified | Correct API scopes; desktop LAN test-only | CORE-T13 isolated published consumers pass | `8f15d75`; pushed with tracker evidence | POM/module/API variant and all platform consumers pass from committed state |
| BUILD-02 | High release blocker | No remote publication target exists | REL-REMOTE-01 | Blocked | Analysis complete; Portal workflow/namespace decision required | Local artifact shape passes; bundle/upload path awaits decision | Pending tracker-only blocker commit | Blocked on namespace type, workflow choice, and credentials; ENV-07 retains live upload proof |
| BUILD-03 | Medium | BuildInfo defeats incremental/reproducible builds | REL-PROV-01 | Implemented | Cacheable typed generator uses commit, stable commit time, and relevant dirty state only | Double-build/reproducibility task | `e19c5c5`; local, push blocked | Repeat is UP-TO-DATE with configuration cache reused in 831 ms at final code HEAD |
| BUILD-04 | Medium | Publication gate can pass invalid release | REL-SUPPLY-01 after BUILD-01/BUILD-02 | Implemented | Local gate validates readable main/source/Dokka archives, exact POM ownership/coordinates, target-aware module metadata, and isolated dependency scopes/consumers | 15-publication artifact gate plus JVM/Android/KMP/iOS consumer matrix | `64ca0a2`; local, push blocked | Local shape/scope gates pass; signed bundle and live remote validation remain BUILD-02/ENV-07 |
| BUILD-05 | Medium | Gradle wrapper components are version-skewed | REL-SUPPLY-01 | Implemented | Regenerated 9.3.1 wrapper JAR, pinned HTTPS distribution SHA/URL validation, and verify every launcher/JAR byte checksum | Valid wrapper plus four independent tamper mutations | `64ca0a2`; local, push blocked | Wrapper, distribution, Unix launcher, and Windows launcher gates pass |
| BUILD-06 | Medium | Supply-chain/release automation controls absent | REL-SUPPLY-01 | Implemented | Repository locks + SHA-256 verification metadata; release-only CycloneDX 1.6 SBOM; pinned-action CI/dependency review/submission; Dependabot; explicit lock maintenance guard | Full write-lock gate, no-write refusal, 38-component SBOM, YAML parse, repository check | `64ca0a2`; local, push blocked | Local controls pass; external current advisory scanner is ENV-06 and credentials/signing remain ENV-07 |
| BUILD-07 | Medium | XCFramework stamp is undeclared/mis-targeted side effect | REL-PROV-01 with LAN-21 | Implemented | Exact final assembly feeds typed cacheable provenance writer/verifier with declared sources, slices, and sidecars | Deletion, incremental, cache-reuse, and preflight gates | `e19c5c5`; local, push blocked | Sidecar deletion restores exact output; final repeat leaves assembly/writer UP-TO-DATE |
| BUILD-08 | Medium | Android sample manifest fails project check | REL-GATE-01 after SAMPLE-LIFE-01 | Implemented | Target/compile API 37; explicit Android 12+ cloud/device-transfer exclusions preserve no-backup intent; redundant activity label removed | Warning-as-error `lintDebug`; debug APK/resource/manifest assembly | `e66b139`; local, push blocked | Lint says `No issues found`; `assembleDebug` passes; physical target-37 backup/transfer behavior remains `PS-T04` |
| BUILD-09 | Low | iOS launcher uses predictable/shared paths and name-first selection | REL-PROV-01 | Implemented | Resolve one exact available UDID before build; reject ambiguous names; isolate each run directory/log and serialize shared mutation | Launcher syntax/selection/directory/lock tests | `e19c5c5`; local, push blocked | 7/7 deterministic shell tests pass; physical install/launch remains external |
| BUILD-10 | Low | Nested Gradle/Xcode preflight duplicates framework work | REL-PROV-01 | Implemented | Remove outer framework dependency; Xcode project prebuild verifier is the single authoritative nested invocation | Task-graph/double-run/Xcode build review | `e19c5c5`; local, push blocked | Gradle task graph and generic simulator build show one authoritative prebuild path; repeated framework work is UP-TO-DATE |
| BUILD-11 | Low | Dirty-tree provenance ignores untracked inputs | REL-PROV-01 | Implemented | Relevant Git status includes tracked and untracked framework/build inputs and fails closed on status errors | Untracked probe fingerprint test | `e19c5c5`; local, push blocked | Probe changed fingerprint exactly; removal restored the baseline fingerprint from cache |
| BUILD-12 | Low | Provenance exposes local/volatile build information | REL-PROV-01 with BUILD-03 | Implemented | Remove branch/wall-clock values from artifact identity; preserve deprecated constants with stable compatibility values | Generated-artifact content/reproducibility test | `e19c5c5`; local, push blocked | BuildInfo contains exact commit/commit time/dirty state only; repeat output is stable |
| BUILD-13 | Low | Release usability/maintenance gaps | REL-SUPPLY-01 | Implemented | Strict real Dokka HTML archives for all published variants; ABI baselines; tag/version policy; maintained release docs and local-only dry run | Four strict Dokka tasks, ABI checks, three valid/four invalid tag cases, artifact/POM checker, iOS launcher CI entry point | `64ca0a2`; local, push blocked | Classifiers/coordinates unchanged; remote release policy still awaits REL-REMOTE-01 |
| BUILD-14 | Low | Compiler/test hygiene warnings are not gated | REL-GATE-01 | Implemented | Global Kotlin/Java/Gradle warning promotion plus Swift warnings-as-errors; coroutine opt-ins, JmDNS/lazy-cinterop/Compose deprecations, and no-op Swift await corrected | Forced JVM/iOS/Android/desktop compiles, repository `check`, generated Xcode project build | `e66b139`; local, push blocked | All registered source/compiler warnings are gone and gates pass; host Android SDK XML/AppIntents tool notices are not source diagnostics |
| BUILD-15 | Low | Android lint reports three additional warnings | REL-GATE-01 after SAMPLE-LIFE-01 | Implemented | Fixed every current lint warning: API target, extraction policy, redundant label, and `StorageManager` allocatable-byte preflight | `lintDebug` with `warningsAsErrors=true` | `e66b139`; local, push blocked | Lint text report is exactly `No issues found`; physical provider/storage behavior remains `PS-T04` |

## Explicit test-gap register (54)

Each row represents one bullet from “Missing or weak tests to add” in the source review. A gap may close with the same focused commit as its linked finding, but receives its own result/evidence.

### Core lifecycle/session/identity gaps (13)

| Gap ID | Required coverage | Linked findings | Status | Test files/evidence | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| CORE-T01 | Stop racing connect, advertising, discovery, and delayed observer start | CORE-01 | Verified | Six deterministic races in `KitLifecycleTest`; existing parked data-start test retained | `a4e0bb0`; tracker evidence pending push | Focused committed suite passes; three forced pre-commit repeats green |
| CORE-T02 | Cancellation at every outgoing-connect suspension, then successful retry | CORE-03 | Verified | `SessionOwnershipTest`, `SessionFlowTest`, secure integration: dial, coalesced wait, HELLO/security write, pre-commit | `68934be`; test follow-up `82a9b41`; pushed | Three exact committed-state JVM repeats plus focused iOS and Android compile pass |
| CORE-T03 | Two discovery transports contribute/lose same PeerId | CORE-02 | Implemented | Per-instance merge/loss plus mutable-input snapshot regressions | `ee2cae5`; push blocked | JVM/iOS pass |
| CORE-T04 | Repeated same-direction inbound arbitration | CORE-05 | Implemented | Five repeated inbound candidates under both local-ID orderings | `ee2cae5`; push blocked | JVM/iOS pass |
| CORE-T05 | Slow message subscriber while PONG/CLOSE arrives | CORE-04 | Implemented | Gated subscriber plus PONG/CLOSE and byte-overflow regressions | `ee2cae5`; push blocked | JVM/iOS pass |
| CORE-T06 | Exact keepalive deadline and monotonic clock jumps | CORE-14 | Implemented | Exact 150 ms virtual boundary with extreme wall-clock jumps | `ee2cae5`; push blocked | JVM/iOS pass |
| CORE-T07 | Session child Job completes after remote termination | CORE-09 | Verified | `SessionFlowTest` exact CLOSE/failure runtime joins | `68934be`; pushed | JVM/iOS pass |
| CORE-T08 | Public sessions empty after stop independent of watcher schedule | CORE-10 | Verified | `KitLifecycleTest.sessionCommittedBeforeStopIsIncludedInTeardown` | `68934be`; test follow-up `82a9b41`; pushed | JVM/iOS pass |
| CORE-T09 | Partial multi-transport startup rollback | CORE-11 | Implemented | Failure and cancellation roll back every entered data transport in reverse; same instances retry; pending dials/listeners and failed-cleanup retention are covered | `60aecf6`, `391ae3c`; push blocked | Focused and complete core/LAN JVM+iOS and Android compile pass |
| CORE-T10 | Throwing and permanently hung close operations | CORE-12 | Implemented | Throwing, cancellable-hung, and NonCancellable-hung resources are bounded; later resources run; followers receive the exact leader failure | `60aecf6`; push blocked | Focused repeats plus full JVM/iOS core pass |
| CORE-T11 | Identity sanitizer collisions, concurrent first creation, persistence failure, and atomic replacement | CORE-18, CORE-19, CORE-20, CORE-21 | Verified | JVM process/thread/failure/migration tests + iOS bucket tests | `ee69d09`, pushed | Focused tests pass; committed full JVM 351/351 green |
| CORE-T12 | Path observer close/restart, stale callbacks, unregister failure, generation gating | CORE-22, CORE-23 | Implemented | `NetworkPathCallbackStateTest`: four deterministic generation/retained-ownership tests; existing `NetworkPathRecoveryTest` lifecycle/path transitions retained | `1903b11`; local, push blocked | JVM 422/422 and iOS 397/397 pass; physical Android/iOS callback evidence remains |
| CORE-T13 | Clean external consumer compilation from published temp repository | BUILD-01 | Verified | `scripts/check-published-consumers.sh` | `8f15d75`; pushed with tracker evidence | JVM/Android/KMP/iOS pass from committed state |

### Protocol and transfer gaps (21)

| Gap ID | Required coverage | Linked findings | Status | Test files/evidence | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| PT-T01 | FrameReader fragmented/batched complexity and early bad-magic rejection | PROTO-01 | Verified | Linear relocation and early-header regression suite | `aa3ac0c` | Three forced JVM repeats plus exact committed JVM/iOS pass |
| PT-T02 | Header-version mismatch and every packet-type structural invariant | PROTO-02, PROTO-04 | Verified | Table-driven known-packet invariant matrix and early version/shape rejection | `aa3ac0c` | Exact committed JVM/iOS pass |
| PT-T03 | Large HELLO/OFFER/control payload attacks and log flooding | PROTO-03, PROTO-07 | Verified | Header-only cap attacks and bounded-log assertions | `aa3ac0c` | Exact boundaries and 100-frame flood pass |
| PT-T04 | Strict malformed UTF-8 and invalid flags/LAST | PROTO-04, PROTO-06 | Verified | Strict text decoders plus DATA/FILE_DATA flag and LAST invariants | `aa3ac0c` | JVM/iOS pass |
| PT-T05 | Outbound name/MIME/reason limits | PROTO-03, PROTO-06 | Verified | Encoder/send-path character and UTF-8 byte boundaries | `aa3ac0c`, `6171588` | Exact committed JVM/iOS pass |
| PT-T06 | 2 GiB with chunk size 1 and Long overflow boundaries | FILE-10 | Verified | Configuration and sender pre-read overflow tests | `68c579f`; pushed | JVM/iOS pass |
| PT-T07 | Empty FILE_DATA, changing totalChunks, invalid LAST, data after full size | PROTO-04, FILE-02 | Verified | Parser invariants plus dispatcher terminal/transition tests | `aa3ac0c`, `68c579f`; pushed | JVM/iOS pass |
| PT-T08 | Accepted-transfer idle exhaustion and all 64 admission slots exhausted | FILE-02 | Verified | Exact idle reset/overall bound and 64 accepted-slot release | `68c579f`; pushed | Virtual-time JVM/iOS pass |
| PT-T09 | Cancellation during accept mutex wait and FILE_ACCEPT write | FILE-01 | Verified | Gated accept cancellation with compensating cancel/reference assertion | `68c579f`; pushed | JVM/iOS pass |
| PT-T10 | Cancel/close racing sink write/finish | FILE-03 | Verified | Gated sink write proves per-transfer serialization | `68c579f`; pushed | JVM/iOS pass |
| PT-T11 | Blocking/reentrant source close and sink flush | FILE-08 | Verified | Ownership/I/O moved outside map lock; gated sink regression | `68c579f`; pushed | Diff review + JVM/iOS pass |
| PT-T12 | Timer scheduling before map registration, wire write, and offer emission | FILE-06 | Implemented | Sender timer begins after committed write; receiver entry and retained publication exist before response timer launch | `68c579f`, `ddb7142`; local, push blocked | Exact-deadline and gated-write tests pass on JVM/Apple |
| PT-T13 | Ordered multiple offers and no emission after terminal | FILE-05 | Implemented | Admission-ordered retained snapshots remove terminal offers and reject late responses without revival | `ddb7142`; local, push blocked | Reject/cancel/close/expiry, capacity, duplicate, and concurrent-response tests pass |
| PT-T14 | One exact timeout authority/state on both peers | FILE-02, FILE-07 | Verified | Receiver authority + delayed sender watchdog; exact end-to-end REJECT | `68c579f`; pushed | JVM/iOS pass |
| PT-T15 | Byte progress frozen after terminal | FILE-12 | Verified | Gated outgoing and serialized incoming byte commits | `68c579f`; pushed | JVM/iOS pass |
| PT-T16 | Sender result when receiver flush/durability fails | FILE-04 | Planned | Negotiated receiver durable-commit decision approved | — | Exact sender terminal mapping queued |
| PT-T17 | Source/sink references released after every terminal outcome | FILE-09 | Verified | Close-once matrix plus nullable source/receiver retention assertions | `68c579f`; pushed | JVM/iOS pass |
| PT-T18 | Source mutation/digest mismatch | FILE-13 | Planned | Prepared SHA-256 snapshot and authenticated negotiation decision approved | — | Mutation/digest mismatch tests queued |
| PT-T19 | Deterministic transfer-ID collision | FILE-14 | Verified | Constant-random collision cannot overwrite and closes second source | `68c579f`; pushed | JVM/iOS pass |
| PT-T20 | Android hostile/null/negative provider metadata | FILE-15 | Blocked | Production validation implemented; real provider/instrumentation proof unavailable locally | `68c579f`; pushed | Requires authorized Android provider instrumentation; compile cannot substitute |
| PT-T21 | Fuzz/property tests for codec, reader, reassembler, and transfer transitions | PROTO-01, PROTO-04, FILE-01, FILE-02, FILE-03, FILE-05, FILE-07, FILE-12 | Implemented | Randomized codec/reader/reassembler coverage plus bounded retained-offer transition, duplicate, capacity, concurrency, expiry, and terminal-state properties | `aa3ac0c`, `68c579f`, `ddb7142`; local, push blocked | All local property/transition conjuncts complete; external hostile-network evidence remains separate |

### LAN gaps (11)

| Gap ID | Required coverage | Linked findings | Status | Test files/evidence | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| LAN-T01 | Android instrumentation for multicast locks, callback ordering, network rotation, permissions, unregister failure, process binding, and IPv6 | LAN-10, LAN-13, LAN-19 | Blocked | Common lifecycle fakes cover retained callback/lock ownership and Android production compiles; real platform behavior remains | `83db1ab` partial; push blocked | Authorized physical/emulated Android network instrumentation required |
| LAN-T02 | Partial-completion cancellation for service/listener registration | LAN-01 | Implemented | Non-interruptible create/register/add gates reproduce prompt-cancellation ambiguity and assert compensation/restoration | `83db1ab`; push blocked | Focused 33/33 suite passes repeatedly |
| LAN-T03 | Unregister/remove/close failures and concurrent rebind scheduling | LAN-10, LAN-26 | Implemented | Exact failure matrix plus serialized concurrent scheduling and repeated-stop cleanup | `83db1ab`; push blocked | Focused 33/33 suite passes repeatedly |
| LAN-T04 | Outbound-connect cancellation on JVM, Android, Apple | LAN-05, LAN-06 | Implemented | Deterministic JVM socket and Apple NWConnection cancellation tests; Android behavior is mirrored and cross-compiled | `b21ed84`; push blocked | JVM/Apple focused green; Android runtime evidence remains a final platform gate |
| LAN-T05 | Normal write IOException reaches terminal state | LAN-08 | Implemented | Forced JVM output failure asserts fd close and exact Closed state; Android parity compiles | `b21ed84`; push blocked | Focused plus complete LAN JVM green |
| LAN-T06 | Restart/re-advertise after fatal accept failure | LAN-07 | Implemented | Real listener is forced closed, depublished, rebound on the same advertised port, and accepts a new session/message | `b21ed84`; push blocked | Focused repeat and complete LAN JVM green |
| LAN-T07 | Apple browser/listener recovery, start-close race, parent cancellation, inbound flood/drain, cache-prune race, same-type path rotation, and AWDL | LAN-03, LAN-04, LAN-11, LAN-12, LAN-13, LAN-17 | Blocked | Browser/listener recovery, start cancellation, cancelled close, bounded inbound drain, outbound cancellation, cache-prune race, same-interface address-change rebind decision, and peer-to-peer parameter enablement implemented; physical path/AWDL interop remains | `b21ed84`, `703182a`, `21bc2fc`; push blocked | Complete Apple LAN suite passes; authorized physical path/AWDL matrix required |
| LAN-T08 | Oversized TXT, unsupported pv, control characters, duplicate-PID spoof, off-subnet hosts, alternate address candidates, and connection flood | LAN-04, LAN-15, LAN-16, LAN-18, LAN-22 | Blocked | Connection flood, TXT validation/identity, off-subnet filtering, eight-candidate preservation, failed-first fallback, and exact TXT-less ownership are deterministic locally | `b21ed84`, `2895ff1`, `09af6b5`, `21bc2fc`, `639c508`; push blocked | Deterministic tests pass; two-machine hostile-network/device proof required |
| LAN-T09 | Replace global replay-count assertions with per-test baselines | LAN test fixtures | Implemented | Marker-specific non-retention/live-delivery assertions replace process-global replay-size assumptions | `IosLanDebugTest` | `52e173b`; push blocked | Focused and complete Apple LAN suites pass |
| LAN-T10 | Prove old Apple listener descriptors released, not merely new port differs | LAN-11, LAN-23 | Implemented | Listener lease awaits native terminal acknowledgement, breaks callback cycles, retains unresolved ownership, and exact IPv4/IPv6 bind probes verify the old port after close | `703182a`; push blocked | Real iOS Simulator exact-port test passes pre-commit and from committed state |
| LAN-T11 | Isolate tests that mutate user.home, JmDNS properties, and NSUserDefaults | CORE-18, CORE-19, CORE-20, CORE-21, CORE-30, PROV-D05, LAN tests | Implemented | Shared leases serialize and exactly restore present/absent JVM properties and Apple defaults; storage uses a private defaults suite; Apple LAN identities rotate per test | JVM/Apple guard meta-tests and three forced Apple lifecycle/loopback repeats | `52e173b`, `90b5f20`; push blocked | All exact-restore tests and 3 × 12 focused Apple tests pass; no direct mutations remain outside guards/meta-tests |

### Provisioning and sample gaps (9)

| Gap ID | Required coverage | Linked findings | Status | Test files/evidence | Commit/push | Verification |
| --- | --- | --- | --- | --- | --- | --- |
| PS-T01 | Real Android provisioning callback/cancellation and permission behavior | PROV-A01, PROV-A02, PROV-A03, PROV-A04, PROV-A05, PROV-A06, PROV-A07, PROV-A09, PROV-A10, PROV-A12 | Blocked | Host JVM seams cover failed-result reservation cleanup, parent cancellation, late callback ownership, input boundaries, LinkProperties projection, and bind/permission decisions; physical callback ordering, OEM results, merged-manifest/logcat, and hardware matrix remain | `21dad61` plus `5bc31ce`; local, push blocked | Authorized Android instrumentation/device matrix required |
| PS-T02 | Process-wide binding ownership across managers | PROV-A05, PROV-A08, PROV-A12 | Blocked | Deterministic host `ProcessBindingArbiter` ownership test added; two-manager real process-binding/unbind proof remains | `21dad61`; local, push blocked | Authorized Android instrumentation/device matrix required |
| PS-T03 | Desktop poll boundaries and interface ranking | PROV-D01, PROV-D02 | Implemented | Deterministic interval and address-candidate seam tests; fatal propagation and manual-IP null/non-null coverage now run in the desktop suite | `5efee8f`; local, push blocked | Host JVM suite; OS route/device evidence is not required for the local conjuncts |
| PS-T04 | Android sample unit/instrumentation tests | SAMPLE-03, SAMPLE-04, SAMPLE-05, SAMPLE-06, SAMPLE-07, SAMPLE-08, SAMPLE-10, SAMPLE-14, SAMPLE-15, SAMPLE-16, SAMPLE-17, SAMPLE-25, SAMPLE-26, SAMPLE-27, SAMPLE-28, SAMPLE-34, SAMPLE-36, SAMPLE-39, BUILD-08, BUILD-15 | Blocked | Consent/storage/cleanup/history and lifecycle/permission/toggle implementations compile; target-37 assembly and warning-as-error lint pass. Runtime UI, backup/transfer exclusion, provider storage, permission, cancellation, collision, rapid-toggle, partial-start, and `onCleared` instrumentation remain | `c4b31a1`, `9102b78`, `b90b8bd`, `e66b139`; local, push blocked | Authorized Android instrumentation/physical hardware required |
| PS-T05 | CLI option parsing, target ambiguity, shutdown finally, and terminal collector cleanup | SAMPLE-07, SAMPLE-11, SAMPLE-18, SAMPLE-19, SAMPLE-37, SAMPLE-38 | Blocked | Parser/terminal regressions pass 5/5 and maintained real Alice/Bob processes start, accept `info`/`quit`, persist separate identities, and exit normally; injected process-level close/REPL/shutdown faults need a controlled CLI integration harness not present in this repository | `9102b78`, `b90b8bd`, `52e173b`; local, push blocked | Provide/authorize process fault-injection and packaged-terminal integration infrastructure |
| PS-T06 | Desktop GUI create/stop races, accept cancellation, and byte-budgeted history | SAMPLE-06, SAMPLE-08, SAMPLE-09, SAMPLE-10, SAMPLE-22, SAMPLE-24, SAMPLE-33, SAMPLE-34 | Blocked | Desktop UI path/terminal tests pass 11/11 and lifecycle ownership compiles; injected create/stop failure, rapid toggle, window disposal, accept cancellation, and byte-budget soak require a controlled headful Compose UI/fault-injection harness not present in this repository | `9102b78`, `b90b8bd`; local, push blocked | Provide/authorize desktop GUI automation and lifecycle fault-injection infrastructure |
| PS-T07 | Swift sink failure, atomic filename collision, partial-file cleanup, and unstructured collector cancellation | SAMPLE-01, SAMPLE-02, SAMPLE-05, SAMPLE-12, SAMPLE-13, SAMPLE-23, SAMPLE-24 | Blocked | Swift transfer cleanup removes non-completed partial files, task handles are lifecycle-owned, iOS stop orders SDK quiescence before watcher cancellation, and histories are bounded; device failure/collision/soak evidence remains | `c4b31a1` plus SAMPLE-XFER local code; push blocked | Apple unit-test/device fault-injection target required |
| PS-T08 | Dot-segment names and scoped IPv6 on every sample | SAMPLE-15, SAMPLE-20, SAMPLE-34 | Blocked | Filename/peer-name claims reject path and dot segments; endpoint tests cover DNS, IPv4, bracketed/unbracketed IPv6, and `%` scope IDs | `9102b78`, `b90b8bd`; local, push blocked | Every-sample physical scoped-IPv6 and hostile-name evidence required |
| PS-T09 | KMP Android runtime consumer and iOS consumer target | SAMPLE-21, BUILD-01 | Blocked | Published consumers compile and KMP demo cleanup is exercised by 2/2 JVM loopback/callsite tests | `8f15d75`, `b90b8bd`; local, push blocked | Physical Android/iOS consumer runtime required |

## External-validation register

These seven boundaries were explicitly excluded from the original local review. They are additional final acceptance evidence, not part of the 54 test-gap count and not grounds for silently closing a finding.

| ID | Required evidence | Status | Unblock/evidence |
| --- | --- | --- | --- |
| ENV-01 | Physical Android and Apple device tests | Blocked | Compatible devices and test deployment path required |
| ENV-02 | Two-machine and hostile-network hardware validation | Blocked | At least two hosts/devices plus controlled hostile-network harness required |
| ENV-03 | Simulator app launch and UI automation | Implemented | `ebcf827` adds the generated iOS UI-test target, deterministic exact-UDID runner, and accessibility-anchored Start/Stop assertions; committed simulator run passes 1/1 with 0 failures. Physical/device UI remains ENV-01. |
| ENV-04 | iOS X64 execution on compatible host | Blocked | x86_64 Apple host/runtime required |
| ENV-05 | Force-enable and resolve ignored Apple diagnostic test | Implemented | Force-enabled simulator run passes 1/1 for 60.114 s and local `dns-sd` captures exact add/remove events; KDoc now states the deliberate legacy-v1 versus secure-v2 namespace boundary. Physical secure-v2 interop remains ENV-01/ENV-02. |
| ENV-06 | Third-party dependency CVE/advisory scan | Implemented | Authenticated OSV Scanner 2.4.0 (`9ca3185a…f5`) scanned current supported Gradle lockfiles and the 38-component release SBOM with zero vulnerabilities after build/docs/test dependency floors were applied; pinned v2.3.8 reusable CI scan added. Live advisory data remains time-dependent. |
| ENV-07 | Real remote Central/Portal upload | Blocked | Approved portal, signing identity, namespace access, and CI credentials required |

## Execution record: ENV-03 — simulator UI automation

| Field | Value |
| --- | --- |
| Scope and status | `ENV-03`; Implemented locally. The simulator gate is deterministic and committed-state verified; physical-device UI/lifecycle evidence remains `ENV-01`. |
| Implementation | Added `p2pkit-sample-uitests` and the explicit `p2p-sample-ui` scheme in `iosApp/project.yml`; added accessibility identifiers and Start/Stop transition assertions in `iosApp/ContentView.swift` and `iosApp/UITests/P2pKitSampleUITests.swift`; added `iosApp:runIosUiTests` and `scripts/run-ios-ui-tests.sh` with exact-UDID selection, isolated derived data, serialized launcher/framework mutation, and failure-preserving cleanup. CI shell syntax coverage includes the runner. |
| Exact files changed | `.github/workflows/ci.yml`; `iosApp/ContentView.swift`; `iosApp/UITests/P2pKitSampleUITests.swift`; `iosApp/build.gradle.kts`; `iosApp/project.yml`; `scripts/run-ios-ui-tests.sh`. |
| Tests and commands | `bash -n scripts/run-ios-ui-tests.sh`; `bash -n scripts/run-ios-app.sh`; `sh -n iosApp/scripts/check-xcframework.sh`; `./gradlew :iosApp:checkIosLauncherScripts --console=plain`; `scripts/run-ios-ui-tests.sh` without `SIM_UDID`; exact committed run `SIM_UDID=B93342F4-FC27-4B4C-BD72-E4C9CBAF9583 scripts/run-ios-ui-tests.sh`; staged/committed `git diff --check`. |
| Exact results | Launcher script checks: 7/7 passed. The no-UDID run failed deterministically with the expected duplicate-`iPhone 17` ambiguity safeguard. The exact signed committed run at `ebcf827a7ccef1834ead14c7b1619463b249a8a5` reported `** TEST SUCCEEDED **`: `P2pKitSampleUITests.testLaunchStartAndStopControls`, 1 test, 0 failures; BuildInfo/XCFramework provenance matched `ebcf827` and the source tree was clean. |
| Compatibility and remaining evidence | Additive UI identifiers, a test-only target/scheme, and a Gradle runner; no SDK ABI, wire protocol, persistence, or runtime library contract changed. Physical Android/Apple UI, permission, lifecycle, and transfer evidence remains in `ENV-01`/`PS-T04`/`PS-T07`; x86 host execution remains `ENV-04`; push remains blocked by `SCM-PUSH-01`. |
| Source control | Branch `remediation/full-register-2026-07`; focused commit `ebcf827a7ccef1834ead14c7b1619463b249a8a5` (`test(ios): add simulator UI smoke coverage`). Push not attempted. |

## Execution record: ENV-05 — Apple LAN diagnostic capture

| Field | Value |
| --- | --- |
| Scope and status | `ENV-05`; Implemented locally for simulator/host advertisement capture. Secure-v2 device interoperability remains `ENV-01`/`ENV-02`. |
| Implementation | Force-enabled the documented 60-second diagnostic without changing its timeout or assertions, ran it beside the maintained JVM sample and macOS `dns-sd`, restored `@Ignore`, and corrected the diagnostic KDoc: the test deliberately advertises legacy v1 (`_p2pkit._tcp`), while maintained samples use authenticated v2 (`_p2pkit2._tcp`) and must not cross-discover. No production security downgrade was made. |
| Exact files changed | `p2p-transport-lan/src/appleTest/kotlin/dev/p2pkit/transport/lan/IosLanDiagnosticTest.kt`; `P2PKIT_REMEDIATION_TRACKER_2026-07.md`. |
| Tests and commands | Temporarily removed only `@Ignore`; ran `./gradlew :p2p-transport-lan:iosSimulatorArm64Test --tests 'dev.p2pkit.transport.lan.IosLanDiagnosticTest' --rerun-tasks --console=plain`; ran local `dns-sd -B _p2pkit._tcp local.`; ran `./gradlew :p2p-sample-desktop:run -Pp2pkit.sample.identityProfile=interop --args="JVMInterop" --console=plain`; restored `@Ignore`; `git diff --check`. |
| Exact results | Diagnostic: `BUILD SUCCESSFUL`, 1 test, 0 failures/errors/skips, 60.114 s; twelve five-second samples reported a live kit and clean stop. `dns-sd` recorded the diagnostic instance add on interfaces 1 and 14 and corresponding remove events at shutdown. The authenticated JVM sample exited cleanly and correctly reported no legacy peer. A secure-v2 diagnostic experiment failed deterministically with `LocalIdentityUnavailable(TEMPORARILY_UNAVAILABLE)` because the unsigned Kotlin/Native test executable cannot access Keychain; it was fully reverted, and signed secure-v2 app coverage remains the correct external path. |
| Compatibility and remaining evidence | Documentation/test-only clarification; no SDK API/ABI, runtime behavior, wire protocol, service namespace, or security policy changed. Real signed Android/Apple secure-v2 interop, AWDL, abrupt departure, rotation, and hostile-network evidence remains external. |

## Execution record: SEC-01

### Scope and status

| Field | Value |
| --- | --- |
| Primary findings | CORE-06 (High security limitation), CORE-07 (Medium architecture) |
| Status | Implemented; local SEC acceptance tests pass, external/repository-wide certification gates remain |
| Confirmed on | `6a05ccd04fcb6fb8106ed47941618fb6bcfd3fa6` |
| Source changes | Complete for SEC-01: provider dependencies, canonical identity/storage records, platform storage, Noise v2 engine, strict frame versioning, transport security profile, segregated LAN discovery, manual pinning, and sample migration |
| Approval | Owner approved SEC-01 and storage A on 2026-07-17; provider selection remains an implementation review, not an owner-policy blocker |
| Tightly coupled findings | CORE-13, CORE-18, CORE-19, CORE-20, CORE-21, PROTO-02, authentication portion of LAN-16; each keeps its own row/acceptance/commit evidence |
| Related but independently closable | PROTO-03, PROTO-04, PROTO-06, LAN-15, remaining LAN-16 concerns, FILE-13 |

### Reproduction evidence and root cause

The findings are confirmed in the current branch:

- `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/Config.kt:79` exposes only `SecurityMode.NoneForMvp`.
- `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/dsl/Builders.kt:51` defaults to it, but `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/internal/P2pKitImpl.kt:68` marks the value unused and line 118 always installs `NoOpSecurityManager`.
- `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/security/SecurityManager.kt:33` merely delegates the raw stream and constructs `PeerIdentity` from an unverified `Peer`. `SecureConnection.peerIdentity` is not consumed elsewhere.
- `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/internal/SessionManager.kt:406` launches `protocol.events(rawConnection)` first. It parses plaintext HELLO, trusts/resolves the peer, and only then calls `security.performHandshake` at line 485.
- `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/transport/RawConnection.kt:18` explicitly allows exactly one byte-flow collector. A real security layer cannot take over that stream after the parser has started, while an encrypted wrapper would be bypassed by the existing reader.
- `SessionManager.kt:451` compares only attacker-controlled HELLO strings for outgoing sessions; line 466 explicitly accepts any non-local claimed PeerId inbound. PeerIds are visible through discovery, and manual peers intentionally bypass the expected-ID comparison.
- JVM/Android LAN use ordinary sockets and Apple constructs non-TLS Network.framework parameters. This is acceptable only as a byte-stream substrate if core encrypts above it; today core does not.
- Existing `HandshakeIdentityTest`, `ManualPeerIdentityTest`, and `HandshakeTest` prove claimed-string/plaintext behavior only. There is no key-possession, confidentiality, integrity, replay, downgrade, tamper, or sole-reader security test.

The root cause is ordering and identity ownership, not just the absence of a cipher. The public security contract requires a `Peer` before authentication, while a trustworthy inbound `Peer` cannot exist until authentication. The protocol parser already owns the one raw reader before the extension point runs. Consequently the current extension API cannot be implemented securely.

### Security impact

Current LAN participants can passively read the TCP/data connection, including HELLO identity metadata, messages, file bytes, and control frames. AppId, PeerId, device name, endpoint, and traffic timing/length are also exposed separately by current mDNS discovery; encrypting the data connection does not make discovery private. An active participant knowing the public app ID can claim another visible PeerId, occupy or churn its session slot, or impersonate a manual endpoint. There is no cryptographic confidentiality, integrity, replay protection, authenticated identity, forward secrecy, authorization, or downgrade protection.

### Proposed long-term architecture

Subject to the decisions below, the recommended architecture is transport-independent authenticated encryption in `p2p-core`; LAN TCP remains a plain byte-stream carrier:

1. Open or accept a `RawConnection` and assign an explicit initiator/responder role.
2. Start one bounded setup deadline before the first security byte is written.
3. A v2 security engine becomes the sole owner/collector of `RawConnection.read()` for the connection lifetime.
4. Run a vetted authenticated key exchange with one canonical shared context—protocol name/version, AppId, and an identical direction tuple—bound into its transcript/prologue. Initiator/responder behavior comes from the handshake state machine; the two sides must not hash different local-role strings.
5. Return a bounded, backpressured secure record stream plus authenticated remote public-key identity.
6. Only then start `DefaultP2pProtocol.events()` on decrypted bytes and exchange HELLO inside encryption.
7. Derive/bind `PeerId` through a versioned, domain-separated hash of one canonical raw public-key encoding; validate HELLO metadata against that identity.
8. Apply the approved authorization policy and publish/register the session only after all security, HELLO, expected-peer, lifecycle-generation, and capacity checks commit.
9. On reconnect, require the same authenticated key before rearming the existing public session.

The preferred wire design is a vetted implementation of a Noise-style authenticated handshake, provisionally `Noise_XX_25519_ChaChaPoly_SHA256` for unknown/manual peers, with a pinned expected-key path only when prior trust/pairing supplies one. Unauthenticated discovery may nominate an expected fingerprint hint that the authenticated key must match, but it never creates a pin or authorization. Cryptographic primitives must not be implemented in this repository. Exact provider suitability requires a focused dependency review covering implementation provenance, independent review/audit status, KMP interoperability and platform parity, maintenance and release/CVE response, license, entropy/CSPRNG integration, constant-time/side-channel posture, known-answer vectors, publication impact, and proof that released artifacts contain the reviewed implementation.

There must be a distinct secure preface/protocol major, strict handshake and record size bounds, monotonic nonces, authenticated record framing, counter exhaustion handling, and no automatic plaintext fallback. General application-frame limits and validation remain separate PROTO findings.

If legacy mode remains, it is a whole-kit explicit configuration with segregated discovery capability/advertisement and listener behavior. A secure kit never selects plaintext from attacker-controlled TXT data, and a secure listener closes a legacy preface with uniform bounded behavior rather than detailed plaintext diagnostics.

Standards basis: the [official Noise Protocol Framework](https://noiseprotocol.org/noise.pdf) limits Noise messages, treats remote static-key acceptability as an application decision beyond proof of possession, and warns that negotiation not bound into the prologue can enable rollback. Those constraints are why authorization remains a separate explicit policy and why protocol/security selection must be transcript-bound rather than opportunistically negotiated.

### Expected implementation surface

The exact file list will be finalized after the decisions, but the current architectural surface is:

- Core public/API: `Config.kt`, `Identity.kt`, `Errors.kt`, `P2pKit.kt`, `P2pSession.kt`, `dsl/Builders.kt`, and the security package.
- Core orchestration: `internal/P2pKitImpl.kt`, `internal/SessionManager.kt`, `internal/Handshake.kt`, local identity/key storage, and reconnect/session commit paths.
- Wire: `protocol/ProtocolConstants.kt`, `HelloPayload.kt`, version enforcement in `FrameCodec.kt`/`FrameReader.kt`, plus new bounded secure-handshake/record code.
- Transport contract: `transport/RawConnection.kt` ownership documentation; the external transport SPI should remain a byte stream unless provider review proves a necessary break.
- LAN discovery: common TXT schema and JVM/Android/Apple encode/decode validation if security capability/fingerprint hints are advertised. Discovery hints never establish identity by themselves.
- Manual provisioning: `provisioning/ManualPeerRegistrar.kt`, `NetworkProvisioningTypes.kt`, `NetworkProvisioningFactory.kt`, unsupported/platform managers, Android/desktop sidecars, and all manual-peer tests. A fingerprint, trust token, or pending authenticated identity necessarily changes these contracts.
- Build/publication: `gradle/libs.versions.toml`, `p2p-core/build.gradle.kts`, POM/module metadata, ABI baselines, and external-consumer checks.
- Consumers: samples and KMP/Swift bridges where secure defaults, authorization callbacks, pairing, or migration must be configured.

### Lifecycle, concurrency, cleanup, and performance contract

- One deadline covers security exchange, secure-stream creation, encrypted HELLO write/read, identity/metadata validation, and session commit. It includes previously unbounded first writes and security implementations (CORE-13).
- `CancellationException` propagates unchanged. Cancellation, authentication failure, protocol failure, or timeout cancels security reader/record jobs, closes secure/raw ownership exactly once, performs bounded `NonCancellable` cleanup, releases the inbound permit, and publishes no session.
- Authentication failure never falls back to plaintext or reconnects under another identity.
- One owner reads the raw connection. Bounded ciphertext and plaintext queues apply backpressure; no unbounded handshake/record buffer or task is allowed.
- Secure record writes and nonce allocation are serialized inside the secure stream regardless of which session/control/file producer calls it. Authentication failure atomically terminalizes both directions, discards queued plaintext, and prevents every later record from being processed or reusing a nonce.
- No socket/session/key ownership is committed after terminal kit generation. Simultaneous-open and reconnect roles must be deterministic.
- Long-term key creation is atomic across concurrent instances where the platform permits; persistence corruption/failure fails closed according to the approved contract and never silently rotates identity mid-instance.
- CSPRNG failure is fatal. Private/session keys are never logged or exposed through public models, and temporary key material is cleared where Kotlin/platform runtime semantics permit.
- If interactive authorization is selected, it is not allowed to consume the security-setup deadline or one of the 16 pre-handshake permits indefinitely. It needs a separately bounded/capped pending-authorization state, cancellation-safe callback contract, timeout, denial result, and terminal cleanup.

### API, binary, wire, migration, and cross-platform implications

- The current public `SecurityManager.performHandshake(connection, peer)` contract is unusable and must be deprecated/replaced or break in the next major API. It needs role, expected authenticated identity, local identity, deadline/cancellation ownership, and authorization outcome before `Peer` creation.
- Making secure mode the default changes runtime behavior. Removing `NoneForMvp` is an immediate source/runtime break; retaining it as deprecated explicit opt-in preserves a migration escape hatch but must never be negotiated automatically.
- A secure preface and major protocol version intentionally break wire compatibility with plaintext v1 in secure mode.
- A key-derived PeerId changes existing persisted UUID identities once. Preserving an old UUID cannot retroactively authenticate it without an independently trusted binding; a migration must not claim otherwise.
- Key storage design must distinguish exportable software X25519 key bytes wrapped/stored by Keychain/Keystore, non-exportable keys operated through platform APIs, and hardware-backed keys. A common Noise provider may require raw private bytes, so provider and storage choices are one decision rather than independent promises.
- Key lifecycle must define per-AppId versus device-global identity, backup/restore and uninstall behavior, rotation, compromise/revocation, corruption/lost-key recovery, and orphaned backup state. Android restored data can outlive a Keystore key; iOS Keychain lifetime differs from `NSUserDefaults`.
- `P2pKit.create` currently loads identity synchronously, exposes `localPeerId` immediately, and constructs transports with it. Secure key loading/generation must either preserve that blocking construction contract explicitly or introduce a documented public construction/start/identity migration.
- Android Keystore, iOS Keychain, and the approved JVM key-protection policy affect persistence behavior and tests. All platforms must emit the identical secure wire protocol and pass common known-answer/interoperability vectors.
- If pinning or TOFU exists, its authorization store needs an AppId namespace, atomic/concurrent decisions, denial persistence policy, revocation/reset, key-change handling, and deterministic behavior when callbacks are absent, throw, or are cancelled.
- A crypto dependency may enter public metadata depending on the final API shape; BUILD-01 external-consumer checks must catch the resulting scope.

### Coupled-finding ownership and commit boundaries

SEC-01 is an architectural program, not permission for one large commit. After approval it is split into focused prerequisite/implementation commits:

| Sub-unit | Finding ownership | Required order |
| --- | --- | --- |
| SEC-ID-01 | CORE-18, CORE-19, CORE-20, CORE-21: collision-safe atomic cryptographic identity storage and migration | Before secure sessions consume long-term keys |
| SEC-WIRE-01 | PROTO-02: explicit secure major/preface and frame-version validation | Before secure v2 interoperability is claimed |
| SEC-SETUP-01 | CORE-13: one bounded setup transaction including security and encrypted HELLO | With/before SessionManager secure-stream commit |
| SEC-CORE-01 | CORE-06/CORE-07: secure-stream architecture, authenticated identity, authorization, no downgrade | After the preceding contracts are defined |
| SEC-LAN-AUTH-01 | Authentication portion of LAN-16: discovery is only an untrusted routing/hint source | Verified with secure core; remaining LAN-16 address/ownership/log concerns stay in LAN-NET-01 |

Each row is committed, tested, and marked independently. SEC-01 cannot be Verified until these coupled rows are individually Verified, but their separate units are not double-owned or silently closed by the CORE-06/CORE-07 commit.

### Required tests

Security and wire tests:

- Secure message and file round trips across supported targets.
- Captured raw data-connection bytes contain none of AppId, identifiers, names, message text, or file bytes; repeated equal plaintext produces different ciphertext. A separate test/documented threat limit confirms current mDNS discovery metadata remains observable.
- PeerId is bound to key possession; inbound/outgoing victim-ID claims fail.
- An attacker may copy a victim's mDNS PeerId/endpoint/fingerprint hint but cannot complete a session without the victim private key; advertising a new attacker-owned identity follows the selected authorization policy.
- Tampered authentication tag, replay, reorder, truncation, oversized security message/record, wrong app, wrong key, wrong role, and wrong protocol version each produce one precise local typed outcome and close. Pre-authentication peer-visible behavior stays uniform—a bounded close without detailed plaintext diagnostics—so failures do not become an oracle.
- Secure mode rejects plaintext v1 without downgrade; explicit legacy mode, if retained, works only when both endpoints explicitly select it.
- A fake that fails on a second `RawConnection.read()` proves security plus session traffic uses one raw collector.
- Reconnect accepts the same key and rejects an attacker at a refreshed endpoint; simultaneous-open role resolution is deterministic.
- Manual connections follow the approved high-entropy pin/QR, PAKE, approval, TOFU, or accept-any policy exactly; no test treats a low-entropy code as a raw PSK.
- Canonical public-key encoding, domain-separated PeerId derivation, handshake transcript, record encryption, and cross-platform key parsing have fixed interoperability vectors.
- Authorization tests cover absent/throwing/cancelled callbacks, concurrent decisions, timeout, denial, revocation, key change, reset, and capacity exhaustion if an interactive/persistent trust mode is approved.

Lifecycle tests:

- Cancellation at every security read/write/key-exchange/HELLO/commit suspension leaves no job, socket, permit, pending connect, or session.
- Stalled initial write/security implementation is bounded by the single setup deadline.
- Pre-handshake capacity returns after every failure and repeated failed-then-successful connects remain leak-free.

Identity storage and platform tests:

- Stable fingerprint across restart; collision-prone AppIds remain distinct.
- Concurrent first creation produces one key/identity, persistence failure follows one explicit contract, writes are atomic, corrupted keys fail closed, and migration is deterministic.
- Backup without the corresponding platform key, uninstall/reinstall, rotation, compromise/revocation, lost/corrupt key, and orphaned authorization data follow the approved lifecycle on each platform.
- Common known-answer/interop vectors run on JVM, Android, and Apple; secure LAN loopback runs on JVM/Apple simulator and physical Android/Apple before final verification.
- Samples/KMP/Swift compile with the approved trust configuration; temp-published external consumers compile; ABI/API changes are reviewed.

### Acceptance criteria

SEC-01 cannot be marked Verified until all of the following are true:

- No application protocol parser reads raw bytes before authenticated security succeeds.
- Every non-explicit-legacy session is encrypted and integrity protected with no downgrade path.
- Session identity is derived from and verified against authenticated key material.
- Unknown/manual peer admission follows the approved authorization policy.
- Entire setup and cleanup are bounded, cancellation-correct, and leak-free.
- Identity/key persistence is collision-safe, stable, atomic, and fails closed.
- JVM, Android, and Apple implementations interoperate on one versioned wire format.
- All targeted, module, cross-platform, consumer, repeated concurrency, full-repository, physical-device, and hostile-network gates pass.
- Public API/wire compatibility and one-time identity migration are recorded for consumers.
- Documented threat limits are accurate: this design does not hide mDNS metadata or traffic timing/length, prevent endpoint-advertisement DoS, authorize an unknown key by itself, or protect a compromised local private key.

### Approved owner decisions

The owner approved this package on 2026-07-17:

1. Authenticated encryption is mandatory by default; retain `NoneForMvp` only as deprecated whole-kit migration opt-in with segregated advertisement/listener behavior and never an automatic fallback.
2. Introduce secure protocol major v2 with a distinct preface; reject plaintext v1 uniformly in secure mode.
3. Use versioned, domain-separated, key-derived self-certifying PeerIds and accept a documented one-time identity change from legacy UUIDs.
4. Reject unknown identities by default. For the first secure release, support high-entropy fingerprint/QR pinning and a separately explicit “accept any authenticated same-AppId identity” policy. Do not market AppId as authorization. Defer short human-entered pairing codes until a named PAKE is chosen; never use a low-entropy code directly as a Noise PSK.
5. Decide whether interactive approval is required. Recommended initial scope is preconfigured pin/QR or explicit accept-any, avoiding a human callback inside connection setup. If interactive approval is required now, approve a separate bounded/capped pending-authorization state and UI-safe callback contract.
6. Manual IP requires a high-entropy pinned fingerprint/QR or the approved pending-identity flow; synthetic IDs are not authenticated identities.
7. Replace/deprecate the current `SecurityManager` API with a role-aware v2 contract and built-in audited secure engines. Do not extend P2pKit's security guarantee to arbitrary custom crypto providers; any future custom-engine SPI must be explicitly experimental/unsafe and separately threat-modeled.
8. Approve a focused vetted KMP provider review against the expanded criteria above; no repository-local cryptographic primitives.
9. Decide storage capability with the provider: exportable wrapped software keys versus non-exportable platform-operated keys and whether hardware backing is mandatory. Also approve the JVM at-rest protection policy. Persistence/CSPRNG failure fails closed.
10. Use a per-AppId long-term identity by default and explicitly approve backup/restore, uninstall, rotation, revocation, corruption/lost-key recovery, and authorization-store lifecycle behavior.
11. Preserve synchronous `P2pKit.create` and immediate `localPeerId` for the smallest API change, with the existing off-main-thread requirement, or approve a breaking suspending/lazy construction and nullable/stateful identity contract. The recommendation for this remediation is to preserve the current construction shape unless provider constraints make that unsafe.
12. Approve the focused sub-unit ownership table above so CORE-13, CORE-18, CORE-19, CORE-20, CORE-21, PROTO-02, and LAN-16 receive their own commits/evidence while remaining prerequisites to SEC-01 closure.

### SEC-01 implementation contract freeze — Frozen 2026-07-17

The provider, platform-store, and protocol reviews independently agreed that this contract is implementable. This section is now the normative SEC-01 implementation contract; production edits may proceed only within it. Any change to the fixed suite, identity derivation, authorization semantics, legacy separation, persistent record format, or failure matrix requires a new recorded owner decision before dependent code changes.

#### Protocol version and negotiation

- Secure mode is application protocol 2.0. The only suite is `Noise_XX_25519_ChaChaPoly_SHA256` with suite ID `0x01`; there is no algorithm list, IK/PSK variant, or suite agility in v2. The initiator is always the transport dialer and the responder is always the accepted connection.
- Each side uses this exact 16-byte preface: offsets 0..3 ASCII `P2KS`; offset 4 preface format `0x01`; offset 5 application major `0x02`; offset 6 minor `0x00`; offset 7 suite `0x01`; offset 8 role (`0x01` initiator, `0x02` responder); offset 9 flags `0x00`; offsets 10..15 reserved zero. The initiator writes first, the responder validates then writes its responder preface, and the initiator validates it. Every field is exact.
- Secure v2 has no downgrade-capable negotiation. A secure endpoint accepts only the exact v2 preface/suite it is configured to implement. Missing, legacy, malformed, or unknown prefaces receive a uniform bounded close with no detailed plaintext error.
- `SecurityMode.NoneForMvp` remains source-compatible but deprecated and must be selected explicitly for the whole kit. It uses the existing plaintext protocol/frame major 1. A kit never switches mode from discovery TXT, peer input, handshake failure, retry, or reconnect.
- Secure LAN advertises/browses `_p2pkit2._tcp`; explicit legacy advertises/browses `_p2pkit._tcp`. The discovery protocol/fingerprint values remain untrusted usability/routing claims and never authorize a peer or select a weaker mode. A kit never enables both namespaces.
- Both sides use this byte-identical Noise prologue: ASCII `dev.p2pkit.secure-channel.v2\0`, then U16BE exact UTF-8 AppId byte length, exact AppId UTF-8 bytes, initiator preface, responder preface. AppId UTF-8 is bounded before allocation and is neither normalized nor sent separately in plaintext on the data connection.
- Noise messages have an unsigned U16BE length prefix, empty application payload, and exact body lengths 32, 96, and 64 bytes for XX messages 1, 2, and 3. The initiator authorizes the responder static key after message 2 and before revealing its static in message 3; the responder authorizes after message 3. Split maps cipher state 1 to initiator-to-responder and state 2 to responder-to-initiator.
- After `Split`, records are `U16BE(ciphertextLength) || NoiseTransportCiphertext` with zero-length Noise associated data. Plaintext is segmented at 16,384 bytes; valid ciphertext lengths are 16 through 16,400 bytes. This deliberately follows the standard Noise transport cipher instead of inventing length-header associated data.
- Secure record writes, encryption, and nonce allocation are serialized. Ciphertext records are processed strictly in order. Nonces use the Noise ChaChaPoly encoding and may never wrap; authentication failure, invalid length, truncation, replay/reorder, EOF mid-record, or counter exhaustion atomically closes both directions and discards queued plaintext.
- Directional nonces are implicit `0` through `2^64 - 2`; the maximum unsigned value is never used and v2 performs no rekey. A fresh handshake is required before exhaustion.
- One bounded security-owned raw pump starts before preface parsing and remains the only collector of `RawConnection.read()`. It hands a bounded segmented input from the sequential handshake decoder to the sequential record decoder without a second collector. A bounded plaintext channel propagates application backpressure to the socket.
- The application `DefaultP2pProtocol` reader is created only over the decrypted `SecureConnection` after Noise and authorization succeed. HELLO, frame headers, controls, messages, and files are inside encryption.
- Inner HELLO and every frame use major 2 and are rejected immediately on any other header/payload version. Legacy mode uses major 1. Version checks occur as soon as the fixed header is available.

#### Cryptographic identity and fingerprint format

- Each AppId has one independent long-term X25519 static keypair. The canonical public-key encoding is the 32-byte raw RFC 7748 u-coordinate; provider-specific DER/PEM encodings never enter identity derivation or the wire.
- `fingerprintDigest = SHA-256(ASCII("dev.p2pkit.x25519-fingerprint.v1\0") || rawPublicKey32)`.
- `peerIdDigest = SHA-256(ASCII("dev.p2pkit.peer-id.v2\0") || U16BE(appIdUtf8.length) || appIdUtf8 || fingerprintDigest)`.
- The canonical fingerprint is `p2f1-` plus lowercase unpadded Base32 of the full 32-byte fingerprint digest. The canonical PeerId is `p2id2-` plus lowercase unpadded Base32 of the full 32-byte PeerId digest. Neither authorization value is truncated; decoded digest comparisons are constant-time.
- The Noise static key proves possession; the derived PeerId must exactly equal the encrypted HELLO PeerId and the expected discovery/manual hint. Copied mDNS identifiers cannot authenticate without the corresponding private key.
- `appBinding = p2a1- || Base32LowerNoPad(SHA-256(ASCII("dev.p2pkit.app-binding.v1\0") || U16BE(appIdUtf8.length) || appIdUtf8))`. The exact QR text is `p2pkit:v2:<appBinding>:<fingerprint>`. Parsing requires exactly four colon-separated fields, canonical prefixes/lengths/characters, and an AppId binding equal to the local exact AppId. QR data is only a high-entropy pin transport; no low-entropy code or raw-PSK mode exists.
- Private keys, wrapping keys, handshake/session keys, decrypted queued data, and full fingerprints are never logged. CSPRNG/key-generation failure is fatal. Temporary key bytes are cleared where the runtime permits.

#### Authorization and manual peers

- Unknown authenticated identities are rejected by default.
- Built-in authorization modes are: reject unknown (the default), a static/configured set or trusted store of full v2 fingerprints, a per-connect/manual full fingerprint, and an explicitly selected `AcceptAnyAuthenticatedSameApp` mode. Same AppId is scoping, not authorization. There is no interactive approval callback, TOFU, or human-entered code in this first implementation.
- Outgoing discovered connections must authenticate the exact key-derived PeerId advertised by the selected peer. Inbound identities must satisfy the configured authorization mode before HELLO/session publication.
- Secure manual-IP registration requires an expected v2 fingerprint and derives its real expected PeerId from the exact AppId plus fingerprint. The synthetic manual identity path is legacy-only. Existing no-fingerprint manual APIs remain for source compatibility but fail with a typed security-configuration error when the kit is secure.
- Reconnect permanently retains and authenticates the initially established fingerprint even under accept-any before rearming the existing session. A refreshed endpoint can change routing only, never identity. Simultaneous-open arbitration occurs only after both candidates are authenticated.

#### Identity migration and rollback

- Secure v2 uses a new identity-store namespace and never overwrites/deletes the existing UUID `peer-id` storage. First secure construction creates a new key-derived PeerId, so consumers must treat the device as a new authenticated identity and re-pin it. No claim is made that the legacy UUID was cryptographically bound.
- Successful secure creation is atomic: all concurrent constructors for one AppId return the same committed key. A losing creator discards its uncommitted material and reloads the winner.
- Downgrading application configuration to explicit legacy mode resumes the untouched legacy UUID and v1 wire behavior; it does not translate pins or delete v2 keys. Re-enabling secure mode returns to the same v2 identity. Rollback is therefore operationally possible but always an explicit security downgrade.
- Old binaries interoperate only when the new binary explicitly selects legacy mode. Secure-v2 and legacy-v1 endpoints do not connect. There is no dual listener, fallback retry, opportunistic mode, or same-session upgrade.
- Secure identity material is per-AppId and device-local. Android secure files live in no-backup storage; iOS uses a device-only Keychain accessibility class; neither identity is restored to another device. JVM persistence behavior is defined by the injected store.

#### Storage A and failure behavior

- The canonical inner identity record is exactly 104 bytes: magic ASCII `P2KI` (4), schema `0x01` (1), algorithm X25519 `0x01` (1), zero flags U16BE (2), full 32-byte AppId namespace hash (32), raw private key (32), raw public key (32). `namespaceHash = SHA-256(ASCII("dev.p2pkit.identity-namespace.v2\0") || U16BE(appIdUtf8.length) || appIdUtf8)`. Unknown magic/schema/algorithm/flags, wrong namespace, wrong length, trailing bytes, or public/private mismatch is corruption.
- Every load derives the public key again from the private key and compares it with the record. A store returns only a durable, reread, fully validated winner. Generated and losing private arrays are cleared best-effort.

- Android stores the inner record only as AES-256-GCM ciphertext at `noBackupFilesDir/p2pkit/identity-v2/<full-namespace-hash>/identity.blob`. The non-exportable wrapping key alias is `dev.p2pkit.identity.v2.<full-namespace-hash>` in Android Keystore. The exact strict outer blob is ASCII `P2KB` (4), schema `0x01` (1), AES-GCM algorithm `0x01` (1), zero flags U16BE (2), namespace hash (32), IV length `0x0c` (1), ciphertext length U16BE (2), random IV (12), and ciphertext/tag (120 for the 104-byte inner record). The fixed header through ciphertext length is AAD. A process lock plus cross-process file lock and `AtomicFile` commit protect creation.
- Android's state matrix is exact: blob absent/alias absent creates; present/present decrypts and validates; present/absent is permanent key loss; absent/present is incomplete creation/storage damage; malformed/tag failure is corruption. Only an alias created by a currently failing first transaction may be cleaned up; no state silently regenerates.
- iOS stores the 104-byte record as a non-synchronizable generic-password Keychain item: service `dev.p2pkit.identity.v2`, account full namespace hash, default access group, `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly`. `SecItemAdd` duplicate means reload the winner. The strict nonsecret marker is exactly 72 bytes: ASCII `P2KM` (4), schema `0x01` (1), X25519 algorithm `0x01` (1), zero flags U16BE (2), namespace hash (32), fingerprint digest (32). It is atomically stored under Application Support and excluded from backup.
- iOS's state matrix is exact: item plus matching marker is valid; marker without item is key loss; item without marker is a recoverable interrupted commit/reinstall and recreates the marker after validation; both absent creates; any mismatch/corruption fails closed. `errSecInteractionNotAllowed`/before-first-unlock is retry-after-unlock and never rotates identity.
- JVM secure mode requires an injected synchronous `JvmSecureIdentityStore` with `read`, durable cross-process atomic `putIfAbsent` returning the durable winner, and durable idempotent `delete`. It stores the opaque 104-byte record and is responsible for confidentiality/integrity at rest. Core supplies no silently plaintext “secure” default; absence fails before any transport factory. Tests may use a loudly named in-memory test store; samples must make development-only storage explicit and must not describe it as production protection.
- Existing mobile PeerId-storage fallbacks are legacy-only. Secure mode never falls back to UUID, plaintext file, `NSUserDefaults`, in-memory identity, regenerated key, or legacy mode.
- First-use absence creates and atomically commits a key. Once any v2-store marker/item exists, unreadable, undecryptable, missing-key, corrupt, or inconsistent material is `KeyLostOrCorrupt`: fail closed, preserve evidence, and never auto-rotate.
- Key loss has no implicit recovery. Recovery requires an explicit destructive reset operation initiated by the application/owner, which deletes only the selected AppId's v2 local identity and generates a different PeerId on the next construction. The durable reset marker is exactly 40 bytes: ASCII `P2KR` (4), schema `0x01` (1), local-identity-reset action `0x01` (1), zero flags U16BE (2), namespace hash (32). It is committed before deletion; interrupted reset is completed only by the next explicit maintenance call, never by ordinary construction. The reset API reports the identity-change/re-pinning consequence, rejects a known live in-process kit, documents its cross-process exclusivity precondition, keeps configured remote pins by default, and never runs from connection error handling.
- A failed initial persistence writes no usable kit and exposes no process-local transient identity. Private material from failed/losing creation is discarded/cleared. Store exceptions retain their cause without including secrets.
- On Android uninstall/clear-data, both no-backup ciphertext and Keystore alias are expected to disappear. If restored ciphertext exists without its wrapping key, construction fails closed. On iOS, device-only Keychain material may survive reinstall on the same device according to OS behavior, but never migrates to another device; explicit reset rotates it. JVM lifecycle follows the host store contract.

#### Provider freeze and assurance boundary

- Common code uses `cryptography-core:0.6.0` only as a primitive API. JVM and Android explicitly instantiate `CryptographyProvider.JDK(BouncyCastleProvider())` using `cryptography-provider-jdk:0.6.0` and `bcprov-jdk18on:1.85`. Apple explicitly instantiates `CryptographyProvider.CryptoKit` using `cryptography-provider-cryptokit:0.6.0` and the `dev.whyoleg.cryptography` 0.6.0 linker plugin.
- The engine never uses `CryptographyProvider.Default`, service/provider discovery, the optimal provider, or application-installed provider ordering. Provider types remain internal and publication metadata must not leak them into the public ABI.
- The repository implements the strict Noise state machine but no cryptographic primitive. It follows the official Noise specification and is tested against official/Cacophony vectors plus RFC/provider KATs. All-zero X25519 output is independently rejected with a fixed-work accumulator. Owned key/chaining buffers are wiped best-effort, without claiming JVM/Swift/provider copies are erasable.
- No suitable maintained KMP Noise implementation was found. `cryptography-kotlin` 0.6.0's X25519/ChaCha adapters and the repository's Noise state machine have no identified independent professional audit. Local implementation may proceed, but external cryptographic design/code audit, physical API-24/device interoperability, and hostile-network validation remain mandatory production-release gates. SEC-01 must not be presented as production-security certification without them.

#### Compatibility acceptance matrix

| Local | Remote | Required result |
| --- | --- | --- |
| Secure v2 | Secure v2, same AppId, authorized correct key | Authenticated encrypted session; frame/HELLO major 2 |
| Secure v2 | Secure v2, different AppId | Uniform authentication/setup failure; no session/publication |
| Secure v2 | Secure v2, copied victim discovery ID but attacker key | Exact local identity-mismatch failure; uniform peer-visible close |
| Secure v2 | Secure v2, valid but unknown key under default policy | Typed unauthorized failure; no session/publication |
| Secure v2 | Legacy v1 or old 0.6.x | Uniform bounded close; no fallback or second dial |
| Legacy v1 explicit | Legacy v1 explicit or old 0.6.x | Existing plaintext interoperability preserved |
| Legacy v1 explicit | Secure v2 | Incompatible close; secure endpoint never downgrades |
| Secure v2 after legacy upgrade | Previously known legacy UUID peer | New key-derived identity; explicit re-pin required |
| Explicit rollback to legacy | Old peer | Old UUID/v1 behavior resumes; v2 identity remains stored for re-upgrade |

Compatibility is accepted only when source/API compatibility for explicit legacy callers, binary metadata, public transport SPI, manual-IP overloads/errors, discovery filtering, cross-platform known-answer vectors, sole-reader ownership, raw-wire confidentiality, key-store failure matrix, migration/rollback matrix, and the full test/build/publication gate all pass. Network encryption does not promise mDNS privacy, traffic-shape privacy, authorization of unknown keys, endpoint-advertisement availability, or protection after local private-key compromise.

Implementation is authorized. The exact protocol negotiation, migration/rollback, secure-store/key-loss behavior, provider selection, and compatibility acceptance contract above is frozen and reviewed.

### SEC-01 analysis commands and exact results

No compile/test command had been run at the contract-freeze checkpoint because no production source was yet modified. The prior owner-decision blocker is resolved; implementation and verification evidence is appended here as each focused SEC sub-unit proceeds. Existing red repository gates remain recorded in the baseline table and are not attributed to the security changes without reproduction.

| Command/check | Exact result |
| --- | --- |
| `git branch --show-current; git rev-parse HEAD; git rev-parse origin/main` | Branch `remediation/full-register-2026-07`; HEAD and `origin/main` both `6a05ccd04fcb6fb8106ed47941618fb6bcfd3fa6` |
| `rg -n "SecurityMode\|SecurityManager\|SecureConnection\|performHandshake" p2p-core/src` | Confirmed only `NoneForMvp`, always-installed no-op manager, unusable public extension ordering, and no consumer of authenticated `peerIdentity` |
| `sed`/`nl` inspection of `Config.kt`, `Identity.kt`, `Builders.kt`, `SecurityManager.kt`, `Handshake.kt`, `RawConnection.kt`, `P2pKitImpl.kt`, `SessionManager.kt`, protocol files, and security/identity tests | Confirmed sole raw reader starts at `SessionManager.kt:406` and security runs only at line 485 after plaintext identity parsing |
| `rg` inspection of LAN socket/Apple parameter implementations | Confirmed plain byte-stream transports; Apple explicitly uses non-TLS Network.framework parameters |
| Official Noise specification review | Confirmed key possession is not authorization, negotiation must be transcript/prologue-bound against rollback, records/messages are bounded, and nonce/application termination responsibilities must be specified |
| Independent SEC-01 protocol/store/provider reviews | Frozen 16-byte preface and standard Noise record profile; frozen Android/iOS/JVM state matrices; explicit CryptoKit and JDK+Bouncy Castle providers; no provider-selection blocker |
| Tracker finding-set comparison | 150 finding rows exactly match the 150 source IDs; no omissions/duplicates |
| Tracker gap/phase comparison | 54 gap rows; execution plan assigns 150 findings and 54 gaps exactly once; no omissions/duplicates or range shorthand |

### SEC-01 implementation and review result — 2026-07-18

#### Confirmed root cause and implemented correction

The implementation confirmed the analysis above: plaintext HELLO parsing owned the sole raw reader before the old public security hook ran, so identity was attacker-controlled and no encryption wrapper could safely take ownership. The correction makes `SecurityMode.AuthenticatedV2` the default and moves the complete Noise exchange ahead of construction of `DefaultP2pProtocol`. A security-owned pump is now the only collector of the raw connection for its lifetime; the application parser sees only authenticated decrypted bytes. `NoneForMvp` remains an explicitly selected, deprecated whole-kit v1 migration mode with no negotiation or fallback.

The v2 implementation includes the frozen preface/prologue, Noise XX state machine, bounded authenticated records, exact AppId/key-derived identity validation, reject-unknown/pinned/explicit accept-any authorization, reconnect key pinning, encrypted HELLO major 2, strict frame-major validation, full-fingerprint manual IP registration, secure/legacy Bonjour namespace separation, and typed failure results. A PR-style ownership review found and corrected one double-close path: after secure setup begins the security engine owns and closes raw exactly once; `SessionManager` closes raw directly only for legacy setup. The regression test now asserts the exact close count.

Storage A is implemented with the frozen inner record and reset marker. Android uses AES-GCM with a non-exportable Keystore wrapping key and no-backup atomic ciphertext; Apple uses a device-only nonsynchronizable Keychain item plus atomic nonsecret marker; JVM requires an injected `JvmSecureIdentityStore` and has no plaintext production default. Loads rederive and compare the public key, concurrent creation selects one durable reread winner, corrupt/lost material fails closed, cancellation is preserved, and explicit reset is rejected while a kit owns the identity.

#### Files and components changed

The focused diff contains 92 status entries before expansion of newly added directories. The exact authoritative file ledger is the focused commit's `git show --name-status`; the implementation surface is:

- dependency/provider configuration: `gradle/libs.versions.toml`, `p2p-core/build.gradle.kts`;
- public core/API and DSL: `Config.kt`, `Errors.kt`, `Identity.kt`, `P2pKit.kt`, `P2pSession.kt`, `dsl/Builders.kt`, and the public `security/` additions;
- core ownership/orchestration: `Handshake.kt`, `P2pKitImpl.kt`, `P2pSessionImpl.kt`, `PeerRegistry.kt`, `SessionManager.kt`, transport internal/factory contracts, secure identity factories, and the new `internal/security/` Noise/record engine;
- wire: `DefaultP2pProtocol.kt`, `Frame.kt`, `FrameCodec.kt`, `FrameReader.kt`, `HelloPayload.kt`, and `ProtocolConstants.kt`;
- platform identity/provider implementations: Android secure storage/provider, iOS Keychain storage/CryptoKit provider, and JVM injected-store adapter/JDK+Bouncy Castle provider;
- manual provisioning: common registrar/types/factories plus Android, desktop, and Apple managers;
- LAN discovery/security profile: common `Lan.kt`; JVM, Android, and Apple discovery/DSL implementations and their tests;
- consumers: Android, desktop CLI, desktop UI, KMP JVM/Android, and Swift sample configuration, plus clearly named JVM development-only in-memory sample stores;
- tests: new common Noise, wire, identity, engine, and end-to-end secure-session suites; JVM store/provider tests; iOS Keychain tests; LAN secure loopback/record tests; provisioning secure manual-loopback tests; legacy fixture updates.

No original review report, deferred register, `.review-2026-07/` content, or unrelated user file is part of this unit.

#### Tests added or strengthened

- Noise/KAT: exact Cacophony `Noise_XX_25519_ChaChaPoly_SHA256` handshake flights, handshake hash, and bidirectional transport ciphertext; canonical Base32/RFC vectors; frozen provider/identity derivation values; all-zero DH and nonce/authentication terminal behavior.
- Wire/records: exact v2 preface and prologue, every unsupported field, length/allocation boundaries, exact XX flight sizes, fragmented/batched records, early invalid-length rejection, truncation, one raw collector, no legacy fallback, and initiator-static non-disclosure on rejected responder identity.
- Authentication/authorization: default reject unknown, configured pins, caller-owned pin-set snapshot/immutability, per-connect pin, explicit accept-any, wrong AppId/key/PeerId/fingerprint, copied victim identity, encrypted HELLO, raw-wire confidentiality, manual pin requirement, reconnect pin retention, and explicit legacy incompatibility.
- Lifecycle: cancellation during blocked Noise write, exact raw close count, pending-connect removal, retry after cancellation, setup cleanup, immutable authenticated identities, and live-use reset exclusion.
- Storage: exact 104/72/40-byte formats, strict corrupt-field rejection, AppId collision independence, atomic/concurrent first creation, durable-winner reread, persistence failure/cancellation, mismatch/key loss/interrupted reset, private-array clearing, iOS Keychain/marker state matrix, and absent JVM-store fail-closed behavior.
- Integration: real JVM mDNS/TCP secure text/binary/file loopback, real desktop manual-IP secure loopback, common JVM/Apple fake-raw secure sessions, sample/KMP/Swift compilation, and publication assembly.

#### Verification commands and exact results

| Command/check | Result |
| --- | --- |
| Focused `:p2p-core:jvmTest` security/identity/session suites | Pass; the final post-review run includes `SecureSessionIntegrationTest`, all internal security tests, and all public security tests |
| `:p2p-core:jvmTest` | 336 tests, 0 failures/errors/skips after the final authorization-snapshot regression |
| `:p2p-core:iosSimulatorArm64Test` | 317 tests, 0 failures/errors/skips after the final authorization-snapshot regression; the previously reported reconnect timeout did not reproduce |
| `:p2p-core:compileAndroidMain`, iOS simulator compilation/test linkage | Pass |
| Secure session integration repeated three consecutive forced runs | All three pass with deterministic subscription barriers |
| `:p2p-transport-lan:jvmTest` | 55 tests pass, including real secure mDNS/TCP/message/file loopback |
| Android host provisioning tests and desktop provisioning tests | Pass; desktop includes real secure manual-IP loopback |
| Desktop CLI/UI classes, KMP JVM tests/Android compile, Android sample assemble | Pass |
| Release `P2pKitShared` XCFramework and Swift simulator sample build with signing disabled | Pass; Xcode `BUILD SUCCEEDED` |
| `scripts/check-publish-artifacts.sh` | Pass; all 15 publications contain primary, sources, Javadoc, POM, and Gradle module artifacts |
| `git diff --check` | Pass |
| Final `./gradlew check` | Red only on registered non-SEC baselines: Android sample lint has 1 `CoarseFineLocation` error plus 3 warnings (`SAMPLE-17`/`BUILD-08`), and Apple LAN has 40 tests with the same 2 lifecycle timeouts plus 1 intentional skip (`LAN-03`/LAN lifecycle unit). Core JVM and iOS security tests pass within the run. Existing `ExperimentalCoroutinesApi` test warnings remain `BUILD-14`. |

#### Compatibility, migration, and remaining risk

This is an intentional runtime/wire security change: secure v2 is the default and does not interoperate with v1. Existing source callers can explicitly select deprecated `NoneForMvp` to resume the untouched UUID/v1 identity and wire behavior; there is no automatic fallback. First secure use creates a new key-derived identity and requires re-pinning. Full fingerprints/QRs are required for secure manual IP unless an explicit authorization policy permits the identity. Public transports remain byte-stream providers; security ownership is internal to core. Crypto providers remain implementation dependencies rather than public ABI types.

SEC-01 is `Implemented`, not `Verified`. The exact remaining gates are: independent professional review of the repository Noise state machine and provider integration; physical API-24+ Android Keystore and Android/Apple cross-device secure LAN interoperability; two-machine hostile-network/tamper/resource validation; and a green full repository gate after the independently tracked Android lint and Apple LAN lifecycle failures are fixed. These gates do not justify weakening or bypassing the secure default, but they prohibit a production-security claim.

#### Diff review conclusion

The final local review checked reader ownership, secure/raw close ownership, cancellation propagation, pending-connect cleanup, setup deadlines, record bounds/backpressure, nonce serialization/exhaustion, identity immutability and key clearing, reset/live-use exclusion, discovery downgrade separation, manual pinning, reconnect identity retention, provider selection, sample call sites, legacy-only call sites, and equivalent platform implementations. No new SEC-01 correctness defect remains in the local diff. The pre-existing full-gate failures and external assurance boundaries above remain explicit and are not waived.

#### Source control and post-commit verification

- Branch: `remediation/full-register-2026-07`.
- Focused implementation commit: `b79c9ba` (`SEC-01: add authenticated protocol v2 and protected identities`), 109 files, 10,996 insertions, 326 deletions.
- Committed diff review: `git show --check --stat --oneline b79c9ba` passed; the only remaining worktree entries were the expressly excluded untracked review/deferred files.
- Post-commit gate: `./gradlew :p2p-core:jvmTest :p2p-core:iosSimulatorArm64Test` passed from `b79c9ba` in 1m 5s; 336 JVM and 317 iOS-simulator tests passed. Only the separately registered `BUILD-14` test opt-in warnings were emitted.
- Push: `b79c9ba` pushed successfully to `origin/remediation/full-register-2026-07` on 2026-07-18. The remote reported that the repository has moved to `https://github.com/p2pKit/P2pKit.git`; the configured `origin` accepted the branch and now tracks it.

## Execution record: REL-REMOTE-01

### Finding, status, and confirmed root cause

| Field | Value |
| --- | --- |
| Finding | BUILD-02 — High release blocker — no remote publication target |
| Status | Blocked after analysis; no release behavior was guessed or modified |
| Confirmed root cause | All four library modules apply `maven-publish` and local conditional signing, but no `publishing.repositories`, Central bundle assembly/upload task, Portal status/release task, or CI credential contract exists. Only `publishToMavenLocal` is executable. |
| Affected surfaces | Root/module Gradle publication configuration, release bundle/check scripts, CI secret contract, `dev.p2pkit` namespace ownership, signing identity, and Central deployment policy |
| Dependent evidence | ENV-07 live remote upload; FINAL-01 release proof. REL-ABI-01 and other source units remain independent and are not blocked. |

### Analysis and attempted work

- Inspected root and all module publication/signing configuration, `gradle.properties`, and `scripts/check-publish-artifacts.sh`. No remote repository or Portal integration exists; the property file explicitly records that limitation.
- The existing local checker successfully assembled all 15 publication shapes during SEC-01, but it does not create the exact signed/checksummed Central bundle or exercise upload/status/drop/release behavior.
- Verified the current official service contract. Sonatype states that there is currently no official Gradle Portal plugin; supported choices are a Maven plugin, a Maven-layout bundle uploaded through the Portal/Publisher API, the Portal OSSRH Staging API for applicable existing namespaces, or unsupported community Gradle plugins. See [official Gradle guidance](https://central.sonatype.org/publish/publish-portal-gradle/), [Publisher API](https://central.sonatype.org/publish/publish-portal-api/), [bundle layout](https://central.sonatype.org/publish/publish-portal-upload/), and [artifact/signature/checksum requirements](https://central.sonatype.org/publish/requirements/).
- No token, namespace dashboard state, signing key, or release authorization was accessed or requested. No upload was attempted because Central releases are immutable and the repository does not yet know whether `dev.p2pkit` is a Portal or legacy OSSRH namespace.

### Exact blocker and required owner resources

The owner must provide one policy decision and later CI-held resources:

1. Confirm whether `dev.p2pkit` is verified in the Central Portal, and whether it appears as a **Central Portal namespace** or **OSSRH namespace**.
2. Approve the publication path. Recommended: repository-owned deterministic Maven-layout bundle assembly plus the first-party Portal Publisher API, initially `USER_MANAGED`. This avoids trusting an unsupported third-party Gradle publication plugin and prevents a valid CI build from irreversibly auto-publishing before owner review.
3. Confirm whether releases remain `USER_MANAGED` or become `AUTOMATIC` after validation. Automatic release shortens the pipeline but turns a credentialed CI invocation into an irreversible publish action.
4. Provide only through CI secrets when live validation is authorized: Portal token username/password, ASCII-armored signing private key/password, and the verified namespace/account access. No secret belongs in Git, Gradle properties committed to the repository, logs, or the tracker.

Realistic alternatives and consequences:

- Portal Publisher API + repository-owned bundle (**recommended**): smallest trust surface and exact local bundle dry-run; requires maintaining a small uploader/status client or CI script and checksum/signature validation.
- Portal OSSRH Staging API: reuses Gradle `maven-publish`, but is appropriate only if the namespace/account state supports that workflow and requires staging cleanup/status integration.
- Community Gradle/JReleaser plugin: less custom code, but adds a release-critical third-party plugin not supported by Sonatype; it requires separate provenance, maintenance, and supply-chain approval.
- Manual Portal upload: simple for rare releases but weakens repeatability/auditability and leaves BUILD-02's automated release path incomplete.

Precise owner reply format for the later consolidated decision package:

`REL-REMOTE-01: namespace=PORTAL|OSSRH; path=PORTAL_API_BUNDLE|OSSRH_STAGING|COMMUNITY_PLUGIN|MANUAL; release=USER_MANAGED|AUTOMATIC; live_credentials=AVAILABLE_IN_CI|NOT_YET`

Until that reply is supplied, only REL-REMOTE-01, ENV-07, and their genuinely dependent final-release proof are blocked. No assumptions from this unit may constrain REL-ABI-01 or other dependency-ready remediation.

## Current execution record: REL-ABI-01

### Analysis and confirmed reproduction

| Field | Value |
| --- | --- |
| Finding/gaps | BUILD-01 (High), CORE-T13, and the publication-consumer portion of PS-T09 |
| Status | In Progress; analysis and plan complete, no implementation edit made before this record |
| Root cause | Gradle `implementation` was used for dependencies whose types occur in public Kotlin metadata. Generated target POMs therefore mark them runtime and Gradle API variants omit them entirely. |
| Affected modules | `p2p-core`, `p2p-transport-lan`, Android provisioning, desktop provisioning; every published JVM/Android/KMP/iOS target and downstream Kotlin/Java consumer |
| Public dependencies | Core exposes coroutines `Flow`/`SharedFlow`/`StateFlow`/`Job` and kotlinx-io `RawSource`/`RawSink`; LAN exposes core types and direct coroutine diagnostics/implementations; both provisioning sidecars expose core provisioning types and coroutine flow types. Crypto/serialization/JmDNS/provider types remain internal and must stay runtime/implementation. |

Generated metadata reproduces the defect exactly:

- `p2p-core` JVM/Android POMs publish coroutines at runtime and API variants list only kotlinx-io/stdlib, even though core's public API contains coroutine types.
- LAN JVM/Android POMs correctly publish core at compile but publish coroutines at runtime; API variants omit coroutines even though `JvmLanDiag.events` and Apple diagnostics expose `SharedFlow` directly.
- Android provisioning publishes both core and coroutines at runtime and its API variant lists neither.
- Desktop provisioning publishes core, LAN, and coroutines at runtime and its API variant lists none. Production desktop code does not import LAN; LAN is used only by `ManualIpLoopbackTest`, so that dependency is both incorrectly scoped and unnecessarily shipped.
- The KMP root POMs flatten dependencies to runtime as a Kotlin publication convention; the authoritative Gradle module target variants still must carry correct API dependencies, and Maven target POMs must use compile scope.

### Comprehensive implementation plan

1. Change `p2p-core` common coroutines from `implementation` to `api`; retain kotlinx-io as `api`; keep serialization, cryptography core/providers, and Bouncy Castle internal.
2. Change LAN common coroutines to `api`; keep core as `api`; keep JmDNS internal.
3. Change Android provisioning core and coroutines to `api`.
4. Change desktop provisioning core and coroutines to `api`; move LAN from production `implementation` to `testImplementation` because only its test loopback uses it.
5. Add a repository-owned external-consumer gate that publishes to a throwaway Maven repository, creates isolated builds outside the composite/project dependency graph, and compiles consumers that declare only one published top-level artifact. Cover core JVM, LAN JVM, desktop provisioning JVM, Android provisioning, KMP common/JVM, and iOS simulator publication consumption. Do not add the transitive dependencies explicitly in the consumer; that would hide this regression.
6. Assert generated POM/API-variant scopes so a consumer compilation cannot accidentally pass because another test dependency supplies the missing library.
7. Run targeted metadata generation/consumer gates, all four affected module tests/compiles, Android/KMP/iOS consumer compilation, publication shape validation, samples, and the full repository gate. Repeat the clean consumer gate from committed state.

Expected files: the four module `build.gradle.kts` files; one new isolated publication-consumer check script and its generated inline fixtures; tracker updates. No production Kotlin source or public signature should change.

Compatibility and risk: this is source/binary compatible and only adds required transitive compile dependencies. Consumer classpaths become correct and slightly more explicit. Runtime artifacts/versions do not change. Moving desktop LAN to test-only removes an erroneous runtime dependency from that sidecar; applications needing LAN already select the LAN transport separately. The external-consumer test must use a throwaway repository/directory and never write credentials or publish remotely.

Cleanup/rollback: the gate uses `mktemp`, traps cleanup, and does not touch `~/.m2`. A failed nested build preserves only ordinary module `build/` diagnostics until the script exits; no external state changes. Rollback is the focused build-script/test-script commit only.

Acceptance criteria:

- Generated JVM/Android target POMs classify every public ABI dependency as compile and every implementation-only dependency as runtime.
- Gradle module API variants include core/coroutines/kotlinx-io exactly where public ABI requires them and exclude serialization, crypto providers, Bouncy Castle, JmDNS, and desktop's test-only LAN.
- Fresh isolated consumers compile without directly declaring transitive ABI dependencies for JVM, Android, KMP common/JVM, and iOS simulator targets.
- Existing module/platform/sample/publication tests pass; no warning is introduced.
- Full gate has no new failure; any existing unrelated baseline remains exact and separately owned.

### Implementation, tests, and diff review result

Implementation is complete in five focused files:

- `p2p-core/build.gradle.kts`: coroutines is now `api`; kotlinx-io remains `api`; serialization and every security provider remain `implementation`.
- `p2p-transport-lan/build.gradle.kts`: coroutines is now `api` alongside core; JmDNS remains `implementation`.
- `p2p-network-provisioning-android/build.gradle.kts`: core and coroutines are now `api`.
- `p2p-network-provisioning-desktop/build.gradle.kts`: core and coroutines are now `api`; LAN moved from production to `testImplementation`.
- `scripts/check-published-consumers.sh`: new executable, isolated, cleanup-safe publication consumer gate.

The new gate publishes all modules to an `mktemp` Maven repository, asserts exact target-POM scopes, confirms desktop does not publish LAN, generates fresh projects outside the repository dependency graph, and compiles:

- a core-only JVM consumer using `StateFlow` and `RawSink`;
- a LAN-only JVM consumer using direct LAN `SharedFlow` plus transitive core types;
- a desktop-provisioning-only JVM consumer using transitive core/coroutine types;
- an Android-library consumer depending only on the Android provisioning AAR;
- a KMP LAN consumer for common, JVM, Android, and iOS simulator source sets.

The gate deliberately does not declare core, coroutines, or kotlinx-io directly where they must arrive transitively. Review caught and removed an initial fixture weakness where LAN could have masked Android provisioning metadata; Android provisioning now has its own isolated module. It also imports only `sdk.dir` into the temporary project when Android environment variables are absent, never copies other local properties, removes the entire validated `mktemp` directory through a quoted trap, and performs no remote publication.

Regenerated API variants now contain:

- core JVM/Android: coroutines, kotlinx-io, stdlib;
- LAN JVM: core, coroutines, stdlib;
- Android provisioning: core, coroutines, stdlib;
- desktop provisioning: core, coroutines, stdlib and no LAN.

Serialization, cryptography core/providers, Bouncy Castle, and JmDNS remain runtime-only. No production source/public signature changed, and no equivalent module retained the wrong scope.

### Verification results before commit

| Command/check | Exact result |
| --- | --- |
| `bash -n scripts/check-published-consumers.sh` | Pass |
| `scripts/check-published-consumers.sh` | Pass; exact POM assertions plus isolated core JVM, LAN JVM, desktop JVM, Android provisioning, KMP JVM/Android/iOS simulator consumers |
| Generated target module JSON review with `jq` | API variants contain exactly the required public dependencies listed above |
| Affected module command: core JVM/iOS/Android, LAN JVM/Android/iOS compile, Android/desktop provisioning tests | Pass; core JVM 336/336, core iOS 317/317, LAN JVM 55/55, Android provisioning 18/18, desktop provisioning 6/6 |
| `:p2p-transport-lan:iosSimulatorArm64Test` | Registered baseline unchanged: 40 tests, 2 lifecycle timeouts, 1 intentional skip |
| `scripts/check-publish-artifacts.sh` | Pass; all 15 publication rows |
| `./gradlew check` | No new failure. Same registered baseline only: Android sample lint 1 error/3 warnings and Apple LAN 2 lifecycle timeouts/1 skip; existing BUILD-14 test opt-in warnings remain. |
| `git diff --check` | Pass |

Compatibility: source/binary/runtime behavior is unchanged. Published consumers now receive required compile dependencies; desktop provisioning loses only an unused production LAN runtime edge and retains it for its loopback test. Remaining PS-T09 physical/runtime SAMPLE-21 work is not claimed by this build-only unit.

### Source control and committed-state verification

The focused implementation is commit `8f15d75` (`BUILD-01: publish public ABI dependencies correctly`) on branch `remediation/full-register-2026-07`. `git show --check --stat --oneline 8f15d75` reports a clean six-file diff limited to the four affected Gradle modules, the new consumer gate, and this tracker. The committed-state rerun of `scripts/check-published-consumers.sh` passed every exact POM assertion and isolated JVM, Android, KMP, and iOS simulator consumer.

BUILD-01 and CORE-T13 are therefore `Verified`. The unrelated registered Android lint and Apple LAN baselines remain owned by their existing remediation units and do not weaken this unit's exact publication-metadata acceptance criteria. PS-T09 remains `Planned` because its SAMPLE-21 runtime/device conjunct is independent and incomplete.

## Current execution record: LIF-GEN-01

### Analysis and confirmed reproduction

| Field | Value |
| --- | --- |
| Finding/gap | CORE-01 (High) and CORE-T01 |
| Status | In Progress; current code and tests reviewed and implementation plan frozen before source modification |
| Root cause | `ensureStarted()` serializes only data-transport binding. `startAdvertising()`, `startDiscovery()`, and `connect()` perform resource creation and final publication outside that mutex. `stop()` latches a Boolean and closes a point-in-time snapshot, but there is no operation generation carried through the late commit. The observer-start path also checks `stopped` before, but not after, the suspending observer call. |
| Affected components | `P2pKitImpl` lifecycle/state/observer logic; `SessionManager` outgoing and inbound registration boundary; discovery transports on every platform; all data transports used by outgoing connect; public `state`, `sessions`, and `incomingSessions` observations |
| Public/API compatibility | No public signature or wire-protocol change is required. The terminal contract becomes stricter: an operation that loses the race to `stop()` fails with the same post-stop `IllegalStateException` family and cannot publish or relatch state. |

The defect is still deterministically reachable on the current branch:

1. Advertising/discovery call `ensureStarted()`, then suspend in a discovery transport outside `startMutex`. `stop()` can set `Stopped` and invoke the corresponding stop method; the original start then returns late and either leaves the platform resource reopened or writes `Running`/`Failed` after terminal shutdown.
2. Outgoing `connect()` calls `ensureStarted()`, then performs transport dial, secure v2 setup, `session.start()`, and `SessionStore.tryRegister()` outside the kit lifecycle lock. `stop()` can take `activeSnapshot()` before registration; the late session is then added to the public `sessions` flow after teardown.
3. `ensureStarted()` rechecks `stopped` after data binding but not after suspending `pathObserver.start()`. If stop's bounded fallback finishes while observer start is blocked, the late starter launches the collector, latches `startResult`, and writes `Running` over `Stopped`.
4. Existing `KitLifecycleTest` covers a hung data-transport start and cancelled observer start, but deliberately cancels the late observer instead of releasing it, and has no stop race for advertising, discovery, or connect. CORE-T01 is therefore open.

### Comprehensive implementation plan

1. Introduce one private lifecycle generation gate in `P2pKitImpl`. Every public start/advertise/discover/connect operation captures the current generation before creating resources. `stop()` atomically makes the generation terminal before taking snapshots or closing resources. Concurrent `stop()` callers join one completion signal rather than starting overlapping teardown or returning while the leader is incomplete.
2. Thread the captured generation through `ensureStarted()`. Validate it under the gate before the start fast path, after every suspending data-transport bind, after observer startup, and at the final `startResult`/`Running` commit. A late observer start must execute bounded non-cancellable close and a late data bind must close terminal resources; neither may launch a collector or mutate terminal state.
3. Gate advertising/discovery success and failure state commits by generation. If terminal stop wins while a platform start is suspended, run the matching stop operation in bounded non-cancellable cleanup before returning the terminal lifecycle failure. Preserve `CancellationException` exactly when cancellation, rather than terminal invalidation, is the cause. Do not use this slice to claim ordinary multi-transport failure rollback; that remains CORE-11/CORE-T09 because retry-safe data-transport rollback needs a separate transport lifecycle decision.
4. Add an internal session-registration commit callback to `SessionManager`. Outgoing setup carries the exact kit generation; inbound setup requires only a currently active kit. `SessionStore.tryRegister()` and the accepted incoming publication decision execute only when the kit gate admits the commit. If rejected, close the uncommitted session in bounded non-cancellable cleanup and fail setup. If registration wins first, stop acquires the gate afterward and its session snapshot necessarily includes the committed session.
5. Recheck the generation before returning an existing/coalesced outgoing session. If stop already won, do not return a stale terminal handle. Preserve the existing coalescing, secure handshake, duplicate arbitration, capacity, and reconnect behavior.
6. Review every lifecycle state write and every `SessionManager` registration path for an equivalent late commit. No test timeout will be increased and no assertion will allow multiple terminal outcomes.

Expected files: `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/internal/P2pKitImpl.kt`, `SessionManager.kt`, `KitLifecycleTest.kt`, a focused common test fixture/test file if connect needs separation, and this tracker. No platform implementation or public interface is expected to change.

Concurrency/cleanup rules:

- The lifecycle mutex is never held across platform transport dial/start/stop, cryptography, raw I/O, or session close. It is held only for generation checks and the short atomic registration/state commit.
- Stale resource cleanup runs in `NonCancellable`, is individually bounded, logs failures, and never converts an original caller cancellation into a lifecycle success.
- `stop()` remains terminal and idempotent. Only one caller owns teardown; followers await its completion. A non-cooperative platform operation can exceed the stop bound, but its later completion cannot commit and must immediately execute stale cleanup.
- Lock ordering is lifecycle gate before the short `SessionStore` registration lock only at commit; no store path calls back into the lifecycle gate. Session/resource close occurs after releasing both locks.

Tests to add/update:

- stop while advertising start is parked: release it after `Stopped`; assert precise terminal failure, compensating stop, and permanent `Stopped` state;
- the equivalent discovery race;
- stop while a data-transport start is parked and then released (retain existing coverage, assert generation cleanup path);
- stop while path-observer start is parked, then release rather than cancel; assert no collector/state resurrection and observer cleanup;
- stop while outgoing transport connect is parked, then finish a real secure handshake; assert the connection/session is closed, `sessions` stays empty, and connect fails precisely;
- registration winning immediately before stop: assert stop observes/closes it and `sessions` is empty on return;
- concurrent stop callers: both complete only after the same teardown and resources close once where the contract permits counting;
- repeat the race suite to demonstrate deterministic synchronization without sleeps.

Acceptance criteria:

- No state write after terminal stop can produce `Running` or `Failed`.
- No outgoing or inbound session can enter public session state after the terminal generation is latched; a registration committed first is included in stop cleanup.
- Any advertising/discovery/observer/data resource that completes after losing the generation race is compensatingly closed/stopped before that operation returns.
- Cancellation remains a `CancellationException`; lifecycle invalidation has one precise post-stop failure; no errors are swallowed silently.
- All new CORE-T01 tests, full core JVM/iOS tests, affected Android compilation, samples/publication checks, and the repository gate show no new failure or warning. Existing independently registered baselines remain exact until their units are fixed.

### Implementation and review result

Status: `Verified`; implementation commit `a4e0bb0` and committed-state checks complete.

Confirmed root cause: the kit previously serialized only part of startup. The terminal Boolean was not an operation token, state writes were not lifecycle commits, and session registration happened after `stop()` took its active-session snapshot. A platform call that ignored or narrowly outran cancellation could therefore create a resource or publish state after terminal teardown.

Implementation:

- `P2pKitImpl` now owns one lifecycle mutex, monotonically changing generation, and one teardown completion signal. Every public start/advertise/discover/connect operation captures the generation; `stop()` atomically invalidates it and publishes `Stopping` before any resource snapshot.
- `Starting`, `Running`, and `Failed` writes are generation-checked commits. Data-transport and path-observer startup recheck after every suspending resource boundary. A stale completion performs individually bounded, logged, `NonCancellable` compensation before returning.
- Advertising and discovery validate before and after each transport. If terminal stop wins, the matching stop operation is applied across the transport set in reverse order, and neither success nor failure can overwrite `Stopped`.
- `SessionManager` receives an internal lifecycle gate. Outgoing dial ownership is rechecked immediately after transport return, and an uncommitted raw connection is closed before protocol setup. Session-store registration and accepted-incoming publication admission are one lifecycle commit, so either registration precedes stop and is present in its snapshot, or the candidate is rejected and closed.
- Existing/coalesced outgoing sessions are checked against the caller's captured generation before return. Concurrent `stop()` followers now wait for the leader's complete teardown rather than returning while resources remain live.
- Stale connection/session/resource cleanup is bounded at 2,000 ms per resource and logs timeout/failure. No public API, binary ABI, stored identity, security mode, protocol frame, or wire-version change was made.

Files changed:

- `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/internal/P2pKitImpl.kt`
- `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/internal/SessionManager.kt`
- `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/internal/KitLifecycleTest.kt`
- `P2PKIT_REMEDIATION_TRACKER_2026-07.md`

Tests added:

1. `lateAdvertisingCompletionIsRolledBackAfterStop`
2. `lateDiscoveryCompletionIsRolledBackAfterStop`
3. `observerThatReturnsAfterStopCannotResurrectKit`
4. `outgoingConnectThatReturnsAfterStopCannotPublishSession`
5. `sessionCommittedBeforeStopIsIncludedInTeardown`
6. `concurrentStopCallersJoinOneTeardown`

All new races use `CompletableDeferred` entry/release gates. They contain no arbitrary delay, relaxed terminal outcome, retry, or increased production/test timeout. The pre-existing `stopCompletesWhenATransportStartHangs` continues to cover the data-start boundary released after terminal stop.

External-PR review:

- Every lifecycle state write was searched. Non-terminal writes are now inside generation commits; only the stop leader writes `Stopping`/`Stopped`.
- Every `SessionManager` registration path, including inbound setup, goes through the lifecycle gate. The gate/store lock order is one-way; store code does not acquire the lifecycle gate. Platform start/dial/close, cryptography, protocol I/O, and session close do not run while the lifecycle mutex is held.
- Registration's store mutation is short and cancellation-safe: mutex acquisition happens before mutation, and the mutation body has no suspension. Loser/capacity session closes remain launched after store mutation. Incoming publication uses the already bounded flow capacity; exhaustion now removes and closes the candidate explicitly instead of suspending while holding the lifecycle commit.
- Equivalent implementations were searched in startup, discovery, observer, outgoing and incoming session setup. No platform source implements a second kit-level registration/state commit.
- Ordinary partial startup rollback without terminal stop remains CORE-11/CORE-T09. Store cleanup after all sessions close remains CORE-10. Prompt-cancellation ownership inside individual platform transports remains in the relevant LAN/provisioning findings. None is claimed fixed by this unit.

### Pre-commit verification evidence

| Command/check | Exact result |
| --- | --- |
| Focused `KitLifecycleTest` JVM suite during implementation | Passed after the late-connect regression test first exposed raw connection setup proceeding after terminal stop; the lifecycle check was moved directly after dial ownership transfer and the suite then passed |
| Six focused lifecycle tests, forced three times | All three runs passed with deterministic gates and one precise outcome per race |
| `./gradlew :p2p-core:jvmTest :p2p-core:iosSimulatorArm64Test :p2p-core:compileAndroidMain` | `BUILD SUCCESSFUL`; JVM 342 tests, iOS Simulator ARM64 323 tests, zero failures/errors/skips; Android common/main compilation passed |
| `./gradlew check` | Reached the exact registered independent baselines: `FileTransferFlowTest.cancelMidStreamPropagatesToReceiver` expected `Cancelled` but observed `Completed`; Android lint reported the registered `CoarseFineLocation` error plus three registered warnings. No CORE-01 test failed |
| `./gradlew :p2p-transport-lan:iosSimulatorArm64Test` | Reproduced the registered Apple LAN baseline exactly: 40 tests, two lifecycle timeouts, one intentionally skipped diagnostic |
| `scripts/check-publish-artifacts.sh` | Passed all 15 publication rows |
| `scripts/check-published-consumers.sh` | Passed isolated JVM, Android, KMP, and iOS consumer builds |
| `./gradlew :p2p-transport-lan:assembleP2pKitSharedReleaseXCFramework` | `BUILD SUCCESSFUL`; release XCFramework assembled |
| `xcodegen generate` | Passed |
| Isolated Swift sample `xcodebuild` using `build/DerivedDataCore01` | Exit 0 with code signing disabled for generic iOS Simulator. A preceding reuse of the default build database reported a lock after an ambiguous verbose invocation; isolation proved this was concurrent DerivedData ownership, not a source failure |
| `git diff --check` | Passed |

The first attempted Android task name, `compileDebugKotlinAndroid`, does not exist in this multiplatform module; this was a command-selection diagnostic, not a product failure. It was corrected to the module's authoritative `compileAndroidMain` task, which passed. Existing `ExperimentalCoroutinesApi` warnings in unrelated core tests are registered under BUILD-14; the new lifecycle test file introduced no warning.

Compatibility and remaining risk:

- Calls linearized before terminal generation invalidation may succeed and are then included in teardown; calls losing the generation fail with the existing post-stop `IllegalStateException` contract. There is no automatic retry or fallback.
- Incoming session pressure now has a precise bounded refusal at publication capacity rather than allowing setup to block the terminal lifecycle commit. The refused session is removed from the store and closed.
- Full repository verification is not green because the independently registered FILE-04/SAMPLE-17/BUILD-08 and Apple LAN failures remain. They do not intersect the lifecycle paths or invalidate the focused committed-state evidence.

Committed-state evidence for `a4e0bb0`: `git show --check` passed; the focused JVM `KitLifecycleTest` suite passed under `--rerun-tasks`; the complete iOS Simulator ARM64 suite and Android main compilation passed under `--rerun-tasks`. The forced full JVM suite reproduced only FILE-04 (`cancelMidStreamPropagatesToReceiver`, 342 tests/1 failure), exactly matching the registered baseline and leaving all CORE-01 tests green.

## Active batch: ID-STORE-01 — legacy PeerId persistence

| Item | Concise plan |
| --- | --- |
| Findings/gap | CORE-18, CORE-19, CORE-20, CORE-21, CORE-29, CORE-T11 |
| Root causes | Lossy AppId sanitizers are storage identities; first creation is read/generate/write without process or cross-process exclusion; fixed temp names/direct target fallback are crash-unsafe; failed writes are not memoized; JVM root selection accepts blank/unusable properties. |
| Implementation | Use a domain-separated full SHA-256 AppId namespace while retaining legacy read migration. Serialize JVM/Android commits with a process lock plus file lock, reread under lock, write via JVM fsync+atomic move and Android `AtomicFile`, and memoize every returned ID. On iOS store a hash-keyed dictionary in the existing sanitized NSUserDefaults bucket under a process/POSIX file lock, preserving current test/reset and rollback behavior. Validate JVM home/temp roots and never select `.` implicitly. |
| Main files | Common storage-key helper; JVM/Android `FilePeerIdStorage`; iOS `NSUserDefaultsPeerIdStorage`; JVM factory; JVM/iOS tests; affected Apple LAN test cleanup only if the persisted representation requires it. |
| Tests | Sanitizer collisions and long-prefix collisions; repeated same-instance calls after persistence failure; concurrent threads and child JVM processes; legacy migration/rollback; corrupt-record replacement without partial target; JVM property fallback; iOS hash-bucket collision/migration/concurrency with deterministic fakes where OS behavior is not injectable. |
| Compatibility/migration | Public API unchanged. Existing JVM/Android paths and iOS string values are read-only migration inputs and are not deleted, allowing rollback. Updated processes coordinate with each other; an concurrently running pre-update process cannot honor the new lock/record format and is outside the compatibility guarantee. Explicit plaintext mode remains deprecated. |
| Acceptance | Distinct AppIds never share writable storage; one durable winner is returned to concurrent creators; target writes are atomic; an instance never changes its returned ID after a write failure; no working-directory fallback; JVM/iOS tests and Android/iOS compilation pass with no new warnings. |

Implementation result: full UTF-16 AppId hashing (including malformed-surrogate distinction), legacy read-only migration, JVM/Android process+file locks, JVM unique fsynced temp/atomic move, Android `AtomicFile`, iOS hash-bucket records under NSLock/POSIX lock, instance memoization, and explicit JVM root validation are complete. Public API is unchanged.

Verification: focused JVM storage and kit-persistence suites pass; contention passed in three executions including 16 threads and four child JVM processes; complete JVM core is 351/351 green; isolated iOS storage tests pass; Android main and affected Apple LAN tests compile. The complete iOS run executed 326 tests and reproduced only the registered `SessionReconnectRotationTest` timeout; no identity test failed. Existing BUILD-14 warnings are unchanged.

Committed/pushed evidence: `ee69d09` is on `origin/remediation/full-register-2026-07`; forced committed-state JVM core (351 tests), isolated iOS identity tests, Android main compilation, and affected Apple LAN test compilation all pass.

## Completed batch: CORE-SESSION-01 — session ownership and terminal state

| Item | Concise plan |
| --- | --- |
| Findings/gaps | CORE-03, CORE-08, CORE-09, CORE-10, CORE-13, CORE-23; CORE-T02, CORE-T07, CORE-T08 |
| Root causes | Raw/session ownership is not represented through every cancellable connect boundary; registration diagnostics read a concurrently mutated map; terminal cleanup does not terminate the session-wide job; stop relies on asynchronous watchers; path state is an event rather than retained authority. SEC-01 already moved security and initial HELLO inside one outer deadline, but lacks a focused regression. |
| Implementation | Make connector ownership transactional with non-cancellable close and pending completion; publish an immutable registration map; add atomic store shutdown drain; cancel the session runtime only after terminal resource cleanup; retain versioned path state and apply it to every registration; inject only an internal setup-timeout test value while keeping the production constant. |
| Main files | `SessionManager`, `SessionStore`, `P2pSessionImpl`, `P2pKitImpl`/internal builder timeout threading, focused session/path/handshake tests. |
| Tests | Cancellation during dial return, HELLO/security setup, and registration wait followed by exact successful retry; concurrent registration diagnostics; remote CLOSE/failure job termination; stop with watcher deliberately unable to run; hung initial write/security bounded by the outer deadline; registration racing retained `Unsatisfied`. |
| Compatibility | No public API or wire change. Cancellation remains `CancellationException`; stop empties public state earlier; a session created while the authoritative path is unsatisfied may enter reconnect/failure before `connect()` returns, which is the intended correction. |
| Acceptance | No stale pending slot or unowned raw/session after cancellation; waiters get one exact result; no mutable-map data race; terminal jobs become inactive; stop returns with empty sessions independent of watchers; all setup phases obey one deadline; no session misses retained path loss. |

Implementation result: connector/raw/session ownership is transactional through cancellation; pending cleanup is non-cancellable; uncommitted sessions use forced terminal rollback; diagnostics read an immutable map; terminal cleanup cancels the session runtime last; shutdown atomically drains public/store state; setup uses one outer deadline; network-path authority is retained and applied to every registration. No public API or wire format changed.

Verification: focused JVM suites passed three times; the complete pre-final-hook JVM suite was 358/358 and the final post-dial cancellation test passed on JVM/iOS. The final forced JVM gate reproduced only the registered FILE-04 race; no CORE-SESSION-01 test failed. A later full JVM and full iOS run with the final source both passed. Exact committed-state verification at `82a9b41` found and corrected a replay-zero test-subscription race without changing timeouts; `SessionOwnershipTest` then passed three forced JVM repeats, focused iOS Simulator, and Android compilation. Only registered BUILD-14 warnings were emitted. Commits `68934be` and `82a9b41` are pushed on `remediation/full-register-2026-07`.

## Completed batch: PARSE-01 — bounded protocol parser and validation

Scope: `PROTO-01` through `PROTO-07` and the parser portions of `PT-T01` through `PT-T05`/`PT-T21`. `PROTO-08` is split into the owner-approved `PARSE-META-01` negotiated authenticated-envelope batch. The transfer transition portion of `PROTO-04`/`PT-T07` remains assigned to `XFER-01`.

Concise plan:

- Root causes: repeated whole-buffer/tail copies; generic-only payload cap; dispersed structural checks; permissive UTF-8/text validation; trace/log callbacks on the protocol path.
- Implementation: reusable cursor buffer with in-place decode; header-time per-packet caps; one encoder/decoder structural validator; strict bounded text codecs; stable DATA flags/LAST checks; trace isolation; rate-limited peer-controlled warnings.
- Main files: `ProtocolConstants`, `FrameCodec`, `FrameReader`, `DefaultP2pProtocol`, `HelloPayload`, `FileOfferPayload`, `Reassembler`, `FrameTrace`, and their common tests.
- Tests: fragmented/batched linear-work accounting, early header rejection, all packet invariant/cap boundaries, strict malformed UTF-8, outbound text boundaries, throwing trace, bounded logs, and deterministic randomized codec/reader/reassembler streams.
- Compatibility: secure-v2 and explicit legacy-v1 framing remain version-specific with no fallback; unknown packet types remain skippable for forward compatibility. Newly rejected frames were already malformed or exceeded newly documented limits. No public API/wire field is added in this batch.
- Acceptance: no payload-sized allocation before header validation; relocation work is linear in input; every known packet is validated before delivery; malformed text cannot be canonicalized silently; diagnostics cannot fail I/O or grow with attacker frame count; focused common JVM/iOS tests and affected target compiles pass.

Implementation/review result: commits `aa3ac0c` and `6171588` implement the parser/text portion and were reviewed as a 21-file protocol-only batch plus a six-file canonical-text follow-up. The reader buffers only the fixed header before validation, reuses one frame buffer, decodes from an offset window, and never copies an accumulated tail. Packet caps/shape rules are centralized and shared by encoder, direct decoder, and streaming reader. Known text is strict UTF-8 with invalid local Unicode rejected before encoding. Trace/log failures cannot alter protocol I/O, while `CancellationException` is preserved. Equivalent protocol decode/encode sites were searched; no second parser remained unchanged. Public ABI signatures and secure-v2/explicit-legacy framing are unchanged.

Tests added/strengthened: linear relocation and early header rejection, packet-family cap/shape tables, strict UTF-8/local Unicode/control/bidi/path/blank/boundary cases, stable DATA flags/LAST, FILE_DATA empty/total/LAST/post-size rules, throwing trace, bounded malformed/unknown logs, and deterministic randomized 500-frame fragmentation plus randomized reassembly.

Verification:

- Focused protocol JVM suite passed after the initial run exposed and corrected two assertion-message mismatches; no assertion was weakened.
- Final complete affected-module run: JVM `389/389`, iOS Simulator `364/364`; Android main and iOS Arm64 compilation passed.
- Parser/codec/reader/reassembler subset passed three forced JVM repeats.
- Exact committed state at `aa3ac0c`: full JVM `389/389`, focused iOS protocol `134/134`, Android/iOS device compilation passed.
- Exact final tip at `6171588`: full JVM plus focused iOS protocol passed; Android/iOS device compilation passed.
- `checkKotlinAbi` completed with the repository's ABI comparison task skipped by its own configuration; manual/public-diff review found no signature change.
- Repository-wide `./gradlew check --offline` reached and passed core, provisioning, desktop/sample, and KMP work, then failed only the registered unrelated baselines: LAN iOS `40` tests with the same two lifecycle timeouts and one intentional skip, and Android sample lint with the same `CoarseFineLocation` error plus three warnings. No PARSE-01 test failed and no new warning was introduced.

Remaining: `PROTO-04` stays `In Progress` solely for the ACCEPT/REJECT dispatcher transition assigned to XFER-01. `PT-T07` and `PT-T21` stay open for their dispatcher/transfer-transition conjuncts. `PROTO-08` remains separately blocked on the metadata API/wire decision.

`PARSE-META-01` approval record: on 2026-07-22 the owner approved a negotiated authenticated secure-v2 application-message envelope while legacy v1 remains metadata-free. `PROTO-08` and its envelope/compatibility tests are queued; no wire change is claimed before that focused batch lands.

`XFER-OFFER-API-01` implementation record: approved on 2026-07-22 and locally implemented in `ddb7142`. Authoritative `pendingFileOffers` state is immutable, admission ordered, bounded to 64 active offers, available to late subscribers, duplicate-safe, and removed on every terminal/expiry/close path. `FILE-05`, the receiver conjunct of `FILE-06`, `PT-T12`, `PT-T13`, and the retained-offer portion of `PT-T21` are Implemented; ABI and Kotlin/Java/KMP/Swift consumer gates pass.

`XFER-ERROR-API-01` implementation record: approved on 2026-07-22 and locally implemented in `ddb7142`. Structured `FileTransferFailed` exposes stable failure kind, phase, retryability, transfer ID, and bounded reason; the original cause is retained internally without entering value equality/copy. Intentional cancellation and rejection remain distinct terminal states, and `CancellationException` still propagates. `FILE-11` and its JVM/Apple/API/consumer compatibility tests are Implemented.

### XFER-01 concise implementation record

Commit `68c579f` is pushed. It verifies `PROTO-04` and `FILE-01/02/03/07/08/09/10/12/14`; `FILE-15` is implemented pending the explicitly external Android-provider instrumentation conjunct. Root change: dispatcher maps now own only phase/timer references; each transfer serializes state, progress, and source/sink ownership independently. Acceptance has bounded transactional compensation, accepted transfers have progress-sensitive idle plus overall deadlines, timeout roles are ordered, chunk arithmetic is bounded, IDs cannot overwrite, and platform helpers measure the opened resource. Public constructor/wire behavior is unchanged; the only observable runtime changes are deterministic timeout/cancellation/error cleanup and stricter invalid-configuration rejection.

Changed components: `FileTransferDispatcher`, incoming/outgoing handles, streaming sender/receiver, `FileTransferConfig`, Android/JVM helpers, common/JVM regression tests. Acceptance: no user I/O under the dispatcher map lock; cancellation preserves `CancellationException`; terminal state freezes progress and clears references; conforming unanswered offers have one exact sender state. Focused concurrency tests passed three forced JVM repeats; focused JVM/iOS tests and Android/iOS-device compilation passed again from the pushed commit. The complete JVM suite passed 401/401; a complete iOS run exposed three registered lifecycle/reconnect flakes outside this batch, and all three passed isolated forced reruns. Remaining local decision-independent risk: FILE-04 durability acknowledgement still permits the known completion/cancel race and is not claimed here.

### PEER-CTRL-01 concise implementation record

Decision-free scope: `CORE-02/04/05/14`, `CORE-T03..06`, plus safe portions of `CORE-17/28`. Discovery now retains one contribution per transport instance and merges routes/capabilities until the final source is lost or stale. Session registration records direction and never applies simultaneous-open arbitration to same-direction duplicates. Application messages use a count/byte-bounded delivery queue separate from protocol controls; overflow produces one terminal failure instead of silent loss. Keepalive deadlines use platform monotonic time and fire at the exact boundary. Binary messages and registry inputs are defensively snapshotted; device/manual-host/same-factory validation is early and deterministic. Complete core verification is 411/411 JVM and 386/386 iOS, with Android/iOS-device compilation green. Local commit `ee2cae5` passed focused committed-state JVM/iOS checks; push awaits `SCM-PUSH-01` approval.

`PEER-STATE-API-01` approval record: on 2026-07-22 the owner approved separate authoritative advertising/discovery feature-state flows while `P2pState` remains the core lifecycle. `CORE-15` is locally implemented in `18723c0` with default-accessor migration, concurrency, permission, failure-isolation, cleanup, ABI, and consumer tests.

`IMMUTABLE-MODEL-API-01` approval record: on 2026-07-22 the owner approved deep cast-proof snapshot values across the documented public model graph. `CORE-17` is queued with Kotlin/Java/Swift mutation, data-class migration, serialization, and ABI consumer tests.

`TRANSPORT-FACTORY-API-01` approval record: on 2026-07-22 the owner approved required descriptor-first capabilities, nullable data/discovery paths, and pre-build duplicate-kind rejection. `CORE-27` and the remaining `CORE-28` conjunct are locally implemented in `18723c0`; external-factory and ABI tests pass.

### CORE-LIFE-02 concise implementation record

Commit `60aecf6` implements `CORE-12`, `CORE-16`, `CORE-T10`, and the discovery portion of `CORE-11/CORE-T09`. Advertising and discovery now track every entered transport and compensate in reverse order on failure, cancellation, or loss of the lifecycle generation. Inbound acceptance uses one active collector with bounded exponential recollection after transient failure; Apple LAN now exposes a sequentially re-collectable channel flow. Terminal kit/session cleanup runs every resource attempt through an independently owned real-time deadline, so cancellation-ignoring or `NonCancellable` cleanup cannot hold structured teardown forever. Failures are logged, retained in one aggregate `P2pError.ConnectionFailed`, and shared exactly by concurrent/later `stop()` callers after state reaches `Stopped`.

Changed components: public lifecycle KDoc/transport recovery contract, `P2pKitImpl`, `P2pSessionImpl`, `SessionManager`, `LifecycleCleanup`, Apple LAN incoming flow, and deterministic lifecycle/accept/session-close tests. Compatibility: no signature or wire change; `DataTransport.incomingConnections()` now explicitly requires sequential recovery after abnormal completion, while preserving the single-active-collector guarantee. Focused tests were exercised repeatedly. Batch checkpoint results: core JVM 417/417, core iOS Simulator 392/392, Android/iOS and Apple LAN compilation green. The affected LAN iOS suite retained only its registered unrelated baseline (40 tests: two Bonjour lifecycle timeouts, one skipped diagnostic).

### LAN-CONN-01 concise implementation record

Commit `b21ed84` implements `LAN-04/05/06/07/08/09/23/24` and the complete local portions of `LAN-T04/05/06`. Apple now admits at most 16 already-started inbound connections and synchronously cancels overflow, post-close, and drained entries. JVM/Android dialers retain socket ownership until cancellation-safe commit. Apple dial cancellation owns and cancels the NWConnection. Raw read cancellation closes the native descriptor on all implementations; ordinary JVM/Android write failure closes and publishes `Closed`; Apple queued writers recheck terminal state inside the mutex. Fatal JVM/Android listener failure atomically depublishes its endpoint, and core recollection can rebind the same advertised port. Terminal close clears listener/socket references and port/registration state. The related CORE-12 follow-up gives JmDNS's documented five-second unregister wait a six-second resource deadline and bounds only `startMutex` acquisition, never the teardown body.

Changed components: `P2pKitImpl`; JVM/Android LAN data and raw transports; Apple LAN data/raw transport plus `BoundedInboundQueue`; deterministic JVM/Apple lifecycle, cancellation, write, queue, and recovery tests. There is no public API, ABI, persistent-data, or wire change. Focused JVM and Apple tests passed repeatedly; `:p2p-core:jvmTest` passed 417/417, `:p2p-core:iosSimulatorArm64Test` passed 392/392, and `:p2p-transport-lan:jvmTest` passed 58/58. JVM, Android, iOS Arm64, iOS X64, and iOS Simulator compilation passed. From committed state, the nine focused JVM tests and six focused Apple tests passed again with Android plus both additional Apple targets compiled. The complete Apple LAN checkpoint ran 44 tests and retained exactly the registered independent baseline: the two `IosLanLifecycleTest` Bonjour churn/lost timeouts plus one intentionally skipped diagnostic. Android real-device cancellation evidence and Apple descriptor/AWDL/browser recovery remain assigned to their still-open gap rows. Push is blocked only by `SCM-PUSH-01`; the implementation is therefore `Implemented`, not `Verified`.

Commands: focused `:p2p-transport-lan:jvmTest --tests ...` and `:p2p-transport-lan:iosSimulatorArm64Test --tests ...`; complete `:p2p-core:jvmTest`, `:p2p-core:iosSimulatorArm64Test`, `:p2p-transport-lan:jvmTest`, and `:p2p-transport-lan:iosSimulatorArm64Test`; cross-target `compileKotlinJvm`, `compileAndroidMain`, `compileKotlinIosArm64`, `compileKotlinIosX64`, and `compileKotlinIosSimulatorArm64`; `git diff --check` and committed diff review.

### LAN-JMDNS-02 concise implementation record

Commit `83db1ab` implements `LAN-01/02/10/25/26` and the local coverage in `LAN-T02/T03`. JVM and Android now share one transactional JmDNS coordinator. Service/listener tokens exist before blocking registration, so cancellation after an ambiguous side effect retains enough ownership to close or compensate it. Rebind commits its network markers only after every intended resource is restored; failures close the partial owner and retry with a budget scoped to that exact primary/default target pair. Listener leases deactivate callbacks before removal/handle replacement. Failed unregister, listener removal, handle close, Android callback unregister, or multicast-lock release retains ownership for a later stop instead of clearing it. Debounce-job replacement is serialized. No public API, wire, persistent-data, or security behavior changed.

Changed components: shared `JmdnsLifecycleCoordinator`; JVM and Android discovery transports; deterministic coordinator regression suite. Diff review found and fixed the equivalent JVM/Android implementations plus a repaired-binding duplicate-registration edge case. Focused coordinator results are 33/33 and passed before the checkpoint, inside the complete suite, and again from committed state. Real JVM discovery/record/heartbeat/loopback tests passed 7/7. The full LAN JVM checkpoint passed 73/73 with zero skips; Android, iOS Arm64, iOS X64, and iOS Simulator compilation passed. The only emitted compiler diagnostics are the registered pre-existing JmDNS deprecation and Android nullable-`CharSequence` warnings. Real Android callback/unregister/multicast behavior remains explicitly open in `LAN-T01`; Apple browser/listener/cache lifecycle remains in `LAN-03/11/12` and `LAN-T07/T10`. Push is blocked only by `SCM-PUSH-01`, so rows are `Implemented`, not `Verified`.

Commands: focused `:p2p-transport-lan:jvmTest --tests 'dev.p2pkit.transport.lan.JmdnsLifecycleCoordinatorTest'`; real JVM JmDNS tests for `JvmLanDiscoveryHeartbeatTest`, `JvmDiscoveryRecordValidationTest`, and `JvmLanLoopbackTest`; complete `:p2p-transport-lan:jvmTest`; cross-target `compileAndroidMain`, `compileKotlinIosArm64`, `compileKotlinIosX64`, and `compileKotlinIosSimulatorArm64`; `git diff --check`, staged/committed diff review, and the focused committed-state rerun.

### LAN-APPLE-03 concise implementation record

Commit `703182a` implements `LAN-03/11/12`, completes local gap `LAN-T10`, and completes the browser/listener/cache portions of `LAN-T07`. Root causes were conflated host intent and live native handles, blocking semaphore readiness, cancellation without retained terminal ownership, no self-recovery after a listener/browser terminal transition, ghost heartbeats while no browser was ready, a two-phase cache prune/Lost race, and removal records being rejected when Network.framework omitted TXT. Listener leases now await cancellable readiness and bounded native termination, retain cleanup ownership across cancellation/timeouts, clear callback-retention cycles, serialize start/rebind/close, retry missing listeners, and make close non-cancellable after terminal ownership begins. Browser leases separate host intent, generation, readiness, and recovery attempt; unexpected termination depublishes current ownership and schedules bounded recreation. Cache reconciliation, endpoint removal, Lost/Updated publication, and fresh confirmation are one native-lock transaction. Found records require Bonjour service identity to equal the validated TXT peer ID; incomplete removals may affect only an already cached peer whose complete record passed app/protocol/fingerprint validation.

Changed files: Apple data and discovery transports, one real Network.framework recovery suite, and a static-inline BSD bind probe in the existing native interop header. Six regressions cover cancelled listener start with retry, unexpected listener recovery without a path event, exact IPv4/IPv6 descriptor release, cleanup from an already-cancelled caller, unexpected browser generation recovery, and the gated stale-prune/fresh-confirmation race. The existing public lifecycle tests now prove clean peer loss and advertise stop/restart churn; both previously timed out for 30 seconds. No public API, wire format, persistent data, security-mode, source, or binary compatibility changed.

Verification: focused recovery plus former-timeout tests passed; complete Apple Simulator suite registered 50 tests, executed 49 with zero failures, and retained one explicitly manual 60-second external interop capture as ignored; the complete suite passed twice, including once from committed state. LAN JVM passed 73/73 with zero skips/failures. Android main, iOS Arm64, iOS X64, and iOS Simulator Arm64 compilation passed. The native header passed Clang Objective-C syntax with warnings as errors. `git diff --check`, equivalent native cancel/handler searches, exact staged diff review, and committed-state review found no unrelated change. Remaining Apple same-interface path-rotation/AWDL work stays in `LAN-13/17` and `LAN-T07`; cross-platform cache TTL/trust/address hardening stays in `LAN-14/16/18`. Push remains blocked only by `SCM-PUSH-01`, so rows are `Implemented`, not `Verified`.

### LAN-NET-SCHEMA-01 concise implementation record

Commits `2895ff1` and `09af6b5` implement `LAN-15/22` and the bounded-schema/service-instance-ownership portions of `LAN-16/LAN-T08`. One common parser now bounds every consumed DNS-SD TXT field to its 255-byte character-string, rejects blank required fields, terminal/bidi controls, malformed Unicode, wrong app/security version, excessive capability tags, and LAN records that do not claim LAN. Local advertising fails before JmDNS/native registration for oversized identity, app, capability, protocol, or fingerprint values; only the display name is truncated on a valid UTF-8 boundary. Network.framework setter rejection is now propagated. JVM/Android accept a removal only for the exact service instance last admitted by a fully validated active listener generation, so TXT-less JmDNS goodbyes work without trusting removal metadata and stale replacement listeners cannot withdraw newer ownership. Apple distinguishes absent from malformed removal TXT and restricts incomplete removal to its previously validated cache identity.

Diagnostics now replace control/bidi/malformed surrogate input and cap peer-controlled fields without splitting a surrogate pair. JVM applies this at its central sink. Apple makes the 200-line replay an explicit `retainHistory` opt-in while preserving delivery to already active subscribers; the Swift diagnostic sample opts in. Android routes routine LAN lifecycle/data/discovery logs through a default-off `AndroidLanDiag.enabled` gate and one sanitized tag; bounded cleanup warnings remain visible, and the official Android diagnostic sample opts in. This adds only public diagnostic opt-in flags, changes no wire version or persistent format, and preserves every valid v1/v2 discovery record. Oversized non-display local identifiers now fail explicitly rather than silently advertising a corrupt record; an oversized display name is intentionally shortened only on the discovery surface.

Regression coverage: common `PeerRecordValidationTest` covers outbound UTF-8 boundaries, invalid local capabilities, optional-field fallback, hostile size/control/app/capability inputs, and Unicode-safe diagnostic truncation. `JvmDiscoveryRecordValidationTest` uses a real crafted JmDNS advertiser to reject blank/other-app/service-TXT mismatch records, observe exact TXT-less `Lost`, and prove listener survival. `JvmLanDiagTest`, `IosLanDebugTest`, and `IosBonjourTest` cover sink sanitization, replay opt-in/live delivery, unknown-key allocation avoidance, native setter bounds, and strict malformed UTF-8 rejection.

Verification: focused JVM/Apple/Android checks passed throughout. The complete affected checkpoint passed LAN JVM 77/77 and Apple Simulator 56 executed/0 failed with the one pre-existing explicitly manual diagnostic ignored; Android main plus iOS Arm64/X64/Simulator compilation passed. The release XCFramework assembled and the Swift sample compiled/linked for arm64 and x86_64 generic iOS Simulator with code signing disabled. From committed `2895ff1`, 14 focused JVM tests and 15 focused Apple tests plus Android compile passed forced in 41 seconds. From committed `09af6b5`, a forced Android library/sample compile passed. Android lint reproduced exactly the registered independent baseline (one `CoarseFineLocation` error and three warnings), with no new diagnostic-gate issue. The full staged/committed diffs passed `git diff --check` and equivalent JVM/Android/Apple implementation searches. `LAN-14`, subnet/interface affinity and alternate candidates in `LAN-16/18`, Android real logcat/callback instrumentation, hostile-network hardware evidence, and the final repository gate remain open. Push remains blocked only by `SCM-PUSH-01`, so rows are `Implemented`, not `Verified`.

### Current execution record: LAN-NET-01 — native liveness, rotation, and route candidates

| Field | Value |
| --- | --- |
| Findings/gaps | `LAN-13/14/16/17/18`; local conjuncts of `LAN-T07/T08` |
| Shared root cause | Core's generic 15-second eviction and platform cache re-emission treated cached presence as liveness; each platform also collapsed DNS-SD routes to one address or allowed a stale/default network binding to survive rotation. |
| Status | Implemented locally; external/device acceptance and push remain open. |
| Implementation | Added an ABI-preserving discovery-lifetime contract (`TransportManaged` vs core stale timeout); removed synthetic JVM/Android/Apple cache heartbeats; made native removal/TTL ownership emit exact `Lost`; added forced same-network rebind and JVM interface/address fingerprint watching; selected Android Wi-Fi/Ethernet routes and shared the selected `Network` with TCP; enabled Apple peer-to-peer listener/dial parameters and same-interface address-change rebind; retained up to eight ordered, deduplicated JVM/Android IPv4/IPv6 candidates with same-subnet filtering when prefixes are known and bounded 1.5-second fallback attempts inside one 5-second monotonic dial budget. |
| Files changed | `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/internal/PeerRegistry.kt`; `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/transport/Internal.kt`; `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/internal/DiscoveryReemitContractTest.kt`; `p2p-transport-lan/src/androidMain/kotlin/dev/p2pkit/transport/lan/AndroidLanDataTransport.kt`; `AndroidLanDiscoveryTransport.kt`; `AndroidLanDsl.kt`; new `AndroidLanNetworkState.kt`; `p2p-transport-lan/src/appleMain/kotlin/dev/p2pkit/transport/lan/IosLanDataTransport.kt`; `IosLanDiscoveryTransport.kt`; `IosRawConnection.kt`; `p2p-transport-lan/src/appleTest/kotlin/dev/p2pkit/transport/lan/AnnounceCacheReconcileTest.kt`; `IosLanRecoveryTest.kt`; `p2p-transport-lan/src/commonMain/kotlin/dev/p2pkit/transport/lan/JmdnsLifecycleCoordinator.kt`; `Lan.kt`; `p2p-transport-lan/src/commonTest/kotlin/dev/p2pkit/transport/lan/PeerRecordValidationTest.kt`; `p2p-transport-lan/src/jvmMain/kotlin/dev/p2pkit/transport/lan/JvmLanDataTransport.kt`; `JvmLanDiscoveryTransport.kt`; `p2p-transport-lan/src/jvmTest/kotlin/dev/p2pkit/transport/lan/HostSelectorTest.kt`; `JmdnsLifecycleCoordinatorTest.kt`; `JvmLanDiscoveryHeartbeatTest.kt`; new `JvmLanCandidateFallbackTest.kt`; `p2p-transport-lan/src/nativeInterop/cinterop/p2pkit_nw.h`. |
| Regression coverage | `DiscoveryReemitContractTest` covers event-based expiry and transport-managed exact `Lost`; `HostSelectorTest` covers IPv4/IPv6 order, loopback/wildcard/link-local rejection, subnet affinity, invalid-prefix fallback, deduplication, and the eight-candidate cap; `JvmLanCandidateFallbackTest` proves failed-socket close before second-candidate success; coordinator tests cover forced same-network rebind surviving callback coalescing; Apple tests cover peer-to-peer parameters, same-interface address-change decisions, and cache reconciliation without synthetic `Updated`. |
| Commands/results | `git diff --check` — pass. Focused cross-platform command from the worktree (`:p2p-core:jvmTest` named lifetime/registry tests, LAN named selector/fallback/coordinator/schema tests, `:p2p-transport-lan:iosSimulatorArm64Test` named cache/recovery/schema tests, `:p2p-transport-lan:compileAndroidMain`) — `BUILD SUCCESSFUL` in 42s. `./gradlew :p2p-core:jvmTest --console=plain` — `BUILD SUCCESSFUL`, 417 tests/0 failures/0 skipped, 42s. `./gradlew :p2p-core:iosSimulatorArm64Test --console=plain` — `BUILD SUCCESSFUL`, 392/392/0, 50s. `./gradlew :p2p-transport-lan:jvmTest --console=plain` — `BUILD SUCCESSFUL`, 78/78/0, 2m03s. `./gradlew :p2p-transport-lan:iosSimulatorArm64Test --console=plain` — `BUILD SUCCESSFUL`, 60 tests/0 failures/1 pre-existing manual skip, 24s. `./gradlew :p2p-transport-lan:compileAndroidMain :p2p-transport-lan:compileKotlinIosArm64 :p2p-transport-lan:compileKotlinIosX64 :p2p-transport-lan:compileKotlinIosSimulatorArm64 --console=plain` — `BUILD SUCCESSFUL` in 6s. High-risk repeat (`JmdnsLifecycleCoordinatorTest`, `HostSelectorTest`, `JvmLanCandidateFallbackTest`, `IosLanRecoveryTest`, Android compile) — `BUILD SUCCESSFUL` in 14s. Committed-state focused command from `21bc2fc` (same cross-platform named set) — `BUILD SUCCESSFUL` in 41s. |
| Diff/equivalent review | Complete diff reviewed across common, JVM, Android, Apple, native interop, and tests. JVM/Android selectors remain behaviorally mirrored; Android's first primary callback skips only a network proven equal to the selected JmDNS binding; Apple listener/dial parameters both enable peer-to-peer; no new production warning appeared. Existing JmDNS `getInterface()` deprecation and core `ExperimentalCoroutinesApi` test opt-ins remain the registered BUILD-14 baseline. |
| Compatibility | No public constructor, binary ABI, wire frame, or persistent-data change. Discovery lifetime uses reserved metadata on the existing `TransportHint` value and preserves its constructor shape. Secure/legacy service namespaces remain segregated. Multi-address discovery is capped at eight hints; single-candidate dialing retains the 5-second budget, while alternates use bounded slices within the same global budget. |
| Remaining evidence/blockers | `LAN-T01` Android callback/multicast/unregister/logcat instrumentation; real Android selected-network and IPv6 behavior; native TTL/abrupt-departure behavior; physical Apple same-interface rotation and AWDL interoperability; two-machine hostile-network/spoof/off-subnet validation; and final repository gate remain open. `SCM-PUSH-01` blocks push, so these rows remain `Implemented`, not `Verified`. |
| Source control/post-commit | Branch `remediation/full-register-2026-07`; focused commit `21bc2fc` (`fix(lan): make native liveness and routes authoritative (LAN-13/14/16/17/18)`). `git show --stat --oneline HEAD` and `git diff HEAD^ HEAD --check` pass; committed-state focused command above passes. Push remains blocked by `SCM-PUSH-01`; no remote was changed or contacted. |

### Current execution record: PATH-PERM-01 — path generations, Android defaults, and manifest diagnostics

| Field | Value |
| --- | --- |
| Findings/gaps | `CORE-22/25/26`; `CORE-T12`; `CORE-23` remains previously verified by `CORE-SESSION-01` and is regression-covered here. |
| Shared root cause | Platform callbacks had no shared generation authority: Android swallowed unregister failures while clearing state and allowed a later start to race the old registration; iOS cancelled monitors without invalidating late callback writes. Android's default factory also discarded an already registered application context, and its construction diagnostic checked only two of four documented normal permissions. |
| Status | Implemented locally; physical platform evidence and push remain open. |
| Implementation | Added a common generation-gated callback state that clears retained networks/status only after successful native detach, rejects stale callbacks after close/restart, preserves ownership when detach fails, and keeps multi-network Satisfied/last-loss semantics. Android now wraps ConnectivityManager behind a monitor seam, retries failed unregister without attaching a second callback, resets to Unknown after successful close, and auto-wires from `P2pKitAndroid.initialize(context)` with an initialization-free no-op fallback. iOS now serializes generation/status writes with `NSLock`, invalidates the cancelled monitor before a subsequent generation, and resets status to Unknown on close. The Android construction diagnostic now checks all four install-time LAN permissions without exposing them through the runtime permission API. |
| Files changed | `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/NetworkPath.kt`; `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/internal/AndroidPlatformDefaults.kt` (new); `NetworkPathCallbackState.kt` (new); `NetworkPathObserverFactory.kt`; `NoOpNetworkPathObserver.kt`; `PermissionManagerFactory.kt`; `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/internal/NetworkPathCallbackStateTest.kt` (new); `PermissionGateTest.kt`; `p2p-core/src/androidMain/kotlin/dev/p2pkit/core/AndroidNetworkPathObserver.kt`; `android/P2pKitAndroid.kt`; `internal/AndroidNetworkPathObserverFactory.kt`; `internal/PermissionManagerFactory.android.kt`; `p2p-core/src/iosMain/kotlin/dev/p2pkit/core/internal/IosNetworkPathObserver.kt`. |
| Regression coverage | `NetworkPathCallbackStateTest` covers close clearing, stale callback rejection, multi-network last-loss, failed-unregister ownership retention/retry, and context-backed default selection; `PermissionGateTest.androidManifestDiagnosticCoversEveryDocumentedInstallTimePermission` covers the exact four-entry matrix; existing `NetworkPathRecoveryTest` covers six path/session lifecycle transitions without weakening timeouts or accepting alternate terminal states. |
| Commands/results | `git diff --check` — pass. Focused revised command (`:p2p-core:jvmTest` named `NetworkPathCallbackStateTest`, `NetworkPathRecoveryTest`, `PermissionGateTest`; `:p2p-core:iosSimulatorArm64Test` same named tests; `:p2p-core:compileAndroidMain`; all three iOS target compiles) — `BUILD SUCCESSFUL` in 19s; 15/15 JVM and 15/15 iOS focused tests, zero failures/skips. Complete affected command (`:p2p-core:jvmTest :p2p-core:iosSimulatorArm64Test :p2p-core:compileAndroidMain :p2p-core:compileKotlinIosArm64 :p2p-core:compileKotlinIosX64 :p2p-core:compileKotlinIosSimulatorArm64 :p2p-core:checkKotlinAbi`) — `BUILD SUCCESSFUL` in 1m33s; JVM 422/422/0 and iOS 397/397/0; ABI task was skipped because no baseline dump is configured. An exploratory Android-host test wiring was removed before commit because the unfiltered host target inherited common integration tests that require a real Android runtime (9/16 failed with Android stub `RuntimeException`); no host-test task remains as a required gate. |
| Diff/equivalent review | Reviewed the complete common/JVM-independent state, Android monitor/factory/permission paths, iOS monitor, KDoc, tests, and lifecycle interactions. Android and iOS both use the same generation state and invalidate callback writes before publishing a new status; Android unregister ownership is retained on failure and iOS cancellation is serialized with callback publication. No public constructor signature, wire format, persistent data, or runtime permission surface changed. |
| Compatibility | Existing `AndroidNetworkPathObserver(Context, P2pLogger)` source/binary constructor shape is preserved through a public secondary constructor; initialization-free Android remains the prior Unknown/no-op behavior. `P2pPermissionManager` still reports no runtime LAN permissions; the four normal permissions are diagnostic-only and remain manifest-owned. Successful observer close now reports Unknown until a fresh callback arrives. |
| Remaining evidence/blockers | Real Android callback registration/unregister failure, selected Wi-Fi/Ethernet route transitions, PackageManager/logcat warnings for each omitted normal permission, and iOS device monitor cancellation/restart remain physical/platform evidence. Push remains blocked by `SCM-PUSH-01`; rows remain Implemented, not Verified. |
| Source control/post-commit | Branch `remediation/full-register-2026-07`; focused commit `1903b11f9cf15f01a724f62eaa691e00ad74e516` (`fix(core): harden path observer generations and Android defaults`). `git show --check --stat --oneline HEAD`, `git diff HEAD^ HEAD --check`, and clean-status review pass; committed-state focused command — `BUILD SUCCESSFUL` in 17s with 15/15 JVM and 15/15 iOS focused tests and Android/all Apple target compiles. Push remains blocked by `SCM-PUSH-01`; no remote was changed or contacted. |

### Current execution record: PROV-A-HIGH-01 — terminal provisioning ownership under close/start/join races

| Field | Value |
| --- | --- |
| Findings/gaps | `PROV-A03`; shared terminal-gate portion of `PROV-A02`; local `PS-T01` conjuncts for parent cancellation and late callback ownership. |
| Shared root cause | `AndroidNetworkProvisioningManager.close()` and parent-job completion mutated hotspot/join handles outside the suspending `lifecycleLock`, so a callback completing after close could install a new reservation/binding that teardown had already missed. |
| Status | Implemented locally; physical callback/process-binding evidence and push remain open. |
| Implementation | Added a terminal `closed` gate and a synchronized handle-ownership lock. Explicit close and parent-job completion atomically mark the manager closed, detach/cancel watcher jobs, claim each current handle exactly once, and close claimed resources. Start/join callbacks install handles only through the ownership gate; if close won, late handles are immediately closed and the operation returns one deterministic `HotspotStopped`/`JoinFailed` result. Existing-handle paths recheck the gate before returning, and starts after parent cancellation are refused before wrapper invocation. Public `close()` signature and provisioning API remain unchanged. |
| Files changed | `p2p-network-provisioning-android/src/androidMain/kotlin/dev/p2pkit/provisioning/android/AndroidNetworkProvisioningManager.kt`; `p2p-network-provisioning-android/src/androidHostTest/kotlin/dev/p2pkit/provisioning/android/AndroidNetworkProvisioningManagerTest.kt`. |
| Regression coverage | `startAfterParentJobCancellationIsRefusedAndDoesNotAcquireReservation`; `closeRacingHotspotStartClosesLateReservationAndReturnsOneTerminalFailure`; `closeRacingJoinClosesLateBindingAndReturnsOneTerminalFailure`; existing parent-job reservation/binding cleanup, system-release, and start/join state tests remain in the suite. Deferred fake callbacks use `CompletableDeferred` gates, not sleeps or widened timeouts. |
| Commands/results | `git diff --check` — pass. Focused command (`:p2p-network-provisioning-android:testAndroidHostTest` with the three named race/parent tests plus `:p2p-network-provisioning-android:compileAndroidMain`) — `BUILD SUCCESSFUL` in 2s. Complete affected command (`:p2p-network-provisioning-android:testAndroidHostTest :p2p-network-provisioning-android:compileAndroidMain`) — `BUILD SUCCESSFUL` in 2s; 20/20 host tests, zero failures/skips. |
| Diff/equivalent review | Reviewed the full manager, parent-completion, explicit-close, hotspot callback, join callback, system-release, and test-fake paths. Both hotspot and join use the same atomic install/claim pattern; all resource closes remain idempotent and cancellation is not converted into a success. Existing Android wrapper behavior and callback contracts are unchanged. |
| Compatibility | No public API or wire/data format change; `close()` remains a non-suspending public method. Closing is now terminal and post-close/post-parent-cancel start/join calls return typed failures instead of acquiring resources outside teardown ownership. Existing successful start/join and stop/release behavior remains intact. |
| Remaining evidence/blockers | Real Android `LocalOnlyHotspotCallback`/`NetworkCallback` cancellation and callback ordering, process-binding unbind verification, OEM permission/location behavior, and PS-T01 physical instrumentation remain. `PROV-A01/A04/A05/A06/A07/A08/A09/A10/A11/A12` remain independent next-unit work. Push remains blocked by `SCM-PUSH-01`; rows remain Implemented, not Verified. |
| Source control/post-commit | Branch `remediation/full-register-2026-07`; focused commit `5bc31ce1f9a11c583096a0af7cd82305cb961b10` (`fix(provisioning): serialize Android teardown ownership`). `git show --stat --oneline HEAD`, `git show --check --oneline HEAD`, `git diff HEAD^ HEAD --check`, and clean-status review pass; committed-state complete affected command — `BUILD SUCCESSFUL` in 3s with 20/20 host tests and Android main compile. Push remains blocked by `SCM-PUSH-01`; no remote was changed or contacted. |

### Current execution record: PROV-A-01 — Android callback, binding, permission, and input ownership

| Field | Value |
| --- | --- |
| Findings/gaps | `PROV-A01/A04/A05/A06/A07/A08/A09/A10/A11`; local conjuncts of `PS-T01/PS-T02`. `PROV-A12` remains the physical callback test gap. |
| Shared root cause | Android provisioning treated native callback delivery, process-wide binding, joined-network address selection, and builder input constraints as independent best-effort operations. Cancellation/terminal races could leave a reservation or binding live while a typed failure was returned, and normal manifest omissions were not surfaced. |
| Status | Implemented locally; real Android callback/permission/process-binding evidence and push remain open. |
| Implementation | Added result-failure cleanup in `AndroidNetworkProvisioningManager`; introduced strict SSID/password validation and joined-address projection helpers; made hotspot reservation and join callback ownership atomic across cancellation, unavailable, reconnect, and close races; checked binding Boolean results and surfaced rejected/throwing binds; serialized process-wide binding ownership with `ProcessBindingArbiter`; limited snapshots to `ConnectivityManager.getLinkProperties(joinedNetwork)`; diagnosed the exact install-time permission matrix; and completed KDoc for the runtime/install-time split. No public API or wire format changed. |
| Files changed | `p2p-network-provisioning-android/src/androidMain/kotlin/dev/p2pkit/provisioning/android/AndroidNetworkProvisioningManager.kt`; `AndroidP2pPermissionManager.kt`; `AndroidDsl.kt`; `WifiManagerWrapperImpl.kt`; `ProvisioningAndroidValidation.kt` (new); `p2p-network-provisioning-android/src/androidHostTest/kotlin/dev/p2pkit/provisioning/android/AndroidNetworkProvisioningManagerTest.kt`. |
| Regression coverage | `failedStartedHotspotClosesReservationAndPublishesFailedState`; `joinInputValidationPreventsPlatformBuilderErrors`; `provisioningOwnershipAndManifestSeamsAreDeterministic` covers reservation/join pending ownership, process-binding arbitration, exact normal permissions, and address deduplication; existing manager cancellation, release, state, and failure tests remain. Callback continuations use deterministic gates/ownership checks; no timeout was widened. |
| Commands/results | `git diff --check` — pass. Focused command with the three new tests plus `:p2p-network-provisioning-android:compileAndroidMain` — `BUILD SUCCESSFUL` in 2s. Complete affected command (`:p2p-network-provisioning-android:testAndroidHostTest :p2p-network-provisioning-android:compileAndroidMain`) — `BUILD SUCCESSFUL` in 2s; host XML reports 23/23 tests, 0 failures, 0 skipped. The initial exploratory `:p2p-network-provisioning-desktop:jvmTest` task name was unavailable; corrected `:p2p-network-provisioning-desktop:test` — `BUILD SUCCESSFUL` in 2s, 6/6 tests, 0 failures, 0 skipped. |
| Diff/equivalent review | Reviewed the complete Android manager/wrapper/permission/DSL/helper/test diff and the unchanged JVM provisioning implementation/interface. Android hotspot and join paths now share explicit ownership/terminal semantics; JVM remains unsupported for programmatic Wi-Fi and retains its manual-IP behavior. Apple has no provisioning sidecar in this repository; common `NetworkState`/credentials contracts are unchanged. |
| Compatibility | No public API, ABI, wire, or persistence change. Existing runtime permission results remain target-SDK aware; three normal permissions are manifest-owned diagnostics only. Joined snapshots may now contain fewer addresses by design because unrelated interfaces are excluded. Rejected input returns typed `JoinFailed` before invoking Android builders. |
| Remaining evidence/blockers | Physical Android tests must exercise real `LocalOnlyHotspotCallback` and `NetworkCallback` cancellation/order, Boolean/exception bind and unbind behavior, queued reconnect callbacks, two-manager arbitration, `LinkProperties` AP subnet, target-SDK/merged-manifest/logcat permission matrix, and SDK/OEM input behavior. `PROV-A12`, `PS-T01`, and `PS-T02` remain open; the disposal API is locally implemented in the lifecycle batch. Push remains blocked by `SCM-PUSH-01`; rows remain Implemented, not Verified. |
| Source control/post-commit | Branch `remediation/full-register-2026-07`; focused commit `21dad61d0a3d6d36908dbca461c3a221dbb9cc10` (`fix(provisioning): harden Android callback and binding ownership`). `git show --stat --oneline HEAD`, `git show --check --oneline HEAD`, and `git diff HEAD^ HEAD --check` pass. Committed-state `:p2p-network-provisioning-android:testAndroidHostTest :p2p-network-provisioning-android:compileAndroidMain :p2p-network-provisioning-desktop:test` — `BUILD SUCCESSFUL` in 2s; Android host 23/23 and desktop 6/6, zero failures/skips. Push remains blocked by `SCM-PUSH-01`; no remote was changed or contacted. |

### Current execution record: PROV-D-01 — desktop validation, address ranking, and test isolation

| Field | Value |
| --- | --- |
| Findings/gaps | `PROV-D01/D02/D03/D04/D05`; `PS-T03`. |
| Shared root cause | Desktop provisioning accepted invalid polling intervals, treated every non-loopback interface address as a usable Wi-Fi indication, caught fatal failures as empty results, and left tests dependent on host interfaces and mutable process-global properties. |
| Status | Implemented locally; push remains blocked. |
| Implementation | Added a positive interval guard before scope initialization; introduced an internal address-scanner seam; narrowed polling recovery to `SocketException` while preserving cancellation/fatal propagation; filtered down/loopback/virtual/link-local candidates, deduplicated them, and ranked site-local LAN addresses first; made manual-info tests deterministic for both null and non-null outcomes; and removed `user.home` mutation from the manual-IP loopback test while retaining per-kit in-memory identity isolation. Public two-argument construction remains source/binary-compatible and the JVM unsupported hotspot/join behavior is unchanged. |
| Files changed | `p2p-network-provisioning-desktop/src/main/kotlin/dev/p2pkit/provisioning/desktop/JvmNetworkProvisioningManager.kt`; `p2p-network-provisioning-desktop/src/test/kotlin/dev/p2pkit/provisioning/desktop/JvmNetworkProvisioningManagerTest.kt`; `p2p-network-provisioning-desktop/src/test/kotlin/dev/p2pkit/provisioning/desktop/ManualIpLoopbackTest.kt`. |
| Regression coverage | `pollIntervalMustBePositive`; `addressSelectionDropsInactiveAndLinkLocalCandidates`; `fatalScannerErrorsAreNotConvertedToNoNetwork`; deterministic manual-info present/null tests; and the existing manual-IP loopback round trip now without process-global mutation. |
| Commands/results | `git diff --check` — pass. Complete affected command `:p2p-network-provisioning-desktop:test` — `BUILD SUCCESSFUL` in 2s; `JvmNetworkProvisioningManagerTest` 9/9 and `ManualIpLoopbackTest` 1/1, zero failures/skips. Exploratory `:p2p-network-provisioning-desktop:jvmTest` was unavailable (the module exposes `test`); no code failure was inferred from that task-name mismatch. |
| Diff/equivalent review | Reviewed the full desktop implementation/test diff and the Android provisioning contract; Android remains the only platform with programmatic Wi-Fi callbacks, while JVM continues to expose Unsupported hotspot/join and manual-IP fallback. KDoc now documents positive polling, candidate filtering, and JVM Wi-Fi/Ethernet limitations. |
| Compatibility | No public API or wire/persistence change; existing two-argument constructor shape and default 5-second polling remain. Manual info still returns null without a TCP port or usable addresses, and network state still uses `ConnectedToWifi`/`NoNetwork` with SSID null. |
| Remaining evidence/blockers | JVM cannot portably prove a default route or physical Wi-Fi association; real OS route/interface behavior remains outside host tests. The disposal API is locally implemented; push remains blocked by `SCM-PUSH-01`. |
| Source control/post-commit | Branch `remediation/full-register-2026-07`; focused commit `5efee8faad1d2e3a2c1c279097b3d6d8471f4bbd` (`fix(provisioning): validate desktop network discovery`). `git show --stat --oneline HEAD`, `git show --check --oneline HEAD`, and `git diff HEAD^ HEAD --check` pass. Committed-state `:p2p-network-provisioning-desktop:test` — `BUILD SUCCESSFUL` in 2s; `JvmNetworkProvisioningManagerTest` 9/9 and `ManualIpLoopbackTest` 1/1, zero failures/skips. Push remains blocked by `SCM-PUSH-01`; no remote was changed or contacted. |

### Current execution record: SAMPLE-HIGH-01 — consent, bounded storage, and iOS receive integrity

| Field | Value |
| --- | --- |
| Findings/gaps | `SAMPLE-01/02/03/04`; local conjuncts of `PS-T04/PS-T07`. |
| Shared root cause | Sample receivers treated authenticated peers as implicitly trusted for storage, opened destinations before consent/quota checks, and used UI/file adapters that could retain binary payloads or turn local I/O failure into an apparent success. |
| Status | Implemented locally; Apple and Android physical/UI evidence plus push remain open. |
| Implementation | Android, desktop CLI/GUI, and iOS now queue file offers without allocating a destination and expose explicit Accept/Reject controls or CLI commands. Acceptance enforces a 50 MiB sample quota and a 1 MiB free-space reserve before opening the file; teardown rejects pending offers. iOS sanitizes and atomically claims numbered destinations, synchronizes and records FileHandle write failures, surfaces them in the UI, and removes the partial file. Android converts incoming binary messages to a size-only timeline string before Compose state, so payload bytes are not retained. Public SDK APIs and wire behavior are unchanged. |
| Files changed | `iosApp/ContentView.swift`; `p2p-sample-android/src/main/java/dev/p2pkit/sample/android/MainActivity.kt`; `p2p-sample-android/src/main/java/dev/p2pkit/sample/android/P2pKitViewModel.kt`; `p2p-sample-desktop-ui/src/main/kotlin/dev/p2pkit/sample/desktop/ui/Main.kt`; `p2p-sample-desktop/src/main/kotlin/dev/p2pkit/sample/desktop/Main.kt`; `P2PKIT_REMEDIATION_TRACKER_2026-07.md`. |
| Regression coverage | Existing desktop `UniqueSaveFileTest` six-test atomic-claim suite remains green; Android, desktop CLI/GUI, and generated iOS simulator app compilation cover all consent/quota UI/state paths. Apple device tests must inject a FileHandle write failure and concurrent same-name offers; Android instrumentation must prove no binary payload retention and accept/reject/storage behavior. |
| Commands/results | `git diff --check` — pass. Initial `:p2p-sample-android:compileDebugKotlin` — `BUILD SUCCESSFUL` in 3s. Initial `:p2p-sample-desktop-ui:test :p2p-sample-desktop-ui:compileKotlin` — `BUILD SUCCESSFUL` in 3s. Initial CLI `:p2p-sample-desktop:compileKotlin` — `BUILD SUCCESSFUL` in 2s. Combined `./gradlew :p2p-sample-desktop:compileKotlin :p2p-sample-desktop-ui:test :p2p-sample-desktop-ui:compileKotlin :p2p-sample-android:compileDebugKotlin --console=plain` — `BUILD SUCCESSFUL` in 4s; desktop `UniqueSaveFileTest` 6/6, zero failures/skips, repeated in committed state. `./gradlew :p2p-transport-lan:assembleP2pKitSharedReleaseXCFramework --console=plain` — `BUILD SUCCESSFUL` in 2m03s. `xcodegen generate` — passed. Isolated generic-iOS-Simulator `xcodebuild` with `build/DerivedDataSampleHigh01` and code signing disabled — pre-commit and committed-state exit 0, `** BUILD SUCCEEDED **`; committed provenance reports `c4b31a1 (matches HEAD)`. There is no Swift unit-test target, so device/failure injection remains explicit rather than inferred from compilation. |
| Diff/equivalent review | Reviewed all four receive paths, consent lifecycle, quota/free-space ordering, destination claiming, sink teardown/failure reporting, and Android timeline representation. Android/desktop use their existing atomic `createNewFile` helpers; iOS now mirrors numbered atomic claims. Every receiving sample requires user consent before acceptance. Stale auto-accept documentation and an iOS teardown-order issue were corrected during review. |
| Compatibility | Sample UI behavior intentionally changes from auto-accept to explicit consent. SDK API/ABI, transfer protocol, and persistent formats are unchanged. The 50 MiB limit and 1 MiB reserve are sample policies, not library limits. |
| Remaining evidence/blockers | Real Android/iOS UI consent, offer timeout, low-storage, cancellation, same-name concurrency, and forced sink-write failure need physical/device evidence. A hostile authenticated LAN sender must exercise oversized/repeated offers and low-storage behavior on real filesystems. The absence of an iOS unit-test target blocks local Swift failure injection; `PS-T04/PS-T07` remain In Progress. Push remains blocked by `SCM-PUSH-01`. |
| Source control/post-commit | Focused code commit `c4b31a1` (`fix(samples): require consent for incoming files`). `git show --stat --oneline HEAD`, `git show --check --oneline HEAD`, and `git diff HEAD^ HEAD --check` pass. Committed-state Gradle sample gate — `BUILD SUCCESSFUL` in 4s; desktop `UniqueSaveFileTest` 6/6. Committed-state provenance-fresh generic-iOS-Simulator `xcodebuild` — `** BUILD SUCCEEDED **`, reports `c4b31a1 (matches HEAD)`. Tracker-only follow-up records this documentation update. Push not attempted because `SCM-PUSH-01` remains unapproved. |

### Current execution record: SAMPLE-XFER-01 — transfer cleanup, bounded histories, and path claims

The `fix(samples): harden transfer cleanup and histories` shorthand in the SAMPLE-XFER finding rows denotes focused commit `9102b78`.

| Field | Value |
| --- | --- |
| Findings/gaps | `SAMPLE-05/06/07/12/13/14/15/22/23/24/34`; local conjuncts of `PS-T04/PS-T05/PS-T06/PS-T07/PS-T08`. |
| Shared root cause | Sample transfer ownership was split across never-ending collectors and cancellation paths, while destination claims and UI histories lacked one bounded failure/size contract. |
| Status | Implemented locally; physical/device evidence and push remain open. |
| Implementation | Android and desktop transfer watchers now use one terminal state watcher plus a cancellable byte child; non-cancellable cleanup closes incoming streams and deletes every non-completed destination. Desktop UI/CLI claim helpers sanitize controls/separators/dot segments, atomically create numbered candidates, and fail explicitly after 10,000 alternatives; Android uses the same bounded claim contract. iOS receive cleanup removes non-completed files, task-owned collectors replace detached tasks, `kit.stop()` quiesces SDK writers before transfer watcher cancellation, and transfer/message histories are bounded (24 rows and 256 KiB message bytes). Desktop room history stores binary messages as size text only. |
| Files changed | `iosApp/ContentView.swift`; `p2p-sample-android/src/main/java/dev/p2pkit/sample/android/P2pKitViewModel.kt`; `p2p-sample-desktop-ui/src/main/kotlin/dev/p2pkit/sample/desktop/ui/Main.kt`; `p2p-sample-desktop-ui/src/main/kotlin/dev/p2pkit/sample/desktop/ui/UniqueSaveFile.kt`; `p2p-sample-desktop-ui/src/test/kotlin/dev/p2pkit/sample/desktop/ui/UniqueSaveFileTest.kt`; `p2p-sample-desktop/src/main/kotlin/dev/p2pkit/sample/desktop/Main.kt`; `P2PKIT_REMEDIATION_TRACKER_2026-07.md`. |
| Regression coverage | Desktop `UniqueSaveFileTest` now has seven tests: repeated/extensionless/leading-dot/pre-existing/concurrent claims, deterministic unwritable-directory failure, and path/dot-segment containment. Android, desktop CLI/GUI, XCFramework, and generated iOS app compile paths cover the platform cleanup and history code. |
| Commands/results | `git diff --check` — pass. `./gradlew :p2p-sample-desktop:compileKotlin :p2p-sample-desktop-ui:test :p2p-sample-desktop-ui:compileKotlin :p2p-sample-android:compileDebugKotlin --console=plain` — pre-commit `BUILD SUCCESSFUL` in 4s and committed-state `BUILD SUCCESSFUL` in 5s; `UniqueSaveFileTest` 7/7, zero failures/skips. `./gradlew :p2p-transport-lan:assembleP2pKitSharedReleaseXCFramework --console=plain` — `BUILD SUCCESSFUL` in 2m13s. `xcodegen generate` — passed. `xcodebuild -project p2pkit-sample.xcodeproj -scheme p2pkit-sample -configuration Debug -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath ../build/DerivedDataSampleXfer01 CODE_SIGNING_ALLOWED=NO build` — pre-commit `** BUILD SUCCEEDED **`; committed-state `xcodebuild -project p2pkit-sample.xcodeproj -scheme p2pkit-sample -configuration Debug -sdk iphonesimulator -destination 'generic/platform=iOS Simulator' -derivedDataPath ../build/DerivedDataSampleXfer01Committed CODE_SIGNING_ALLOWED=NO build` — `** BUILD SUCCEEDED **`, provenance reports `9102b78 (matches HEAD)`. |
| Diff/equivalent review | Reviewed the complete six-file code diff and equivalent Android, desktop CLI/GUI, and Apple receive/send paths. All platform claim helpers now have the same bounded atomic contract; all transfer collectors have terminal/cancellation cleanup; iOS stop ordering and Swift task ownership were corrected; KDoc/comments were checked for stale auto-accept, unbounded-collector, and unsafe-claim descriptions. |
| Compatibility | No SDK API/ABI, wire protocol, persistence, or transfer-state changes. Sample-only history caps are 500 rows/256 KiB on JVM/Android GUI and 200 rows/256 KiB on iOS; transfer history is capped at 24 on iOS and existing Android/desktop caps remain. Explicit claim exhaustion now rejects an offer rather than returning an occupied path. |
| Remaining evidence/blockers | Physical Android/iOS accept cancellation, partial-file deletion, same-name collision, low-storage, forced sink failure, long-run byte-budget, and hostile path/dot-segment evidence remain. CLI/UI lifecycle and Swift collector cancellation require runtime/device instrumentation; no iOS unit-test target exists. Scoped IPv6 remains in SAMPLE-LIFE-01/PS-T08. Push remains blocked by `SCM-PUSH-01`; no remote was changed or contacted. |
| Source control/post-commit | Focused code commit `9102b78` (`fix(samples): harden transfer cleanup and histories`). `git show --stat --oneline HEAD`, `git show --check --oneline HEAD`, `git diff HEAD^ HEAD --check`, and staged diff checks pass; committed-state Gradle sample gate and provenance-fresh generic-iOS-Simulator build pass. Tracker-only follow-up is this documentation commit; rows remain Implemented, not Verified, because push and external evidence are outstanding. |

### Current execution record: SAMPLE-LIFE-01 — lifecycle ownership, deterministic CLI, and sample diagnostics

| Field | Value |
| --- | --- |
| Findings/gaps | `SAMPLE-08/09/10/11/16/17/18/19/20/21/25/26/27/28/29/30/31/32/33/36/37/38/39`; local conjuncts of `PS-T04/PS-T05/PS-T06/PS-T08/PS-T09`. |
| Shared root cause | Sample lifecycle ownership and user-input/output policy were spread across UI callbacks, disposable scopes, and ad hoc CLI parsing, allowing partial startup, failed stop, ambiguous targets, malformed endpoints, or cancellation to leave stale ownership or misleading state. |
| Status | Implemented locally; physical UI/lifecycle/permission, hostile-network, and push evidence remain open. |
| Implementation | Android and desktop startup now publish Running only after advertise and discover both succeed, roll back partial starts, serialize feature toggles, and keep create/stop failure state retryable. Desktop cleanup survives composition disposal. Android declares the required normal/location permissions, preserves permission-diagnostic failures, masks the hotspot password, and keeps durable `onCleared` cleanup. The CLI has a strict named/positional parser, explicit ambiguous-target errors, truthful close output, terminal-text sanitization, and full start/REPL/job/kit `try/finally` ownership. Manual desktop endpoints accept bracketed, unbracketed, and scoped IPv6. The KMP demo always closes its session and kit. iOS peer equality includes mutable metadata, cross-check warnings are signature-bounded per start, unknown transfer states fail terminally, and failed stop retains ownership. |
| Files changed | `iosApp/ContentView.swift`; `p2p-sample-android/src/main/AndroidManifest.xml`; `p2p-sample-android/src/main/java/dev/p2pkit/sample/android/MainActivity.kt`; `p2p-sample-android/src/main/java/dev/p2pkit/sample/android/P2pKitViewModel.kt`; `p2p-sample-desktop-ui/src/main/kotlin/dev/p2pkit/sample/desktop/ui/Main.kt`; `p2p-sample-desktop-ui/src/main/kotlin/dev/p2pkit/sample/desktop/ui/ManualEndpoint.kt`; `p2p-sample-desktop-ui/src/main/kotlin/dev/p2pkit/sample/desktop/ui/TerminalText.kt`; `p2p-sample-desktop-ui/src/test/kotlin/dev/p2pkit/sample/desktop/ui/ManualEndpointTest.kt`; `p2p-sample-desktop-ui/src/test/kotlin/dev/p2pkit/sample/desktop/ui/TerminalTextTest.kt`; `p2p-sample-desktop/build.gradle.kts`; `p2p-sample-desktop/src/main/kotlin/dev/p2pkit/sample/desktop/CliOptions.kt`; `p2p-sample-desktop/src/main/kotlin/dev/p2pkit/sample/desktop/Main.kt`; `p2p-sample-desktop/src/test/kotlin/dev/p2pkit/sample/desktop/CliOptionsTest.kt`; `sample-kmp-shared/src/commonMain/kotlin/dev/p2pkit/sample/kmp/Demo.kt`; `P2PKIT_REMEDIATION_TRACKER_2026-07.md`. |
| Regression coverage | `CliOptionsTest` covers named/positional/help/unknown/duplicate behavior (5/5). `ManualEndpointTest` covers DNS, IPv4, bracketed/unbracketed IPv6, and scope IDs (3/3). `TerminalTextTest` covers control sanitization (1/1); `UniqueSaveFileTest` remains 7/7. KMP loopback/callsite tests pass 2/2. Android lint covers the manifest correction. Deterministic runtime injection on physical/UI surfaces remains explicit rather than inferred from compilation. |
| Commands/results | `git diff --check` — pass. Compile-first `./gradlew :p2p-sample-desktop:compileKotlin :p2p-sample-desktop-ui:compileKotlin :p2p-sample-android:compileDebugKotlin :sample-kmp-shared:compileKotlinJvm --console=plain` — `BUILD SUCCESSFUL` in 7s. Focused JVM command `./gradlew :p2p-sample-desktop:test :p2p-sample-desktop-ui:test :p2p-sample-desktop-ui:compileKotlin :p2p-sample-android:compileDebugKotlin :sample-kmp-shared:jvmTest --console=plain` — `BUILD SUCCESSFUL` in 20s; CLI 5/5, desktop UI 11/11, KMP 2/2, zero failures/skips. Android `:p2p-sample-android:lintDebug` — `BUILD SUCCESSFUL` in 10s. Complete pre-commit affected command adding lint — `BUILD SUCCESSFUL` in 19s; committed-state repeat — `BUILD SUCCESSFUL` in 17s. Release XCFramework — pre-commit `BUILD SUCCESSFUL` in 2m43s and committed-state `BUILD SUCCESSFUL` in 2m21s. `xcodegen generate` passed. Generic-iOS-Simulator `xcodebuild` with code signing disabled — pre-commit and committed-state `** BUILD SUCCEEDED **`; committed provenance reports `b90b8bd (matches HEAD)`. |
| Diff/equivalent review | Reviewed the complete 14-file code diff and equivalent Android, desktop CLI/GUI, KMP, and Apple startup, stop, feature-toggle, endpoint, session, and transfer-terminal paths. KDoc/comments were cleaned for fresh-kit ownership and supported endpoint syntax. Cancellation is preserved, cleanup is ordered before ownership release, and no assertion or timeout was weakened. |
| Compatibility | No SDK public API/ABI, wire protocol, or persistent-format change. Sample CLI launch parsing now rejects unknown/duplicate options and ambiguous targets; known named and positional forms remain. Android adds normal manifest permissions and no longer displays the password. `runDiscoverAndGreet` now enforces its documented fresh-kit lifecycle ownership. iOS treats unknown future transfer states as a visible terminal failure. |
| Remaining evidence/blockers | Physical Android permission/lifecycle/toggle/partial-start/`onCleared` behavior; desktop create/stop failure, rapid-toggle, and window-disposal injection; packaged CLI close/REPL/shutdown failures and hostile terminal names; physical scoped-IPv6 connections on every sample; KMP Android/iOS runtime; and iOS metadata/log-rate/unknown-state/stop-failure injection remain. Hostile-network and device evidence cannot be replaced by local compilation. Push remains blocked by `SCM-PUSH-01`. |
| Source control/post-commit | Focused code commit `b90b8bd4074e64e803de46819aaa5e9bf8db3430` (`fix(samples): harden lifecycle and CLI behavior`). `git show --stat --oneline HEAD`, `git show --check --oneline HEAD`, and `git diff HEAD^ HEAD --check` pass. All committed-state Gradle, XCFramework, project generation, and generic-iOS-Simulator checks above pass. Tracker-only follow-up is this documentation commit; rows remain Implemented, not Verified. Push not attempted because `SCM-PUSH-01` remains unapproved. |

### Current execution record: REL-PROV-01 — reproducible provenance and isolated iOS launcher

| Field | Value |
| --- | --- |
| Findings/gaps | `BUILD-03/07/09/10/11/12`, `LAN-21`; deterministic follow-up coverage for `LAN-14` and the service-identity conjunct of `LAN-T08`. |
| Shared root cause | Build identity mixed source provenance with wall-clock/checkout-local state, XCFramework stamps were undeclared side effects attached to broad task-name matching, and the launcher selected by non-unique name while sharing mutable paths. The committed affected gate also exposed that a real UDP-goodbye assertion assigned a deterministic deadline to multicast delivery and that JVM/Android removal callbacks could nest listener-generation locks. |
| Status | Implemented locally; real simulator/device launch, hostile-network departure, push, and final release-gate evidence remain open. |
| Implementation | Typed buildSrc tasks and configuration-cache-safe Git value sources generate stable BuildInfo and exact XCFramework commit/state/SHA-256 sidecars from declared relevant tracked/untracked inputs; invalid XCFramework Git identity fails closed and deletion repairs declared outputs. Xcode uses one verifier path. The launcher resolves one exact UDID before building, rejects ambiguous names, uses isolated per-run directories/logs, and serializes xcodegen/framework mutation. BuildInfo retains deprecated `BRANCH`/`BUILD_TIME` constants with stable values. The follow-up extracted JVM service-admission ownership for deterministic TXT-less removal tests and made JVM/Android cross-generation active reads lock-free, removing nested callback locks. |
| Files changed | REL-PROV commit: `CLAUDE.md`; `docs/STABILIZATION_AND_RELEASE.md`; `iosApp/build.gradle.kts`; `iosApp/scripts/check-xcframework.sh`; `p2p-core/build.gradle.kts`; `p2p-transport-lan/build.gradle.kts`; `scripts/run-ios-app.sh`; `scripts/tests/run-ios-app-test.sh` (new); nine new `buildSrc/src/main/java/dev/p2pkit/build/{GenerateBuildInfoTask,GitCommitTimeValueSource,GitCommitValueSource,GitDirtyValueSource,GitProcess,ProvenanceFiles,VerifyBuildInfoTask,VerifyXcframeworkProvenanceTask,WriteXcframeworkProvenanceTask}.java`. Corrective commit: JVM/Android `*LanDiscoveryTransport.kt`; `JvmDiscoveryRecordValidationTest.kt`; `JvmLanDiscoveryHeartbeatTest.kt`; new `JvmServiceAdmissionsTest.kt`. This tracker is the separate documentation follow-up. |
| Regression coverage | Launcher fixture 7/7 covers syntax, duplicate-name refusal, exact UDID, parenthesized names, unavailable IDs, unique run directories, and lock rejection/reacquisition. BuildInfo/XCFramework tasks prove stable repeat, exact content, configuration-cache reuse, deleted-sidecar recovery, and an untracked-input fingerprint change/restoration. `JvmServiceAdmissionsTest` 5/5 proves exact one-shot mapping, unadmitted removal rejection, stale/current generation rules, deduplicated terminal drain, and post-deactivation suppression; real multicast tests retain discovery, validation, listener survival, and 35-second idle liveness without imposing a deadline on UDP goodbye. |
| Commands/results | `git diff --check` and staged/committed checks — pass. Focused provenance command (`:p2p-core:verifyBuildInfoReproducibility`, `:iosApp:checkIosLauncherScripts`, release XCFramework verifier, `--configuration-cache`) — pre-commit 3s then 696ms reuse; committed final code HEAD 1m3s rebuild then 831ms reuse, 7/7 launcher tests, all generated/assembly/writer outputs UP-TO-DATE on repeat. Deleted-sidecar run restored the exact file with assembly UP-TO-DATE; untracked probe changed fingerprint `337c2d…a5e` to `19d1d0…ff57`, then removal restored the exact baseline. Pre-commit affected platform command (`core/LAN jvmTest`, Android compile, core/LAN iOS Simulator tests) — `BUILD SUCCESSFUL` in 2m33s; retained reports are core JVM 422/422, core iOS 397/397, LAN iOS 60 executed/0 failed/1 manual skip. The first clean committed repeat correctly failed 1/78 LAN JVM tests at the UDP-goodbye deadline; traced isolation passed and proved multicast timing, so the assertion was moved—not relaxed—to deterministic ownership. Corrected focused classes plus Android compile passed in 1m11s; complete LAN JVM/Android passed 83/83 in 1m46s and again from committed `639c508` in 1m50s. `sh iosApp/scripts/check-xcframework.sh` passes at final HEAD with source state clean. `xcodegen generate` passes; generic iOS Simulator Xcode builds pass before and after commit, with the existing no-op `await` warning retained for `BUILD-14`. |
| Diff/equivalent review | Reviewed all 17 REL-PROV files, generated BuildInfo/sidecars, task graph, executable modes, docs/KDoc, and the complete corrective diff. JVM and Android listener-generation code now has the same lock ordering; Apple already owns generation reconciliation separately. No assertions were weakened, no timeout increased, and the nondeterministic terminal result is recorded rather than retried away. |
| Compatibility | No SDK type or binary member was removed, and no wire/persistent format changed. Deprecated BuildInfo constants remain source/binary-visible but now contain stable compatibility values; `describe()` reports source commit time. New Gradle verification tasks are additive; raw XCFramework assembly no longer creates provenance by an undeclared side effect, so callers requiring attestation must use the documented verifier. `runIosSimulator` now rejects ambiguous names/concurrent launchers and may require `SIM_UDID`; successful default run directories are removed while failed runs are retained. |
| Remaining evidence/blockers | Physical simulator install/container-hash/launch and real concurrent launcher behavior; physical Android parity; clean and abrupt native departure/TTL under loss, interface rotation, and two-machine hostile networks; cryptographic/release external gates; and final warning/static/full-repository gates remain. The final Xcode build's no-op `await`, JmDNS deprecation, and core coroutine opt-in warnings are assigned to `REL-GATE-01/BUILD-14`. Push was not attempted because `SCM-PUSH-01` remains unapproved. |
| Source control/post-commit | Branch `remediation/full-register-2026-07`; provenance commit `e19c5c5740f171796973b0dab2a272ba17567255` (`fix(build): make provenance reproducible`); deterministic LAN follow-up `639c50819f8347e5d1b316d4f201617f7073e6bd` (`fix(lan): make removal ownership deterministic`). Both committed diffs were reviewed and pass `git show --check`; final code-state verification above targets `639c508`. Tracker-only follow-up is this documentation commit. No remote was changed or contacted. |

### Current execution record: REL-GATE-01 — warning-free compiler, Android lint, and repository gates

| Field | Value |
| --- | --- |
| Findings/gaps | `BUILD-08/14/15`; completed the build/lint/storage-policy conjunct of `PS-T04` without claiming its physical Android runtime matrix. |
| Shared root cause | Project diagnostics were informational and split across Kotlin, Java, Gradle DSL/deprecation, Android lint, and Swift. The Android sample also targeted an obsolete API, relied only on `allowBackup=false`, repeated its label, and used raw filesystem space instead of Android's reclaim-aware storage preflight. |
| Status | Implemented locally. The deterministic local static/full gate is green; target-37 physical behavior, `PS-T04`, external validations, and push remain open. |
| Implementation | All subproject Kotlin compilations use `allWarningsAsErrors`; Java uses `-Werror`; Gradle deprecations use `warning.mode=fail`; the generated Xcode project enables Swift warnings-as-errors; and Android lint treats warnings as errors. Registered coroutine, JmDNS, cinterop, Compose accessor, and Swift diagnostics were corrected. Android compile/target SDK moved 36→37, the sample explicitly excludes every credential/device-protected domain from Android 12+ cloud backup and device transfer while retaining `allowBackup=false`/`fullBackupContent=false`, removes the redundant activity label, and uses `StorageManager.getAllocatableBytes(getUuidForPath(...))` on API 26+ with the existing API-24/25 fallback. |
| Files changed | `build.gradle.kts`; `gradle.properties`; `gradle/libs.versions.toml`; `iosApp/ContentView.swift`; `iosApp/project.yml`; `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/internal/SendErrorContractTest.kt`; `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/testfixtures/FixtureContractTest.kt`; `p2p-sample-android/build.gradle.kts`; `p2p-sample-android/src/main/AndroidManifest.xml`; `p2p-sample-android/src/main/java/dev/p2pkit/sample/android/P2pKitViewModel.kt`; new `p2p-sample-android/src/main/res/xml/data_extraction_rules.xml`; `p2p-sample-desktop-ui/build.gradle.kts`; `p2p-transport-lan/build.gradle.kts`; `p2p-transport-lan/src/jvmMain/kotlin/dev/p2pkit/transport/lan/JvmLanDiscoveryTransport.kt`. This tracker is the separate documentation follow-up. |
| Findings/test gaps covered | Forced compilation first reproduced 28 coroutine opt-in diagnostics (14 on each JVM/iOS test compilation), the deprecated JmDNS accessor, lazy-cinterop DSL deprecation, deprecated Compose dependency accessors, and the Swift no-op `await`. Forced Android lint reproduced four current warnings: old target API, redundant activity label, missing Android 12 extraction rules, and raw `usableSpace`. All source/static conjuncts are now covered; `PS-T04` remains In Progress for runtime instrumentation. |
| Commands/results | Inventory `./gradlew :p2p-core:compileTestKotlinJvm :p2p-core:compileTestKotlinIosSimulatorArm64 :p2p-transport-lan:compileKotlinJvm :p2p-transport-lan:compileAndroidMain :p2p-network-provisioning-android:compileAndroidMain :p2p-sample-desktop-ui:compileKotlin :p2p-sample-android:compileDebugKotlin --rerun-tasks --warning-mode all --console=plain` passed in 14s while reproducing the registered warnings. Focused warning/lint command including core JVM/iOS test compiles, LAN JVM/Android, Android sample compile/lint passed: 85/85 tasks in 33s; lint text is exactly `No issues found`. Forced desktop/cinterop correction passed 13/13 tasks in 19s without source diagnostics. Complete affected command (`core/LAN` JVM and iOS Simulator tests, Android library/sample compile+assembly+lint, desktop UI tests) passed 135/135 tasks in 2m43s: core JVM 422/422, core iOS 397/397, LAN JVM 83/83, LAN iOS 60 executed/0 failed/1 documented manual interop skip, desktop UI 11/11. `./gradlew check --rerun-tasks --console=plain` passed 136/136 tasks in 3m48s. Pre-commit release XCFramework passed in 2m22s; `xcodegen generate` and the generic-iOS-Simulator Xcode build passed with `SWIFT_TREAT_WARNINGS_AS_ERRORS=YES`. The first sandboxed Xcode attempt failed only because its nested Gradle process could not open the user-cache lock; the approved identical rerun passed and is the product result. `git diff --check`, staged check, and committed `git show --check` pass. |
| Committed-state verification | At exact clean code commit `e66b13954ec032a3801d1be18df0e26bce5c16b7`, the complete affected 135-task command passed again in 2m36s with the same test counts/skips and lint result. Release XCFramework assembly passed in 2m32s with clean exact-HEAD provenance; regenerated project and generic-iOS-Simulator Xcode build passed with Swift warnings-as-errors. |
| Diff/equivalent review | Reviewed all 14 code/build paths and the generated lint/Xcode settings. JVM's replacement accessor is the supported JmDNS `getInetAddress` property; Android's storage branch is API-gated and retains API-24/25 behavior; cloud backup and device transfer exclude the same complete domain set; Compose coordinates exactly preserve the previously resolved 1.9.0/1.7.3 artifacts. No warning was suppressed, lint ID disabled, test skipped, timeout changed, or assertion weakened. |
| Compatibility | No SDK public API/ABI, wire protocol, or persistent SDK format changed. Android sample target/compile API 37 intentionally opts into current platform behavior and now refuses sample backup/device migration; minSdk remains 24. API 26+ free-space admission may account for reclaimable cache, while API 24/25 keeps `File.usableSpace`. Compose runtime coordinates are unchanged from the prior resolved graph. Stricter warning gates may require future source/tooling upgrades before a build proceeds. |
| Remaining evidence/blockers | Physical API-24/25 fallback and API-26+/37 `StorageManager` behavior, Android 12+ cloud/D2D exclusion across OEMs, target-37 permission/lifecycle/UI regression, and all other `PS-T04` runtime conjuncts require instrumentation/devices. Gradle still prints the host command-line-tools/SDK-XML version notice, Android packaging reports an unstrippable third-party `libandroidx.graphics.path.so`, and Xcode's AppIntents metadata tool notes that no AppIntents dependency exists; none is a Kotlin/Java/Swift compiler or Android lint finding, and lint itself is empty. Push was not attempted because `SCM-PUSH-01` remains unapproved. |
| Source control | Branch `remediation/full-register-2026-07`; focused commit `e66b13954ec032a3801d1be18df0e26bce5c16b7` (`fix(build): enforce warning-free gates`). Tracker-only follow-up is this documentation commit. No remote was changed or contacted. |

### Current execution record: REL-SUPPLY-01 — reproducible release supply chain

| Field | Value |
| --- | --- |
| Findings/test gaps | `BUILD-04/05/06/13`; local publication/consumer, wrapper, dependency-integrity, SBOM, ABI, documentation, tag-policy, CI, and maintenance coverage. `BUILD-02`, `ENV-06`, and `ENV-07` remain separate external/service gates. |
| Shared root cause | Release acceptance previously checked only artifact presence. The repository had a version-skewed wrapper, no component/distribution checksum contract, no dependency locks or verification metadata, empty javadoc archives, no ABI baseline, no release-scoped SBOM, no tag identity gate, and no pinned CI/dependency-maintenance workflows. |
| Status | Implemented locally. All deterministic local gates pass; push, a current external advisory scan, test-key signature evidence, and credentialed Central validation remain open. |
| Implementation | Regenerated and byte-pinned the complete Gradle 9.3.1 wrapper with HTTPS distribution validation; enabled all-configuration dependency locking plus SHA-256 metadata and a guarded explicit refresh task; added a 38-component CycloneDX 1.6 release-only JSON/XML SBOM; enabled Kotlin ABI validation for all four published modules; replaced empty documentation archives with strict Dokka 2.2.0 HTML; hardened the 15-publication checker for readable archives, POM ownership/coordinates, and target-aware module metadata; added deterministic wrapper/tag mutation gates; and added pinned-action CI, release dry-run, dependency review/submission, and Dependabot configuration. A replay-zero test-fixture subscription seam removes the lock-generation scheduling race without changing its assertion or timeout. The first committed full gate then exposed a distinct production close-vs-raw-terminal race: local close now atomically publishes `Closing`, grants one caller cleanup ownership, lets concurrent callers join the exact result, and finishes cleanup non-cancellably before propagating initiating-caller cancellation. |
| Exact files changed | `.gitattributes`; `.github/dependabot.yml`; `.github/workflows/ci.yml`; `.github/workflows/dependency-review.yml`; `.github/workflows/dependency-submission.yml`; `.github/workflows/release-dry-run.yml`; `CLAUDE.md`; `docs/STABILIZATION_AND_RELEASE.md`; root `build.gradle.kts`; `gradle/libs.versions.toml`; `gradle/wrapper/gradle-wrapper.jar`; `gradle/wrapper/gradle-wrapper.properties`; `gradle/verification-metadata.xml`; `gradle.lockfile`; `settings-gradle.lockfile`; `p2p-core/build.gradle.kts`; `p2p-core/gradle.lockfile`; `p2p-core/api/p2p-core.klib.api`; `p2p-core/api/jvm/p2p-core.api`; `p2p-transport-lan/build.gradle.kts`; `p2p-transport-lan/gradle.lockfile`; `p2p-transport-lan/api/p2p-transport-lan.klib.api`; `p2p-transport-lan/api/jvm/p2p-transport-lan.api`; `p2p-network-provisioning-android/build.gradle.kts`; `p2p-network-provisioning-android/gradle.lockfile`; `p2p-network-provisioning-desktop/build.gradle.kts`; `p2p-network-provisioning-desktop/gradle.lockfile`; `p2p-network-provisioning-desktop/api/p2p-network-provisioning-desktop.api`; `p2p-sample-android/gradle.lockfile`; `p2p-sample-desktop/gradle.lockfile`; `p2p-sample-desktop-ui/gradle.lockfile`; `sample-kmp-shared/gradle.lockfile`; KDoc-only cleanup in `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/NetworkPath.kt`, `Peer.kt`, `internal/SessionManager.kt`, `provisioning/ManualPeerRegistrar.kt`, `provisioning/NetworkProvisioningFactory.kt`, `p2p-core/src/iosMain/kotlin/dev/p2pkit/core/internal/NSUserDefaultsPeerIdStorage.kt`, `p2p-core/src/jvmMain/kotlin/dev/p2pkit/core/internal/FilePeerIdStorage.kt`, `p2p-network-provisioning-android/src/androidMain/kotlin/dev/p2pkit/provisioning/android/AndroidDsl.kt`, `AndroidProvisioningFactory.kt`, `p2p-network-provisioning-desktop/src/main/kotlin/dev/p2pkit/provisioning/desktop/JvmNetworkProvisioningManager.kt`, `p2p-transport-lan/src/androidMain/kotlin/dev/p2pkit/transport/lan/AndroidLanDataTransport.kt`, `AndroidLanDiag.kt`, `AndroidLanDiscoveryTransport.kt`, `p2p-transport-lan/src/appleMain/kotlin/dev/p2pkit/transport/lan/IosRawConnection.kt`, `p2p-transport-lan/src/commonMain/kotlin/dev/p2pkit/transport/lan/Lan.kt`, `p2p-transport-lan/src/jvmMain/kotlin/dev/p2pkit/transport/lan/JvmLanDiag.kt`, `JvmLanDiscoveryTransport.kt`; test synchronization in `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/internal/SessionReconnectRotationTest.kt` and `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/testfixtures/FakeDiscoveryTransport.kt`; close serialization in `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/P2pSession.kt`, `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/internal/P2pSessionImpl.kt`, and `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/internal/CloseSemanticsTest.kt`; `scripts/check-gradle-wrapper.sh`; `scripts/check-release-tag.sh`; `scripts/check-sbom.sh`; `scripts/check-publish-artifacts.sh`; `scripts/check-published-consumers.sh`; `scripts/tests/check-gradle-wrapper-test.sh`; `scripts/tests/check-release-tag-test.sh`. This tracker is the separate evidence follow-up. |
| Commands/exact results | `./gradlew resolveAndLockAll --write-locks --write-verification-metadata sha256 --console=plain` — `BUILD SUCCESSFUL` in 3m08s, 189 tasks (25 executed/164 up-to-date). Unguarded full execution exposed `SessionReconnectRotationTest.discoveryRefreshFiresOnReconnectingTransition[jvm]` at the unchanged 3,500 ms initial propagation bound; after the subscription seam, the forced class and five additional sequential repeats all passed. No-write `./gradlew resolveAndLockAll --console=plain` exits 1 during configuration in 818 ms with the exact required-flag message and only two buildSrc tasks up-to-date. Wrapper test accepts the valid wrapper and rejects four URL/JAR/Unix/Windows mutations; tag test accepts three valid and rejects four invalid tags; Ruby parses all five workflow/Dependabot YAML files. The first committed `./gradlew check --console=plain` at `64ca0a2` failed after 4m02s with exactly 1/422 core JVM failures: `KitLifecycleTest.sessionCommittedBeforeStopIsIncludedInTeardown` expected `Closed` but observed `Failed`. After close serialization, the exact failed lifecycle test plus `CloseSemanticsTest` passed three forced JVM repeats; the four high-risk close/lifecycle/reconnect/session-flow classes passed 43/43 on JVM and 43/43 on Apple; complete core passed 424/424 JVM and 399/399 iOS Simulator, with Android and all Apple target compiles green. Corrected committed `./gradlew check --console=plain` then passed in 4m04s, 148 tasks (64 executed/84 up-to-date). Final `scripts/check-sbom.sh` passes CycloneDX 1.6 JSON/XML with exactly 38 release components and no contamination; `scripts/check-publish-artifacts.sh` passes all 15 publications after 221 tasks in 21s; `scripts/check-published-consumers.sh` passes isolated JVM/Android/KMP/iOS consumers after a 221-task publish in 2s and 14/14 consumer tasks in 4s; four strict Dokka tasks, four ABI checks, and release XCFramework provenance pass in 2m06s, 75 tasks (17 executed/58 up-to-date). `git diff --check` passes. |
| Diff/equivalent review | Reviewed the full build/config/script/documentation/test diff, every generated ABI/lock category, workflow permission and pinned-action scope, POM/module output for all 15 variants, and equivalent KDoc on JVM/Android/Apple. Corrected two checker assumptions: target `.module` descriptors identify the root KMP component, and the Android-only root metadata sources archive may contain no Kotlin source. Corrected the lock-maintenance guard to fail before dependencies execute. The committed full-gate failure was treated as a deterministic product defect: raw terminal delivery can no longer outrun committed local-close intent, and cancellation cannot strand `Closing`. No assertion, timeout, warning gate, dependency scope, or terminal outcome was weakened. |
| Compatibility | No SDK public symbol, binary ABI, wire frame, persistent record, artifact coordinate, or classifier changed; ABI baselines pass. The already-public `ConnectionState.Closing` is now observably emitted during bounded local cleanup, as documented; observers gain the intended intermediate state without a source/binary change. Documentation archives now contain real Dokka HTML. Dependency changes require an explicit lock/verification refresh, and CI release gates intentionally fail on unreviewed bytes, ABI drift, documentation warnings, invalid tags, or malformed artifacts. The new workflows publish nothing remotely and store no credentials. |
| Remaining evidence/blockers | `REL-REMOTE-01`/`BUILD-02`/`ENV-07`: owner-selected Central Portal/OSSRH path, namespace, signing identity/test key, credentials, and live upload/status/release. `ENV-06`: current third-party advisory/CVE scan. Existing physical Android/Apple, AWDL/native TTL/address-rotation, two-machine hostile-network, and professional cryptographic-audit gates remain unchanged; supply-chain code does not satisfy them. `SCM-PUSH-01` remains unapproved. |
| Source control | Branch `remediation/full-register-2026-07`; supply-chain commit `64ca0a216be2b031ba3eb265796aa66d8d1a52e6` (`fix(build): harden release supply chain`) and deterministic correction `c68d59ea1f8cca5c99b05b0d888ba5eace1471d7` (`fix(core): serialize local session close`). Both committed diffs pass `git show --check`; final code-state gates target `c68d59e`. Tracker evidence is a separate follow-up. No remote was changed or contacted. |

### Current execution record: GOV-PACKAGE-01 — fixture isolation, LAN packaging, and sample identities

| Field | Value |
| --- | --- |
| Findings/test gaps | `CORE-30`, `LAN-19`, `LAN-20`, `SAMPLE-35`, `LAN-T09`, and `LAN-T11`. |
| Shared root cause | Tests depended on replay history and independently mutated process-global properties/defaults, Apple integration methods reused one coarse app namespace and selected peers by display name, published Android LAN metadata omitted install-time requirements, Apple failures lacked packaging/native codes, and the maintained two-process sample shared one persistent identity namespace. |
| Status | Implemented locally. Every deterministic JVM/Android/Apple/sample/publication acceptance gate passes from final committed code state; push and physical denied-permission/install/network evidence remain open. |
| Implementation | Replaced replay-backed stubs and global replay counts with explicit subscription/marker assertions; centralized exact JVM property and Apple defaults leases; moved identity storage tests to a private defaults suite; assigned UUID Bonjour namespaces per Apple test and selected the current exact PeerId; made clean remote CLOSE assert only `Closed`; declared all four Android LAN normal permissions and inspected the final application merge; added Info.plist preflight and native browser/listener error codes; and gave maintained Alice/Bob launches validated persistent profile homes under one discovery appId. |
| Exact files changed | `.github/workflows/ci.yml`; `.run/JVM CLI Alice.run.xml`; `.run/JVM CLI Bob.run.xml`; `README.md`; `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/testfixtures/FakeDiscoveryTransportTest.kt`; `p2p-core/src/iosTest/kotlin/dev/p2pkit/core/internal/NSUserDefaultsPeerIdStorageTest.kt`; `p2p-core/src/jvmTest/kotlin/dev/p2pkit/core/internal/FilePeerIdStorageTest.kt`; `p2p-core/src/jvmTest/kotlin/dev/p2pkit/core/internal/JvmSystemPropertyTestGuard.kt`; `p2p-core/src/jvmTest/kotlin/dev/p2pkit/core/internal/JvmSystemPropertyTestGuardTest.kt`; `p2p-sample-desktop/build.gradle.kts`; `p2p-transport-lan/src/androidMain/AndroidManifest.xml`; `p2p-transport-lan/src/appleMain/kotlin/dev/p2pkit/transport/lan/AppleLanPackaging.kt`; `p2p-transport-lan/src/appleMain/kotlin/dev/p2pkit/transport/lan/IosLanDataTransport.kt`; `p2p-transport-lan/src/appleMain/kotlin/dev/p2pkit/transport/lan/IosLanDiscoveryTransport.kt`; `p2p-transport-lan/src/appleTest/kotlin/dev/p2pkit/transport/lan/AppleGlobalStateTestGuard.kt`; `p2p-transport-lan/src/appleTest/kotlin/dev/p2pkit/transport/lan/AppleGlobalStateTestGuardTest.kt`; `p2p-transport-lan/src/appleTest/kotlin/dev/p2pkit/transport/lan/AppleLanPackagingTest.kt`; `p2p-transport-lan/src/appleTest/kotlin/dev/p2pkit/transport/lan/IosLanDebugTest.kt`; `p2p-transport-lan/src/appleTest/kotlin/dev/p2pkit/transport/lan/IosLanDiagnosticTest.kt`; `p2p-transport-lan/src/appleTest/kotlin/dev/p2pkit/transport/lan/IosLanLifecycleTest.kt`; `p2p-transport-lan/src/appleTest/kotlin/dev/p2pkit/transport/lan/IosLanLoopbackTest.kt`; `p2p-transport-lan/src/jvmTest/kotlin/dev/p2pkit/transport/lan/JvmDiscoveryRecordValidationTest.kt`; `p2p-transport-lan/src/jvmTest/kotlin/dev/p2pkit/transport/lan/JvmGlobalStateTestGuard.kt`; `p2p-transport-lan/src/jvmTest/kotlin/dev/p2pkit/transport/lan/JvmGlobalStateTestGuardTest.kt`; `p2p-transport-lan/src/jvmTest/kotlin/dev/p2pkit/transport/lan/JvmLanAcceptLoopResilienceTest.kt`; `p2p-transport-lan/src/jvmTest/kotlin/dev/p2pkit/transport/lan/JvmLanAdmissionControlTest.kt`; `p2p-transport-lan/src/jvmTest/kotlin/dev/p2pkit/transport/lan/JvmLanDiscoveryHeartbeatTest.kt`; `p2p-transport-lan/src/jvmTest/kotlin/dev/p2pkit/transport/lan/JvmLanLoopbackTest.kt`; `scripts/check-published-consumers.sh`; `scripts/tests/check-sample-run-profiles.sh`. |
| Tests and commands | `git diff --check`; `bash -n scripts/tests/check-sample-run-profiles.sh`; `scripts/tests/check-sample-run-profiles.sh`; two real `:p2p-sample-desktop:run` launches with `info`/`quit`; focused core fixture/lease and LAN guard/packaging/admission/accept tests; three forced runs of `IosLanLoopbackTest`, `IosLanLifecycleTest`, and `AppleGlobalStateTestGuardTest`; complete affected `:p2p-core:jvmTest :p2p-core:iosSimulatorArm64Test :p2p-transport-lan:jvmTest :p2p-transport-lan:iosSimulatorArm64Test :p2p-transport-lan:compileAndroidMain :p2p-sample-desktop:test :p2p-core:checkKotlinAbi :p2p-transport-lan:checkKotlinAbi --rerun-tasks`; Apple Arm64/x64 compilation; and `scripts/check-published-consumers.sh`. |
| Exact results | Static/profile gates pass; real Alice/Bob launches both exit normally and persist different PeerIds. Pre-commit complete affected gate passed 46/46 tasks in 2m34s. The first committed-state run at `52e173b` correctly failed 1/63 Apple LAN tests: `IosLanLoopbackTest.fileTransferRoundTripsOverTcp` selected a stale same-name Bonjour peer and the session became `Failed`; no retry was accepted. After the namespace/current-PeerId correction, the 12-test Apple lifecycle/loopback set passed three forced runs; complete Apple LAN passed 64 tests, zero failures/errors, one documented 60-second manual diagnostic skip. Final committed-state gate at `90b5f20` passed 46/46 tasks in 2m20s: core 426 JVM + 400 Apple, LAN 84 JVM + 64 Apple, desktop 5; zero failures/errors and only the one documented skip; Android and Apple Arm64/x64 compile and both ABI checks pass. Published consumers pass after 221 publication tasks in 18s plus 18/18 isolated JVM/Android/KMP/iOS tasks in 3s; the final Android application manifest contains all four permissions. |
| Diff/equivalent review | Reviewed the complete 30-path implementation diff and four-path corrective diff, JVM/Android/Apple discovery/data equivalents, final merged Android manifest, Apple KDoc and native error handling, run-profile semantics, staged and committed whitespace checks. The committed failure was traced to stale peer selection and corrected architecturally; no timeout increased, assertion weakened, test skipped, alternate terminal result accepted, or cancellation/failure swallowed. |
| Compatibility | No public SDK API, binary ABI, wire protocol, artifact coordinate, or persistent SDK format changed. Android applications consuming LAN now inherit four normal install-time permissions through manifest merging; none requires a runtime prompt. Apple adds sanitized diagnostics only. Default CLI storage is unchanged; only maintained Alice/Bob run configurations opt into separate profile homes. |
| Remaining evidence/blockers | Physical Android installed-package/PackageManager/logcat permission evidence remains in `LAN-T01`; real Apple denial prompt, host Info.plist, native error-code, AWDL, rotation, and hostile-network evidence remains in `LAN-T07`/`ENV-01`/`ENV-02`. The ignored 60-second interop diagnostic remains `ENV-05`. Push remains blocked by unapproved `SCM-PUSH-01`; no remote was changed or contacted. |
| Source control/post-commit | Branch `remediation/full-register-2026-07`; focused commit `52e173b73a5a5a5e1205b89c759e38e890bb6fe2` (`fix(lan): harden packaging and fixture isolation`); deterministic correction `90b5f200b50d5018060d84241566b192e417063f` (`test(lan): isolate Apple peer identities`). Both committed diffs pass `git show --check`/`git diff HEAD^ HEAD --check`; final gates target `90b5f20`. Tracker update is the separate documentation follow-up. |

### Current execution record: ENV-06 — current dependency advisory scan and recurring gate

| Field | Value |
| --- | --- |
| Findings/test gaps | `ENV-06`; recurring advisory coverage for `BUILD-06` and the repository's build/documentation/test dependency graphs. No new item was added to the fixed 150-finding register. |
| Shared root cause | Dependency locking and checksum verification prove byte identity, not whether a selected component has a newly disclosed advisory. Plugin/tool graphs also execute in trusted CI even when they are intentionally absent from the published release SBOM. |
| Status | Implemented locally. Current lockfiles and the release SBOM scan clean; the recurring workflow is committed only after this batch and cannot supply remote run evidence until authorized push/CI execution. |
| Implementation | Added centralized minimum versions for advisory-affected Jackson, OpenTelemetry, Netty, Bouncy Castle, Commons Lang, Apache HttpClient, jose4j, and JDOM artifacts already requested by build/docs/test configurations. The comparator upgrades only versions below each floor, so a future higher request is not downgraded. Refreshed exact Gradle locks and SHA-256 verification metadata. Added a weekly, pull-request, main-push, and manual OSV reusable workflow pinned to immutable action commit `9a498708959aeaef5ef730655706c5a1df1edbc2` (v2.3.8) and explicit supported lockfiles. A shared Gradle build service now serializes only cross-module `iosSimulatorArm64Test` execution because independent Kotlin/Native test binaries share one simulator host and process-global Apple services; compilation and other targets remain parallel. |
| Exact files changed | `.github/workflows/osv-scanner.yml` (new); root `build.gradle.kts`; `gradle/verification-metadata.xml`; `p2p-core/gradle.lockfile`; `p2p-transport-lan/gradle.lockfile`; `p2p-network-provisioning-android/gradle.lockfile`; `p2p-network-provisioning-desktop/gradle.lockfile`; `p2p-sample-android/gradle.lockfile`; this tracker as evidence follow-up. |
| Scanner provenance and results | Fresh official OSV Scanner 2.4.0 macOS arm64 binary SHA-256 `9ca3185ad63e9ab54f7cb90f46a7362be02d80e37f0123d095a54355ea202f5` exactly matched the published checksum; version reports OSV Scanner 2.4.0, OSV-Scalibr 0.4.5, commit `b56b519`. An initial explicit supported-lockfile plus verification-metadata inventory found 2,093 package records, 64 vulnerable records, and 43 unique advisory IDs, all in build/documentation/test tool graphs; the 38-component published release SBOM already had zero. After the floors, the current explicit lockfile scan has 1,165 records and zero vulnerabilities; the regenerated release SBOM has 38 records and zero vulnerabilities. JSON evidence SHA-256 values are `ed8e04a3f452a622c253d4543bc4099bbcf447e2bf3ee678cf756628107607af` and `644dc846f2fa5b94344088f06d7bc50fd4f2a5a12a03047da07a221499c0673b`. `settings-gradle.lockfile` is not a supported package input; historical unused components retained in verification metadata are not treated as current selection by the recurring scan. |
| Commands/exact results | Binary checksum/version and explicit OSV JSON scans — pass as above. The first `resolveAndLockAll --write-locks --write-verification-metadata sha256 --rerun-tasks` attempt failed only when the host Wi-Fi IPv4 vanished between JmDNS fixture setup and bind (`Can't assign requested address`); the exact heartbeat class then passed three consecutive isolated forced runs, and the complete refresh passed in 4m55s with 188/188 tasks executed. An initial no-write repository `check` exposed cross-module Apple-simulator overlap: nine core tests reported immediate session/raw-channel churn and one LAN lifecycle test timed out, while the exact failing classes and then the complete 400-test core Apple suite passed in isolation. After simulator-task serialization, `./gradlew check --rerun-tasks --console=plain` passed in 4m26s with 148/148 tasks executed. `scripts/check-sbom.sh` passes 38 components; `scripts/check-publish-artifacts.sh` passes 15 publications after 221 tasks; `scripts/check-published-consumers.sh` passes the 221-task publish plus 18/18 isolated consumer tasks; all four ABI checks, all four Dokka generations, and release XCFramework provenance pass in 2m51s with 75/75 tasks executed. Ruby YAML parse and `git diff --check` pass. |
| Diff/equivalent review | Reviewed the complete root resolution/service logic, immutable workflow reference and permissions, all five changed lock categories, and every generated verification-metadata addition. Dependency floors converge duplicate plugin/test versions while preserving higher future versions. The Apple failure was resolved at the shared resource-ownership boundary; no assertion or terminal outcome was broadened, no timeout increased, and no required test was skipped. |
| Compatibility | No SDK source/binary ABI, wire frame, persistent record, artifact coordinate/classifier, or published 38-component dependency graph changed. Only dependencies already requested by build, documentation, Swift-export, and Android test/lint tooling are raised. Cross-module Apple simulator test execution is serialized; target compilation and production behavior are unchanged. |
| Remaining evidence/blockers | Advisory databases evolve, so zero findings describe this authenticated scan time rather than permanent safety. The pinned recurring GitHub workflow still needs an authorized push and remote run. Credentialed publication, physical-device/hostile-network validation, and professional cryptographic audit remain separate. Push was not attempted because `SCM-PUSH-01` is unapproved. |
| Source control/post-commit | Focused commit `3574abd4d034fff2c4d066caecdb463939b9037f` (`build(security): enforce dependency advisory floors`) on `remediation/full-register-2026-07`; `git show --check`, `git diff HEAD^ HEAD --check`, and committed worktree status review pass. Committed-state `./gradlew check --rerun-tasks --console=plain` passes in 5m35s with 148/148 tasks executed. No remote was changed or contacted. |

### Current execution record: CORE-LIFECYCLE-API-01 — restartable data rollback and provisioning disposal

| Field | Value |
| --- | --- |
| Findings/test gaps | `CORE-11`, `CORE-24`, and `CORE-T09`; owner approvals `DATA-TRANSPORT-LIFECYCLE-API-01` and `PROVISIONING-CLOSE-API-01`. |
| Shared root cause | Partial data startup had no nonterminal inverse, while provisioning relied on advisory parent-job ownership instead of a required disposal contract. Both defects allowed acquired listeners/dials/native handles to outlive failed startup or terminal kit teardown. |
| Status | Implemented locally. All deterministic affected-module, ABI, documentation, and isolated external-implementer gates pass; rows remain below Verified because push and required physical evidence are open. |
| Implementation | Added required restartable `DataTransport.stop()` and retained terminal `close()`; core rolls back every entered transport in reverse on failure/cancellation and restores retryable Idle. JVM/Android/Apple LAN stop listeners, drain uncommitted inbound work, generation-gate and cancel pending dials, preserve the prior port where supported, and retain failed cleanup so restart fails until retry succeeds. Added required suspending terminal `NetworkProvisioningManager.close()`, `Closing`/`Closed`, typed `ManagerClosed`/`CleanupFailed`, deterministic future-call behavior, Android manager-owned acquisition cancellation and retained native cleanup, and explicit core teardown ordering before data transports and scope cancellation. |
| Exact files changed | `p2p-core/api/jvm/p2p-core.api`; `p2p-core/api/p2p-core.klib.api`; `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/NetworkProvisioningError.kt`; `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/internal/P2pKitImpl.kt`; `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/provisioning/NetworkProvisioningTypes.kt`; `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/provisioning/UnsupportedNetworkProvisioningManager.kt`; `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/transport/DataTransport.kt`; core lifecycle/transport fixtures and `NetworkProvisioningCloseTest.kt`; Android and Desktop provisioning manager/tests plus Desktop ABI; JVM/Android/Apple LAN data implementations; `BoundedInboundQueue.kt`; Apple manual provisioning; JVM/Apple lifecycle/connect tests; LAN KLIB ABI; `scripts/check-published-consumers.sh`; `P2PKIT_OWNER_DECISION_PACKAGE_2026-07-22.md`; this tracker. |
| Tests added/updated | Reverse partial-start failure and cancellation rollback with same-instance retry; JVM/Apple stop/rebind; JVM/Apple pending-dial cancellation; JVM failed-cleanup retention/retry; unsupported/Desktop/Android/Apple provisioning terminal/idempotent behavior; Android close/start/join and caller-cancellation races; core provisioning-before-data close ordering; isolated published third-party `DataTransport` and `NetworkProvisioningManager` implementations. |
| Commands/results | `git diff --check` passes. Focused core lifecycle/close tests pass in 30s; focused JVM LAN stop/dial/cleanup tests pass; complete affected checkpoint passes in 2m11s with 58 tasks: core JVM 429/429, core Apple 403/403, LAN JVM 87/87, LAN Apple 66 passed plus one registered manual diagnostic skip, Desktop provisioning 11/11, Android provisioning 24/24. Final Android provisioning rerun after caller-cancellation coverage passes 25/25. Android production and all Apple Arm64/x64/simulator compilations pass. Four ABI checks pass. `scripts/check-published-consumers.sh` passes 221 publication/Dokka tasks in 12s and all 18 isolated JVM/Android/KMP/iOS consumer tasks in 2s, including external SPI implementers. |
| Diff/equivalent review | Reviewed core transaction/teardown order and all JVM/Android/Apple implementations. Corrected active Android acquisitions to manager-owned cancellable children, reset caller-cancelled operations to Idle, added pending-dial ownership on all LAN platforms, retained failed JVM/Android socket cleanup, and corrected stale Apple provisioning KDoc. No assertion was weakened, timeout increased, terminal outcome broadened, required test skipped, or cleanup failure swallowed. |
| Compatibility | Intentionally breaking SPI/API changes approved by the owner: third-party data transports must implement suspending `stop()`; provisioning managers must implement suspending `close()`; Desktop's concrete `close()` is now suspending; new sealed provisioning states can break exhaustive Kotlin `when`. Kotlin/Java/Swift generated async surfaces and ABI baselines reflect the change. No wire frame, persistent record, artifact coordinate, or security mode changed in this batch. |
| Remaining evidence/blockers | Real Android hotspot/specifier callback cancellation, bind/unbind and cleanup-failure behavior; physical JVM/Android/Apple port/dial behavior; Apple AWDL and listener cancellation; hostile-network/rebind validation; external third-party migration feedback. `SCM-PUSH-01` remains unapproved, so no remote was changed or contacted. |
| Source control/post-commit | Branch `remediation/full-register-2026-07`; focused implementation commit `391ae3c` (`fix(core): define restartable transport and provisioning lifecycle`) and decision/tracker commit `9305471`. Staged and committed diffs pass whitespace/stat review. From committed state, the focused JVM/core/LAN plus complete Desktop/Android provisioning, Android compilation, and four ABI checks pass in 35s (52 tasks); forced focused core/LAN Apple tests pass in 51s (17/17 tasks executed); and `scripts/check-published-consumers.sh` passes 221 publication/Dokka tasks plus 18/18 isolated consumer tasks. Push is blocked by `SCM-PUSH-01`. |

### Current execution record: IMMUTABLE-MODEL-API-01 — deep public snapshots

| Field | Value |
| --- | --- |
| Finding | `CORE-17`; owner approval `IMMUTABLE-MODEL-API-01`. No separately numbered explicit gap exists; the required Kotlin, Java, Apple, ABI, ownership, and external-consumer coverage is recorded here. |
| Shared root cause | Public read-only collection interfaces retained caller-owned mutable implementations or returned castable mutable copies. That allowed constructor inputs, getters, or published StateFlow values to violate SDK ownership and stable-value assumptions across threads and language bridges. |
| Status | Implemented locally in `04aaef0`. Deterministic complete affected-module/platform and consumer gates pass; the row remains below Verified because the branch cannot be pushed while `SCM-PUSH-01` is unapproved. |
| Implementation | Added private-backing immutable list/set/map snapshots whose storage and iterators never escape. Converted `Peer`, `InternalPeer`, `TransportHint`, `LocalPeerInfo`, `PeerAuthorizationPolicy.PinnedOnly`, `P2pMessage.Text`, the three collection-bearing `NetworkState` variants, `ManualConnectionInfo`, and both permission errors to snapshot once while preserving constructors, getters, equality/hash/string, components, and practical `copy` calls. `P2pMessage.Binary.metadata` now returns one stable immutable snapshot while bytes remain defensive copies. Peer/session StateFlow publications and the internal registration publication now use the same wrappers. |
| Exact files changed | `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/Config.kt`; `Errors.kt`; `NetworkProvisioningError.kt`; `P2pMessage.kt`; `Peer.kt`; `internal/ImmutableSnapshots.kt`; `internal/PeerRegistry.kt`; `internal/SessionStore.kt`; `provisioning/NetworkProvisioningTypes.kt`; `transport/Internal.kt`; `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/PublicModelImmutabilityTest.kt`; `internal/PeerRegistryTest.kt`; `internal/SessionStoreInvariantTest.kt`; `protocol/MessageMetadataContractTest.kt`; `p2p-core/src/jvmTest/java/dev/p2pkit/core/JavaCollectionMutationProbe.java`; `p2p-core/src/jvmTest/kotlin/dev/p2pkit/core/PublicModelJavaImmutabilityTest.kt`; `scripts/check-published-consumers.sh`; this tracker. |
| Tests and commands | Focused JVM/Apple model, peer-list, and session-list mutation tests pass; Java `Collection`, `Map`, and `Map.Entry` mutation probes pass. `git diff --check` and `:p2p-core:checkKotlinAbi` pass without ABI-baseline changes. Complete affected Gradle gate passes in 2m15s/171 tasks: core JVM 434/434; core Apple 407/407; LAN JVM 87/87; LAN Apple 66 passed plus one registered manual diagnostic skip; Desktop provisioning 11/11; Android provisioning 25/25; Desktop CLI 5/5; Desktop UI 11/11; shared KMP 2/2; Android assembly/lint, all Apple targets, and Dokka pass. `scripts/check-published-consumers.sh` passes 221 publication/Dokka tasks and 19/19 isolated Kotlin, Java, Android, KMP, and iOS consumer tasks. Release XCFramework assembly passes in 2m; `xcodegen generate` and the generic iOS Simulator Swift build with warnings as errors report `BUILD SUCCEEDED`. |
| Diff/equivalent review | Audited every documented public collection field plus all common public collection/array declarations. The complete model graph contains only immutable scalar/value elements or nested snapshot-backed values; binary arrays retain copy-in/copy-out. The first full gate exposed one old test that mutated a getter copy; it now requires mutation failure while preserving exact content and hash. No timeout, skip, terminal outcome, or unrelated assertion changed. |
| Compatibility | JVM/KLIB ABI checks remain unchanged: constructor/getter/component/copy descriptors are preserved, as are equality/hash/string behavior. The approved source/reflection change is that affected types are no longer Kotlin `data class` declarations, so `isData`, generated metadata, and tools that specifically require data classes may require migration. Java and Swift initializer/getter surfaces compile; no wire, persistence, artifact, or security-mode format changed. |
| Remaining evidence/blockers | No physical-device or hostile-network conjunct is specific to immutable value ownership; Apple simulator tests and the Swift consumer build cover the language bridge. External reflection/serialization-tool migration feedback remains useful. Repository-wide physical-device, hostile-network, credential, and cryptographic-audit gates are unchanged. Push remains blocked by `SCM-PUSH-01`; no remote was changed or contacted. |
| Source control/post-commit | Branch `remediation/full-register-2026-07`; focused implementation commit `04aaef0` (`fix(core): make public model snapshots immutable`). `git show --check`, exact commit diff whitespace review, and the forced focused JVM/Java/Apple/ABI command pass in 42s with all 18 tasks executed. The committed-state published consumer/Dokka gate passes 221 tasks in 26s plus 19/19 isolated consumer tasks in 3s. Push is blocked by `SCM-PUSH-01`. |

### Current execution record: PEER-STATE-API-01 + TRANSPORT-FACTORY-API-01

| Field | Value |
| --- | --- |
| Findings/test gaps | `CORE-15`, `CORE-27`, and the remaining duplicate-kind conjunct of `CORE-28`; owner approvals `PEER-STATE-API-01` and `TRANSPORT-FACTORY-API-01`. No separately numbered explicit gap exists; deterministic feature-state, factory, ABI, and external-consumer coverage is recorded here. |
| Shared root cause | One global kit state conflated independent advertising/discovery outcomes, while factories exposed neither kind nor static capabilities until after construction and required a non-null data path. That prevented discovery-only providers and made duplicate-kind rejection too late for resource-safe preflight. |
| Status | Implemented locally in `18723c0`. Complete affected-module/platform, ABI, Dokka, and isolated consumer gates pass; these rows remain below Verified because `SCM-PUSH-01` is unapproved. |
| Implementation | Added independent retained `FeatureState` flows for advertising and discovery with Idle/Starting/Active/Stopping/PermissionRequired/Unsupported/Failed states. Feature operations serialize per feature, coalesce starts, make stop intent win an in-flight start, compensate late resources, preserve cleanup failures for retry, and never overwrite global `P2pState`. Added immutable required `TransportDescriptor` capabilities, nullable `TransportPair` paths with a non-empty invariant, descriptor/pair kind/capability validation, typed `TransportInitializationFailed`, discovery-only support, registration-time descriptor snapshots, and duplicate-kind rejection before any factory build. JVM, Android, and Apple LAN factories declare DATA+DISCOVERY. |
| Exact files changed | `p2p-core/api/jvm/p2p-core.api`; `p2p-core/api/p2p-core.klib.api`; `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/Errors.kt`; `P2pKit.kt`; `States.kt`; `dsl/Builders.kt`; `internal/P2pKitImpl.kt`; `transport/TransportFactory.kt`; `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/PublicModelImmutabilityTest.kt`; `internal/FileTransferErrorIsolationTest.kt`; `FileTransferFlowTest.kt`; `HandshakeIdentityTest.kt`; `InboundAcceptResilienceTest.kt`; `InboundAdmissionControlTest.kt`; `KitLifecycleTest.kt`; `KitStrictInvariantsTest.kt`; `LocalIdentityTest.kt`; `ManualPeerIdentityTest.kt`; `NetworkPathRecoveryTest.kt`; `NetworkProvisioningCloseTest.kt`; `PermissionGateTest.kt`; `ReconnectPolicyTest.kt`; `SecureSessionIntegrationTest.kt`; `SessionFlowTest.kt`; `SessionOwnershipTest.kt`; `SessionReconnectRotationTest.kt`; `SimultaneousOpenTest.kt`; `TransportCapabilityTest.kt`; `p2p-core/src/jvmTest/kotlin/dev/p2pkit/core/internal/JvmSecureIdentityStoreAdapterTest.kt`; `PeerIdPersistenceIntegrationTest.kt`; `p2p-core/src/jvmTest/kotlin/dev/p2pkit/core/transfer/FileTransferJvmTest.kt`; `p2p-transport-lan/src/androidMain/kotlin/dev/p2pkit/transport/lan/AndroidLanDsl.kt`; `appleMain/kotlin/dev/p2pkit/transport/lan/IosLanDsl.kt`; `jvmMain/kotlin/dev/p2pkit/transport/lan/JvmLanDsl.kt`; `jvmTest/kotlin/dev/p2pkit/transport/lan/JvmLanAcceptLoopResilienceTest.kt`; `JvmLanAdmissionControlTest.kt`; `scripts/check-published-consumers.sh`; this tracker. |
| Tests and commands | `git diff --check` and `bash -n scripts/check-published-consumers.sh` pass. Focused `:p2p-core:jvmTest` and `:p2p-core:iosSimulatorArm64Test` runs for `KitLifecycleTest`, `PermissionGateTest`, `TransportCapabilityTest`, and `PublicModelImmutabilityTest` each pass 37/37. Android main, Apple Arm64/x64/simulator, core/LAN test compilation, both ABI checks, and core Dokka pass. The forced complete boundary command `:p2p-core:jvmTest :p2p-core:iosSimulatorArm64Test :p2p-core:compileAndroidMain :p2p-core:compileKotlinIosArm64 :p2p-core:compileKotlinIosX64 :p2p-transport-lan:jvmTest :p2p-transport-lan:iosSimulatorArm64Test :p2p-transport-lan:compileAndroidMain :p2p-transport-lan:compileKotlinIosArm64 :p2p-transport-lan:compileKotlinIosX64 :p2p-core:checkKotlinAbi :p2p-transport-lan:checkKotlinAbi --rerun-tasks --continue --console=plain` passes 41/41 tasks in 2m38s: core 444 JVM + 417 Apple, LAN 87 JVM + 67 Apple, zero failures/errors and one registered manual Apple diagnostic skip. `scripts/check-published-consumers.sh` passes 221 publication/Dokka tasks plus 19/19 isolated Kotlin, Java, Android, KMP, and iOS tasks. |
| Diff/equivalent review | Reviewed the complete state-machine and construction diff, all JVM/Android/Apple LAN factories, every in-repository factory migration, generated JVM/KLIB ABI deltas, and external Kotlin/Java implementations. No assertion was weakened, no timeout increased, no required test skipped, and terminal outcomes remain deterministic. |
| Compatibility | Intentionally breaking SPI/KLIB changes approved by the owner: every third-party `TransportFactory` must add `descriptor`, and `TransportPair.data` becomes nullable. JVM descriptors for the existing pair constructor/getter remain type-erased-compatible, but old factory binaries lack the required method and must be rebuilt. The two `P2pKit` state getters are additive with default Idle accessors for one mock-migration release. Java/Kotlin/KMP/iOS consumers compile. No wire frame, persistent record, artifact coordinate, transport security profile, or runtime permission contract changed. |
| Remaining evidence/blockers | No physical-device or hostile-network evidence is specific to static descriptor validation or in-memory feature-state arbitration. Real platform permission/radio/Bonjour failures and external provider migration feedback remain valuable; repository-wide Android device, Apple physical/AWDL, hostile-network, credential, cryptographic-audit, and runtime UI blockers are unchanged. Push remains blocked by `SCM-PUSH-01`; no remote was changed or contacted. |
| Source control/post-commit | Branch `remediation/full-register-2026-07`; focused implementation commit `18723c0` (`feat(core): expose transport capabilities and feature states`). `git show --check`, staged/committed whitespace and stat review pass. From committed code state, the forced focused command passes 35/35 tasks in 1m26s with 37/37 JVM and 37/37 Apple tests plus Android, all Apple target, and both ABI checks; the published consumer/Dokka gate passes 221 tasks plus 19/19 isolated consumers. Push is blocked by `SCM-PUSH-01`. |

### Current execution record: XFER-OFFER-API-01 + XFER-ERROR-API-01

| Field | Value |
| --- | --- |
| Findings/test gaps | `FILE-05`, the receiver-observability conjunct of `FILE-06`, `FILE-11`, `PT-T12`, `PT-T13`, and the retained-offer transition conjunct of `PT-T21`; owner approvals `XFER-OFFER-API-01` and `XFER-ERROR-API-01`. |
| Shared root cause | Incoming offers were transient events with no authoritative ownership after callback delivery, and transfer failures were flattened into misleading session errors or untyped strings. This made late observation, expiry, duplicate IDs, concurrent responses, retry policy, and root-cause diagnosis nondeterministic. |
| Status | Implemented locally in `ddb7142`. Complete core JVM/Apple, Android/Apple compilation, ABI, Dokka/publication, isolated consumer, and release XCFramework provenance gates pass; these rows remain below Verified because `SCM-PUSH-01` is unapproved. |
| Implementation | Added immutable retained `pendingFileOffers` state with admission-before-timer ordering, a 64-offer bound, exact-duplicate idempotency, conflicting-ID protocol failure, one-shot accept/reject arbitration, and removal on accept commit, reject, cancellation, expiry, compensation, disconnect, or close. Deprecated transient `incomingFiles` remains source/ABI compatible. Added `FileTransferFailed` with stable kind, phase, retryability, transfer ID, bounded reason, and an internal preserved cause. Mapped metadata, offer/accept/data writes, source I/O, sink write/flush, timeout, ordering, disconnect, and local-close paths while preserving intentional `Cancelled`, `Rejected`, and coroutine cancellation semantics. |
| Exact files changed | `INTERNAL_TESTING.md`; `P2pKit-Spec.md`; `README.md`; `docs/stabilization-stress-tests.md`; `p2p-core/api/jvm/p2p-core.api`; `p2p-core/api/p2p-core.klib.api`; `p2p-core/src/commonMain/kotlin/dev/p2pkit/core/Errors.kt`; `P2pSession.kt`; `internal/FileTransferDispatcher.kt`; `internal/P2pSessionImpl.kt`; `protocol/FileOfferPayload.kt`; `transfer/FileTransferState.kt`; `transfer/P2pFileOffer.kt`; `p2p-core/src/commonTest/kotlin/dev/p2pkit/core/internal/FileTransferErrorIsolationTest.kt`; `FileTransferFlowTest.kt`; `KitStrictInvariantsTest.kt`; `SendErrorContractTest.kt`; `SessionStoreCapacityTest.kt`; `SessionStoreInvariantTest.kt`; `p2p-core/src/jvmTest/kotlin/dev/p2pkit/core/transfer/FileTransferJvmTest.kt`; `scripts/check-published-consumers.sh`; this tracker. |
| Tests and commands | `git diff --check`, staged/committed `--check`, and shell syntax pass. Development focused JVM transfer tests pass; complete affected boundary command passes in 2m04s with core JVM 458/458 and Apple 431/431, Android plus iOS Arm64/x64/simulator compilation, and both ABI checks. Core Dokka passes. `scripts/check-published-consumers.sh` passes 221 publication/Dokka tasks and 19/19 isolated Kotlin, Java, Android, KMP, and iOS tasks. Release XCFramework assembly/provenance passes. From committed `ddb7142`, forced focused tests pass 59/59 JVM and 56/56 Apple; forced Android/all-Apple compilation and both ABI checks pass; the 221 + 19 consumer gate passes; release XCFramework provenance passes in 2m14s. |
| Diff/equivalent review | Reviewed the complete dispatcher/session/public API and KDoc diff, JVM and KLIB ABI deltas, JVM file-helper behavior, Android/common compilation, generated Apple/Swift headers, and Kotlin/Java/KMP/iOS consumer fixtures. No assertion was weakened, timeout increased, test skipped, or nondeterministic terminal result accepted. |
| Compatibility | Additive public enums/error subtype and retained StateFlow. `incomingFiles` remains present but deprecated; external mocks retain a default empty `pendingFileOffers` accessor for one migration release. Existing intentional cancellation and rejection states are unchanged. Failed handles never resume; retry creates a new transfer. Legacy v1 wire bytes and persistence are unchanged in this batch. Java/Kotlin/KMP/iOS consumers compile, and Swift headers expose the new flow/error types. |
| Remaining evidence/blockers | Physical Android/Apple/Desktop transfer, process-death behavior, and hostile-network timing/duplicate evidence remain in their existing external rows; pending offers are intentionally session-memory-only and do not survive process restart. Rich remote typed-failure propagation requires the separately approved negotiated protocol batch. Cryptographic audit, device/runtime matrices, credentials, remote release, and push remain unchanged. |
| Source control/post-commit | Branch `remediation/full-register-2026-07`; focused implementation commit `ddb7142` (`feat(core): retain file offers and type transfer failures`). `git show --check`, committed stat/name review, focused JVM/Apple tests, cross-target compilation, ABI, publication/Dokka consumers, and release XCFramework provenance all pass from committed source. Push was not attempted because `SCM-PUSH-01` is unapproved. |

`XFER-PROTO-01` approval record: on 2026-07-22 the owner approved negotiated durable receiver commit plus a prepared SHA-256 source snapshot authenticated by the secure envelope. `FILE-04`, `FILE-13`, `PT-T16`, and `PT-T18` are queued; no protocol compatibility claim is made before negotiation and interop tests land.

`PROV-A12-DEVICE-01` blocker (`PROV-A12`, `PS-T01`, `PS-T02`, plus the Android conjuncts of `LAN-T01`, `PT-T20`, and `PS-T04`): host seams deterministically cover ownership, cancellation, validation, binding arbitration, route projection, and permission decisions, but Android framework callback ordering, OEM return values, real process binding/unbinding, provider metadata, merged-package diagnostics, and lifecycle UI behavior cannot be proven by JVM stubs. Exact unblock: provide or authorize an Android API-26-through-37 instrumentation matrix with Wi-Fi/hotspot/local-network capabilities and permission/logcat capture. Emulator coverage may close supported framework conjuncts; hardware-only hotspot/OEM behavior remains physical evidence.

`RUNTIME-UI-MATRIX-01` blocker (`PS-T05`, `PS-T06`, `PS-T07`, `PS-T08`, `PS-T09`): local parser/path/history tests, real happy-path CLI processes, generated iOS simulator UI smoke, compiles, and lifecycle code review are complete. Remaining coverage needs controlled packaged-CLI fault injection, headful Compose Desktop automation, an Apple sink/collision/failure-injection target, physical scoped-IPv6 peers, and Android/iOS KMP consumer runtimes. Exact unblock: provide/authorize those automation targets and device/network endpoints; none is inferred from compilation or happy-path simulation.

`DATA-TRANSPORT-LIFECYCLE-API-01` implementation record (`CORE-11`, `CORE-T09`): approved on 2026-07-22 and locally implemented. `DataTransport.stop()` is restartable and `close()` terminal; core rollback is reverse-ordered for failure/cancellation; JVM/Android/Apple release listeners and pending dials, retain failed cleanup, and gate late success. Full local gates pass; push and physical platform/network evidence remain open.

`PROVISIONING-CLOSE-API-01` implementation record (`CORE-24`): approved on 2026-07-22 and locally implemented as a required suspending, terminal, idempotent close. Core invokes it before data-transport close and scope cancellation; Android cancels/joins active acquisitions and retains failed native cleanup; Desktop, Apple, and unsupported managers expose deterministic terminal behavior. ABI, Dokka, built-in, core-ordering, and isolated third-party implementer gates pass; push and physical Android callback/binding evidence remain open.

`SCM-PUSH-01` blocker (all local commits after `8288df5`): origin is configured as `https://github.com/Apdelrahman1911/P2pKit.git`, while the server reports that the repository moved to `https://github.com/p2pKit/P2pKit.git`. The attempted normal push of `ee2cae5` was rejected by the execution environment because it would export workspace code to an external destination whose ownership/trust was not explicitly reconfirmed after disclosure. No workaround was attempted. Recommended option: explicitly approve changing origin to the reported canonical repository and pushing the remediation branch, if that repository is owner-authorized and credentials permit it. Alternative: explicitly approve the currently configured repository; or provide another authorized private remote. Exact later reply: `Approve SCM-PUSH-01 canonical https://github.com/p2pKit/P2pKit.git` (recommended), `Approve SCM-PUSH-01 current https://github.com/Apdelrahman1911/P2pKit.git`, or provide an authorized URL. Until approval, local commits continue without claiming pushed/Verified status.

## Execution log

| Date | Unit | Action | Result |
| --- | --- | --- | --- |
| 2026-07-17 | GOV-01 | Created isolated remediation branch and reconciled source register | Branch `remediation/full-register-2026-07`; 150 unique finding IDs, 54 unique gap rows |
| 2026-07-17 | SEC-01 | Read referenced implementation/tests and reproduced CORE-06/CORE-07 architecture | Confirmed; no source changes; blocked at mandated breaking security/protocol decision |
| 2026-07-17 | GOV-01/SEC-01 | Independent tracker and security-plan reviews, correction pass, mechanical revalidation | Phase allocation is exactly 150/54; tables valid; all security-review corrections addressed |
| 2026-07-17 | SEC-01 | Owner approved secure protocol v2 and storage A; protocol, provider, and platform-store reviews completed | Decision blocker cleared; exact contract frozen; production implementation authorized |
| 2026-07-18 | SEC-01 | Implemented authenticated secure v2, Storage A, migration/authorization/manual-pin/LAN separation, platform providers, samples, and regression suites | Local targeted, full core JVM/iOS, LAN JVM, provisioning, sample, XCFramework/Swift, and publication checks pass |
| 2026-07-18 | SEC-01/full gate | Ran `./gradlew check` after final ownership correction | SEC paths pass; gate remains red only for the registered Android lint error and two Apple LAN lifecycle timeouts; external security/device/network certification still required |
| 2026-07-18 | SEC-01/source control | Reviewed, committed, reran core JVM/iOS from committed state, and pushed | Implementation commit `b79c9ba`; post-commit 336 JVM + 317 iOS tests pass; pushed branch tracks origin |
| 2026-07-18 | REL-REMOTE-01 | Inspected local publication configuration and current official Central paths | BUILD-02 confirmed; unit blocked only on namespace/workflow/release policy and later CI credentials; independent work continues |
| 2026-07-18 | REL-ABI-01 | Inspected all public ABI imports plus generated POM/module variants and froze the implementation/test plan | BUILD-01 reproduced in core, LAN, and both provisioning sidecars; no source edit made before analysis completion |
| 2026-07-18 | REL-ABI-01 | Corrected API scopes, removed desktop's runtime LAN edge, added isolated consumer matrix, reviewed metadata, and ran all local gates | BUILD-01/CORE-T13 implemented; exact consumer and artifact gates pass; full-gate baselines unchanged |
| 2026-07-18 | REL-ABI-01/source control | Created focused commit and reran the isolated consumer gate from committed state | Commit `8f15d75`; clean committed diff; BUILD-01/CORE-T13 verified; JVM/Android/KMP/iOS consumer matrix passes |
| 2026-07-18 | LIF-GEN-01 | Reviewed current lifecycle/session code and deterministic interleavings; froze focused plan before source changes | CORE-01 confirmed in late advertise/discover/connect/observer commits; CORE-T01 in progress; CORE-11 explicitly remains separate |
| 2026-07-18 | LIF-GEN-01 | Implemented terminal generation commits, stale-resource compensation, atomic session registration, and six deterministic races; reviewed every equivalent path | CORE-01/CORE-T01 implemented; full core JVM/iOS and Android compilation green; independent repository baselines unchanged |
| 2026-07-18 | LIF-GEN-01/source control | Created `a4e0bb0` and reran focused JVM plus complete iOS Simulator/Android checks from committed state | CORE-01/CORE-T01 verified; forced full JVM reproduced only the registered FILE-04 failure |
| 2026-07-18 | ID-STORE-01 | Implemented collision-safe namespaces, transactional cross-process creation, atomic persistence, failure memoization, rollback migration, and JVM root validation | CORE-18/19/20/21/29 and CORE-T11 implemented; JVM 351/351 green; iOS focused green; Android/Apple compile green |
| 2026-07-18 | ID-STORE-01/source control | Committed, forced affected checks from committed state, and pushed `ee69d09` | CORE-18/19/20/21/29 and CORE-T11 verified on `remediation/full-register-2026-07` |
| 2026-07-18 | CORE-SESSION-01 | Implemented transactional connect ownership, immutable store publication, terminal runtime completion, atomic shutdown drain, full setup deadline, and retained path authority | CORE-03/08/09/10/13/23 and CORE-T02/T07/T08 verified locally |
| 2026-07-18 | CORE-SESSION-01/verification | Ran focused JVM/iOS, three concurrency repeats, complete core JVM/iOS, and cross-platform compilation; reviewed the full batch diff | Batch tests green; complete pre-final-hook JVM 358/358 and iOS 334/334; final hook test 4/4 per platform; forced JVM reproduced only FILE-04 |
| 2026-07-18 | CORE-SESSION-01/committed state | Exact verification exposed a replay-zero test collector race; replaced eager `async` scheduling with `CoroutineStart.UNDISPATCHED` and did not alter timeouts/retries | `SessionOwnershipTest` passed three forced JVM repeats, focused iOS, and Android compile at `82a9b41`; both commits pushed |
| 2026-07-18 | PARSE-01 | Replaced quadratic reader, centralized per-packet caps/shape, enforced strict canonical text, isolated diagnostics, and added deterministic properties | `PROTO-01/02/03/05/06/07`, `PT-T01..05` verified; parser portions of `PROTO-04`, `PT-T07`, `PT-T21` committed |
| 2026-07-18 | PARSE-01/verification | Ran focused/full JVM and iOS, Android/iOS compiles, three forced repeats, ABI task, exact committed-state worktrees, and repository `check` | Core JVM 389/389 and iOS 364/364; repository gate failed only registered LAN iOS and Android lint baselines |
| 2026-07-18 | XFER-01 | Refactored transfer ownership, acceptance compensation, deadlines, arithmetic, progress, collision handling, and platform snapshots; isolated two API decisions | Complete core JVM 401/401 green; focused transfer iOS green; complete iOS run exposed three unrelated registered lifecycle/reconnect flakes and each passed an isolated forced rerun; Android/iOS device compilation green |
| 2026-07-18 | XFER-01/determinism | Added gated accept/sink/write, exact virtual-time, 64-slot, transition, collision and retention regressions | Seven timing/concurrency tests passed three forced JVM repeats; updated focused iOS suite passes |
| 2026-07-18 | XFER-01/source control | Reviewed, committed, pushed, then reran focused JVM/iOS and Android/iOS-device compilation | `68c579f` pushed; covered findings/gaps marked Verified except the documented API/external-instrumentation boundaries |
| 2026-07-18 | PEER-CTRL-01 | Implemented per-transport peer aggregation, direction-aware duplicate handling, bounded application delivery, monotonic keepalive, and safe validation/snapshot portions | Complete core JVM 411/411 and iOS 386/386 green; Android/iOS-device compile green; five concurrency/timing tests pass three forced JVM repeats; three public API/model decisions isolated |
| 2026-07-19 | PEER-CTRL-01/source control | Created local `ee2cae5` and reran focused JVM/iOS from committed state | Checks green; push rejected at `SCM-PUSH-01`, so covered rows remain Implemented rather than Verified |
| 2026-07-22 | CORE-LIFE-02 | Implemented transactional discovery rollback, bounded aggregate teardown, recoverable incoming acceptance, and deterministic hang/failure tests | Commit `60aecf6`; 417/417 JVM and 392/392 iOS core green; Android/iOS/LAN compilation green; push remains isolated at `SCM-PUSH-01` |
| 2026-07-22 | LAN-CONN-01 | Bounded connection admission and fixed dial/read/write/listener/terminal ownership on JVM, Android, and Apple | Commit `b21ed84`; focused regressions green; core 417/417 JVM and 392/392 iOS, LAN 58/58 JVM; Apple LAN retains only two registered lifecycle timeouts and one skipped diagnostic; push blocked at `SCM-PUSH-01` |
| 2026-07-22 | LAN-CONN-01/committed state | Reran focused cancellation/lifecycle/recovery/write tests and cross-target compilation after both focused commits | 9/9 JVM and 6/6 Apple tests pass; Android, iOS Arm64, and iOS X64 compile; no new regression |
| 2026-07-22 | LAN-JMDNS-02 | Transactionalized JmDNS registration/restoration/cleanup, listener generations, target-scoped retries, and concurrent scheduling on JVM/Android | Commit `83db1ab`; focused 33/33 repeated, real JmDNS 7/7, full LAN JVM 73/73; Android and all Apple target compiles green; push blocked at `SCM-PUSH-01` |
| 2026-07-22 | LAN-APPLE-03 | Recovered failed/cancelled Apple listeners and browsers, serialized native ownership/cleanup, fixed cache prune ordering, and accepted validated TXT-less removal events | Commit `703182a`; 6/6 focused recovery, both former lifecycle timeouts, Apple 49 executed/0 failed/1 manual skipped twice, LAN JVM 73/73, Android/all Apple compiles and Clang header gate green; push blocked at `SCM-PUSH-01` |
| 2026-07-22 | LAN-NET-SCHEMA-01 | Bounded/validated all LAN TXT records, bound removals to admitted service identity, sanitized diagnostics, and made Apple replay/Android debug logging explicit opt-ins | Commits `2895ff1` and `09af6b5`; LAN JVM 77/77, Apple 56 executed/0 failed/1 manual skipped, all target compiles, release XCFramework, Swift sample, and committed-state focused checks green; Android lint unchanged at registered 1 error/3 warnings; push blocked at `SCM-PUSH-01` |
| 2026-07-22 | LAN-NET-01 | Finished native liveness ownership, forced same-network/address rotation, Android selected-network routing, Apple peer-to-peer parameters, subnet affinity, and bounded alternate candidates; reviewed equivalent implementations and selector KDoc | Local batch complete; core JVM 417/417, core iOS 392/392, LAN JVM 78/78, LAN iOS 60/0 failures/1 manual skip, Android and all Apple target compiles green; remaining physical/hostile evidence and `SCM-PUSH-01` are explicit |
| 2026-07-22 | LAN-NET-01/source control | Staged only the 23 reviewed batch paths, ran `git diff --cached --check`, created focused commit, reviewed committed diff, and reran the full focused cross-platform command | Commit `21bc2fc`; committed-state focused command `BUILD SUCCESSFUL` in 41s; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | PATH-PERM-01 | Implemented shared observer generation/cleanup ownership, Android context-backed default wiring, and the complete four-entry manifest diagnostic; added deterministic common lifecycle/permission regressions and reviewed Android/iOS equivalents | `CORE-22/25/26` and `CORE-T12` implemented locally; focused 15/15 JVM + 15/15 iOS tests and Android/all Apple target compiles pass; complete core JVM 422/422 and iOS 397/397 pass; physical callback/manifest evidence remains |
| 2026-07-22 | PATH-PERM-01/source control | Staged only the 14 reviewed batch paths, ran staged diff checks, created focused commit, reviewed committed diff, and reran the focused cross-platform command | Commit `1903b11`; committed-state focused command `BUILD SUCCESSFUL` in 17s; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | PROV-A-HIGH-01 | Implemented terminal provisioning ownership, parent-cancellation refusal, and deferred hotspot/join callback cleanup under concurrent close; reviewed equivalent paths and added deterministic host-gated regressions | `PROV-A03` and shared `PROV-A02` local conjunct implemented; complete Android provisioning host suite 20/20 and Android main compile pass; PS-T01 physical instrumentation remains open |
| 2026-07-22 | PROV-A-HIGH-01/source control | Staged only the three reviewed batch paths, ran staged diff checks, created focused commit, reviewed committed diff, and reran the complete affected command | Commit `5bc31ce`; committed-state host suite/Android compile `BUILD SUCCESSFUL` in 3s; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | PROV-A-01 | Hardened Android provisioning result/callback ownership, process binding, LinkProperties snapshots, normal-permission diagnostics, and input validation; reviewed JVM parity and KDoc; added deterministic host seams | `PROV-A01/A04/A05/A06/A07/A08/A09/A10/A11` implemented locally; Android host 23/23 and compile pass; desktop compatibility 6/6; physical PS-T01/PS-T02 and PROV-A12 evidence remains |
| 2026-07-22 | PROV-A-01/source control | Staged only the seven reviewed batch paths, ran staged diff checks, created focused commit, reviewed committed diff, and reran Android/desktop committed-state checks | Commit `21dad61`; committed-state Android host 23/23, Android main compile, and desktop 6/6 `BUILD SUCCESSFUL` in 2s; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | PROV-D-01 | Hardened desktop poll validation/interface ranking/fatal propagation and removed process-global test mutation; reviewed Android contract parity and KDoc | `PROV-D01..05` and `PS-T03` implemented locally; desktop suite 10/10 pass; push remains blocked at `SCM-PUSH-01` |
| 2026-07-22 | PROV-D-01/source control | Staged only the four reviewed desktop batch paths, ran staged diff checks, created focused commit, reviewed committed diff, and reran the complete desktop suite | Commit `5efee8f`; committed-state desktop suite 10/10 `BUILD SUCCESSFUL` in 2s; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | SAMPLE-HIGH-01 | Replaced automatic incoming-file acceptance with explicit consent and bounded storage across Android, desktop CLI/GUI, and iOS; made iOS destination claims/write-failure reporting safe and removed Android binary payloads from UI state | `SAMPLE-01/02/03/04` implemented locally; all four samples compile, desktop UI 6/6 passes, and real-device/hostile-sender evidence remains open in `PS-T04/PS-T07` |
| 2026-07-22 | SAMPLE-HIGH-01/source control | Staged only the six reviewed sample batch paths, ran staged/committed diff checks, reviewed the committed diff, and reran the affected Android/desktop/iOS checks | Commit `c4b31a1`; committed Gradle gate `BUILD SUCCESSFUL` in 4s with desktop UI 6/6; committed provenance-fresh generic-iOS-Simulator build `** BUILD SUCCEEDED **`; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | SAMPLE-XFER-01/source control | Completed bounded transfer cleanup, path claims, and history ownership across sample platforms; reviewed and committed the focused batch | Commit `9102b78`; desktop UI 7/7, Android/desktop compile, release XCFramework, and provenance-fresh generic-iOS-Simulator build pass; physical transfer evidence remains |
| 2026-07-22 | SAMPLE-LIFE-01 | Transactionalized sample startup/stop and feature toggles; hardened CLI parsing/targeting/shutdown/output, endpoint parsing, Android diagnostics/permissions, KMP cleanup, and iOS metadata/terminal handling | `SAMPLE-08/09/10/11/16/17/18/19/20/21/25/26/27/28/29/30/31/32/33/36/37/38/39` implemented locally; desktop samples 16/16, KMP 2/2, Android lint, XCFramework, and generic-iOS-Simulator build pass |
| 2026-07-22 | SAMPLE-LIFE-01/source control | Staged only the 14 reviewed code paths, created the focused code commit, reviewed the committed diff, and reran complete affected JVM/Android/Apple checks | Commit `b90b8bd`; provenance reports `b90b8bd (matches HEAD)` and all committed-state checks pass; physical/hostile evidence remains; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | REL-PROV-01 | Replaced volatile/undeclared build identity with typed reproducible BuildInfo/XCFramework provenance and isolated exact-UDID launcher ownership | `BUILD-03/07/09/10/11/12` and `LAN-21` implemented; launcher 7/7, deletion/untracked/cache gates, core/LAN JVM/Android/iOS, XCFramework, and generic simulator build pass |
| 2026-07-22 | REL-PROV-01/determinism | Clean committed affected gate exposed one UDP-goodbye timeout; traced it, moved exact removal ownership to a deterministic production seam, and removed JVM/Android nested listener locks | Corrective commit `639c508`; LAN JVM 83/83 twice, Android compile, final provenance repeat 831ms; real departure timing remains hostile-network evidence |
| 2026-07-22 | REL-PROV-01/source control | Reviewed and committed the focused build batch and LAN deterministic follow-up; reran final exact-HEAD provenance and Xcode checks | Commits `e19c5c5`, `639c508`; XCFramework matches clean `639c508`; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | REL-GATE-01 | Removed every registered Kotlin/JmDNS/Gradle-DSL/Compose/Swift and Android lint source diagnostic; promoted future Kotlin/Java/Gradle/Swift/lint warnings to failures | `BUILD-08/14/15` implemented; Android lint says `No issues found`; complete affected tests and 136-task repository `check` pass |
| 2026-07-22 | REL-GATE-01/source control | Reviewed and committed the 14-path gate batch; reran complete JVM/Android/Apple tests, release XCFramework provenance, generated Xcode project, and warnings-as-errors Swift build from exact committed state | Commit `e66b139`; core 422 JVM/397 iOS, LAN 83 JVM/60 Apple with one manual skip, desktop UI 11, Android assembly/lint, and Xcode build pass; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | REL-SUPPLY-01 | Added deterministic wrapper, dependency integrity/locking, release-only SBOM, ABI, real Dokka, hardened publication/consumer/tag gates, and pinned CI/dependency maintenance | `BUILD-04/05/06/13` implemented locally; lock refresh 189 tasks, repository check 148 tasks, SBOM 38 components, 15 publications, isolated JVM/Android/KMP/iOS consumers, wrapper/tag mutations, ABI/Dokka, and focused race repeats pass |
| 2026-07-22 | REL-SUPPLY-01/determinism | The first committed full gate exposed local close racing raw terminal classification; serialized close intent/ownership and made committed cleanup cancellation-safe | Corrective commit `c68d59e`; exact regression passed three JVM repeats, focused JVM/Apple 43/43 each, core 424/424 JVM and 399/399 Apple, and final repository `check` passed 148 tasks in 4m04s |
| 2026-07-22 | REL-SUPPLY-01/source control | Reviewed both committed diffs and reran the complete supply and platform evidence set from final code state | Commits `64ca0a2`, `c68d59e`; SBOM 38, publications 15, isolated consumers 14/14, ABI/Dokka/XC provenance 75-task gate, and `git diff --check` pass; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | GOV-PACKAGE-01 | Isolated replay-zero/global-state fixtures, added Android/Apple packaging diagnostics, and separated maintained Alice/Bob identities | `CORE-30`, `LAN-19/20`, `SAMPLE-35`, `LAN-T09`, and `LAN-T11` implemented; complete core/LAN/desktop, cross-target, ABI, profile, and published-consumer gates pass |
| 2026-07-22 | GOV-PACKAGE-01/determinism | First committed gate exposed a stale same-name Bonjour peer selected across Apple test methods; assigned per-test UUID namespaces, selected exact current PeerIds, and made clean remote stop require exact `Closed` | Corrective commit `90b5f20`; 12/12 focused Apple tests pass three forced runs; final complete Apple LAN 64 tests/0 failures/1 manual skip |
| 2026-07-22 | GOV-PACKAGE-01/source control | Reviewed both focused commits and reran all affected checks from final committed state | Commits `52e173b`, `90b5f20`; final 46-task gate passes in 2m20s; published consumers pass 221 + 18 tasks; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | ENV-03 | Added deterministic iOS simulator UI target/runner and accessibility-anchored Start/Stop regression; reviewed generated scheme and committed diff | Commit `ebcf827`; ambiguous simulator-name invocation fails with the expected safeguard; exact signed committed run passes 1/1 with 0 failures; physical/device UI remains `ENV-01` |
| 2026-07-22 | ENV-05 | Force-enabled the 60-second Apple diagnostic, captured native Bonjour add/remove, restored its skip, and corrected the legacy-v1 versus secure-v2 KDoc boundary | Diagnostic 1/1 passes in 60.114 s; `dns-sd` records add/remove; maintained authenticated JVM sample correctly does not cross-discover; physical secure-v2 interop remains external |
| 2026-07-22 | ENV-06 | Authenticated and ran current OSV Scanner 2.4.0, raised advisory-affected build/docs/test dependency floors, refreshed locks/checksums, and added a pinned recurring explicit-lockfile workflow | Initial tool graph had 43 unique advisories while the release SBOM was clean; current 1,165-record lock scan and 38-record release scan both have zero vulnerabilities |
| 2026-07-22 | ENV-06/determinism | Full repository execution exposed concurrent cross-module Apple simulator binaries sharing process-global platform services; serialized only simulator test tasks with a shared Gradle service | Exact failures and the complete core Apple suite passed alone; corrected full `check` passes 148/148 tasks in 4m26s without assertion, timeout, skip, or terminal-outcome changes |
| 2026-07-22 | ENV-06/source control | Reviewed and committed the nine-path advisory batch, then reran the full repository gate from committed state | Commit `3574abd`; `git show`/committed whitespace checks pass; committed `check` passes 148/148 tasks in 5m35s; push not attempted because `SCM-PUSH-01` remains unapproved |
| 2026-07-22 | FINAL-01/blocker audit | Inspected every remaining Planned/In-Progress finding and explicit gap after all dependency-ready local batches; converted only owner/API/protocol/device/network/credential/audit/infrastructure boundaries to Blocked | Findings: 0 Planned, 107 Implemented, 29 Verified, 14 Blocked. Gaps: 21 Verified, 15 Implemented, 18 Blocked. Repository is not production-ready; exact unblock packages are recorded above |
| 2026-07-22 | OWNER-DECISIONS-01 | Owner approved all nine recommended lifecycle, state, immutable-model, factory, metadata, offer, error, and transfer-protocol contracts | Decision-only blockers cleared; unimplemented rows moved to Planned and remain unclaimed until their focused batches pass |
| 2026-07-22 | CORE-LIFECYCLE-API-01 | Implemented restartable data rollback, pending-dial/listener cleanup, required provisioning disposal, active-operation cancellation, terminal states/errors, teardown ordering, ABI/Dokka updates, and external implementer fixtures | `CORE-11`, `CORE-24`, and `CORE-T09` implemented locally in `391ae3c`; complete affected JVM/Android/Apple and published-consumer gates pass; push blocked at `SCM-PUSH-01` |
| 2026-07-22 | CORE-LIFECYCLE-API-01/source control | Reviewed commits `391ae3c` and `9305471` and reran the required affected checks from committed state | JVM/core/LAN, complete Desktop/Android provisioning, Android compile, and four ABI checks pass in 35s/52 tasks; forced Apple lifecycle tests pass in 51s/17 executed tasks; published-consumer/Dokka gate passes 221 + 18 tasks; no push attempted because `SCM-PUSH-01` is unapproved |
| 2026-07-22 | IMMUTABLE-MODEL-API-01 | Replaced caller-owned/castable public collections with stable private-backing snapshots across models and peer/session publications while preserving practical value semantics | `CORE-17` implemented in `04aaef0`; 434 JVM core, 407 Apple core, all affected LAN/provisioning/sample tests, Android lint, ABI/Dokka, 221 + 19 published-consumer tasks, XCFramework, and Swift warnings-as-errors build pass; push blocked at `SCM-PUSH-01` |
| 2026-07-22 | IMMUTABLE-MODEL-API-01/source control | Reviewed `04aaef0` and forced focused model/metadata/publication-list/ABI checks plus the isolated consumer gate from committed code state | 18/18 focused tasks pass in 42s; publication/Dokka 221 tasks and 19/19 Kotlin/Java/Android/KMP/iOS consumers pass; no push attempted because `SCM-PUSH-01` is unapproved |
| 2026-07-23 | PEER-STATE-API-01 + TRANSPORT-FACTORY-API-01 | Added independent retained advertising/discovery state machines, descriptor-first capabilities, discovery-only providers, typed initialization failures, and pre-build duplicate-kind validation | `CORE-15/27/28` implemented in `18723c0`; focused 37/37 JVM + 37/37 Apple, complete core/LAN JVM/Android/Apple/ABI/Dokka, and 221 + 19 published-consumer tasks pass; push blocked at `SCM-PUSH-01` |
| 2026-07-23 | PEER-STATE-API-01 + TRANSPORT-FACTORY-API-01/source control | Reviewed committed `18723c0` and reran forced feature-state/capability, Android/Apple target, ABI, Dokka, and isolated consumer checks from the committed implementation | 35/35 tasks pass in 1m26s with 37 JVM + 37 Apple focused tests; publication/Dokka 221 tasks and 19/19 isolated consumers pass; no push attempted because `SCM-PUSH-01` is unapproved |
| 2026-07-23 | XFER-OFFER-API-01 + XFER-ERROR-API-01 | Added bounded authoritative retained offers, deterministic response/expiry ownership, and structured phase/retry-aware transfer failures with preserved internal causes | `FILE-05/06/11` and `PT-T12/13/21` implemented in `ddb7142`; complete core JVM 458/458 and Apple 431/431, cross-target, ABI, Dokka, consumer, and XCFramework gates pass |
| 2026-07-23 | XFER-OFFER-API-01 + XFER-ERROR-API-01/source control | Reviewed committed `ddb7142` and reran forced focused JVM/Apple, Android/all-Apple compilation, ABI, publication/Dokka consumer, and release XCFramework provenance checks | 59/59 JVM and 56/56 Apple focused tests pass; publication 221 tasks plus 19/19 consumers and XCFramework provenance pass; no push attempted because `SCM-PUSH-01` is unapproved |
